{"date":"2026-09-02","technique":"sidecar-provisioning/atomic-downloads","seam":"scripts/case-loops/money/harvest-contract-dumps.ts:116-118","mode":"experiment","verdict":"better","finding":"the index's per-month byte size (entry.velikostDumpu) is printed and stored into state.months[k].bytes but never compared to the received byte count; a server that advertises and delivers a smaller total is marked done. The runtime's fetch already fails a short body under a truthful Content-Length and stops at the header's length on an oversized body, so those two checks are made by the transport, not by the script. Short-body failure leaves a truncated file at the FINAL name (retry rewrites it).","next":"stage to a .partial name, compare received bytes to entry.velikostDumpu before marking the month done, rename last; optionally resume with Range against the partial (verify Content-Range start == partial size, restart on 200).","instrument":"state.months[k].bytes vs on-disk size of .smlouvy-dump-cache/dump_<k>.xml for every harvested month - no harvest state present on this checkout on 2026-09-02, so unrun.","registry":"knowledge/software-engineering/llm-agent/runtime-and-io/sidecar-provisioning/applications/node--atomic-downloads.md"}
{"date":"2026-09-03","technique":"durable-intermediate-representation","subject":"import-normalization","bundle":"software-engineering","run":"direction-exec-0903","mode":"task","verdict":"better","seam":"lib/ingest/parsedText.ts + scripts/case-loops/sources/kiosek-slice.ts:130,187","seam_notes":"Sidecar `<pdf>.txt` beside the bytes; line 1 is `#parsed-text/1 unpdf@<version> sha256:<16>` — format version, parser identity+version and a source fingerprint, all three required to match. A missing/older/foreign stamp is a loud miss, never a silent pass; an unreadable unpdf version disables the sidecar rather than degrading it to an unverifiable cache (absent-guard-is-loud). Written .part-then-rename; deleteParsedText() is the reaper. Gate green: npm run typecheck clean, unit lane lib/ingest 228/228 (8 new). Second ingest: unpdf invocations 1 -> 0 per PDF, i.e. 23 -> 0 for the slice.","falsifier":"Wall time is not the payoff. Measured on this box: 23 parses of a synthetic 55-line posting PDF = 104,7 ms vs 1,6 ms of sidecar reads. The real .justice-samples/ corpus is gitignored and absent from every checkout, so the proposal T1 timing could not be run at all. If the payoff has to be seconds, this is not worth its coherence surface; it is worth it only for the other two forces — re-scoring the corpus when unpdf is absent or bumped, and attributing a diff to the extractor change rather than to a silent re-parse. Divergence from the proposal: it copied LightRAG warn-never-reparse; a version mismatch here re-parses and re-stamps, because warn-never-reparse leaves the corpus permanently mixed (new PDFs still enter at the new engine) while re-parse converges it."}
{"date": "2026-09-03", "technique": "query-decomposition-before-the-lanes (offline oracle)", "subject": "retrieval", "bundle": "software-engineering", "run": "direction-exec-0903", "mode": "task", "verdict": "unmeasurable", "seam": "scripts/sentinel/path-oracle.ts + lib/testing/sentinel/path-oracle.json", "seam_notes": "Gate: `tsc --noEmit` exit 0; `npm run path-oracle` exit 2 (9 of 9 rows unevaluable) because NO ./.pglite exists on this machine or in the main checkout - the proposal assumed the store is present, and its whole measurable (`rows whose winning path changed across a da:kg-compute --commit`) therefore stays unmeasured here. The instrument itself is proven end-to-end: against a throwaway 4-node/2-edge store seeded with two of the oracle's real documented pairs, --update froze 2 rows, and after mutating that store the check exited 1 naming both clauses correctly - `hop 1 (psp:person:6150 -linked_to-> company:ico:26185610) citation kg-compute/pass-39 -> kg-compute/pass-41-RECOMPUTED` and `pendingCount 1 -> 0 (a tie was reviewed)`. Liveness is asserted before any path is evaluated, so an endpoint that has left the graph is unevaluable, never a silent pass. --update is flag-only, writes only rows that would otherwise fail, never on a green run, and the review surface is the JSON diff. | verdict recorded as unevaluable; instrument that would make it measurable: a local .pglite store (none on this machine) - run `npm run path-oracle -- --update` once against it, then the check pins 9 rows", "falsifier": "Freeze the 9 rows with `npm run path-oracle -- --update` on the machine that holds ./.pglite, then re-run after the next `da:kg-compute --commit`. If most rows go red on two consecutive refreshes, path ranking is genuinely unstable under data refresh - that is a PRODUCT finding about what the /graf permalink promises a reader, and belongs in docs/routes/graf-permalink.md, not in a looser check. If most rows go `unevaluable` within a term instead, the pairs are keyed on individuals who lose mandates and must be re-keyed on structurally durable entities (party, large organ, long-lived company)."}
