# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# data layer (embedded Postgres + cached open-data dumps + analysis working set)
/.pglite
/.pglite-*
/.data
/.data-analysis
/.kg-analysis
/.hybrid-bench

# Personal cost data - the Personas passport reads it locally; never in VCS (rule committed on master)
app-cost.json
/scripts/**/node_modules/
/.db-bench
/benchmark-data
.justice-samples/
.kiosek-cache/
.dataor-cache/

# Claude Code session logs (per-run, large, not project content)
.claude/logs/

.smlouvy-dump-cache/
docs/data-analysis/case-money/contracts-harvest.jsonl

# Claude Code git worktrees (nested checkouts, never repo content)
.claude/worktrees/

# raw source harvests (Registr smluv dumps etc.) — re-derivable, never documentation.
# Moved out of docs/ on 2026-08-22: a 100 MB jsonl does not belong where humans read.
/data/raw/

# BEGIN ai-registry linked skills (managed by ai-registry/scripts/link-registry.mjs)
# Links are machine state; the declarations live in .ai/manifest.yaml (`skills:` and `knowledge.domains`).
/.claude/skills/architect
/.claude/skills/conform
/.claude/skills/consult
/.claude/skills/explorer
/.claude/skills/i18n-translate
/.claude/skills/npm-updates
/.claude/skills/perfect
/.claude/skills/research
/.claude/skills/ship-loop
/.claude/skills/uat
/.claude/rules/ai-registry-access.md
/.claude/rules/ai-registry-software-engineering.md
/.claude/rules/ai-registry-civic-intelligence.md
/.claude/rules/ai-registry-llm-observability.md
# END ai-registry linked skills
