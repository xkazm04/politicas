## Run: 2026-09-01 - budget-mirror (any)

Sampled: 17 files (14 in area, 3 generated modules by header only; 7 thin routes after one widening)
Surfaced: 9 items (stopped one short of 10 rather than pad; one candidate killed at the premise gate)
Accepted: [1, 2, 3, 4, 5, 6, 7, 8, 9] — all xs/s, all auto band, 7 commits
Declined: [] (nothing to decline: no m/l item)
Deferred: []

### Self-reflection
- Categories that resonated: `data` (the fold hiding MP ties, measured at four rows for Brno) and the cross-layer lens — items 1, 4 and 9 are each "one layer claims something another layer does not do" (a comment claiming prerender, a comment claiming revalidate, a section claiming ties are read live while hiding four of them).
- Categories that didn't: perf, a11y, sec — the area is small, pure functions over generated modules; nothing measurable.
- Calibration: one high, three medium, five low. The low band was comment-and-test work; a future run could raise the bar by skipping comment-only fixes unless the comment carries a decision (item 4 did: the sitemap rationale rested on it).
- Tools: a throwaway vitest file in the repo's own lane was the reliable probe (tsx -e with @/ aliases printed nothing). Reading the store directly from a script worked only because no politicas process held it. Port 3000 was a DIFFERENT project — check the page title before trusting a port as the smoke server.
- Gate cost: the commit-msg doc-sync hook makes every features/** or app/** change owe README, AGENTS, ROADMAP plus the coupled route docs; scoped Doc-sync(<doc>) trailers per commit are the price of atomic commits here, and satisfying the route doc with a dated entry is the honest half.

## Run 2: 2026-09-01 - budget-mirror (any, --triage-all)

Sampled: 19 files (17 carried, + SectionHeading + the /poslanec route)
Surfaced: 7 items (stopped at 7; two measured candidates dropped)
Accepted: [1, 2, 3, 4, 5, 6, 7] - all seven put to the user in one multi-select, all accepted, 7 commits
Declined: []
Deferred: []

### Self-reflection
- Second pass over a just-swept area ran low: one medium, six low. That is the honest reading of a 14-file area after nine fixes, not a calibration miss - coverage.md now says to wait for a data batch or a UI change before a third visit.
- --triage-all worked as ONE multi-select question with the four small items bundled; the user took everything, which suggests the auto band's sizing yesterday was not running small.
- The index-stat check fired for real: another session had pre-staged two lib/analysis files; the commit was refused by the hook, `git restore --staged` on those two paths fixed it, and the working tree was never touched. Keep the one-invocation stage-verify-commit shape.
- Three of seven items (3, 5, 6) are the kind a browser settles; with no politicas dev server they shipped on tests and typecheck only, and every commit body says so.
