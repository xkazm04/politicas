# Tender batch 010 — the winner circle works, and the ten-batch reflection

Case ④ needles · 2026-08-24 · **pass 73** (133 authority nodes,
`tender_winner_circle`, ns=circle). Evidence `winner-circle-cpv45-*.json`.

## 1. The statistic that survived (where b009's died)

`winner-circle.ts` reads each authority's **dated win sequence** — winners are
always identified, so the b009 denominator problem does not exist. Two numbers
separate the species that b008 could only narrate:

| authority | circle3_share | switch_rate | reading (numbers only) |
|---|---|---|---|
| Moravská galerie | 64 % | **89 %** | three individuals alternating almost every consecutive win — the ROTATION shape, now quantified |
| Bytová správa MV | 70 % | **24 %** | one winner in runs (OTISTAV 23 of 37) — the LOCK shape |
| Teplárny Brno (MP-tied) | **77 %** | 19 % | two-supplier duopoly (DOMA-invest 8 · UCHYTIL 7) |
| Město Bechyně | 56 % | 31 % | an individual as house supplier (Stanislav Návrat 24 of 59) |

Distribution over 133 measured authorities: p50 = 0,29 · p90 = 0,53 ·
p95 = 0,64. Persisted with floors (≥ 20 dated wins), `dated_wins_only: true`,
monopoly counterparties skipped, provenance ns=circle. The surface rule stands:
state circle share and switch rate, never intent — high/low switch label the
*shape*, not a motive.

## 2. Ten batches in one session — what the loop proved

**Extractable from RVZ record-level data (all persisted or evidenced):**
- 61 421 CPV-45 lots as graph objects with register facts verbatim (pass 70);
- six deterministic lot flags with corpus-derived thresholds and per-node
  inputs — 4 392 flagged lots (passes 71–72);
- authority/winner pictures with small-n floors + a five-species taxonomy;
- authority-level statistics where lot flags would lie (winner circle, pass 73);
- deep-read case files per subject, ARES ownership hops included;
- 18 MP-graph touches with zero join logic — as AUTHORITIES.

**Proven NOT extractable (negative results, kept in tools/docs):**
- bidder-pool composition (losing bidders anonymised — 95 % of authorities
  unreadable; b009);
- per-lot overpricing (estimate fill 14 % — b001 demoted it to authority
  level);
- cadence across the 2024-12 coverage boundary (b008: reads as decline).

**Method rules the ten batches earned** (each paid for by a caught error):
hand-read before persist (b004, b006, b009 — three flags rewritten, one
statistic killed); thresholds from the corpus, but LEGIBILITY bars for
tail-shaped signals (b006); denominator visibility computed before metric
design (b009); species before rank (b008); inference vs register fact marked
in prose (b009's correction of b008); re-ingest wipes flags by design —
ingest, then reflag (b006); negative results stay in the repo (b009).

## 3. What the product can render tomorrow

The needle doctrine has its first four rendering species, all with inputs and
sources on every number: the conveyor city case file (Havířov), cover-bid lots
(tight_spread with the two bids shown), circle shapes (rotation vs lock, with
the sequence), and the monopoly-counterparty disclosure pattern. All language
stays „signál/neefektivita"; person-level claims remain outside this case.

## 4. Next campaign steps (beyond this session's ten)

1. Monthly increment: new RVZ month → persist → full reflag → recompose
   (the loop's steady state; every tool exists).
2. The needle product surface (species-typed authority pictures).
3. CPV 72 (IT services) as campaign 2 — the user's original scope question.
