# Moonshot round — 2026-09-01 (ad-hoc lens `moonshot-architect`, strategy develop, all 10 groups)

Ten read-only scouts, one per context-map group, each returned 3–5 **L/XL** cards in the
scan-sweep §4.10 form. The coordinator clustered them by *corroboration* — the number of
independent scouts that arrived at the same spine from different code — and that count is
the primary ranking signal, impact second. Nothing was built: every card is L or larger,
so every card routes to a human by the sweep's own table. **Decisions are pending** — this
file is the durable deck; the outbox carries the top 29 bodies (its ingest cap).

Raw decks (per group) are in the session scratchpad; this file is the record.

## Decision table

| # | Corr. | Theme | Card | Group | Size | E/I/R | Result | Gate | Route | Decision |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 5 | T1 | Time-travelling claim gate: `asOf()` under /zdroj, /overeni and the schránka | Civic Feed & Transparency | XL | 7/9/5 | unmeasurable | contract | human (unmeasurable) | pending |
| 2 | 5 | T1 | Sealed instrument readings — the velín as a dated series, not a live-only view | Landing & Navigation | XL | 8/9/5 | unmeasurable | contract | human (unmeasurable) | pending |
| 3 | 5 | T2 | Receipts for derived figures: `claim:` refs resolve at /zdroj with their inputs | Shared UI Primitives | XL | 8/9/4 | better | contract | human (contract) | pending |
| 4 | 5 | T2 | Citation registry: every SourceNote keyed to a declared source with a live as-of | Shared UI Primitives | XL | 8/9/5 | better | contract | human (contract) | pending |
| 5 | 5 | T3 | One audited review door for every claim kind, not just money ties | Infrastructure & Observability | XL | 8/9/5 | better | irreversible | human (irreversible) | pending |
| 6 | 5 | T3 | Published correction ledger: the right of reply as a hash-chained record | Civic Feed & Transparency | XL | 8/9/6 | unmeasurable | irreversible | human (unmeasurable) | pending |
| 7 | 5 | T2 | Chamber readings as a claim family — every velín tile citable, resolved live | Landing & Navigation | L | 5/8/3 | better | none | human (size L — never built by the sweep) | pending |
| 8 | 5 | T2 | Verification as a protocol: machine verdicts, JSON receipts and whole-article audit | Civic Feed & Transparency | L | 5/8/4 | unmeasurable | contract | human (unmeasurable) | pending |
| 9 | 5 | T1 | Record-time citations: every receipt re-derives at the instant it was cited | Data Layer | L | 6/8/4 | better | contract | human (contract) | pending |
| 10 | 5 | T1 | Temporal receipts: /zdroj and the gate answer "as of the day you cited it" | Shared UI Primitives | L | 6/8/4 | better | contract | human (contract) | pending |
| 11 | 5 | T2 | Claim-ref ratchet: the lint doctrine moves from "a citation somewhere in the file" to "an address on every figure" | Infrastructure & Observability | XL | 7/8/4 | unmeasurable | policy | human (unmeasurable) | pending |
| 12 | 5 | T3 | One review lane for person-level verdicts — effort claims get the money layer's gate | MP Profiles & Rankings | L | 7/8/4 | better | contract | human (contract) | pending |
| 13 | 5 | T1 | Moved citations shown with both sides: as-of re-derivation from the bitemporal graph | Knowledge Graph Explorer | XL | 8/8/5 | unmeasurable | contract | human (unmeasurable) | pending |
| 14 | 5 | T3 | Identity ledger — one cross-source person resolution table with confidence and a review door | Data Ingestion | XL | 8/8/6 | unmeasurable | policy | human (unmeasurable) | pending |
| 15 | 5 | T2 | Vote figures enter the claim gate: a `vote` claim dataset for /overeni | Voting & Legislation | L | 5/7/2 | better | contract | human (contract) | pending |
| 16 | 5 | T2 | Every export carries a verifiable address: poster, embed and OG go through the gate | Shared UI Primitives | L | 5/7/3 | better | contract | human (contract) | pending |
| 17 | 5 | T3 | Declared attribution perimeter + a downward exposure lane in the review console | Financial Transparency | L | 5/7/4 | unmeasurable | policy | human (unmeasurable) | pending |
| 18 | 4 | T7 | Buyer edge for Registr smluv contracts — the authority as a graph endpoint | Data Ingestion | L | 5/8/4 | unmeasurable | contract | human (unmeasurable) | pending |
| 19 | 4 | T7 | Obec-keyed deník: the municipality as a dated subject, lifting the `obec:` refusal | Civic Feed & Transparency | L | 6/8/4 | unmeasurable | policy | human (unmeasurable) | pending |
| 20 | 4 | T7 | The municipality as a graph subject shared by /rozpocty, /volby and /penize | Financial Transparency | XL | 8/8/4 | unmeasurable | contract | human (unmeasurable) | pending |
| 21 | 4 | T7 | Release-stamped entity sitemap — case files discoverable without per-request scans | Landing & Navigation | L | 4/6/2 | unmeasurable | contract | human (unmeasurable) | pending |
| 22 | 3 | T4 | Structured graph provenance: source + run + pass on every kg row, sealed per pass | Infrastructure & Observability | XL | 8/9/6 | better | irreversible | human (irreversible) | pending |
| 23 | 3 | T5 | World time on the graph: valid_from/valid_to written, read, and joined to votes | Data Layer | XL | 8/9/6 | better | policy | human (policy) | pending |
| 24 | 3 | T5 | Three money channels as dated graph edges (subsidy + donation join contracts) | Financial Transparency | XL | 8/9/5 | unmeasurable | contract | human (unmeasurable) | pending |
| 25 | 3 | T4 | Sealed graph passes with per-claim inclusion proofs | Data Layer | L | 6/8/3 | better | contract | human (contract) | pending |
| 26 | 3 | T4 | Sentinel-certified releases: the verdict travels with the manifest hash | Infrastructure & Observability | L | 6/8/3 | better | contract | human (contract) | pending |
| 27 | 3 | T5 | Aligned reach: the role-window weight beside every raw money figure | Financial Transparency | L | 6/8/3 | unmeasurable | contract | human (unmeasurable) | pending |
| 28 | 3 | T4 | Derivation stamp for graph views: path-rule ref + whole-graph provenance aggregate | Knowledge Graph Explorer | L | 5/7/3 | unmeasurable | contract | human (unmeasurable) | pending |
| 29 | 3 | T5 | Club affiliation dated at the ballot: membership windows in the record | Voting & Legislation | L | 6/7/6 | unmeasurable | policy | human (unmeasurable) | pending |
| 30 | 2 | T8 | The vote→bill spine: a `decides` edge from the agenda-item join | Voting & Legislation | XL | 8/9/5 | unmeasurable | contract | human (unmeasurable) | pending |
| 31 | 2 | T8 | A deterministic theme layer for the dead Kompas, from committee routing | Voting & Legislation | L | 5/8/3 | unmeasurable | contract | human (unmeasurable) | pending |
| 32 | 2 | T6 | Formula revision ledger — published correction history for the index | MP Profiles & Rankings | L | 6/8/3 | better | contract | human (contract) | pending |
| 33 | 2 | T8 | Roll calls in the deník: the chamber's own activity as dated rows | Civic Feed & Transparency | L | 7/8/5 | unmeasurable | none | human (unmeasurable) | pending |
| 34 | 2 | T6 | Score-move ledger — the recompute becomes a dated fact with a size | Landing & Navigation | XL | 8/8/6 | unmeasurable | irreversible | human (unmeasurable) | pending |
| 35 | 2 | T6 | Served reproduction — the index replays itself on every read and ships its inputs | MP Profiles & Rankings | L | 5/7/2 | better | none | human (size L — never built by the sweep) | pending |
| 36 | 1 | S | Three-state gate and source on every graph edge (rejected is not verified) | Knowledge Graph Explorer | L | 6/9/4 | better | contract | human (contract) | pending |
| 37 | 1 | T9 | Ingest run as a diffed, event-emitting, recompute-triggering protocol | Data Ingestion | XL | 8/9/6 | unmeasurable | contract | human (unmeasurable) | pending |
| 38 | 1 | S | Term-keyed chamber pass — per-term rankings, archived, for the whole career spine | MP Profiles & Rankings | XL | 9/9/6 | better | irreversible | human (irreversible) | pending |
| 39 | 1 | S | Statute version graph — e-Sbírka znění as nodes, real before/after on /zakony | Data Ingestion | XL | 9/9/5 | unmeasurable | contract | human (unmeasurable) | pending |
| 40 | 1 | S | Gate replay lane: one gate registry, re-run over every stored reader string | Infrastructure & Observability | L | 6/8/3 | unmeasurable | contract | human (unmeasurable) | pending |
| 41 | 1 | S | The procurement layer reaches the canvas: on-demand neighbourhoods as a citable view | Knowledge Graph Explorer | L | 7/8/5 | unmeasurable | contract | human (unmeasurable) | pending |
| 42 | 1 | S | One Store contract, two drivers: a hosted-Postgres implementation proven by a shared conformance suite | Data Layer | XL | 9/8/8 | better | irreversible | human (irreversible) | pending |
| 43 | 1 | S | Registry-link contract sentinel: every built source link is probed against the entity it names | Shared UI Primitives | L | 5/7/3 | better | policy | human (policy) | pending |
| 44 | 1 | S | Registry sector codes (CZ-NACE) on every company node, one sector definition | Data Ingestion | L | 5/7/3 | unmeasurable | contract | human (unmeasurable) | pending |
| 45 | 1 | S | Reader-seeded slices — the velín as a lens over any neutral population | Landing & Navigation | L | 6/7/4 | unmeasurable | contract | human (unmeasurable) | pending |
| 46 | 1 | S | Legislative substance ledger — first-signature, fate and interpellation kind beside the count | MP Profiles & Rankings | L | 6/6/3 | better | contract | human (contract) | pending |
| 47 | 1 | S | Statute collisions computed for the whole corpus, close-reads as the overlay | Voting & Legislation | L | 7/6/4 | better | contract | human (contract) | pending |

## Themes

### T1 — As-of re-derivation: moved/gone claims shown with both sides from the bitemporal store

Corroborated by 5 scout(s): Civic Feed & Transparency, Landing & Navigation, Data Layer, Shared UI Primitives, Knowledge Graph Explorer.

- **#1 Time-travelling claim gate: `asOf()` under /zdroj, /overeni and the schránka** — Civic Feed & Transparency; contexts: claim-verifier, schranka-notifications, civic-chronicle, shared-provenance, civicscore-leaderboard, data-layer; advances: claim-verification-and-provenance / derivation-comparison + recomputation-receipts; law: provenance-or-nothing, missing-is-not-zero
- **#2 Sealed instrument readings — the velín as a dated series, not a live-only view** — Landing & Navigation; contexts: dashboard-instruments, dashboard-state-graph, landing-page, app-shell (cross: data-releases, graph-writers, overeni); advances: claim-verification-and-provenance / recomputation-receipts + three-verdict-vocabulary ("moved — shown with both sides and both dates"); accountability-publishing-ethics / real-vs-illustrative-form-encoding + honest-empty-states; law: missing-is-not-zero
- **#9 Record-time citations: every receipt re-derives at the instant it was cited** — Data Layer; contexts: db-repositories, db-store, claim-gate (features/overeni), provenance-receipts (features/shared/provenance), graph-permalink (features/graph), money-ledger (features/money); advances: claim-verification-and-provenance / recomputation-receipts + derivation-comparison ("moved is shown with both sides and both dates"); law: provenance-or-nothing
- **#10 Temporal receipts: /zdroj and the gate answer "as of the day you cited it"** — Shared UI Primitives; contexts: shared-provenance, claim-verifier, db-repositories, civic-chronicle, graph-explorer, money-cases-review; advances: claim-verification-and-provenance/three-verdict-vocabulary ("moved … shown with BOTH sides and BOTH dates") + claim-ref-addressing ("a claim the current store no longer carries gets an honest page that states what the address asserted"); _laws.md#disclose-never-repair
- **#13 Moved citations shown with both sides: as-of re-derivation from the bitemporal graph** — Knowledge Graph Explorer; contexts: graph-explorer, claim-verifier, db-repositories, shared-provenance; advances: claim-verification-and-provenance / three-verdict-vocabulary ("moved … shown with both sides and both dates") + recomputation-receipts; civic-knowledge-graphs / per-claim-provenance-stamping; law: provenance-or-nothing, disclose-never-repair

### T2 — A claim address on every figure: registry-keyed citations, figure receipts, exports and the machine gate

Corroborated by 5 scout(s): Shared UI Primitives, Landing & Navigation, Civic Feed & Transparency, Infrastructure & Observability, Voting & Legislation.

- **#3 Receipts for derived figures: `claim:` refs resolve at /zdroj with their inputs** — Shared UI Primitives; contexts: shared-provenance, claim-verifier, money-cases-review, money-ledger-graph, civicscore-leaderboard, contribution-scoring, lawwatch, mp-profile; advances: claim-verification-and-provenance/recomputation-receipts ("the receipt is an addressed computation, not a stored document") + derivation-comparison; _laws.md#deterministic-code-owns-numbers, #every-cap-ships-its-population
- **#4 Citation registry: every SourceNote keyed to a declared source with a live as-of** — Shared UI Primitives; contexts: shared-primitives, shared-provenance, civic-kg-primitives, data-quality (features/atlas via getAtlasData), data-releases (features/data-releases), civicscore-leaderboard, money-ledger-graph, lawwatch, votetrack-ledger, budget-mirror, volby-election-mirror, landing-page; advances: _laws.md#one-definition-one-import + #provenance-or-nothing; claim-verification-and-provenance/recomputation-receipts ("a cached receipt is a claim about the past wearing the tense of the present"); accountability-publishing-ethics/real-vs-illustrative-form-encoding
- **#7 Chamber readings as a claim family — every velín tile citable, resolved live** — Landing & Navigation; contexts: dashboard-instruments, landing-page (cross: overeni, shared-provenance / lib/claims, civicscore, money); advances: claim-verification-and-provenance / derivation-comparison ("for a composite, the citation's derivation comes from the population aggregate") + claim-ref-addressing; laws: provenance-or-nothing, one-definition-one-import; ROADMAP "Next" 3 (newsroom API) made concrete as a claims endpoint
- **#8 Verification as a protocol: machine verdicts, JSON receipts and whole-article audit** — Civic Feed & Transparency; contexts: claim-verifier, civic-feeds-verification, shared-provenance, data-releases; advances: claim-verification-and-provenance / claim-ref-addressing + structured-review-emission; ROADMAP "API pro redakce" made concrete as the verification protocol rather than a generic read API
- **#11 Claim-ref ratchet: the lint doctrine moves from "a citation somewhere in the file" to "an address on every figure"** — Infrastructure & Observability; contexts: civic-transparency-eslint-plugin, eslint-rules-shims, app-config, overeni, claims-core, shared-ui; advances: claim-verification-and-provenance / claim-ref-addressing + recomputation-receipts; repo convention: every rendered number cites its source (the brand rule, made addressable)
- **#15 Vote figures enter the claim gate: a `vote` claim dataset for /overeni** — Voting & Legislation; contexts: votetrack-ledger, votetrack-kompas, volby-election-mirror, civic-feeds-verification, mp-profile; advances: claim-verification-and-provenance (claim-ref-addressing, recomputation-receipts, gate-state-as-modifier); law: provenance-or-nothing, every-cap-ships-its-population
- **#16 Every export carries a verifiable address: poster, embed and OG go through the gate** — Shared UI Primitives; contexts: shared-provenance, shared-primitives, claim-verifier, landing-page, civicscore-leaderboard, graph-explorer, dashboard-instruments; advances: claim-verification-and-provenance/claim-ref-addressing ("claims leave the platform immediately … copied into articles, screenshots") + three-verdict-vocabulary; accountability-publishing-ethics/right of reply ("a permanent address per claim")

### T3 — One audited review door for every claim kind (bill verdicts, effort verdicts, identity, corrections, exposure)

Corroborated by 5 scout(s): Infrastructure & Observability, Civic Feed & Transparency, MP Profiles & Rankings, Data Ingestion, Financial Transparency.

- **#5 One audited review door for every claim kind, not just money ties** — Infrastructure & Observability; contexts: admin-control, graph-admin-data-routes, analysis-quality, testing-sentinel, db-repositories, review-console, lawwatch-forensics; advances: llm-forensic-gating / human-review-doors (steps 1-6); law lead-not-finding; law incident-anchored-doctrine
- **#6 Published correction ledger: the right of reply as a hash-chained record** — Civic Feed & Transparency; contexts: claim-verifier, civic-chronicle, schranka-notifications, shared-provenance, data-releases, data-layer, admin-console; advances: accountability-publishing-ethics — "right of reply and correction: a permanent address per claim, a published correction history, a visible route to contest"; law: incident-anchored-doctrine, provenance-or-nothing
- **#12 One review lane for person-level verdicts — effort claims get the money layer's gate** — MP Profiles & Rankings; contexts: contribution-scoring, civicscore-leaderboard, mp-profile, money-cases-review, analysis-quality, db-store; advances: accountability-publishing-ethics — lead-not-verdict-framing, the ladder of assertion (rung 3 vs rung 4 stated on the surface); politician-performance-scoring — structural-low-score-corrections "stored as a separate reviewable fact with pending-review provenance, written by an enrichment pass that a human gates"; law lead-not-finding
- **#14 Identity ledger — one cross-source person resolution table with confidence and a review door** — Data Ingestion; contexts: ingest-external-sources, ingest-psp-sources, money-analysis-triage, money-contract-ingest, czech-civic-data-parsing; advances: civic-source-adapters / entity-name-normalization ("a name match is never an identity claim by itself"); beneficial-ownership-resolution / name-to-identifier-discipline + officer-record-reading; llm-forensic-gating / human-review-doors; ROADMAP "Next" 1 ("entity-resolution service with confidence + human-review gate") made concrete
- **#17 Declared attribution perimeter + a downward exposure lane in the review console** — Financial Transparency; contexts: money-cases-review, money-ledger-graph, kg-analysis, shared-provenance; advances: public-money-attribution/attribution-perimeter; beneficial-ownership-resolution/ownership-chain-traversal ("downward from a person's known entities builds exposure breadth — a candidate set, never a money number"); laws lead-not-finding, non-partisan-symmetry, every-cap-ships-its-population

### T4 — Structured, sealed provenance per graph pass with a certified release

Corroborated by 3 scout(s): Infrastructure & Observability, Data Layer, Knowledge Graph Explorer.

- **#22 Structured graph provenance: source + run + pass on every kg row, sealed per pass** — Infrastructure & Observability; contexts: graph-admin-data-routes, analysis-quality, testing-sentinel, data-releases, graph-writers, kg-pipeline; advances: claim-verification-and-provenance / derivation-comparison (uniform/mixed/absent over populations); law provenance-or-nothing; law every-cap-ships-its-population; civic-knowledge-graphs
- **#25 Sealed graph passes with per-claim inclusion proofs** — Data Layer; contexts: db-store, db-repositories, graph-writers (scripts/data-analysis), data-releases (features/data-releases), provenance-receipts (features/shared/provenance), graph-permalink (features/graph), sentinel (lib/testing/sentinel); advances: claim-verification-and-provenance / claim-ref-addressing ("permanent machine-checkable address") + recomputation-receipts; civic-knowledge-graphs / destructive-rebuild-guard; laws: provenance-or-nothing, incident-anchored-doctrine (the 2026-08-05 restore-under-you)
- **#26 Sentinel-certified releases: the verdict travels with the manifest hash** — Infrastructure & Observability; contexts: testing-sentinel, db-hybrid-benchmarks, graph-admin-data-routes, data-releases, observability, admin-control; advances: claim-verification-and-provenance / derivation-comparison ("never ran rendered as passed"); law provenance-or-nothing; law every-cap-ships-its-population
- **#28 Derivation stamp for graph views: path-rule ref + whole-graph provenance aggregate** — Knowledge Graph Explorer; contexts: graph-explorer, claim-verifier, testing-sentinel, dashboard-instruments, kg-pipeline; advances: claim-verification-and-provenance / derivation-comparison (moved-by-basis; uniform/mixed/absent; store-vs-code sentinel); law: one-definition-one-import, missing-is-not-zero

### T5 — World time on the graph: dated edges, role-window alignment, club-at-vote

Corroborated by 3 scout(s): Data Layer, Financial Transparency, Voting & Legislation.

- **#23 World time on the graph: valid_from/valid_to written, read, and joined to votes** — Data Layer; contexts: db-store, db-repositories, kg-analysis, graph-writers (scripts/data-analysis), money-ledger (features/money), profile (features/profile), votetrack, volby; advances: civic-knowledge-graphs / civic-entity-ontology (temporal validity of a hop) + evidence-path-finding; conflict-of-interest-detection (tie held AT the vote); laws: missing-is-not-zero, non-partisan-symmetry
- **#24 Three money channels as dated graph edges (subsidy + donation join contracts)** — Financial Transparency; contexts: kg-analysis, money-ledger-graph, money-cases-review, ingest-external-sources, graph-writers; advances: public-money-attribution/citable-money-claims + conflict-of-interest-detection/temporal-alignment-of-money-and-role + statute-relevance-mapping; laws every-cap-ships-its-population, deterministic-code-owns-numbers
- **#27 Aligned reach: the role-window weight beside every raw money figure** — Financial Transparency; contexts: money-ledger-graph, money-cases-review, kg-analysis, graph-writers, mp-profile; advances: conflict-of-interest-detection/temporal-alignment-of-money-and-role ("alignment as weight, not only as gate"); beneficial-ownership-resolution/role-period-reconciliation; law missing-is-not-zero
- **#29 Club affiliation dated at the ballot: membership windows in the record** — Voting & Legislation; contexts: votetrack-ledger, votetrack-kompas, volby-election-mirror, kg-analysis, mp-profile; advances: parliamentary-data-modeling (membership-window-modeling, party-club-vs-electoral-list); roll-call-vote-analysis (club-line-and-rebellion); law: incident-anchored-doctrine, non-partisan-symmetry

### T6 — The index publishes its own history and reproduction: revision ledger, score-move events, served replay

Corroborated by 2 scout(s): MP Profiles & Rankings, Landing & Navigation.

- **#32 Formula revision ledger — published correction history for the index** — MP Profiles & Rankings; contexts: contribution-scoring, civicscore-leaderboard, mp-profile, mp-rankings-routes, db-store, civic-claim-gate; advances: politician-performance-scoring — golden path "Corrections are published, not absorbed"; formula-lineage-stamping; accountability-publishing-ethics "right of reply and correction — a published correction history"; laws provenance-or-nothing, incident-anchored-doctrine
- **#34 Score-move ledger — the recompute becomes a dated fact with a size** — Landing & Navigation; contexts: dashboard-state-graph, dashboard-instruments (cross: graph-writers, schranka, denik, civicscore); advances: claim-verification-and-provenance / derivation-comparison (carry `ref@pass` per version, compare across passes); accountability-publishing-ethics / severity-free-factual-framing (a move is a fact about the DATA, framed by formula, never a verdict about the person); laws: deterministic-code-owns-numbers, missing-is-not-zero (pre-epoch versions stay "size unknown")
- **#35 Served reproduction — the index replays itself on every read and ships its inputs** — MP Profiles & Rankings; contexts: civicscore-leaderboard, contribution-scoring, civic-claim-gate, data-releases, mp-profile; advances: politician-performance-scoring — "the moment one number in the ranking cannot be re-derived by an outsider, the whole product's claim to checkability collapses"; laws deterministic-code-owns-numbers, provenance-or-nothing; repo convention: `npm run sentinel` is the only path on which the recompute invariant executes (zebricek.md:126-128)

### T7 — The municipality as one graph subject across /rozpocty, /volby, /penize and the deník

Corroborated by 4 scout(s): Data Ingestion, Civic Feed & Transparency, Financial Transparency, Landing & Navigation.

- **#18 Buyer edge for Registr smluv contracts — the authority as a graph endpoint** — Data Ingestion; contexts: money-contract-ingest, money-analysis-triage, kg-pipeline; advances: public-money-attribution (both ends of a payment resolved through registry identifiers — the identifier-join contract in civic-knowledge-graphs); civic-entity-ontology (a new relation by deliberate amendment); law: non-partisan symmetry (an obec's contracts render for every obec, from the register)
- **#19 Obec-keyed deník: the municipality as a dated subject, lifting the `obec:` refusal** — Civic Feed & Transparency; contexts: civic-chronicle, schranka-notifications, civic-feeds-verification, claim-verifier, volby, tender-loop; advances: accountability-publishing-ethics / accountless-notification-privacy + severity-free-factual-framing; law: non-partisan-symmetry, every-cap-ships-its-population
- **#20 The municipality as a graph subject shared by /rozpocty, /volby and /penize** — Financial Transparency; contexts: budget-mirror, money-ledger-graph, kg-analysis, graph-writers, volby, data-ingestion; advances: state-budget-analysis/municipal-money-trail (clauses 1-4 + "live state over frozen batch"); civic-knowledge-graphs; laws one-definition-one-import, missing-is-not-zero, every-cap-ships-its-population
- **#21 Release-stamped entity sitemap — case files discoverable without per-request scans** — Landing & Navigation; contexts: app-shell, shell-navigation (cross: data-releases, graph-writers); advances: law non-partisan-symmetry ("the method covers the whole population or covers nothing" — all 207 MPs, all firms, all laws listed, or none); accountability-publishing-ethics / public-role-scope-limit; repo convention: base URL from request headers, never a guessed domain

### T8 — The vote→bill spine and what it unlocks (kompas themes, roll calls in the deník)

Corroborated by 2 scout(s): Voting & Legislation, Civic Feed & Transparency.

- **#30 The vote→bill spine: a `decides` edge from the agenda-item join** — Voting & Legislation; contexts: votetrack-ledger, lawwatch, volby-election-mirror, votetrack-kompas, voting-legislation-routes, kg-analysis, psp-ingest; advances: legislative-change-tracking (bill-fate-dating, committee-routing-reconstruction) + roll-call-vote-analysis (club-line-and-rebellion); law: provenance-or-nothing, every-cap-ships-its-population
- **#31 A deterministic theme layer for the dead Kompas, from committee routing** — Voting & Legislation; contexts: votetrack-kompas, votetrack-ledger, lawwatch, kg-analysis; advances: political-compass-from-votes (theme-balanced-drawing, divisive-vote-selection); law: deterministic-code-owns-numbers, missing-is-not-zero
- **#33 Roll calls in the deník: the chamber's own activity as dated rows** — Civic Feed & Transparency; contexts: civic-chronicle, schranka-notifications, civic-feeds-verification, votetrack-ledger, mp-profile; advances: roll-call-vote-analysis (measured deviation framing) + accountability-publishing-ethics / severity-free-factual-framing; law: deterministic-code-owns-numbers, non-partisan-symmetry

### T9 — Ingest as a diffed, event-emitting refresh protocol

Corroborated by 1 scout(s): Data Ingestion.

- **#37 Ingest run as a diffed, event-emitting, recompute-triggering protocol** — Data Ingestion; contexts: ingest-psp-sources, kg-pipeline, effort-case-loop, law-triage-batch, money-contract-ingest; advances: civic-source-adapters / fail-loud-schema-drift (snapshot diffing for versionless sources, epoch discipline); civic-knowledge-graphs / pass-based-incremental-enrichment; law: missing-is-not-zero (a row that left the dump is a fact, not a ghost)

### S — Singletons (one scout each)

Corroborated by 1 scout(s): Knowledge Graph Explorer, MP Profiles & Rankings, Data Ingestion, Infrastructure & Observability, Data Layer, Shared UI Primitives, Landing & Navigation, Voting & Legislation.

- **#36 Three-state gate and source on every graph edge (rejected is not verified)** — Knowledge Graph Explorer; contexts: graph-explorer, shared-provenance, claim-verifier, db-repositories, civic-kg-primitives; advances: civic-knowledge-graphs / per-claim-provenance-stamping + evidence-path-finding; claim-verification-and-provenance / gate-state-as-modifier; law: lead-not-finding, provenance-or-nothing
- **#38 Term-keyed chamber pass — per-term rankings, archived, for the whole career spine** — MP Profiles & Rankings; contexts: civicscore-leaderboard, contribution-scoring, mp-profile, mp-rankings-routes, psp-ingest, db-store; advances: politician-performance-scoring — weighted-component-index over every population the registry covers; formula-lineage-stamping per term; laws non-partisan-symmetry (whole population per term), missing-is-not-zero (coverage per term), every-cap-ships-its-population; repo convention: politicas.md §9 Phase 4 election-cycle hardening
- **#39 Statute version graph — e-Sbírka znění as nodes, real before/after on /zakony** — Data Ingestion; contexts: law-amends-analysis, law-triage-batch, kg-pipeline, ingest-external-sources; advances: legislative-change-tracking / amendment-instruction-grammar + bill-fate-dating; civic-knowledge-graphs / civic-entity-ontology (a new kind by deliberate amendment); law: provenance-or-nothing (the refusal in `docs/routes/zakony.md` is lifted only with verbatim `text-fragmentu`)
- **#40 Gate replay lane: one gate registry, re-run over every stored reader string** — Infrastructure & Observability; contexts: analysis-quality, testing-sentinel, db-hybrid-benchmarks, effort-loop-scripts, law-loop-scripts, money-loop-scripts; advances: llm-forensic-gating ("the whole stack must be re-runnable as one command over stored verdicts"), prose-register-gates, whole-artifact-invariants; law one-definition-one-import
- **#41 The procurement layer reaches the canvas: on-demand neighbourhoods as a citable view** — Knowledge Graph Explorer; contexts: graph-explorer, db-repositories, shared-provenance, money-cases-review; advances: civic-knowledge-graphs / forensic-view-filtering ("show less by default, never less than asked") + evidence-path-finding; law: every-cap-ships-its-population, missing-is-not-zero
- **#42 One Store contract, two drivers: a hosted-Postgres implementation proven by a shared conformance suite** — Data Layer; contexts: db-store, db-repositories, sentinel (lib/testing/sentinel), graph-writers (scripts/data-analysis), deploy (docs/deploy), data-releases; advances: repo convention: `lib/db/config.ts` "the move to a hosted Postgres is a new file, not a rewrite"; roadmap Next-1 "Postgres per politicas.md §5" made concrete; laws: one-definition-one-import (the store contract), incident-anchored-doctrine ("never ran must not render as passed")
- **#43 Registry-link contract sentinel: every built source link is probed against the entity it names** — Shared UI Primitives; contexts: civic-kg-primitives, testing-sentinel, data-quality (features/atlas), shared-provenance, graph-explorer, civic-chronicle; advances: _laws.md#provenance-or-nothing + #every-cap-ships-its-population + #incident-anchored-doctrine; claim-verification-and-provenance/derivation-comparison (the sentinel discipline: "never ran" must not read as "passed")
- **#44 Registry sector codes (CZ-NACE) on every company node, one sector definition** — Data Ingestion; contexts: ingest-external-sources, law-collision-analysis, money-analysis-triage, kg-pipeline; advances: conflict-of-interest-detection / statute-relevance-mapping; law: one-definition-one-import (the sector rule exists as a name heuristic + 18 hand overrides today); disclose-never-repair (no NACE → `null`, never a guessed sector)
- **#45 Reader-seeded slices — the velín as a lens over any neutral population** — Landing & Navigation; contexts: dashboard-state-graph, dashboard-instruments, landing-page (cross: volby, overeni); advances: claim-verification-and-provenance / claim-ref-addressing ("content-hash permalink family: address = view config + hash; format migrations decode both shapes"); law: non-partisan-symmetry (the reader picks the population by a registry key, the ordering rule inside stays metric-free and printed)
- **#46 Legislative substance ledger — first-signature, fate and interpellation kind beside the count** — MP Profiles & Rankings; contexts: contribution-scoring, mp-profile, civicscore-lens-duel, civicscore-leaderboard, psp-ingest; advances: politician-performance-scoring — golden path "narrow the claim": raw counts reward the gameable (mass-filed questions, co-signatures); laws missing-is-not-zero, every-cap-ships-its-population, deterministic-code-owns-numbers; NOT a formula change (annotate, never adjust)
- **#47 Statute collisions computed for the whole corpus, close-reads as the overlay** — Voting & Legislation; contexts: lawwatch, voting-legislation-routes, civic-chronicle, kg-analysis; advances: legislative-change-tracking (amendment-instruction-grammar, statute-collision-clustering); law: lead-not-finding, every-cap-ships-its-population, one-definition-one-import

## Cards (full §4.10 bodies)

### #1 Time-travelling claim gate: `asOf()` under /zdroj, /overeni and the schránka

`civic-feed-transparency#0` · theme T1 · corroboration 5 · size XL · effort 7 / impact 9 / risk 5 · result `unmeasurable` · gate `contract`
contexts: claim-verifier, schranka-notifications, civic-chronicle, shared-provenance, civicscore-leaderboard, data-layer
advances: claim-verification-and-provenance / derivation-comparison + recomputation-receipts; law: provenance-or-nothing, missing-is-not-zero

#### Summary
The store already keeps every superseded node and edge version (`kg_node_history` / `kg_edge_history`) and exposes `asOf()` time-travel reads, yet no reader-facing surface reads them: the gate can only say `moved` when the reader pastes the old value, the receipt shows one tense, and the schránka refuses to say how far a score moved because "the graph keeps no prior-value snapshots" — a sentence that stopped being true when the bitemporal layer landed.

#### Description
Today: `lib/db/pglite/repositories/kg.ts:14-35` documents that every replaced version is appended to history with a half-open `[recorded_at, superseded_at)` span, `supersedeUpsertChunk` (`kg.ts:116-144`) is the ONE upsert path (`kg.ts:208`), and `asOf(at)` (`kg.ts:67`, `:388-438`) returns `listKgNodes` / `listKgEdges` / `kgNeighbours` at an instant. Grep over `features/**` and `lib/**` finds no consumer of `asOf` except `scripts/data-analysis/kg-repair-orphans.ts:14,81`. Meanwhile `features/schranka/recomputeFact.ts:97-101` and `:178-184` state the graph "NEUCHOVÁVÁ předchozí hodnoty skóre" and emit a magnitude-free row; `features/overeni/verdict.ts:341-354` and `:389-399` derive `moved` only from `det.pasted.value` / `retrievedAt` (a bare ref with no pasted element can never be `moved`); `features/shared/provenance/getReceiptData.ts:57-80` reads the current edge only and answers `gone` with no "last seen" instant. The gap: the platform holds the evidence to answer "what did this address say on date D, and when did it change" and answers "unknown" instead.

Built: a server-only `features/shared/provenance/getClaimHistory.ts` that decodes a claim ref (edge/node/figure subject) and reads the version spine from `kg_node_history` / `kg_edge_history` (diffing only the prop the claim's metric reads — a wholesale-replaced `props` blob changes for unrelated passes). `/zdroj/<ref>` renders a "verze záznamu" ledger (recorded_at, superseded_at, gate state per version) and a `gone` receipt states the instant the record left the graph. `/overeni?ref=…&k=YYYY-MM-DD` re-derives via `asOf(k)` and `moved` shows both sides with both dates from the store, not from the pasted element. The schránka `recompute` delta gains a per-MP magnitude ONLY where the person node's history carries a prior `contribution_score` under a uniform prior pass (else the current "size unknown" sentence stays — missing is not zero). `/denik` gets a `recompute` row kind per MP dated `superseded_at`.

#### Flow
- Add `KgAsOfReads.getKgNodeVersions(id)` / `getKgEdgeVersions(src,rel,dst)` in `lib/db/pglite/repositories/kg.ts` (indexed on `kg_node_history_id_idx`, cap + `warnIfTruncated`).
- Pure `features/shared/provenance/claimHistory.ts`: version spine → per-metric value series; unit tests over a PGlite fixture that upserts twice (the `kg-bitemporal.test.ts` pattern).
- `/zdroj`: version ledger section; `gone` receipt cites `superseded_at`.
- `/overeni`: optional `k=` (as-of day) param; `figuraVerdict` gains `store`-sourced `citedValue`/`citedDate` when no element is pasted; three verdicts unchanged.
- `recomputeFact.ts`: read prior uniform pass from history via `summarizeContributionProvenance`; emit magnitude only under uniform prior; copy for the mixed case.
- `/denik` + feeds: `recompute` kind; `/schranka` `KIND_ORDER`/`KIND_NOUNS` extended in the same order.
- Docs: `docs/routes/{overeni,schranka,denik}.md` dated entries; memory note retiring "no prior snapshots".

#### Expected impact
Journalists and MPs get "what changed and when" for any citation without keeping their own screenshots; the schránka's most-read delta stops hedging. Measured by the share of `/overeni` `moved` verdicts that carry a store-sourced date (today 0). Could break: history rows written by `clearKg --reset` (`kg.ts:333-377`) archive whole-graph rebuilds as one instant, so a version ledger must label a reset as "rebuild", not "change".

#### Evaluation
Claim: user - a bare ref can be dated and diffed without the reader supplying the old value
Before: 0 features read `asOf`; `moved` requires `det.pasted` (`verdict.ts:389-391`); recompute delta magnitude = never (`recomputeFact.ts:99`)
After: MISSING INSTRUMENT — count of person-node history versions per MP on the live store (not measured; needs a read of `kg_node_history` grouped by id)
Method: probe - grep for `asOf(` consumers, read of the history DDL and the three refusing modules
Result: unmeasurable
Gate: contract

#### Evidence
- `lib/db/pglite/ddl.ts:313-338` — `kg_node_history` / `kg_edge_history`, append-only, indexed by `(id, recorded_at desc)`
- `lib/db/pglite/repositories/kg.ts:67`, `:116-144`, `:208`, `:388-438` — `asOf()` exists and every upsert supersedes
- `features/schranka/recomputeFact.ts:97-101`, `:178-184` — "graf NEUCHOVÁVÁ předchozí hodnoty" → magnitude refused
- `features/overeni/verdict.ts:341-354`, `:389-399` — `moved` only from pasted value/derivation
- `features/shared/provenance/getReceiptData.ts:57-80` — current-only read, `gone` has no instant
- `MEMORY.md` → recompute-replay-gate: a formula correction must replay the old formula; history gives that gate an independent witness

---

### #2 Sealed instrument readings — the velín as a dated series, not a live-only view

`landing-navigation#0` · theme T1 · corroboration 5 · size XL · effort 8 / impact 9 / risk 5 · result `unmeasurable` · gate `contract`
contexts: dashboard-instruments, dashboard-state-graph, landing-page, app-shell (cross: data-releases, graph-writers, overeni)
advances: claim-verification-and-provenance / recomputation-receipts + three-verdict-vocabulary ("moved — shown with both sides and both dates"); accountability-publishing-ethics / real-vs-illustrative-form-encoding + honest-empty-states; law: missing-is-not-zero

#### Summary
Every chamber reading the velín and the landing publish exists only as "today's re-derivation": the store keeps no sealed reading per data release, so (a) a store outage still degrades to the invented sample (`buildStateGraph()`, `CHAMBER_STATS`, `CHAMBER_TREND`), (b) the platform cannot show a real time series of its own headline figures, and (c) a stale exhibit can only say "the hash differs", never what the cited page actually said.

#### Description
Today `getDashboardData()` dates its output by the wall clock (`getDashboardData.ts:574` `builtOn = new Date()…`) and the page falls back to fiction when the loader is null: `DashboardPage.tsx:74` (`slice?.graph ?? buildStateGraph()` — invented MPs and firms), `:96` (`CHAMBER_TREND` — a fake six-quarter trend the file itself calls "no real analog"), `:421` (`MockStatTiles` over `lib/civic` `CHAMBER_STATS`), `:597` (`MockRankingLedger`). The landing refuses the mock (`LandingPage.tsx:16-17`, `:201-215`) but can only say "unavailable" — it has nothing dated to show instead. The exhibit (`exhibit.ts:195-197`) carries `urlHash`/`currentHash`/`fresh`; `ExhibitPage.tsx:254-257` renders a stale notice with two hashes, and `/overeni`'s `hashedVerdict` (`verdict.ts:198`) answers `moved` with only today's side — the registry's own technique says a `moved` verdict shows BOTH sides and both dates, which is impossible without the cited content.

Meanwhile the data layer already has the seal: `ingest_run.merkle_root` (`getAtlasData.ts:66-73`), a release `version`/`cutAt` (`features/data-releases/manifest.ts:46-51`), and a hash-chained ledger (`getDataReleasesData.ts` header). What is missing is a **reading table**: one row per data release holding the velín's projection (`DashboardData` minus `slice.sources`, i.e. exactly the `publicWire` shape), the landing's `LandingData`, the slice `graph`+`rule` and its `hashSlice`, and the release version that produced it — written by the same writer that cuts the release, never at request time.

"Built" looks like: (1) `/dashboard` renders today's re-derivation exactly as now, and when the store is down renders the LAST SEALED READING stamped "odečet z verze 2026.08.04 — úložiště momentálně nečitelné" in the ILLUSTRATIVE visual form the design already has (ochre edge), with the mock renderers deleted; (2) the histogram tile gains a real series — average index per release — replacing the fake `CHAMBER_TREND` with the one time series the platform can honestly claim; (3) `/dashboard/exponat/rez.<hash>` on a mismatch looks the hash up in the sealed readings and shows "what was cited (version V, date D) — what today derives (pass P, date D2)" side by side; the gate's `moved` verdict carries both.

#### Flow
- Define `InstrumentReading` (pure, `features/dashboard/reading.ts`): `{releaseVersion, cutAt, pass, provenance, tiles, top, histogram, slice: Pick<StateSlice,"graph"|"rule">, sliceHash, landing: LandingData}` — the wire shapes, imported not copied; canonical JSON + `contentHash` for a reading fingerprint.
- Add `instrument_reading` table (additive DDL via `npm run db:migrate`; one writer repository, append-only, keyed by release version) and have the release-cutting path in `scripts/data-analysis` write one reading per successful `kg-compute`/`kg-promote` run, sealed into the ledger chain like `merkle_root`.
- Loader: `getLatestSealedReading()` (server-only, `react.cache`); `getDashboardData()` unchanged; `app/dashboard/page.tsx` passes `{live, sealed}` and `DashboardPage` renders live → sealed-labelled → `DataUnavailable`, in that order; delete `buildStateGraph()`/`MockStatTiles`/`MockRankingLedger`/`CHAMBER_TREND` consumers from the velín (the `lib/civic` shapes stay for tests until their last consumer goes).
- Landing: `SpecimenUnavailable` and the ranking section render the sealed reading with its version, using the same `LiveDataNotice` wording pattern ("stav k", "vysazeno" precedent from the referendum embed).
- Series: `histogram` tile gets `avgByRelease[]` from the reading table (every point cites its release version); `ChamberChart` draws it instead of the mock trend.
- Exhibit + gate: `getExhibitData()` on `fresh === false` queries readings by `sliceHash` = `urlHash`; `ExhibitViewModel` grows `cited: {version, cutAt, graph, rule} | null`; `hashedVerdict` `moved` carries both sides; `/overeni` prints both dates.
- Tests: reading fingerprint determinism, fallback order, `publicWire.test.ts` pinned shape equality, `freshness.test.ts` unchanged; a sealed reading is NEVER served as "today" (the label is asserted in `messages.test.ts`).

#### Expected impact
Readers and citing newsrooms see a dated reading instead of a sample when the store is down, get the first honest time series of the index, and a stale citation shows what it said. Measured by: mock renderers referenced from `features/dashboard` → 0; exhibits answering `moved` that can show the cited side → from 0 to all whose hash exists in the reading table. What could break: a reading written from a half-recomputed store (`provenance.state !== "uniform"`) must be refused at write time or the series inherits the mixed-store lie.

#### Evaluation
Claim: quality | resilience — the front door stops degrading to fiction and the platform gains a citable series of its own headline figures
Before: 4 mock renderers on the velín's fallback path (`DashboardPage.tsx:74,96,421,597`); 0 stored readings; `moved` verdicts carry one side (`verdict.ts:198`)
After: 0 mock renderers; N readings = N successful releases; `moved` carries both sides whenever the cited hash was ever sealed — MISSING INSTRUMENT today: nothing records what a page said at release time
Method: probe — read the fallback branches, the exhibit view model, the gate verdict, and the release/ledger seal path
Result: unmeasurable
Gate: contract — new table + new writer in the release path; DDL change goes through `db:migrate`

#### Evidence
- `features/dashboard/DashboardPage.tsx:74` `const graph = useMemo(() => slice?.graph ?? buildStateGraph(), [slice]);`
- `features/dashboard/DashboardPage.tsx:96` `CHAMBER_TREND.map(...)` — fallback trend; `lib/civic/data.ts:447` `CHAMBER_TREND = [54.2, …]`
- `features/dashboard/DashboardPage.tsx:421` `<MockStatTiles />`; `:597` `<MockRankingLedger />`; `components/MockStatTiles.tsx:9` imports `CHAMBER_STATS` from `lib/civic/data`
- `features/dashboard/getDashboardData.ts:574` `const builtOn = new Date().toISOString().slice(0, 10);`
- `features/dashboard/exhibit.ts:195-197` `currentHash`, `fresh: boolean` — no cited content
- `features/dashboard/ExhibitPage.tsx:254-257` stale notice = two hashes
- `features/overeni/verdict.ts:198` `lookup.view.fresh ? verified : moved` — one side only
- `features/data-releases/manifest.ts:46-51` `version`/`cutAt` per release; `features/atlas/getAtlasData.ts:66-73` `merkle_root` sealed runs
- `features/landing/LandingPage.tsx:201-215` unavailable branch — nothing dated to render

---

### #3 Receipts for derived figures: `claim:` refs resolve at /zdroj with their inputs

`shared-ui-primitives#1` · theme T2 · corroboration 5 · size XL · effort 8 / impact 9 / risk 4 · result `better` · gate `contract`
contexts: shared-provenance, claim-verifier, money-cases-review, money-ledger-graph, civicscore-leaderboard, contribution-scoring, lawwatch, mp-profile
advances: claim-verification-and-provenance/recomputation-receipts ("the receipt is an addressed computation, not a stored document") + derivation-comparison; _laws.md#deterministic-code-owns-numbers, #every-cap-ships-its-population

#### Summary
The product has two claim grammars and only one of them has a receipt: a graph record (`h.`/`u.` → `/zdroj/<ref>`) gets a page showing provenance, gate and registries, while a derived FIGURE (`claim:<dataset>:<metric>:<subject>`, minted by `CitableNumber` in 12 files) can only be re-verified as a bare value at `/overeni` — nothing shows what rows it summed, under which formula, over which population.

#### Description
`decodeClaimRef` (`features/shared/provenance/claimRef.ts:71-87`) accepts exactly `u.<b64>` and `h.<b64>.<b64>.<b64>`; a `claim:` ref answers `null` → 404. `getReceiptData` (`getReceiptData.ts:44-104`) reads one node or one edge + `review_audit`. The value-claim side lives in `lib/claims/claim.ts` (`Claim.derivation` at `:69`, `reviewStatus` `:55`) and is verified by `features/overeni/liveFigures.ts`, which forwards to the owning loaders (`getMoneyMpDetail`, `getCompanyDetail`, `getLeaderboardData`, `getLawData`; `:28-44`) and by `figuraVerdict` (`verdict.ts:104-121`) which compares value and derivation only. So when a newsroom pastes "23 653 407 340,55 Kč" the gate says verified/moved but cannot show the 1 000+ `supplies` edges, the `moneyReachesCompany` attribution rule (memory: supplies-is-not-attribution), the `KG_READ_CAP` that bounded the read, or the `pending_review` share of the ties underneath. That is the "footnote that outlives its number" failure mode: the value is re-derived, its derivation is not shown.

"Built" = `/zdroj/<claim-ref>` accepts the `claim:` family (refDetect already knows it: `refDetect.ts:7,66`) and renders a **figure receipt**: the value as today's loader mints it; `derivation` (`CONTRIBUTION_FORMULA_REF`, `kg-pass:N`) and the methodology link; the INPUT ROW SET as a capped, population-stated list (n edges of N, ordered by `byListOrder`, with the cap printed); the population's gate mix (verified / pending / rejected counts — the "uniform/mixed/absent" idea applied to the gate); and each input edge linking to its own `h.` receipt. The receipt is derived per request by the SAME owning loader (extend `IssuedFigure` with an optional `inputs: {rows, total, cap, gateMix}` that each loader fills from what it already read — `getMoneyMpDetail` already holds the ties; `getLeaderboardData` already holds the component inputs per MP). ClaimReview JSON-LD stays gated by `toClaimReviewJsonLd` (`receipt.ts:348-353`): a figure over a population with any non-verified input emits nothing.

#### Flow
- Extend `ClaimRef` union with `{kind: "figure", ref: ClaimRefParts}` and teach `decodeClaimRef`/`encodeClaimRef` the `claim:` prefix (append-only: existing `h.`/`u.` addresses unchanged; pin in `claimRef.test.ts`).
- Add `FigureReceipt` to `receipt.ts` (`kind: "figure"`) with `value`, `unit`, `derivation`, `methodologyUrl`, `inputs` (capped list + `total` + `cap` + `gateMix`), `computedAt`; a pure `deriveFigureReceipt(issued: IssuedFigure)`.
- Extend `IssuedFigure` in `liveFigures.ts` with optional `inputs`; fill it in the money loaders first (ties are already in memory), then contribution (six components + committee seats), then law.
- `getReceiptData` routes `figure` refs through `liveFigures` (registry-then-live order preserved; store down → `unavailable`).
- `ReceiptBody` gains the figure layout: value, formula line, inputs table with population line, gate-mix line, link to `/overeni?ref=`.
- `CitableNumber` (`lib/claims/CitableNumber.tsx`) wraps the `<data>` in the `/zdroj/<ref>` link so the 12 files' figures become clickable receipts without per-site work.
- `liveFiguresRoundTrip.test.ts` grows: every metric constant must yield a receipt with `inputs.total ≥ inputs.rows.length` and a printed cap.

#### Expected impact
A journalist quoting an MP total can open the sum and see the rows, the attribution rule and how much of it a human has confirmed; the `/overeni` `moved/basis` verdict finally has a page to point at for "what the basis is". Measured: number of metric families with a resolvable receipt (0 of 3 today → 3), and the share of figures whose receipt states its population and cap. What could break: the figure receipt is a heavier read than an edge receipt (a company reach can be ~784 edges) — it must reuse the loader's already-materialised rows, never issue a second `kgNeighbours` (memory: default limit 500 truncation).

#### Evaluation
Claim: quality — a derived figure's receipt shows inputs, formula and gate mix, not only the value
Before: `/zdroj/claim:…` → `decodeClaimRef` returns null → 404 (`claimRef.ts:86`); `/overeni` verdict for figura carries `figure.value`, `citedValue`, `citedDerivation` only (`verdict.ts:57-79`); 12 files render `CitableNumber` with no receipt link
After: `/zdroj/<claim-ref>` renders a figure receipt for all metric constants enumerated by `liveFiguresRoundTrip.test.ts`; the round-trip test asserts population + cap are present
Method: probe — read `claimRef.ts` decoder, `getReceiptData.ts` branches, `verdict.ts` figura union, grep of `CitableNumber` consumers (12 files)
Result: better
Gate: contract — `ClaimRef` union and `IssuedFigure` grow additively; address space stays append-only

#### Evidence
- `features/shared/provenance/claimRef.ts:71-87` — only `u`/`h` prefixes decode; `:86 return null;`
- `features/shared/provenance/getReceiptData.ts:44-104` — node/edge only; `:67` `store.kgNeighbours({ id: ref.src, rels: [ref.rel], limit: KG_READ_CAP })`
- `features/overeni/refDetect.ts:7` "figura — claim-ref (`claim:<dataset>:<metric>[:<subject>]`, lib/claims)"; `:66` figura family
- `features/overeni/liveFigures.ts:11-12,28-44` — re-derivation through the owning loaders
- `features/overeni/verdict.ts:104-121` `figuraVerdict` compares `value` and `derivation`; no inputs
- `lib/claims/claim.ts:55,69` `reviewStatus?`, `derivation?`
- `CitableNumber` consumers: `features/money/{MpCaseFilePage,CompanyCaseFilePage,components/TiesLedger}.tsx`, `features/civicscore/{KrajPage,components/LeaderboardTable}.tsx`, `features/lawwatch/{StatuteDossierPage,components/BillDetail,components/ForensicIndexSection}.tsx`, `features/profile/components/MoneySection.tsx`, `features/overeni/OvereniPage.tsx`, `features/shared/components/AnimatedScore.tsx`, `app/svedectvi/page.tsx`
- `docs/routes/overeni.md` — "the MP total (23 570 594 009,66) and the company reach verify the same way, and all of them carry `pending` because all 211 ties do" — the gate mix exists as a fact, not on any receipt

---

### #4 Citation registry: every SourceNote keyed to a declared source with a live as-of

`shared-ui-primitives#0` · theme T2 · corroboration 5 · size XL · effort 8 / impact 9 / risk 5 · result `better` · gate `contract`
contexts: shared-primitives, shared-provenance, civic-kg-primitives, data-quality (features/atlas via getAtlasData), data-releases (features/data-releases), civicscore-leaderboard, money-ledger-graph, lawwatch, votetrack-ledger, budget-mirror, volby-election-mirror, landing-page
advances: _laws.md#one-definition-one-import + #provenance-or-nothing; claim-verification-and-provenance/recomputation-receipts ("a cached receipt is a claim about the past wearing the tense of the present"); accountability-publishing-ethics/real-vs-illustrative-form-encoding

#### Summary
The brand rule is enforced as "a file contains a `<SourceNote>`" (lint) and the note's text is a free string — 427 call sites, 311 of them `t("…")` catalog strings, with "psp.cz" spelled 128 different ways in `messages/cs.json` and no join from any citation to the twelve declared sources in `lib/analysis/atlas.ts` or to `ingest_run.finished_at`. A citation therefore states a *cadence* ("denně", "7 dní") that is a promise, never the measured day the data actually holds.

#### Description
Today `SourceNote` (`features/shared/components/SourceNote.tsx:83-95`) takes `children: ReactNode`, measures their length to pick label/sentence typography and renders them verbatim; the only structured hook is the opt-in `provenance?: ProvenanceReceipt` (`:81`), which is used by **0 of 427** call sites (measured by a node walk over `app/**` + `features/**`: total 427, withProv 0, t-calls 311, expressions 92, literals 24). The lint rule `packages/eslint-plugin-civic-transparency/rules/require-source-citation.cjs` checks file-scoped *presence* of a satisfier element, not what it says. Meanwhile the store carries the truth the citation should print: `ingest_run(source, finished_at, source_last_modified, rows_written)` (`lib/db/pglite/ddl.ts:14-25`), the declared per-source cadence `SOURCE_CADENCE_DAYS` (`lib/analysis/atlas.ts:58-69`) and the atlas scorecard (`SOURCE_SPECS` at `:193-208`, consumed only by `features/atlas/getAtlasData.ts`, `features/landing/components/DataSources.tsx`, `app/atlas/atlas.json/route.ts`). The gap: the citation and the freshness/quality record are two systems that never meet, so the same source can score "zastaralé" on `/atlas` while every `SourceNote` on `/hlasovani` still reads "psp.cz — jmenovitá hlasování, denně".

"Built" = a `lib/sources/registry.ts` (pure, one definition) declaring each source id from `SOURCE_SPECS` with its proper name, registry host, license note and cadence key; a server-side `sourceStamps()` read (one `ingest_run` query per request, `react.cache`d) returning `{sourceId → {asOf, runStatus, staleness}}`; a `<SourceNote source="psp-hlasovani" …>` form that renders the declared name + the MEASURED as-of ("stav k 28. 8. 2026") and, when `stalenessOf()` says stale, carries the ochre form marker the same way `StatTile variant="illustrative"` does (`StatTile.tsx:39-45`) — the staleness must survive a skim, not just a caption. Unknown source id → the note renders the literal and a `bez registru` badge, never silently. The lint rule grows a second trigger: a `SourceNote` whose text names a host that exists in the registry but carries no `source=` is a warning that ratchets like `require-source-citation` did. The 12 atlas cards and every citation then cite ONE vocabulary, and `/atlas` becomes the place a reader lands from any citation (card link = `/atlas#<sourceId>`).

#### Flow
- Extract `SOURCE_SPECS` + `SOURCE_CADENCE_DAYS` into `lib/sources/registry.ts`; `lib/analysis/atlas.ts` imports them (no behaviour change; pinned by existing atlas tests).
- Add `sourceStamps()` server loader (`lib/db/`-level, `reportLoaderFailure` on null) reading the latest `finished` `ingest_run` per source; expose through the page-level loaders' props (the `/hlasovani` template pattern), NOT via a client fetch.
- Extend `SourceNote` additively: `source?: SourceId`, `stamp?: SourceStamp | null`; when both present render "‹name› · stav k ‹date›" and the stale form marker; `stamp === null` renders "‹name› · stav neznámý" (missing is not zero).
- Migrate the 311 catalog citations by source family (votes → money → law → budget), replacing free-text hosts with `source=`; keep the descriptive tail ("jmenovitá hlasování") as `children`.
- Add the lint trigger (`custom/require-source-registry`) at `warn` under `features/**`, measured count published in `eslint.config.mjs` like the 11 citation violations were.
- `/atlas` cards get stable anchors; every stamped citation links to its card.

#### Expected impact
Readers and newsrooms see on every figure the day the underlying dataset was last ingested, not the day the page rendered; the sentinel's freshness finding (`lib/testing/sentinel/invariants.ts` header, "zastaralé") stops being a hidden operator fact and reaches the citation. Measured by the count of citations carrying a measured as-of (0 today) and by the number of distinct host spellings in `messages/cs.json` (≥15 for psp.cz today → 1). What could break: a `SourceNote` string change touches ~240 files of snapshot/i18n parity tests (`messages.test.ts` per feature) — the migration must be per-family with the cs/en key-parity tests as the gate.

#### Evaluation
Claim: quality — a citation states a measured as-of instead of a declared cadence
Before: 427 SourceNote sites, 0 with structured provenance, 0 joined to `ingest_run`; "psp.cz" appears in 128 catalog strings in ≥15 spellings; `/atlas` freshness is reachable only from `/atlas` and the landing panel
After: every migrated citation carries `source=` + as-of; the MISSING INSTRUMENT is a lint count for citations-without-registry-key (analogous to the burned-down `require-source-citation` count)
Method: probe — node walk over all `.tsx` in `app/`+`features/` counting `<SourceNote>` forms; grep of `messages/cs.json`; read of atlas.ts specs and ddl
Result: better
Gate: contract — `SourceNote` gains props additively; the lint trigger is a new rule and needs the ratchet policy the repo already uses

#### Evidence
- `features/shared/components/SourceNote.tsx:81` `provenance?: ProvenanceReceipt;` — `:83` `const mode = as ?? (textLength(children) > LABEL_MAX_CHARS …` — `:94` capsule only when `provenance` given
- node walk result: `{ files: 240, total: 427, withProv: 0, tcalls: 311, expr: 92, literalDistinct: 24 }`
- `lib/analysis/atlas.ts:58-69` `SOURCE_CADENCE_DAYS` (3 entries) and `:193-208` twelve `SOURCE_SPECS` (`psp-poslanci`, `psp-hlasovani`, `pumper-psp-opendata`, `psp-tisky`, `psp-interp`, `psp-steno`, `psp-tisky-law`, `kiosek-uredni-deska`, `smlouvy-gov-cz`, `dataor-justice-cz`, `monitor-statni-pokladna`, `volby-ps2025-candidates`)
- `lib/db/pglite/ddl.ts:14-25` `ingest_run(source, started_at, finished_at, status, source_url, source_last_modified, rows_written)`
- `grep -c "psp.cz" messages/cs.json` → 128; top spellings: `psp.cz — jmenovitá hlasování (vote_event …` ×5, `psp.cz · zveřejněné váhy {weights}` ×3, `psp.cz ⋈ ares ⋈ registr smluv ⋈ e-sbírk…` ×2, `psp.cz — omluvy (omluvy.unl…` ×2 …
- `packages/eslint-plugin-civic-transparency/rules/require-source-citation.cjs:70-77` `SATISFIER_ELEMENTS` = element names only; no check of content
- `docs/routes/atlas.md` "Unrated is structurally never zero" — the registry inherits that rule for a citation whose source has no run

---

### #5 One audited review door for every claim kind, not just money ties

`infrastructure-observability#0` · theme T3 · corroboration 5 · size XL · effort 8 / impact 9 / risk 5 · result `better` · gate `irreversible`
contexts: admin-control, graph-admin-data-routes, analysis-quality, testing-sentinel, db-repositories, review-console, lawwatch-forensics
advances: llm-forensic-gating / human-review-doors (steps 1-6); law lead-not-finding; law incident-anchored-doctrine

#### Summary
The repo has exactly ONE human review door (`setTieReviewState` on `linked_to` edges, audit-chained) while three other machine-produced claim kinds — bill forensic verdicts, tripwire candidates, money web-leads — have no writer at all, so their `pending_review` is permanent by construction and nothing can ever promote, reject or even record a human look at them.

#### Description
Today: `lib/db/pglite/repositories/review.ts:1,22,48` is "the human-review write path for `linked_to` ties" — the only writer of review state and the only appender to the tamper-evident chain (`ledger.ts:3`); `review_audit` itself is tie-shaped (`lib/db/pglite/ddl.ts:253-265`: `src/rel/dst`, decision in `confirm|reject|needs-more`). Bill forensic verdicts are written `pending_review` by `kg-forensics.ts` (`lib/analysis/law-verdict.ts:12-14`) and read by `features/lawwatch/getLawData.ts:292,340`, which even DEFAULTS an absent state to `"pending_review"` — grep shows no writer of `forensic_review_state` anywhere outside the triage readers (`scripts/case-loops/law/triage-core.ts:206`). `/zakony` prints `pending_review · 141` beside an "ungated" note (`ForensicIndexSection.tsx:21-27`): the review console for bills is a sentence, not a door. Tripwire candidates are "KANDIDÁT PRO LIDSKOU REVIZI, nikdy zjištění" (`lib/analysis/tripwires.ts:9-15`) and deep-link to `/penize/kontrola#tie-…` (`TripwireSection.tsx:24,79`) — but an operator who examines one and finds nothing can record NOTHING, so the same candidate returns on every read (`tripwires.ts:6-7`: derived per read, no state). Money leads (`getAdminData.ts:456-481`) are JSON payloads on disk with no state at all. The one append-only operator journal that exists, `driveLog`, admits it is "VLASTNÍ řetěz žurnálu — Merkle pečeť revizního ledgeru se na ně NEVZTAHUJE" (`driveState.ts:12-16`). The registry's human-review-doors technique is explicit: one code path is the only writer of review state, audit-append then update in one transaction, reject terminal, and "if a surface renders a model's interpretation about a person to the public, it takes the door" — 141 bill verdicts render on `/zakony` today with no door.

Built: `review_audit` generalised to a claim-kind discriminator (`subject_kind: tie | bill_verdict | tripwire | lead`, subject id = the existing stable address — edge triple, `bill:tisk:N`, `tripwireCandidateId`, `leadId`) with ONE `setReviewState(subject, decision, reviewer, note)` writer that appends the chained audit row and then applies the subject-specific prop write (edge props for ties, `forensic_review_state` for bills, a `tripwire_review` prop on the tie edge keyed by candidate id, a lead sidecar). The sentinel's `audit-chain` check then covers all four populations by construction. `/admin`'s "Fronta revize" becomes a real queue (population + decided/pending per kind), `/zakony` shows a verified/rejected verdict the way `/penize` shows a tie, and `/overeni` receipts for bill claims carry a gate state that can actually change.

#### Flow
- Add `subject_kind` + generic `subject_id` columns to `review_audit` via `npm run db:migrate` (additive; existing tie rows backfilled as `tie` with their triple — no chain rewrite, prior hashes untouched, new rows hash the wider payload under a new domain tag `politicas-audit-v2`)
- Extract the transaction skeleton of `setTieReviewState` into a `ReviewRepository.setReviewState()` dispatcher; ties keep their exact write; add `bill_verdict` (writes `forensic_review_state` + history row), `tripwire` (records look/dismiss/escalate against the candidate id — a dismissal is a decision, not a deletion), `lead`
- Reviewer identity: reuse `REVIEWER_TOKEN` (kontrola) for ties and bills; `ADMIN_TOKEN` operator for tripwires/leads; both go through the same door, never a second endpoint
- Sentinel: `audit-chain` reads `ReviewAuditCounts` per kind and ships the four populations; add `review-coverage` check = for each kind, pending / decided / total (never a rate without its denominator)
- Surfaces: `/admin` ReviewHubSection turns its four read-only counters into queues with decision buttons (same two-phase confirm pattern as `LoopMissionControl`); `/zakony` BillDetail shows the gate state from the store instead of the defaulted string; `getLawData.ts:340` stops defaulting to `pending_review` — an absent state renders as absent
- `gateVocabulary.ts` grows the bill/tripwire kinds; `/overeni` verdict for a bill claim carries the modifier; ClaimReview JSON-LD stays gated on `verified` only
- Tests: chain verifies across mixed kinds; reject is terminal per kind; a tripwire dismissal is invalidated when the candidate's evidence fingerprint changes (same rule as `loopState` alert acks, `loopState.ts:217-219`)

#### Expected impact
Reviewers get one queue over four claim kinds instead of one door plus three permanent "pending" labels; readers of `/zakony` see verdicts that can actually be confirmed or refused; the sentinel's chain finally vouches for every machine claim that reaches a reader. Measured by: number of claim kinds with a writer (1 → 4) and the fraction of rendered `pending_review` labels that can change. What could break: the `review_audit` hash preimage — must be a new domain tag, never an edit of the old rows.

#### Evaluation
Claim: quality — every reader-facing machine claim gains an auditable human decision path
Before: 1 review writer (`review.ts:48`), 211/211 ties pending, 141/141 bill verdicts pending with no writer, tripwire candidates re-derived with zero recorded dispositions
After: 4 claim kinds behind one chained writer; `/admin` queue shows decided/pending/total per kind; sentinel `audit-chain` covers all four
Method: probe — grep for writers of `forensic_review_state` (0 outside triage readers), read `review.ts`, `ddl.ts`, `tripwires.ts`, `TripwireSection.tsx`
Result: better
Gate: irreversible

#### Evidence
- `lib/db/pglite/repositories/review.ts:1` "the human-review write path for `linked_to` ties"; `:22` `const LINKED_TO = "linked_to"`; `:48` `setTieReviewState(src, dst, decision, reviewer, note)`
- `lib/db/pglite/ddl.ts:253-265` review_audit shape is `src, rel, dst, decision check in ('confirm','reject','needs-more')`
- `lib/analysis/law-verdict.ts:12-14` "Findings that pass are written pending_review — a lead for a human, never a published verdict."
- `features/lawwatch/getLawData.ts:340` `reviewState: state ?? "pending_review"`; `scripts/case-loops/law/triage-core.ts:206` is the only other reader; no writer found
- `features/lawwatch/components/ForensicIndexSection.tsx:21-27` the `pending_review · 141` line next to the ungated note
- `lib/analysis/tripwires.ts:6-7` "NIC SE NEZAPISUJE — kandidáti se odvozují znovu při každém čtení"; `:9-15` candidate-not-finding discipline
- `features/admin/components/TripwireSection.tsx:24,79` links out to `/penize/kontrola#tie-…`, no local action
- `features/admin/loops/driveState.ts:12-16` the operator journal's own admission that the Merkle ledger does not cover it
- `features/admin/getAdminData.ts:456-481` leads read from `payloads/batch-*-lead-*.json`, no state

---

### #6 Published correction ledger: the right of reply as a hash-chained record

`civic-feed-transparency#4` · theme T3 · corroboration 5 · size XL · effort 8 / impact 9 / risk 6 · result `unmeasurable` · gate `irreversible`
contexts: claim-verifier, civic-chronicle, schranka-notifications, shared-provenance, data-releases, data-layer, admin-console
advances: accountability-publishing-ethics — "right of reply and correction: a permanent address per claim, a published correction history, a visible route to contest"; law: incident-anchored-doctrine, provenance-or-nothing

#### Summary
A named MP or firm who believes a politicas figure is wrong has a `mailto:` link that renders only when an env var is set, and the platform records nothing about the complaint or its outcome; the only chained, public decision log is `review_audit`, which covers tie confirmations and nothing else. The ethics standard the repo subscribes to names a published correction history as a duty — it does not exist.

#### Description
Today: `features/shared/components/ReportClaimLink.tsx:11-14`, `:33-34` — env-gated `mailto:` composed on click, no server-side trace; `lib/db/pglite/ddl.ts:253-262` — `review_audit` is the sole append-only log (decision ∈ confirm/reject/needs-more over an edge triple), chained by `lib/db/pglite/repositories/review.ts:96-116` under the `politicas-audit-v1` domain (`lib/db/pglite/ledger.ts:5-7`, `:22-27`: a serialization change "requires a new domain-separation tag, never an edit"); `features/dukazy/deriveFeed.ts:85` — the public bulletin knows `"tie" | "forensic"` entries only; `/zdroj` and `/overeni` have no notion of "this address has a filed correction". The `recompute-invalidates-prose` and `recompute-replay-gate` memories describe corrections that DID happen (pass 42 moved 33 scores, 30 dossier sentences stale) with no public record of them beyond route docs.

Built: a `correction` table in the same tamper-evident ledger (own domain tag `politicas-correction-v1`, `chain_pos`/`prev_hash`/`row_hash`), one row per adjudicated report: the claim ref (decoded and refused on mismatch, the `edgeClaimRef` precedent), `kind` ∈ `withdrawn | corrected | upheld`, the value before/after or null, the reason, who decided, `decided_at`, and a pointer to the incident doc. Written ONLY from `/admin` (the existing ADMIN_TOKEN lane) — a reader's report becomes a `lens`-style anonymous submission (`ddl.ts:375-384` pattern: no identity), queued for the same human gate. Renders: `/dukazy` gains a `correction` entry kind and the feeds carry it; `/denik` a `correction` row keyed by the claim's entities; `/schranka` a `correction` delta ("a claim about a followed entity was corrected"); `/zdroj/<ref>` a banner above the receipt with the chained row; `/overeni` a verdict MODIFIER (never a fourth verdict): "adresa nese opravu ze dne D"; `/data` prints the correction chain head beside the review chain head. `upheld` renders with the same weight as `corrected` — a contested-and-held figure is a finding too.

#### Flow
- `db:migrate`: `correction` + `correction_report` tables; `ledger.ts` new domain tag + `verifyCorrectionChain` (pure, reused by `/data`'s integrity read).
- `lib/db/pglite/repositories/corrections.ts` — the ONLY writer; PGlite test writes two rows and verifies the chain (the `chainRow.test.ts` pattern).
- `/admin`: report queue + decision form; `ReportClaimLink` posts the report (server action, no identity, `mailto:` kept as fallback when the store is down).
- `features/dukazy/deriveFeed.ts`: `correction` kind; `readReviewAudit.ts` sibling `readCorrections.ts` with cap + `truncated`.
- `/denik`, `/schranka` kinds + vocabularies in shared order; `/zdroj` banner; `verdict.ts` modifier field + copy keys in both catalogs through the language gate.
- `/data`: manifest `integrity.correctionChain`; `feedIndex` note.
- Docs: `docs/routes/{dukazy→denik,schranka,overeni,data}.md`; incident anchor per correction in `docs/`.

#### Expected impact
A named subject can contest a figure and see the outcome published at the claim's own address; readers who cited it are notified through the inbox they already use. Measured by corrections filed vs adjudicated per month and by the count of `/zdroj` receipts carrying a chained correction (today 0, and unmeasurable). Could break: a correction that changes a stored value must go through the recompute replay gate — the ledger records the decision; it never edits the graph itself.

#### Evaluation
Claim: quality - every published correction has a permanent, chained, machine-checkable address
Before: 0 correction records anywhere; the contest route is `mailto:` gated on `NEXT_PUBLIC_CONTACT_EMAIL` (`ReportClaimLink.tsx:33-34`); the bulletin knows two entry kinds (`deriveFeed.ts:85`)
After: MISSING INSTRUMENT — the count of corrections historically warranted (pass-42: 33 moved scores, 30 stale sentences per MEMORY) vs the count publicly recorded (0)
Method: probe - read of ReportClaimLink, review_audit DDL + chain writer, ledger domain contract, dukazy entry kinds, MEMORY incident notes
Result: unmeasurable
Gate: irreversible

#### Evidence
- `features/shared/components/ReportClaimLink.tsx:11-14`, `:33-34`, `:36-46` — env-gated `mailto:`, nothing recorded
- `lib/db/pglite/ddl.ts:253-262` — `review_audit` decisions limited to edge triples
- `lib/db/pglite/repositories/review.ts:96-116` — chain append lives only here
- `lib/db/pglite/ledger.ts:5-7`, `:22-27` — versioned domain tags; new domain, never an edit
- `lib/db/pglite/ddl.ts:375-384` — `lens_submission`: the identity-free submission precedent
- `features/dukazy/deriveFeed.ts:85` — `kind: "tie" | "forensic"`
- `MEMORY.md` → recompute-invalidates-prose, recompute-replay-gate — corrections occurred with no public ledger

---

### #7 Chamber readings as a claim family — every velín tile citable, resolved live

`landing-navigation#2` · theme T2 · corroboration 5 · size L · effort 5 / impact 8 / risk 3 · result `better` · gate `none`
contexts: dashboard-instruments, landing-page (cross: overeni, shared-provenance / lib/claims, civicscore, money)
advances: claim-verification-and-provenance / derivation-comparison ("for a composite, the citation's derivation comes from the population aggregate") + claim-ref-addressing; laws: provenance-or-nothing, one-definition-one-import; ROADMAP "Next" 3 (newsroom API) made concrete as a claims endpoint

#### Summary
The four figures the velín makes the largest on the page — chamber attendance mean (with its population), attributable CZK, the forensic counts, the average index — mint no claim-ref; and the one chamber figure the claim registry DOES know ("prumerna-dochazka") resolves against the `lib/civic` sample tile dated 2026-07-30, which the gate consults BEFORE the live store, so a pasted attendance citation is verified against a mock.

#### Description
`lib/claims/registry.ts:22-23` imports `CHAMBER_STATS` / `CHAMBER_SUMMARY` from `lib/civic`; `:50` `SAMPLE_RETRIEVED_AT = "2026-07-30"`; `:87-106` mints `prumerna-dochazka` from the mock tile string via `czechTileNumber`, `issuedAt: "/svedectvi"`. `features/overeni/liveFigures.ts:66-70` limits live resolution to `MONEY_CLAIM_DATASET`, `SCORE_CLAIM_DATASET`, `LAW_CLAIM_DATASET`, and `docs/routes/overeni.md:13-15` states the order is registry-then-live. The velín's real readings are computed in `getDashboardData.ts:532-542` (attendance mean over `counted`/`total`, nulls excluded) and projected from owners (`moneyHeadline` `:303-322`, `lawHeadline` `:343-356`), yet `grep CitableNumber features/dashboard` returns nothing — no tile is a claim. `MONEY_METRIC` (`liveFigures.ts:95-102`) has per-MP and per-company metrics but no chamber total; `LandingPage.tsx:135-141` mints only the per-MP composite; `HeroStory` prints `count` and the hemicycle from `scores` with a SourceNote and no ref.

"Built": `features/dashboard/readingClaims.ts` (pure; the `scoreClaim.ts` discipline) mints chamber-wide claims — `psp.cz — omluvy : prumerna-dochazka` (value + `population: counted/total` in the claim body), `registr smluv ⋈ ares : pripsatelne-kc` (with `isFloor` as a modifier, never a different number), `lawwatch : forenzni-posudky`, `psp.cz — příspěvkový index : prumerny-index` — each with `derivation` from the POPULATION aggregate (`provenance.state === "uniform"` → `ref@pass`, else omitted, per the technique). Tiles render through `<CitableNumber>`; `liveFigures.ts` resolves them by calling `getDashboardData()` (the owner); the registry's sample attendance figure is retired (or its dataset renamed so it can never shadow a live one), and `/overeni` verifies the front door's numbers against the front door's loader. The same pure module feeds `GET /dashboard/odecet.json`: the chamber reading as `{claims[], derivation, population, builtOn}` — the first concrete endpoint of the "API pro redakce".

#### Flow
- `readingClaims.ts` + test: metric slugs, dataset constants imported from the owners' vocabularies (`COMPONENT_DEFS` source string for omluvy, `MONEY_CLAIM_DATASET`, `LAW_CLAIM_DATASET`, `SCORE_CLAIM_DATASET`); `readingDerivation(provenance)` reuses `scoreDerivation`.
- `DashboardPage.tsx` stat tiles + `HeroStory` count: wrap the numeral in `<CitableNumber claim=…>`; the money tile passes `isFloor` as claim modifier text, not into the value.
- `liveFigures.ts`: add the chamber datasets to `LIVE_DATASETS`; resolvers call `getDashboardData()` (already `react.cache`-adjacent via the memoized money layer); `gone` when the population is empty; `unavailable` when the store is down.
- `lib/claims/registry.ts`: drop the mock-derived `prumerna-dochazka` (keep the constitutional 200 and the `/svedectvi` sample under a dataset name that says "vzorek"); `overeni.md` and `guide.ts` example updated.
- `app/dashboard/odecet.json/route.ts`: serialises `readingClaims(getDashboardData())` with `Cache-Control` bound to `MONEY_MEMO_TTL_MS`; listed on `/data`'s feed address book (`feedIndex.ts`).
- `messages.test.ts` (dashboard) pins that every tile with a numeral carries a claim; `liveFiguresRoundTrip.test.ts` gains the four chamber refs.

#### Expected impact
Newsrooms quoting "průměrná docházka 87,3 %" get a permanent address that re-derives from the omluvy register with its population, and the gate stops confirming a mock; the velín's four numerals become `verified`/`moved` answers instead of unaddressable. Measured by: claim-bearing numerals on `/dashboard` 0 → 4 (+1 on `/`); registry figures resolving from `lib/civic` 3 → 1. What could break: a chamber claim minted while `provenance.state === "mixed"` must ship without derivation — the pure module has to refuse to invent one.

#### Evaluation
Claim: quality — the highest-traffic figures gain provenance addresses, and the verification gate stops answering from sample data for a chamber metric
Before: `grep -c CitableNumber features/dashboard/**` = 0; `resolveClaimRef("claim:<CHAMBER_STATS attendance source>:prumerna-dochazka")` returns the mock value dated 2026-07-30 (`registry.ts:87-106`)
After: 4 tiles + landing count minted; the same ref resolves through `getDashboardData()` with `population {counted,total}` — instrument: `liveFiguresRoundTrip.test.ts` mint → bare ref → verified with identical value
Method: probe — grep for claim primitives in the group, read the registry and the live resolver's dataset set
Result: better (the shadowing is reproducible from the registry order alone)
Gate: none

#### Evidence
- `lib/claims/registry.ts:22-23` `import { CHAMBER_STATS } from "@/lib/civic/data"; import { CHAMBER_SUMMARY } from "@/lib/civic/leaderboard";`
- `lib/claims/registry.ts:50` `const SAMPLE_RETRIEVED_AT = "2026-07-30";`; `:90-106` attendance figure from the mock tile string
- `features/overeni/liveFigures.ts:66-70` `LIVE_DATASETS = new Set([MONEY_CLAIM_DATASET, SCORE_CLAIM_DATASET, LAW_CLAIM_DATASET])`
- `docs/routes/overeni.md:13-15` "Order is registry-then-live"
- `features/dashboard/getDashboardData.ts:532-542` attendance mean with `counted`/`total`
- `features/dashboard/getDashboardData.ts:303-322` money headline, `isFloor`; `:343-356` law headline counts
- `features/landing/LandingPage.tsx:135-141` only `contributionScoreClaim` is minted on the landing
- grep `CitableNumber` over `features/dashboard`, `features/landing`, `features/shell`: 0 hits (only a `Claim` type import in `LiveSpecimen.tsx:22`)

---

### #8 Verification as a protocol: machine verdicts, JSON receipts and whole-article audit

`civic-feed-transparency#3` · theme T2 · corroboration 5 · size L · effort 5 / impact 8 / risk 4 · result `unmeasurable` · gate `contract`
contexts: claim-verifier, civic-feeds-verification, shared-provenance, data-releases
advances: claim-verification-and-provenance / claim-ref-addressing + structured-review-emission; ROADMAP "API pro redakce" made concrete as the verification protocol rather than a generic read API

#### Summary
The Civic Claim Gate answers one pasted ref with an HTML page; a newsroom that quotes twelve politicas figures in one article has no way to re-check them together, and no machine can ask the gate anything — `/data` lists three machine endpoints and none of them verifies.

#### Description
Today: `app/overeni/page.tsx:21-28` reads a single `?ref=` string; `ls app/overeni app/zdroj/[ref]` shows `page.tsx` only — no `route.ts` under either; `features/data-releases/feedIndex.ts:83-99` enumerates `MACHINE_ENDPOINTS` (novinky, manifest, snapshot) with no verification address; `features/overeni/refDetect.ts` classifies one URL (`detectAt` unwraps our own `?ref=` at depth 1). The three-verdict vocabulary, the gate-as-modifier (`verdict.ts:522-526`) and the derivation comparison are all pure and key-returning already (`docs/routes/overeni.md` "Standing rules") — the protocol exists as functions and is missing its wire.

Built: (1) `/overeni.json?ref=…` (and `/zdroj/<ref>.json`) — the same `getVerdictData` / `getReceiptData` serialised: `{ verdict: verified|moved|unknown, gate: verified|pending|rejected|ungated, value, derivation, citedValue, citedDate, receipt: "/zdroj/…", schemaVersion }`, `Cache-Control` per the feed precedent, 503 on an unreadable store (never an empty frame). (2) A batch mode: `POST /overeni.json` with up to `AUDIT_REFS_CAP` refs (or a pasted article body from which `refDetect` extracts every politicas address and `data-claim-*` element) → one verdict per ref plus the cap and the count dropped; the HTML `/overeni` gains the same textarea and a verdict table. (3) `feedIndex.ts` lists both endpoints as machine endpoints; `/data` prints them. ClaimReview JSON-LD stays gated at the emitter — the batch API returns the gate state as a field, never as review markup.

#### Flow
- `features/overeni/wire.ts`: pure serializer from `GateVerdict` → JSON (message KEYS, not sentences — the consumer localises), pinned by a test that walks `VERDICT_COPY_KEYS`.
- `app/overeni.json/route.ts` (GET single, POST batch) + `app/zdroj/[ref].json/route.ts`, sharing the loaders; per-request `cache()` dedupes repeated refs in one article.
- `refDetect.ts`: `detectAll(text)` over a document — every URL and every `data-claim-ref` attribute; count of non-citation URLs reported, never silently dropped.
- `OvereniPage.tsx`: document mode; verdict table; each row links its receipt.
- `feedIndex.ts` + `feedIndex.test.ts` (asserts the routes exist in `app/`); `/data` copy.
- Docs: `docs/routes/{overeni,data}.md`; `memory/` note on batch cost.

#### Expected impact
Editors re-verify an article in one request before print, and CMS plugins can auto-check a draft; measured by batch requests per week and by the ratio of `moved` verdicts caught before publication. Could break: each ref costs a loader call (~806 ms cold for the guide example); an uncapped batch is a DoS — the cap ships its population and the 429 path is part of the design.

#### Evaluation
Claim: user - a newsroom can verify N claims in one request, by machine
Before: 1 ref per request, HTML only (`app/overeni/page.tsx:24-28`); 0 verification endpoints in `MACHINE_ENDPOINTS` (`feedIndex.ts:83-99`)
After: MISSING INSTRUMENT — p95 latency of a 12-ref batch on the live store (no batch exists to time)
Method: probe - directory listing of `app/overeni` / `app/zdroj/[ref]`, read of the route, feedIndex and verdict modules
Result: unmeasurable
Gate: contract

#### Evidence
- `app/overeni/page.tsx:21-28` — single `ref` search param, HTML page
- `app/overeni/`, `app/zdroj/[ref]/` — `page.tsx` only, no `route.ts`
- `features/data-releases/feedIndex.ts:83-99` — three machine endpoints, none verifying
- `features/overeni/verdict.ts:306`, `:522-526` — closed vocabulary and gate tone already pure
- `features/overeni/getVerdictData.ts:34-104` — family router reusable by a JSON route unchanged
- `docs/ROADMAP.md` "Next" §3 — "API pro redakce" named, undesigned

---

### #9 Record-time citations: every receipt re-derives at the instant it was cited

`data-layer#0` · theme T1 · corroboration 5 · size L · effort 6 / impact 8 / risk 4 · result `better` · gate `contract`
contexts: db-repositories, db-store, claim-gate (features/overeni), provenance-receipts (features/shared/provenance), graph-permalink (features/graph), money-ledger (features/money)
advances: claim-verification-and-provenance / recomputation-receipts + derivation-comparison ("moved is shown with both sides and both dates"); law: provenance-or-nothing

#### Summary
The store already keeps every superseded version of every kg claim and exposes `asOf()`, but the API is not in the `Store` contract, has one script consumer, and no citation surface can show what the graph said WHEN the reader cited it — a `moved` verdict today shows only today's side.

#### Description
`lib/db/pglite/repositories/kg.ts:53-66` declares `BitemporalKnowledgeGraphRepository.asOf()` and says it is "deliberately declared HERE, not in lib/db/store.ts … later batches can lift it into the Store interface" and "an un-indexed union of serving + history tables — a history instrument, not a hot serving path". `lib/db/store.ts:171-195` (`KnowledgeGraphRepository`) carries no `asOf`; the only non-test caller is `scripts/data-analysis/kg-repair-orphans.ts:14,81`. Meanwhile the write side is complete: `supersedeUpsertChunk` (kg.ts:107-150) and the review flip (`review.ts:131-146`) archive every prior version with a contiguous `[recorded_at, superseded_at)` span, and `review.ts:136-140` states that "this tie sat in pending_review for 4 months" is "a queryable fact" — yet nothing queries it.

On the citation side, `lib/claims/claim.ts:51` already stamps `retrievedAt` (day precision) and `:57-69` the `derivation`; `/overeni` (`features/overeni/liveFigures.ts`) re-derives against TODAY only, and `docs/routes/overeni.md` records the vocabulary as `verified | moved | unknown` with `moved` carrying one side. The registry technique requires `moved` to show "both sides and both dates"; the repo cannot, because no read path asks the store for the cited instant.

Built: (1) `asOf` lifted into `Store` with an explicit contract ("record time; before the bitemporal epoch answers `unknown`, never a value"); (2) `kg_edge_history_key_idx` / `kg_node_history_id_idx` (ddl.ts:325,339) promoted to the serving path for point reads — a single-key `asOf` read via those indexes is cheap even if the whole-relation union is not; (3) `getReceiptData.ts:65-67` (today `kgNeighbours` on the serving table) gains a second leg: when the pasted claim carries `retrievedAt`, the receipt also renders the version visible at that instant, and `verdict.ts` compares cited-instant vs today and prints both values, both `review_state`s and both `provenance.pass` stamps; (4) the `/zdroj` receipt gains a "review standing over time" strip from `kg_edge_history` joined to `review_audit` (the 4-months fact); (5) `retrievedAt` upgraded to an instant on newly minted claims (the day-precision field stays decodable — append-only address space).

#### Flow
- Lift `asOf` into `KnowledgeGraphRepository` (`lib/db/store.ts`) with the epoch rule as a typed result (`{ known: false, epoch }`), not a silent fallback to serving rows.
- Add a point-read `asOfClaim({src,rel,dst}, at)` / `asOfNode(id, at)` that uses the history key indexes (measure on the 2 GB copy; the union lister is not the path).
- Extend `Claim` with `retrievedAtInstant` (append-only key order, claim.ts:71-90) and mint it from every live figure (`liveFigures.ts`, `moneyClaims.ts`, `scoreClaim.ts`).
- `getReceiptData` + `verdict.ts`: two-sided `moved` (value, basis, review_state each compared), with both dates; a cited instant before the epoch reports "record time unknown before <epoch>" and compares nothing.
- `/graf/p/[ref]`: the second fingerprint (`docs/routes/graf-permalink.md` "both fingerprints") becomes a re-derived as-of fingerprint, not a stored one.
- Pin with `kg-bitemporal.test.ts` extensions + `liveFiguresRoundTrip.test.ts` (mint → advance store → `moved` with both sides).

#### Expected impact
A journalist who cited a tie in July and re-verifies in September sees "then: pending_review, 23,65 mld.; now: rejected, 23,65 mld." instead of a bare `moved`; measured as the share of `moved` verdicts that render both sides (0 today). The epoch boundary (all pre-migration rows share one `recorded_at`, `changes.ts:19-24`) is the thing that could break honesty — it must render as unknown, never as "unchanged since 2026-08".

#### Evaluation
Claim: quality - a `moved` verdict becomes a two-sided, dated statement instead of a one-sided one
Before: `asOf` has 0 feature consumers (grep across features/app: only `scripts/data-analysis/kg-repair-orphans.ts`); `moved` in `features/overeni/verdict.ts` carries today's value only; 211/211 `linked_to` ties `pending_review` (overeni.md) so the review-over-time strip would currently show one span each
After: every claim minted with an instant re-derives two-sided; the instrument is a count of `moved` verdicts with `both sides present` in the gate's own test suite
Method: probe - read kg.ts/store.ts/claim.ts/getReceiptData.ts, grep for consumers
Result: better
Gate: contract

#### Evidence
- `lib/db/pglite/repositories/kg.ts:53-66` — `asOf` declared outside the Store contract, "not a hot serving path"
- `lib/db/store.ts:171-195` — `KnowledgeGraphRepository` without `asOf`
- `lib/db/pglite/repositories/review.ts:131-146` — flip supersedes; "4 months in pending_review" called queryable, unqueried
- `lib/db/pglite/ddl.ts:288-339` — bitemporal columns + history tables + key indexes
- `lib/claims/claim.ts:51,57-69` — `retrievedAt` (day) and `derivation` already on the claim
- `features/shared/provenance/getReceiptData.ts:65-67` — receipt reads serving table only
- `lib/db/pglite/repositories/changes.ts:19-24` — the shared-epoch caveat

---

### #10 Temporal receipts: /zdroj and the gate answer "as of the day you cited it"

`shared-ui-primitives#2` · theme T1 · corroboration 5 · size L · effort 6 / impact 8 / risk 4 · result `better` · gate `contract`
contexts: shared-provenance, claim-verifier, db-repositories, civic-chronicle, graph-explorer, money-cases-review
advances: claim-verification-and-provenance/three-verdict-vocabulary ("moved … shown with BOTH sides and BOTH dates") + claim-ref-addressing ("a claim the current store no longer carries gets an honest page that states what the address asserted"); _laws.md#disclose-never-repair

#### Summary
The store is bitemporal (`kg_node_history`, `kg_edge_history`, `review_audit` hash chain) and the repository exposes `asOf(at)` — but no reader-facing path reads it: a `moved` verdict shows today's side only, and a `gone` receipt shows the decoded address plus whatever endpoints survive, never the last recorded version of the edge.

#### Description
`lib/db/pglite/ddl.ts:300-335` adds `recorded_at`/`superseded_at` to `kg_node`/`kg_edge` and the append-only `kg_node_history`/`kg_edge_history`; `lib/db/pglite/repositories/kg.ts:58-68` declares `BitemporalKnowledgeGraphRepository.asOf(at)` ("the graph as it was known at `at`"). `grep -rn "\.asOf(" features app lib` outside tests/repositories → **0 hits**. `getReceiptData.ts:70-81` on a missing edge returns `gone` with `toDecodedClaim` over the current nodes; the header comment calls this "the WORST moment for the reader". `figuraVerdict` (`verdict.ts:104-121`) and the zdroj/graf verdicts carry `citedDate` but never query the store at that date: `moved` is "today differs from what you pasted", with no statement of what the platform itself published on `citedDate`. The registry says a moved claim must show both sides and both dates; today one side is the reader's paste.

"Built" = (1) the claim address family gains an optional `k=<YYYY-MM-DD>` (as-of) query on `/zdroj/<ref>` and `/overeni?ref=&k=`: the receipt is re-derived through `store.asOf(k)` and typeset with a dated banner ABOVE the content (the `/graf/p` staleness rule, `docs/routes/graf-permalink.md`), both fingerprints shown; (2) a `gone` receipt additionally reads the last history version (`kg_edge_history` newest by `recorded_at`) and renders it as "naposledy zaznamenáno … / nahrazeno …" — disclosed as history, never re-promoted to a current claim; (3) `moved` verdicts (zdroj and figura where `citedDate` is present) fetch the platform's own value at `citedDate` and print three columns: cited by reader · published by us on that day · today. Where the history has no row spanning `citedDate` (store predates history), the column says "záznam k tomu dni neexistuje" (missing is not zero). Every change surfaced this way already has a `change_event` twin (`ddl.ts:343-356`, writer `repositories/changes.ts`) — the receipt links the events between the two dates so `/denik` becomes the narrative of why the claim moved.

#### Flow
- Add `asOf` reads to `getReceiptData` behind a validated `k` param (ISO day only; anything else is refused, not repaired — the `forensicMode.ts` "adresa je tvrzení" discipline).
- Index `kg_edge_history(src, rel, dst, recorded_at desc)` is already there (`ddl.ts:340`); add a `lastVersion(src, rel, dst)` repository read for `gone`.
- `receipt.ts`: `ProvenanceReceipt` gains `asOf: string | null` and `history: {recordedAt, supersededAt} | null`; `ReceiptBody` posts the dated banner above the content.
- `verdict.ts`: `moved` variants gain `publishedAtCitedDate: {value | receipt, recordedAt} | null`; pure, tested with fixture history rows.
- `/denik` link: `change_event` rows between `citedDate` and today for the claim's `src`/`dst` keys, capped with the cap printed.
- `docs/routes/overeni.md` + `graf-permalink.md` dated record entries.

#### Expected impact
A reader defending a screenshot from June can prove what the platform said in June; a subject contesting a tie can see when it changed and which gate decision changed it. Measured: `moved` verdicts that show both sides (0 today → all where history spans the date), `gone` receipts that show a last version (0 → all with a history row). What could break: `asOf` runs over an un-indexed union (kg.ts:64-65) — must be bounded to the single claim key, never a neighbourhood, or the receipt page becomes a slow path.

#### Evaluation
Claim: user — a moved/gone claim shows what the platform published on the cited day
Before: `asOf` has 0 reader-facing consumers; `gone` = decoded address + surviving endpoints (`getReceiptData.ts:70-81`); `moved` = pasted value vs today (`verdict.ts:107-121`), `citedDate` carried but unused for lookup
After: the same three cases render the dated side; the MISSING INSTRUMENT is a history-coverage count (share of current edges that have ≥1 history row) to state when "no record for that day" is the honest answer
Method: probe — grep for `.asOf(` consumers; read ddl history tables, repository interface, receipt loader and verdict
Result: better
Gate: contract — new optional query param on issued addresses; address space unchanged

#### Evidence
- `lib/db/pglite/ddl.ts:300-335` bitemporal columns + `kg_node_history`/`kg_edge_history`; `:340` `kg_edge_history_key_idx(src, rel, dst, recorded_at desc)`
- `lib/db/pglite/repositories/kg.ts:58-68` `asOf(at: Date | string): KgAsOfReads` — "a history instrument, not a hot serving path"
- `grep -rn "\.asOf(" features app lib | grep -v test | grep -v repositories/` → no output
- `features/shared/provenance/getReceiptData.ts:40,61,70-81` the `gone` arm
- `features/overeni/verdict.ts:57-79` `moved` carries `citedValue`, `citedDate`, `citedDerivation` — nothing read at `citedDate`
- `lib/db/pglite/ddl.ts:343-356` `change_event` "derived from the bitemporal kg history + review_audit"
- registry: three-verdict-vocabulary — "moved … shown with both sides and both dates"

---

### #11 Claim-ref ratchet: the lint doctrine moves from "a citation somewhere in the file" to "an address on every figure"

`infrastructure-observability#4` · theme T2 · corroboration 5 · size XL · effort 7 / impact 8 / risk 4 · result `unmeasurable` · gate `policy`
contexts: civic-transparency-eslint-plugin, eslint-rules-shims, app-config, overeni, claims-core, shared-ui
advances: claim-verification-and-provenance / claim-ref-addressing + recomputation-receipts; repo convention: every rendered number cites its source (the brand rule, made addressable)

#### Summary
`require-source-citation` is satisfied by ONE `<SourceNote>` anywhere in a file (120 files), while the machine-verifiable form — a `CitableNumber` with a claim ref the `/overeni` gate can re-derive — exists on 19 files and a registry of three chamber figures; the brand rule is enforced at file granularity and verifiable at figure granularity, and nothing ratchets one toward the other.

#### Description
Today: `packages/eslint-plugin-civic-transparency/rules/require-source-citation.cjs:29-58` — "What satisfies the rule … FILE-scoped: any of these anywhere in the same file: a provenance element … `data-undisclosed` … `// citation-ok`", and `:219-226` clears every candidate the moment `fileSatisfied` is true. So a file with twelve figures and one caption is clean, and no rule can say WHICH figure the caption covers. The addressable form exists and is well-built: `lib/claims/claim.ts:36-70` (ref, dataset, metric, subject, derivation, reviewStatus), `claimDataAttributes` (`:140-157`) emits `data-claim-*` on the DOM, `lib/claims/registry.ts:112` holds `ISSUED_FIGURES` — three figures — and `features/overeni/liveFigures.ts:89-94` routes a handful of metrics to the loaders that own them. `/overeni` (`docs/routes/overeni.md`) therefore verifies money ties, company figures, leaderboard scores, law census — and answers `politicas-neni-citace` for every other number on the site, which is most of them (120 SourceNote files vs 19 CitableNumber files). The registry's claim-ref-addressing technique is that the address carries the claim's identity so the server can re-derive from the address alone; today a reader who pastes `/rozpocty`, `/hlasovani` discipline, `/volby` ledger figures or `/dashboard` tiles gets "our page, not a citation".

Built: a second doctrine rule, `require-claim-ref`, in the plugin: a formatter call in JSX child position must be a `<CitableNumber claim={…}>` (or sit inside an element carrying `data-claim-ref`) — same triggers as `require-source-citation` (`:9-27`), satisfier per FIGURE not per file, `// claim-ok: <reason>` for the genuinely un-addressable (an index, a column count). Ships at `warn` with a measured inventory written into `eslint.config.mjs` exactly the way the two existing ratchets did (`eslint.config.mjs:86-135`), then graduates route by route as each feature's numbers get a metric in `liveFigures.ts` — `liveFiguresRoundTrip.test.ts` already fails on a metric without a routing branch, so a claim ref can never be minted without a re-derivation path. `lib/claims/registry.ts` stops being a hand list: `ISSUED_FIGURES` is derived from each feature's `*Claims.ts` module (the `lawClaims.ts` precedent) so the finite registry and the live router share one declaration. The `/overeni` gate then accepts any figure on the site, and the `SourceNote` primitive stays as the human caption — the two are complementary, not competing.

#### Flow
- Write `rules/require-claim-ref.cjs` + RuleTester suite mirroring `require-source-citation`'s trigger set; satisfier = enclosing `CitableNumber` or `data-claim-ref` on the nearest JSX ancestor; `claim-ok` opt-out with a reason
- Measure: `eslint --format json` repo-wide, record the count per file in the config comment (the ratchet convention), ship at `warn` under `features/**`, `error` under `app/**` if measured zero there
- `lib/claims/registry.ts`: `ISSUED_FIGURES = [...civicscoreClaims, ...lawClaims, ...moneyClaims, ...]` with a test that every ref parses, is unique, and routes in `liveFigures.ts`
- Per route (order by reader traffic: `/zebricek`, `/poslanec`, `/hlasovani`, `/rozpocty`, `/volby`, `/dashboard`): add a `<feature>Claims.ts` with `makeClaimRef({dataset, metric, subject})` per figure, wrap the render in `CitableNumber`, add the loader branch to `liveFigures.ts`, flip the route's severity to `error`
- `/overeni` `refDetect`: recognise `data-claim-ref` pasted from any page (already does), and the `politicas-neni-citace` reason shrinks as routes graduate — its count is the burn-down metric, printed on `/data`'s feed address book as "citovatelné plochy N z M"
- `no-source-note-size-override` precedent: the citation primitive owns its legibility; `CitableNumber` gets the same rule

#### Expected impact
A journalist can paste ANY number from the site into `/overeni` and get `verified | moved | unknown`, not "not a citation"; the brand rule gains a per-figure machine check that cannot be satisfied by a caption three sections away. Measured by: `require-claim-ref` warning count (unknown today; the rule is the instrument) and the share of public routes whose figures route in `liveFigures.ts` (today ~4 families of ~16 routes). What could break: a claim ref minted without a re-derivation path — prevented by the round-trip test; and DOM weight from `data-claim-*` on dense tables (measure on `/zebricek`).

#### Evaluation
Claim: user — every rendered figure becomes independently re-derivable by address
Before: `require-source-citation` satisfier is file-scoped (`:219-226`); 120 SourceNote files vs 19 CitableNumber files; `ISSUED_FIGURES` = 3 registry figures; `/overeni` answers `politicas-neni-citace` for most routes
After: per-figure rule with a printed inventory that only goes down; registry derived from feature claim modules; `/overeni` coverage printed as N of M routes
Method: probe — read the rule, `claim.ts`, `registry.ts`, `liveFigures.ts`; `grep -rl CitableNumber` (19) vs `grep -rl "<SourceNote"` (120)
Result: unmeasurable (the count the rule would produce does not exist until the rule does)
Gate: policy

#### Evidence
- `packages/eslint-plugin-civic-transparency/rules/require-source-citation.cjs:29-58` file-scoped satisfiers; `:219-226` `if (fileSatisfied) return;`
- `lib/claims/claim.ts:36-70` the claim shape incl. `derivation`; `:140-157` `claimDataAttributes`
- `lib/claims/registry.ts:59,76,95,112` three issued figures
- `features/overeni/liveFigures.ts:89-94` metric router; `docs/routes/overeni.md` "A metric added without a routing branch FAILS `liveFiguresRoundTrip.test.ts`" and the `politicas-neni-citace` reason
- `grep -rl CitableNumber app features lib` → 19 files; `grep -rl "<SourceNote" app features` → 120 files
- `eslint.config.mjs:86-135` the two ratchets' measured-inventory convention this rule would follow
- `app/svedectvi/page.tsx:10-16` "Široká adopce (žebříček, spisy) je práce pozdějších dávek" — the adoption was deferred in 2026-07 and never scheduled

---

### #12 One review lane for person-level verdicts — effort claims get the money layer's gate

`mp-profiles-rankings#3` · theme T3 · corroboration 5 · size L · effort 7 / impact 8 / risk 4 · result `better` · gate `contract`
contexts: contribution-scoring, civicscore-leaderboard, mp-profile, money-cases-review, analysis-quality, db-store
advances: accountability-publishing-ethics — lead-not-verdict-framing, the ladder of assertion (rung 3 vs rung 4 stated on the surface); politician-performance-scoring — structural-low-score-corrections "stored as a separate reviewable fact with pending-review provenance, written by an enrichment pass that a human gates"; law lead-not-finding

#### Summary
Every effort-loop verdict about a named person — low-score reason (34/207), workhorse (≈16/207), rapporteur load, tenure class, public-role prose — is a model-authored, deterministically-gated claim rendered on /zebricek rows, /kraj posters and /poslanec with a date but no review state, no human gate and no audit chain, while the money layer's 211 ties get all three.

#### Description
`readChamber()` reads `effort_low_score_reason`, `effort_workhorse`, `effort_workhorse_flavour`, `effort_rapporteur_load` straight off the node (`features/civicscore/getLeaderboardData.ts:569-577`) and the profile reads the prose fields the same way (`features/profile/getProfileData.ts:538-558`, behind `publicCopyOrNull` only). `LowScoreReasonChip` renders vocabulary copy plus `recordedAt` (`LowScoreReasonChip.tsx:52`); `WorkhorseBadge` renders the claim, the turn count and the date (`WorkhorseBadge.tsx:66-67`) — nothing says whether a person or a pipeline decided it. The vocabulary already carries a per-reason tone (`lib/analysis/low-score-reason.ts:53-54, 90`) and the golden-path technique requires the reason to be „stored as a separate reviewable fact with pending-review provenance… that a human gates" and „each assignment carries who or what asserted it". The writers are `scripts/case-loops/effort/gate.ts` (deterministic gate, no human decision recorded — `grep human|reviewer` over `merge-batch.ts`/`finalize-ledger.ts` → 0) and the props are stamped `effort_provenance{computedAt}` only. The money layer, by contrast, has `ClaimReviewStatus` (`lib/claims/claim.ts:33`), a review console, a tamper-evident `review_audit` chain and sentinel coverage — and the sentinel record itself notes that layers outside contribution have ZERO invariants (`docs/routes/zebricek.md:159-164`); the effort layer is one of them.

"Built" = effort verdicts become claims on the ladder: (1) each `effort_*` verdict prop gains `review_state ∈ {machine, verified, rejected}` + `decided_by/at` inside `effort_provenance` (prop-registry extension), written `machine` by the loop; (2) the review console (`/penize/kontrola`'s pattern) gains a „Verdikty o poslancích" lane over the same `review_audit` chain — decisions append to the chain, rejections withhold the badge; (3) every badge renders its rung: „strojově odvozeno" vs „ověřeno (kdo, kdy)", through one shared `VerdictProvenance` primitive in `features/shared/components/` (catalog); the `Claim` shape (`ungated`) is reused where a figure is minted; (4) sentinel invariants `effort-review-chain` (every non-machine verdict has an audit row) and `effort-vocabulary` (every stored reason is in `LOW_SCORE_REASONS`) — turning the effort layer from unobservable to checked; (5) symmetry: the console orders by evidence completeness, never by score or party, and the verified-negative („genuine_absentee") sits in the same lane with the same weight.

#### Flow
- Extend `lib/kg/prop-registry.json` and the effort writers (`gate.ts`, `merge-batch.ts`) to stamp `review_state: "machine"` on every effort verdict; backfill the 207 nodes merge-preservingly on a copy with a full props diff.
- `lib/analysis/verdict-provenance.ts` (pure): read state + decider off `effort_provenance`; `rungKey()` → message keys; tests pin the language gate.
- Review console lane: `lib/db/pglite/repositories/review.ts` gains a subject kind for `psp:person:<id>` × `effort_*` field; `review_audit` chain unchanged in shape (the tamper-evidence invariant covers it for free).
- Render: `LowScoreReasonChip`, `WorkhorseBadge`, `RapporteurBadge`, `DossierSection` prose, Souboj — one primitive, both densities, poster footer via `posterProvenanceNote()`.
- Sentinel: two invariants above; `facts.ts` collects effort provenance; `sentinel.test.ts` proves a mass `machine → verified` flip without audit rows fires.
- /metodika: a §"Verdikty" line stating the rung vocabulary and the gate.
- Docs: `zebricek.md`, `poslanec.md`, `penize.md` (console), `analysis-quality` notes.

#### Expected impact
Readers see which rung each person-level badge stands on; a cited MP can contest a verdict through the same console that already exists for ties; the effort layer stops being the one claim class about named people that no invariant watches. Measured by: badges carrying a rung (0 → all), effort verdicts with a review row (0 → N decided), sentinel invariants over `effort_*` (0 → 2). Could break: badge density on /zebricek rows — the rung label must not push `LeaderboardRow` past its compact grid; and a `rejected` verdict must withhold, never blank a row to „no reason" (the honest empty state is „verdikt zamítnut", disclosed).

#### Evaluation
Claim: quality — rung inflation made structurally impossible for the person-level verdicts
Before: `grep -rn "review_state\|reviewStatus" features/civicscore features/profile lib/analysis/low-score-reason.ts lib/analysis/workhorse-flavour.ts` → 0; 34 low-score chips + ≈16 workhorse badges + 18 rapporteur badges render dated but ungated; 0 sentinel invariants over `effort_*`
After: every rendered verdict carries `machine|verified|rejected` + decider; a fault-injected flip without audit rows is a sentinel violation; count of undated-or-unranged verdicts = 0
Method: probe — read both loaders' prop reads and both badge components; grepped the effort scripts for a human-decision field
Result: better
Gate: contract — prop-registry extension and a review-console subject kind; the badge copy is a claim-shape change on a reader-facing surface

#### Evidence
- `features/civicscore/getLeaderboardData.ts:569-577` — effort verdict props read raw, no state
- `features/profile/getProfileData.ts:538-558` — prose fields behind `publicCopyOrNull` only
- `features/civicscore/components/LowScoreReasonChip.tsx:52`, `WorkhorseBadge.tsx:66-67` — date, count, no rung
- `lib/analysis/low-score-reason.ts:27, 53-54, 90` — closed vocabulary with tone, no review dimension
- `lib/claims/claim.ts:33` — `ClaimReviewStatus = "verified" | "pending" | "rejected" | "ungated"` exists for figures, unused for verdicts
- `docs/routes/zebricek.md:159-164` — sentinel: non-contribution layers have zero invariants
- registry technique `structural-low-score-corrections` §"Stored as a separate reviewable fact"

---

### #13 Moved citations shown with both sides: as-of re-derivation from the bitemporal graph

`knowledge-graph-explorer#1` · theme T1 · corroboration 5 · size XL · effort 8 / impact 8 / risk 5 · result `unmeasurable` · gate `contract`
contexts: graph-explorer, claim-verifier, db-repositories, shared-provenance
advances: claim-verification-and-provenance / three-verdict-vocabulary ("moved … shown with both sides and both dates") + recomputation-receipts; civic-knowledge-graphs / per-claim-provenance-stamping; law: provenance-or-nothing, disclose-never-repair

#### Summary
A stale `/graf/p/[ref]` says only "hash in address ≠ hash today"; the reader never sees WHAT changed. The store already keeps bitemporal history and an `asOf()` read, used by nothing outside tests and a repair script — the permalink could re-derive the cited view at its issue instant and print the diff.

#### Description
`getPermalinkData` re-derives from today's store and sets `fresh: currentHash === decoded.hash` (`features/graph/getPermalinkData.ts:160-189`); the page posts `staleNotice` with two hashes (`PermalinkPage.tsx:501-507`), the card model gives `citedHash` (`permalink.ts` `permalinkCardModel`), and `/overeni` maps `fresh` → `verified | moved` with nothing but the two hashes (`features/overeni/verdict.ts:183-199`). The address `g.<state>.<hash8>` carries no issue time (`permalink.ts:148-164`), so even with history the server cannot know WHEN the citation was minted; `retrievedOn` exists only in the copy-paste citation line. Meanwhile `kg_edge_history`/`kg_node_history` record `valid_from/valid_to/recorded_at/superseded_at` (`ddl.ts:327-337`) and `KgAsOfReads` + `asOf(at)` (`lib/db/pglite/repositories/kg.ts:44-67, 388-410`) replay the graph as known at an instant; `grep -rl "asOf("` finds only the repo itself, its tests and `kg-repair-orphans.ts`. The registry's vocabulary requires `moved` to be "shown with both sides and both dates"; today the product shows one side and one date.

Built: a second address shape `g2.<state>.<hash8>.<issuedAt YYYYMMDD>` decoded beside `g.` (append-only address space; `g.` keeps working as "moved without a replay date"). `resolveView` gains an `at` parameter and every graph loader gets an `asOf`-parametrised twin over the same pure code (`buildTrails`, `buildPathAdjacency`, `getNodeDetail` take a `KgAsOfReads` instead of the live store — the memo cells stay live-only). On `fresh: false` the page renders the then-view and the now-view through one pure `diffViews()` (edges added/removed, gate flips, weight changes, node facts changed, path re-routed), with each side dated; the JSON-LD bundle carries `hasPart` for both with `validFrom`/`validThrough`; `/overeni` prints the diff table under `moved`; the OG card says "změněno od <date>".

#### Flow
- `permalink.ts`: `encodeGraphRef` emits `g2.` with `issuedAt`; `decodeGraphRef` accepts both; `GraphRef.issuedAt: string | null`; tests for refusal on malformed dates (no repair).
- `db-repositories`: extend `KgAsOfReads` with `kgNeighbours` parity (already in the interface) and assert `asOf(now)` row-identity is covered by `kg-bitemporal.test.ts`; expose `store.asOf` through `lib/db/store.ts`'s public type.
- `graphLoader.ts`: factor `buildTrails`/`buildPathAdjacency`/`getNodeDetail` bodies into `(reads: KgAsOfReads) => …` pure-ish builders; live callers pass the store, replay callers pass `store.asOf(issuedAt)`; replay results are never memoised (per-request receipts).
- `getPermalinkData.ts`: when `issuedAt` is present and `fresh` is false, resolve `then = resolveView(state, asOf(issuedAt))`; `PermalinkView` gains `then: {content, retrievedOn} | null` and `diff: ViewDiff | null`.
- `diffViews()` in a new pure module with fixtures: path → hop-by-hop diff; trail → node/edge set diff with gate flips; node → fact diff (weights "exactly as stored", no rounding).
- `PermalinkPage`: staleness block becomes a two-column "tehdy / dnes" ledger above the content; `permalinkCardModel` adds `changedSince`; OG prints it; `toEvidenceJsonLd` adds the then-side claims with `validThrough`.
- `/overeni` `hashedVerdict`: `moved` verdict carries `diff` when available; `CiteView` issues `g2.` refs from now on.

#### Expected impact
Readers who arrive from a months-old article see exactly which tie was rejected, which contract appeared, which hop re-routed — the difference between "this citation is stale" and "this citation was wrong". Measured by: share of `moved` verdicts at `/overeni` that carry a non-null diff (0 % today; 100 % of `g2.` refs after). Could break: `asOf` replays over the full edge relation are unmemoised full scans (~25 k edges today, 150 k contracts) per stale request — needs a `KG_READ_CAP`-aware replay budget and the truncation warning already in `asOf.listKgEdges`.

#### Evaluation
Claim: user - a moved citation becomes a dated, two-sided record instead of a hash mismatch
Before: `PermalinkPage.tsx:501-507` renders `staleNotice{urlHash,currentHash}` only; `verdict.ts:190-198` `HashComparison` = two hashes + one date; `asOf(` unused by any feature
After: the MISSING INSTRUMENT is the replay itself — count of stale views re-derivable at issue time (needs `g2.` refs in the wild plus history rows older than the citation; measure via a fixture store with two recorded passes)
Method: probe - traced address codec, view resolution and history schema; no replay attempted (read-only, store closed)
Result: unmeasurable
Gate: contract - new address shape and JSON-LD fields are external promises; old `g.` refs must keep decoding

#### Evidence
- `features/graph/permalink.ts` codec block (`g.<b64>.<hash8>`, `HASH_RE`, `MAX_REF_LENGTH`), `permalinkCardModel`, `toEvidenceJsonLd` (`fresh` only as yes/no)
- `features/graph/getPermalinkData.ts` `resolveView` (live loaders only), `fresh: currentHash === decoded.hash`
- `features/graph/PermalinkPage.tsx:501-507` — stale block prints two hashes
- `features/overeni/verdict.ts:183-199` — `hashedVerdict` verified/moved on `fresh` alone
- `lib/db/pglite/ddl.ts:327-337` — `kg_edge_history(valid_from, valid_to, recorded_at, superseded_at)`
- `lib/db/pglite/repositories/kg.ts:44-67, 388-410` — `KgAsOfReads`, `asOf(at)`; consumers: tests + `scripts/data-analysis/kg-repair-orphans.ts` only

---

### #14 Identity ledger — one cross-source person resolution table with confidence and a review door

`data-ingestion#4` · theme T3 · corroboration 5 · size XL · effort 8 / impact 8 / risk 6 · result `unmeasurable` · gate `policy`
contexts: ingest-external-sources, ingest-psp-sources, money-analysis-triage, money-contract-ingest, czech-civic-data-parsing
advances: civic-source-adapters / entity-name-normalization ("a name match is never an identity claim by itself"); beneficial-ownership-resolution / name-to-identifier-discipline + officer-record-reading; llm-forensic-gating / human-review-doors; ROADMAP "Next" 1 ("entity-resolution service with confidence + human-review gate") made concrete

#### Summary
Four adapters resolve "is this registry person our MP" with four private rules — exact birth date (ARES VR, dataor), folded full name with region/elected tie-breaks (volby), a Hlídač bridge — and the answer lives only as props on whichever edge the adapter was building. There is no table of person identities across sources, so a confirmation made for one tie is invisible to the next adapter, a rejection is not terminal, and no surface can say "this MP is the same person as this OR officer" with a review state.

#### Description
`scripts/case-loops/money/reconcile-ares-vr.ts:6-8` matches "by EXACT birth date … never a name-only guess"; `dataor-corroborate.ts:25-26` repeats the rule against the bulk export; `lib/ingest/sources/volby.ts:274-307` joins MPs to PS2025 candidates "by folded full name", disambiguating by region then by `elected` and returning `matched|ambiguous|unmatched`; `lib/analysis/money-feed.ts:488/516` `bridgePerson`/`bridgeSearchByBirthdate` bridge psp.cz → Hlídač. Each writes its verdict where it happens to be standing: `corroboration` on `linked_to` edges, `bridge_provenance` on person nodes (`prop-registry.json` nodes.person), a `JoinStatus` in a batch payload. `lib/ingest/sources/dataor.ts:178-184` records that officer birth dates are extracted "ONLY to compare against our own roster birth date" — the comparison itself is re-implemented per script. The kernel says the human gate flips only `review_state` on ties (`docs/case-loops.md` Authority) — identity, the claim underneath every tie, has no gate at all. "Built" is a `person_identity` table (`person_psp_id`, `source` ∈ {ares-vr, dataor, volby, hlidac, kiosek}, `source_key`, `method` ∈ {birthdate-exact, name-fold+region, bridge}, `confidence`, `review_state`, `decided_by`, `audit_id`) written through ONE repository (append-only audit like `review_audit`), one pure `lib/analysis/identity.ts` holding the match rules (imported by all four adapters, with the `czech-civic-data` fold as the only name normalizer), a `/penize/kontrola` lane "totožnost" for `ambiguous` rows, and every tie-building path (`kg-money-ingest`, `reconcile-ares-vr`, `dataor-corroborate`, the volby signal, kiosek `concerns`) resolving through the ledger first — an unconfirmed identity yields a `pending_review` tie, a rejected identity yields no tie and stays rejected on re-run.

#### Flow
- Add `person_identity` + its audit to CORE_DDL (`npm run db:migrate`), a repository outside the `Store` facade (the `ledger.ts`/`changes.ts` precedent), and a `Store`-free reader for loaders.
- Extract the four matching rules into `lib/analysis/identity.ts` (pure, fixture-tested: exact-birthdate, name-fold with region/elected tie-break, ambiguity rules), importing `asciiFold` from `packages/czech-civic-data`.
- Backfill: derive rows from what the graph already asserts (`corroboration: registry-confirmed` ties → `ares-vr` identities confirmed-by-machine; volby `matched` rows; Hlídač bridges) with `review_state: pending_review` and the originating pass as provenance — never marked human-confirmed.
- Convert the four adapters/scripts to resolve through the ledger before building an edge; a rejected identity short-circuits; new candidates insert as `pending`.
- Review lane: extend `ReviewRepository` + the token-gated server action with an identity decision (append-only audit row first); the `/penize/kontrola` "rozpor role × rejstřík" lane links each tie to its identity row.
- Surface: `/poslanec/[id]` money block states the identity basis per tie ("shoda data narození v OR, potvrzeno strojově") through the gate vocabulary; `/overeni` mints an identity claim family.

#### Expected impact
The reviewer who opens the first human-gate session (money STATE item 0) decides identity once per (MP, registry person) instead of once per tie, and the decision holds across every later adapter run; a namesake can never re-enter as a fresh lead after rejection; the platform can finally print, per tie, which identity method stands behind it. Measured by: rows in `person_identity` vs distinct (MP, source key) pairs currently implied by ties (211 ties → fewer identities), and the share of ties whose identity row is `pending` vs human-decided. What could break: the backfill must not upgrade any machine match to human-confirmed; the volby name join is a lead-grade method and must never seed a `linked_to` tie by itself.

#### Evaluation
Claim: quality | resilience - identity becomes one claim with one rule set and one door, instead of four private rules with no memory
Before: 4 matching implementations in 4 files (`reconcile-ares-vr.ts`, `dataor-corroborate.ts`, `volby.ts:274-307`, `money-feed.ts:488-530`); 0 tables holding a person-to-source identity; identity decisions stored as edge props (`corroboration`) or batch payload fields; 211 ties all `pending_review` and the gate "never exercised" (money STATE)
After: 1 rule module, 1 table, N identity rows with a review state; every tie resolves through a row — the instrument is the ledger's `review_state` histogram and the count of adapters importing `lib/analysis/identity.ts`
Method: probe - read of the four matchers, the prop registry and the case-loop authority rules
Result: unmeasurable
Gate: policy

#### Evidence
- `scripts/case-loops/money/reconcile-ares-vr.ts:6-8` — "matches the MP against the officer/shareholder list by EXACT birth date … never a name-only guess"
- `scripts/case-loops/money/dataor-corroborate.ts:25-26` — "Exact-birthdate match against the roster (same discipline as reconcile-ares-vr.ts — NEVER a name-only guess)"
- `lib/ingest/sources/volby.ts:263` `export type JoinStatus = "matched" | "ambiguous" | "unmatched";` `:274-307` — folded-name join, region and elected-pool tie-breaks
- `lib/analysis/money-feed.ts:488` `export function bridgePerson(`; `:516` `export function bridgeSearchByBirthdate(`
- `lib/ingest/sources/dataor.ts:178-184` — birth dates extracted "ONLY to compare against our own roster birth date"
- `lib/kg/prop-registry.json` → `nodes.person` includes `bridge_provenance`; `edges.linked_to` carries corroboration state (per `memory/money-stored-review-rank-is-a-stale-cache.md`)
- `docs/data-analysis/case-money/STATE.md` — "ties · review state: 211 · all `pending_review` — the human gate has never been exercised"
- `docs/case-loops.md` Authority — "No loop, at any autonomy level, flips a `review_state` … only a human verifies"
- `docs/ROADMAP.md:59-61` — "Entity-resolution service with confidence + human-review gate is the critical component"

---

### #15 Vote figures enter the claim gate: a `vote` claim dataset for /overeni

`voting-legislation#3` · theme T2 · corroboration 5 · size L · effort 5 / impact 7 / risk 2 · result `better` · gate `contract`
contexts: votetrack-ledger, votetrack-kompas, volby-election-mirror, civic-feeds-verification, mp-profile
advances: claim-verification-and-provenance (claim-ref-addressing, recomputation-receipts, gate-state-as-modifier); law: provenance-or-nothing, every-cap-ships-its-population

#### Summary
The most-quoted numbers of this group — a named MP's rebellion rate, a club's cohesion, the reconciliation discrepancy count, the volby contested record — cannot be pasted into /overeni and re-derived, because no vote figure mints a claim ref and the live resolver knows only money, score and law.

#### Description
`features/overeni/liveFigures.ts:66-70` enumerates `LIVE_DATASETS` as `MONEY`, `SCORE`, `LAW`; `features/lawwatch/lawClaims.ts` shows the pattern (subject codec, metric, gate state, basis = corpus aggregate, refusals for un-formable subjects). `features/votetrack/**` mints nothing: the only occurrences of „claim" are the kompas URL codec and tests; `RealRebellions.tsx`, `RealDisciplineBoard.tsx` and the seismograf print `topRebels[].rate`, `clubs[].cohesion`, `reconciliation` counts and `coverage.thresholdDiffers` (`record/derive.ts:480-496, 434-446, 507-514, 543`) as plain numbers with a `SourceNote`, never through `CitableNumber`. /volby carries the gap forward: `lib/analysis/volby/types.ts:36` lets evidence refs be `claim:…`, and `docs/routes/volby.md` records that „the baseline `source` refs (`claim:volby-census:…`) are labels, not yet resolvable at `/zdroj`". So a journalist who quotes „rebeloval v 12 % hlasování" from /poslanec has nothing to paste into the gate that would re-run the definition.

Built: `features/votetrack/voteClaims.ts` mints refs for four metrics with subject codecs that already exist — `psp:person:<pspId>` (rebellion rate + eligible population, floor `MIN_ELIGIBLE_VOTES` in the basis), club organ id (cohesion + `riceVotes`, floor `MIN_CLUB_POSITIONAL`), the chamber (reconciliation discrepancies over `valid`, threshold-differs over `valid`), and a vote id (contestedness, `MIN_POSITIONAL_BALLOTS` from `contested.ts:57`). Gate state is `ungated` (deterministic arithmetic, the lawClaims precedent for census claims); basis is `coverage.from…to` + `valid` + the imported floor. `liveFigures.ts` adds `VOTE_CLAIM_DATASET` and re-derives through `getFullVoteRecord()` — a cold miss costs the ~16 s ledger read the memo already amortises, an unreadable ledger answers `unavailable`, a person below the floor answers with the floor, not `gone`. The kompas alignment is explicitly REFUSED as a claim: it is the reader's artefact (the compass subject's lens rule), and the refusal is written in the module beside the mint.

#### Flow
- `voteClaims.ts` + tests: subject codecs, metric enum, basis, refusals (below-floor, unaffiliated, voided vote, kompas alignment).
- `CitableNumber` on the rebel board, club board, reconciliation sentence, threshold coverage, /poslanec rebellion header, /volby contested rows (`FindingRow.tsx` already renders refs).
- `liveFigures.ts`: dataset + resolver branch; `liveFiguresRoundTrip.test.ts` extended with a vote fixture.
- /volby baseline refs become resolvable or are renamed as labels (no half-state).
- `docs/routes/{hlasovani,overeni,volby}.md` entries; `require-source-citation` count stays flat.

#### Expected impact
Newsrooms can verify a rebellion or cohesion figure against today's record with the definition attached; the volby ledger stops carrying refs that resolve nowhere. Measured by the count of rendered vote figures wrapped in `CitableNumber` and by round-trip tests. What could break: a re-derivation on a cold ledger takes seconds — the verdict surface must stream or state the wait, never time out into `gone`.

#### Evaluation
Claim: quality - every published vote figure is re-derivable at the gate with its floor and population
Before: `LIVE_DATASETS` = 3 datasets, 0 vote metrics; 0 `CitableNumber` in `features/votetrack`; volby `claim:volby-census:…` refs unresolvable
After: N vote figures citable (rebel rate ×12 rows, cohesion × clubs, 2 chamber counts, contested × rows) and 4 metrics resolvable at /overeni; instrument = grep of `CitableNumber` under `features/votetrack` + the round-trip test
Method: probe - grep for claims/CitableNumber across the group and read the resolver's dataset set
Result: better
Gate: contract

#### Evidence
- `features/overeni/liveFigures.ts:66-70` — `LIVE_DATASETS = {MONEY, SCORE, LAW}`
- `features/lawwatch/lawClaims.ts` — the mint pattern; `lib/analysis/volby/types.ts:36` — `ref = h.<edge> | u.<node> | claim:…`
- `features/votetrack/record/derive.ts:479-496` (`topRebels`, `qualifiedRebels`), `:434-446` (`clubs[].cohesion`, `riceVotes`), `:507-514` (reconciliation), `:543` (threshold summary)
- `lib/analysis/volby/contested.ts:57` — `MIN_POSITIONAL_BALLOTS = 100`
- grep `CitableNumber|claimRef` over `features/votetrack` → only `kompas/codec.ts`, tests

---

### #16 Every export carries a verifiable address: poster, embed and OG go through the gate

`shared-ui-primitives#4` · theme T2 · corroboration 5 · size L · effort 5 / impact 7 / risk 3 · result `better` · gate `contract`
contexts: shared-provenance, shared-primitives, claim-verifier, landing-page, civicscore-leaderboard, graph-explorer, dashboard-instruments
advances: claim-verification-and-provenance/claim-ref-addressing ("claims leave the platform immediately … copied into articles, screenshots") + three-verdict-vocabulary; accountability-publishing-ethics/right of reply ("a permanent address per claim")

#### Summary
The three artifacts most likely to leave the site — the printed poster (`PosterFrame`), the iframe embed (`app/embed/zebricek`) and the OG images — carry a source line and a live URL but no claim address; pasting what they print into `/overeni` answers `politicas-neni-citace`, so the least-correctable outputs are exactly the ones the gate cannot verify.

#### Description
`buildPosterCitation` (`features/shared/poster/citation.ts`) composes source · as-of · methodology · pass · formula-mismatch · `liveLine` = `posterDisplayUrl(sourceUrl)` — a SURFACE URL (`/zebricek`), and `PosterFrame.tsx:94-110` prints it. The embed footer (`features/landing/referendum/embed.ts:102`) prints "zdroj: politicas — otevřený index přispění · psp.cz, deterministický výpočet · ‹status›" with an `href` to the leaderboard. `refDetect.ts:129-132` recognises only `/zdroj/`, `/graf/p/`, `/dashboard/exponat/` paths and `claim:` refs; a surface path is answered with `politicas-neni-citace` (`docs/routes/overeni.md`). Only `/graf/p/[ref]` has a fingerprinted OG image (`app/graf/p/[ref]/opengraph-image.tsx:90-126` reads `pending`/`fresh`); `app/opengraph-image.tsx` and `app/referendum/og/route.tsx` carry none. So the artifacts that get screenshotted into articles are addressable only to a page whose content moves daily — "the footnote that outlives its number".

"Built" = ONE export-address builder in `features/shared/provenance/` (`exportRef.ts`): for a poster/embed/OG it issues an address that carries what was claimed — the metric family's `claim:` ref(s) or the view fingerprint + the as-of day + the derivation (`CONTRIBUTION_FORMULA_REF`/pass) — and `refDetect` learns the family so pasting the printed line yields the 3-verdict answer with "moved" showing both dates (ties into card 3). `PosterCitation` gains `claimLine` (typeset by `PosterFrame` as a 4th footer line, and as a QR rendered from the same string — QR is a typographic form of the address, not a second address); the embed footer's `href` becomes the export address and the `<div class="foot">` carries `data-claim-*` from `lib/format.ts formatCitable` so a scraped embed still testifies; every OG route posts the as-of and the pending count the way `/graf/p`'s does, through one shared `ogCitationLines()`. The poster's existing honesty (`posterUndatedNote`, `posterProvenanceNote` for mixed/absent provenance) carries into the address: an undated or mixed sheet issues an address WITHOUT a date/derivation segment, and the gate then says so — it never fabricates the day.

#### Flow
- `exportRef.ts`: pure encode/decode (append-only grammar, refuse-not-repair, `MAX_REF_LENGTH` discipline from `claimRef.ts`), unit-tested for byte-stable serialisation.
- `refDetect` gains the `export` family; `verdict.ts` maps it onto the figura/graf verdicts it already has (no fourth verdict).
- `citation.ts` + `PosterFrame`: `claimLine` + QR (SVG path, tokens only, no external lib per DESIGN §1).
- `embed.ts`: footer `href` = export address; `data-claim-*` attributes on the figure cells via `formatCitable`.
- `app/opengraph-image.tsx`, `app/referendum/og/route.tsx`: shared `ogCitationLines()` printing as-of + pending count + short address.
- `docs/routes/{overeni,landing,zebricek}.md` records; `liveFiguresRoundTrip.test.ts` extended with an export address per metric.

#### Expected impact
A printed sheet on a notice board or an embedded widget in a news article can be pasted into `/overeni` and answered; corrections become findable from the artifact itself (right of reply needs the address on the thing the subject saw). Measured: export lanes with a gate-recognised address (1 of 5 today: graf OG only → 5). What could break: the poster footer is a print artifact — the extra line must fit A4 within the `poster-sheet` print layer (`globals.css`) or the sheet overflows.

#### Evaluation
Claim: user — every artifact that leaves the site can be re-verified at /overeni
Before: poster `liveLine` = surface URL; embed footer `href` = leaderboard; `refDetect` path families = 3; pasting a poster's URL → `politicas-neni-citace`; 1 of 3 OG routes fingerprinted
After: all export lanes issue a decodable address; the round-trip test enumerates them
Method: probe — read `citation.ts`, `PosterFrame.tsx` footer, `embed.ts:102`, `refDetect.ts:129-132`, the three OG routes
Result: better
Gate: contract — a new address family in the append-only ref grammar

#### Evidence
- `features/shared/poster/citation.ts` `liveLine: \`živá verze: ${displayUrl}\`` and `posterDisplayUrl(input.sourceUrl)`; `PosterFrame.tsx:99,106` retrieved/live lines only
- `features/landing/referendum/embed.ts:102` `zdroj: <a href="${escapeHtml(url)}" …>politicas — otevřený index přispění</a> · psp.cz, deterministický výpočet`
- `features/overeni/refDetect.ts:129-132` `PATH_PATTERNS` = zdroj, graf, exponat; `docs/routes/overeni.md` "`politicas-neni-citace` … our page but not a citable address"
- OG routes: `app/opengraph-image.tsx`, `app/referendum/og/route.tsx` (no fingerprint), `app/graf/p/[ref]/opengraph-image.tsx:90-126` (pending + fresh)
- `lib/format.ts:160-166` `formatCitable` returns `data-claim-*` attrs — reusable in the embed's static HTML
- registry golden path: "those claims leave the platform immediately … copied into articles, screenshots, and arguments"

---

### #17 Declared attribution perimeter + a downward exposure lane in the review console

`financial-transparency#3` · theme T3 · corroboration 5 · size L · effort 5 / impact 7 / risk 4 · result `unmeasurable` · gate `policy`
contexts: money-cases-review, money-ledger-graph, kg-analysis, shared-provenance
advances: public-money-attribution/attribution-perimeter; beneficial-ownership-resolution/ownership-chain-traversal ("downward from a person's known entities builds exposure breadth — a candidate set, never a money number"); laws lead-not-finding, non-partisan-symmetry, every-cap-ships-its-population

#### Summary
The perimeter of "firms tied to this MP" is the MP's own Hlídač-listed roles, one hop, and it is declared nowhere on the surface; the graph now holds 113 dated `owns_stake` edges (84 current) that the product reads only upward for classification, never downward from an owner-operator's firm to the subsidiaries that actually sign the contracts.

#### Description
`kg-money.ts:39-45` states the tie source (declarations / OR officer records) and `money-feed.ts` builds every tie from a person's own `udalosti[]`; `ownership.ts:15-19` rule 1: "ONE hop … no chain walk, no exposure"; `ownership-depth2.ts` (batches 016-018) walks UPWARD to find public owners. `TrailMethod.tsx` prints four steps and no perimeter (`features/money/components/TrailMethod.tsx:2`; `grep perimet` → 0 hits in `features/money`). The registry says a direct-ties-only table is a floor of the reachable surface and the copy must say so; and that the downward walk is the legitimate way to grow the candidate set without asserting exposure. On the live store the case exists: AGROFERT a.s. carries the attributable tie (Babiš `manager`, 8,71 mil. Kč — `docs/routes/penize.md`), while the group's operating firms (Lovochemie, PRECHEZA, Fatra, Kostelecké uzeniny — b019's contradiction list) sit in the graph as steward-class dozorčí-rada ties or as `owns_stake` counterparts, their contract volume outside every attributable figure and outside every review lane.

Built: (1) `ATTRIBUTION_PERIMETER` as a versioned constant in `reachableMoney.ts` (`{ subjects: "official-only", relations: ["registered-role","direct-holding"], depth: 1, stakeThreshold: null, version: "perimeter-v1" }`) rendered verbatim in `TrailMethod`, the metodika, the packet `citeCs` and the claim derivation string (`moneyDerivation`), so every reach figure names its inclusion rule and the "nejméně" wording gains its second reason. (2) A pure `exposureBreadth(tie, ownsStakeOut)` in a new `features/money/exposure.ts`: for each attributable tie's company, one hop DOWN over current `owns_stake` edges to subsidiaries, collecting each subsidiary's `CompanyContracts` via the existing `readCompanySupplies` — output is structure (rows: subsidiary, stake wording, period, contracts count/czk, `moneyReachesCompany`-filtered), never added to any bucket. (3) A seventh console lane „expozice přes vlastnictví" listing those rows with a reviewer action that can only create a NEW `linked_to` lead (`pending_review`, source = the chain, provenance = the two edges) — never a class change, never a figure; (4) on `/penize/firma/[ico]` the ownership block gains "dceřiné firmy v grafu: n, smluv m" as counts with links, no CZK on the parent's file. Depth and fetch budget ship with the result (`walked / not attempted` counts), per the technique.

#### Flow
- Declare `ATTRIBUTION_PERIMETER` (one definition), thread into `TrailMethod`, `/metodika` money section, `packet.ts` cite block, `moneyClaims.moneyDerivation`; messages pin the sentence shape.
- `exposure.ts` pure module + tests over `owns_stake` fixtures (open vs closed period, sole-owner wording, non-canonical counterpart → dropped and counted).
- Loader: extend `loadCompanyMoneySlice` (already reads `owns_stake` both legs, `moneyLoader.ts:820-862`) to fetch subsidiaries' supplies via `companySuppliesMemo`; cap + `truncated` threaded.
- Console: new lane driven by the same predicate; action `proposeLeadFromChain` in `reviewActions.ts` writes a `linked_to` edge `pending_review` with `source: "ownership-chain:<parent>→<child>"` and appends an audit row; the human gate then decides it like any tie.
- Company file: subsidiaries block (counts + links); parent file never gains money.
- Symmetry check script: run the walk over ALL attributable ties, report per-club counts of chain leads before the lane ships (whole population or nothing).

#### Expected impact
Readers see what "firms tied to" means and why the figure is a floor; reviewers get the one lead class the current perimeter structurally hides (holding → operating firm) as leads with the chain attached; no headline moves until a human verifies a chain-born tie. Measured: chain leads found (n ties, m subsidiaries, contract volume outside all buckets today), per-club distribution, and after the first gate session how many became `verified`/`rejected`. Could break: nothing numeric; the risk is copy — a lane that reads as an accusation, hence the lead-only action and the counts-not-CZK rule on the parent file.

#### Evaluation
Claim: quality - the inclusion rule becomes a published, versioned part of the metric and the review queue sees the evasive tie class
Before: 0 perimeter statements on any money surface; 0 downward chain reads (`ownership.ts` rule 1); 113 `owns_stake` edges used only for the company axis
After: perimeter printed with every reach figure; MISSING INSTRUMENT: the count of subsidiaries reachable one hop down from the 18 owner-operator MPs' firms and their contract volume — computable today on a store copy, not yet computed
Method: probe - grep for perimeter copy, read ownership.ts rules, batch 016-018 walk direction, b019 contradiction list
Result: unmeasurable
Gate: policy

#### Evidence
- `lib/analysis/kg-money.ts:39-45`; `lib/analysis/money-feed.ts:1-30` — tie perimeter = the person's own declared roles.
- `features/money/ownership.ts:15-19` — "JEN ZAPSANÉ VLASTNICTVÍ, JEDEN KROK … žádná expozice".
- `features/money/components/TrailMethod.tsx:2` — four steps, no perimeter; no `perimet*` token under `features/money`.
- `features/money/moneyLoader.ts:820-862` — `owns_stake` already read both directions per company.
- `docs/data-analysis/case-money/STATE.md` — 113 `owns_stake` edges / 84 current; 18 role×register contradictions (AGROFERT-group dozorčí rady).
- `features/money/reachableMoney.ts:137-140` — attribution rule has role and company axes, no perimeter axis.

---

### #18 Buyer edge for Registr smluv contracts — the authority as a graph endpoint

`data-ingestion#3` · theme T7 · corroboration 4 · size L · effort 5 / impact 8 / risk 4 · result `unmeasurable` · gate `contract`
contexts: money-contract-ingest, money-analysis-triage, kg-pipeline
advances: public-money-attribution (both ends of a payment resolved through registry identifiers — the identifier-join contract in civic-knowledge-graphs); civic-entity-ontology (a new relation by deliberate amendment); law: non-partisan symmetry (an obec's contracts render for every obec, from the register)

#### Summary
Every harvested contract knows who published it (`publisher: {ico, nazev}` on the contract node), but the publishing authority is a string prop, not a node — so the graph can answer "which companies tied to MPs supply the state" and cannot answer "which MP-tied companies does THIS obec pay", while `/volby` builds its zadavatel pages from the ISVZ tender layer alone.

#### Description
`scripts/case-loops/money/persist-contract-harvest.ts:141` writes `publisher: rec.subjekt ? { ico: rec.subjekt.ico, nazev: rec.subjekt.nazev } : null` onto the contract node; `supplies` edges run company → contract only (`prop-registry.json` → `edges.supplies` carries `direction`, `recipients_shared`). The tender loop already mints authority nodes as ordinary `company:ico:*` and links them with `procures` (`scripts/case-loops/tender/persist-month.ts:146-148`), and `/volby` reads exactly that fold — "kind `tender` nodes + `procures`/`wins` edges + authority `company` nodes" (`docs/routes/volby.md:30-33`) — so an obec's page shows its tenders but none of its signed contracts, even where the harvest holds them. `smlouvy-dump.ts:32-42` (the `party_idnum` vs `subject_idnum` finding) and `harvest-contract-dumps.ts:41-45` (`publisherOnlyByIco` — publisher-side matches dropped, "kept as an auditable total") show the group has measured the buyer side and deliberately parked it. "Built" is an `awards` edge authority → contract (register fact: the `<subjekt>` of the record; weight = the contract's `amount` with its `amountBasis`; props `{platce, prijemce}` verbatim), authority nodes minted through the SAME `mintCompany` rule the tender loop uses (CZE only, 8-padded IČO, `memory/ico-node-id-canonical-form.md`), a `publisherOnly` harvest mode that keeps the records the current allow-list drops (the steward institutions' own contracting, sized in the state file), and a shared `features/volby/tenderLayer.ts`-style contract fold so `/volby/obec/[ico]` and `/volby/kraj/[slug]` render "smlouvy s firmami napojenými na poslance" beside tenders. `moneyReachesCompany()` / `tieIsAttributable()` stay the only attribution rules — the buyer edge adds an endpoint, never a new claim of reach.

#### Flow
- Amend `KG_EDGE_RELS` with `awards`; register `awards` props (`amount`, `amountBasis`, `platce`, `prijemce`, `direction_basis`) in `prop-registry.json` + `graph-schema.md`.
- Extract `mintCompany`/`isCzech` from `tender/persist-month.ts` into `lib/analysis/company-node.ts` (one definition) and import it from both persisters.
- `persist-contract-harvest.ts`: emit `awards` edges from `rec.subjekt` for every harvested record (existing and new), merge-preserving; disclose records whose `subjekt.ico` is null or foreign as a counted gap.
- `harvest-contract-dumps.ts --publisher-side`: for the arena authorities `/volby` serves (obce/kraje IČOs from `mirrorData.ts`), keep records where `subjekt.ico` is in the allow-list, bounded and disclosed (`every cap ships its population`), never a bulk personal-data corpus.
- Fold + surface: an `awards` read in the volby loader with `warnIfTruncated`, a `SourceNote` per figure citing `sourceUrl`; `/penize/firma/[ico]` gains "kdo platí" (payers by authority) using the same fold.
- Re-run `reachable-metric-audit.ts` and `supplies-coverage-audit.ts` on a copy to prove `/penize`'s headline is unchanged (the buyer edge must not move attribution).

#### Expected impact
A voter on `/volby/obec/[ico]` sees the register's own record of what their town signed with MP-tied companies — the first Registr smluv fact on the flagship voter surface; the money case files can name the paying authority; the tender loop's "authority" and the contract corpus's "publisher" become one node with one IČO rule. Measured by: `awards` edges ≈ harvested contract count (currently 2 287 + the batch-012 harvest), and the number of obec/kraj pages with ≥1 `awards` row. What could break: `awards` on a `payer` record would read as money flowing to the supplier — carry `platce/prijemce` verbatim and never let `awards` feed `moneyReachesCompany`.

#### Evaluation
Claim: user | quality - both endpoints of a public payment resolved through registry identifiers
Before: 0 authority → contract edges (edge rels: `supplies`, `procures`, `bids_on`, `wins` only); `publisher` exists only as a jsonb prop on contract nodes; `/volby` zadavatel pages read the tender fold only
After: `awards` edge count vs harvested contracts (disclosed split: CZE IČO / foreign / missing); obec pages with ≥1 register contract — the instrument is the volby loader's `awards` count per arena
Method: probe - read of the persister, the tender persister, the prop registry and the volby route contract
Result: unmeasurable
Gate: contract

#### Evidence
- `scripts/case-loops/money/persist-contract-harvest.ts:141` `publisher: rec.subjekt ? { ico: rec.subjekt.ico, nazev: rec.subjekt.nazev } : null,`
- `lib/kg/prop-registry.json` → `edges` = `[amends, assigned_to, owns_stake, supplies, cites, belongs_to, owns, co_votes_with, influential_in, linked_to, proposes_amendment, rebels_against, spoke_on, sponsors, rapporteur, procures, bids_on, wins]` — no authority → contract relation
- `scripts/case-loops/tender/persist-month.ts:146-148` — `mintCompany(l.authority)` + `rel: "procures"` from `company:ico:${l.authority.ico}`
- `docs/routes/volby.md:30-33` — reads "kind `tender` nodes + `procures`/`wins` edges + authority `company` nodes"
- `lib/ingest/sources/smlouvy-dump.ts:106-111` — `platce`/`prijemce` direction flags; `:32-42` in `smlouvy.ts` — `party_idnum` misses publisher-side contracts
- `scripts/case-loops/money/harvest-contract-dumps.ts:41-45` — `publisherOnlyByIco` dropped-but-counted
- `memory/supplies-is-not-attribution.md`, `memory/attribution-has-two-axes.md` — reach stays with `moneyReachesCompany` / `tieIsAttributable`

---

### #19 Obec-keyed deník: the municipality as a dated subject, lifting the `obec:` refusal

`civic-feed-transparency#1` · theme T7 · corroboration 4 · size L · effort 6 / impact 8 / risk 4 · result `unmeasurable` · gate `policy`
contexts: civic-chronicle, schranka-notifications, civic-feeds-verification, claim-verifier, volby, tender-loop
advances: accountability-publishing-ethics / accountless-notification-privacy + severity-free-factual-framing; law: non-partisan-symmetry, every-cap-ships-its-population

#### Summary
`/schranka` still parses `obec:<ičo>` follows but withdrew the affordance because "no stream is keyed by a municipality" — while `/volby` now folds 61 421 tender nodes with dated `wins.decided_on` per obec-as-zadavatel and composes dated findings the deník never sees; the voter surface has no feed and the inbox has nothing to deliver.

#### Description
Today: `features/schranka/followCodec.ts:114-122` accepts `obec:\d{6,8}` and `:291-297` says the key is deliberately not offered "dokud obecní datovaný fakt nebude existovat"; `features/denik/deriveDenik.ts:221-223` mints only `poslanec:`/`firma:`/`tisk:` keys. But `features/volby/tenderLayer.ts:31`, `:205-212`, `:245` carry `decided_on` per `wins` edge and `ended_on` per tender (earliest dated winner kept, never guessed), `lib/analysis/volby/rules.ts:102`, `:153`, `:170-171`, `:188` compose findings with `decidedOn`, and `features/volby/getVolbyHomeData.ts:30` already sorts "the 20 latest dated findings". `docs/routes/volby.md` lists no feed family and `features/data-releases/feedIndex.ts:40-70` names four families, none municipal. The dated fact exists; the chronicle does not carry it.

Built: a fifth deník layer — `tenderWins` (registry fact, rung 1: "obec X uzavřela zakázku s firmou Y, rozhodnuto D", source = the tender node's own URL, both `obec:<ičo>` and `firma:<ičo>` as entities) and `authorityFinding` (derived figure, rung 2: rule id + verbatim rule sentence from `/metodika#volby-<ref>`, dated `decidedOn`, no person name — preserving volby's refusal). `obec:` re-enters `deriveDenik` keys and `followCodec.entityHref` points to `/volby/obec/<ičo>`; `/volby/obec/<ičo>` links its deník and grows the `FollowButton`; a `/volby/feed.{xml,json}` family joins `feedIndex.ts` through the ONE codec (`features/denik/feedCodecs.ts`) as a channel. `change_event.entity_keys` (`ddl.ts:352-364`, GIN-indexed jsonb) already admits arbitrary keys, so the "zaznamenáno" stream carries `tender-new` without schema change.

#### Flow
- `features/denik/getDenikData.ts`: `readTenderLayer()` over the memoised `tenderLayer` fold (cross-request memo already exists; cite its cost 3,8 s cold / 134–242 ms warm), with `limits.tendersUndated` counted and rendered.
- `deriveDenik.ts`: kinds `tenderWin` + `authorityFinding`, `obecEntityKey`, KIND_ORDER after `organRole`; `kindLabels.ts`, `/schranka` `kindVocabulary.ts` in the same order.
- `followCodec.ts`: re-offer `obec:`; `FollowButton` on `/volby/obec/[ico]`; `telemetryScrub` already scrubs by parameter — no change, but the test gains an `obec:` case.
- `feedIndex.ts` + `app/volby/feed.{xml,json}/route.ts`; `feedIndex.test.ts` verifies against `app/`.
- Symmetry test: every authority in the fold with a dated win can produce a row; findings render P1 "čistý radar" with the same weight as N1–N4.
- Docs: `docs/routes/{denik,schranka,volby,data}.md`.

#### Expected impact
A voter following their obec receives dated procurement facts and rule-based findings in the same inbox as their MP; measured by `obec:` follows that deliver ≥1 row (today structurally 0) and by the volby feed's subscriber count. Could break: the deník's day-book grows by up to 31 744 dated wins — `DAYS_SHOWN`/`FEED_ENTRIES` caps bind sooner and must ship their population.

#### Evaluation
Claim: user - an `obec:` follow delivers rows instead of an explanation of why nothing arrives
Before: `obec:` keys parse (`followCodec.ts:122`) and deliver nothing by design (`:291-297`); 0 feed families keyed by obec (`feedIndex.ts:40-70`)
After: MISSING INSTRUMENT — per-obec count of dated `wins` in the live fold (16 955/31 744 wins carry `price_czk` per volby.md; the dated share is not recorded)
Method: probe - read of followCodec, deriveDenik keys, tenderLayer date handling, rules.ts `decidedOn`, feedIndex families
Result: unmeasurable
Gate: policy

#### Evidence
- `features/schranka/followCodec.ts:114-122`, `:291-297` — key accepted, affordance withdrawn "until a municipal dated fact exists"
- `features/denik/deriveDenik.ts:221-223` — three entity keys only
- `features/volby/tenderLayer.ts:31`, `:205-212`, `:245` — `decided_on` / `ended_on`, never guessed
- `lib/analysis/volby/rules.ts:102`, `:153`, `:170-171`, `:188` — findings carry `decidedOn`
- `features/volby/getVolbyHomeData.ts:30` — "latest dated findings" already sorted
- `features/data-releases/feedIndex.ts:40-70` — four families, no volby
- `lib/db/pglite/ddl.ts:352-364` — `change_event.entity_keys` jsonb GIN; keys are open-ended

---

### #20 The municipality as a graph subject shared by /rozpocty, /volby and /penize

`financial-transparency#2` · theme T7 · corroboration 4 · size XL · effort 8 / impact 8 / risk 4 · result `unmeasurable` · gate `contract`
contexts: budget-mirror, money-ledger-graph, kg-analysis, graph-writers, volby, data-ingestion
advances: state-budget-analysis/municipal-money-trail (clauses 1-4 + "live state over frozen batch"); civic-knowledge-graphs; laws one-definition-one-import, missing-is-not-zero, every-cap-ships-its-population

#### Summary
The obec exists three times: as a packed registry row + a frozen pass-41 supplier batch on `/rozpocty`, as an authority `company` node with `electoral_arena` in the tender layer on `/volby`, and as an owner node in `owns_stake` on `/penize/firma`. There is no municipality node kind, so the three surfaces cannot cite one address, the trail is 33 passes stale, and the budget covers 132 of 6 254 obce although the bulk source covers all.

#### Description
- `/rozpocty`'s supplier trail is a checked-in batch: `SUPPLIERS_PASS = 41`, `SUPPLIERS_RETRIEVED_ON = "2026-07-27"` (`features/budget/data/municipalSuppliers.generated.ts:10-13`); the graph is at pass 74 (`docs/data-analysis/case-tender/STATE.md`) with batch-014's `direction` semantics and batch-015/018's `public_mandate` — none of which the trail sees. `deriveMunicipalSupplierRows` re-derives direction from `partyDirections` with its own two-party rule (`features/budget/supplierTrail.ts:243-256`) rather than importing `moneyReachesCompany`; `getSupplierTies` joins ties live but against the stale IČO set (`features/budget/getSupplierTies.ts:36-43`).
- Budget series: `SNAPSHOT_FLOOR_POPULATION = 10000`, 132 towns (`budgetSnapshots.generated.ts:13`), while `docs/data-analysis/budget-sources.md` verifies the FINM bulk ZIP carries every obec per period and the SIMU API gives debt p.c. + population per IČO. `peerGroups.ts:9-11, 30` then widens to the whole country at `MIN_PEERS = 5` because the kraj group is thin — a small-sample widening forced by the ingest cap, not by the population.
- `/volby` folds obce as authority `company` nodes and corrects `electoral_arena` from the 6 254-obec registry at read time (`features/volby/tenderLayer.ts:19-29`), and it explicitly refuses "any obec↔authority link beyond IČO equality (příspěvkovky and city companies are the national `nejasne` count, never localised)" (`docs/routes/volby.md`); the tender STATE's open item 1 is exactly that: resolve 1 575 `nejasne` authorities via ownership/legal form with `public-body.ts`.
- `graph-schema.md:37-45` has no municipality kind; `public-body.ts` already knows form 801/811/804 (`lib/analysis/public-body.ts:52-57`), and `owns_stake` already carries Město Plzeň / Praha as parents (`features/money/ownership.ts:5-9`).

Built: a `municipality:<ico>` node kind (or a declared `company` subtype keyed by legal form 801/811/804 — one ruling, recorded) carrying the registry props (name, kraj, population per period) and yearly FINM/SIMU budget props for ALL obce; edges: `party_to` municipality→contract with the register's own direction flag (the money graph already holds `parties`/`partyDirections` on contract nodes — `supplierTrail.ts:225-243`), `procures` (exists), `founded`/`owns_stake` municipality→příspěvková organizace / city company from ARES VR (the `ownership-from-vr.ts` tool, generalised). Then: `/rozpocty`'s trail becomes an indexed `kgNeighbours` read on the municipality node (the `/penize/firma` pattern: `loadCompanyMoneySlice`), the batch file is deleted; the "doložená platba" rule and the reach rule are one import; `/volby`'s arena correction reads the node instead of the packed registry; the `nejasne` class resolves through the disclosed one-hop ownership edge and stays SEPARATE from the town's own figure (clause 1: never summed upward — rendered as "zřizuje / vlastní", a link, not a number); the budget mirror's peer groups widen only when the population is genuinely thin.

#### Flow
- Ruling first: node kind vs subtype; record in `graph-schema.md` + prop-registry; municipality id = canonical 8-digit IČO (`companyId.ts`).
- Ingest: FINM bulk + SIMU per IČO for all obce (annual, cached) → `municipality` props by period; registry props from the existing generator; pass-stamped via `persist-batch.ts`.
- Edges: `party_to` from contract node `parties`/`publisher` (already in the graph, no network); `founded`/`owns_stake` from ARES VR (`spolecnici`/`zakladatel`) for obce ≥ the tender authority set first, all obce later; every edge dated + sourced.
- Reads: `features/budget/getTownData.ts` server loader (`kgNeighbours` on the municipality node for `party_to` + `procures`, then per-company `readCompanySupplies`); `supplierTrail.ts` keeps its pure derivation but consumes graph rows and imports `moneyReachesCompany`; `generate-municipal-suppliers.ts` retired.
- `/volby`: `tenderLayer.ts` arena from the node; `nejasne` resolution via the one-hop edge; the `/volby/obec/[ico]` page and `/rozpocty/[ico]` link each other and cite the same claim subject (`municipality:<ico>`).
- Claims: `MONEY_METRIC` gains `smlouvy-obce`; `/zdroj` re-derives from the node read.
- Peer groups: recompute `MIN_PEERS` widening over the full population; print the sample per median (already the rule).

#### Expected impact
A voter looking up their town gets one address that carries budget, contracts (live, direction-disclosed), tender radar and the town's own companies as a disclosed hop — instead of a 2026-07-27 snapshot beside a pass-74 radar. Measured: trail staleness (days since `SUPPLIERS_RETRIEVED_ON` → same-pass as the graph), budget coverage 132 → 6 254 obce, `nejasne` authorities resolved with a cited owner (0 → n of 1 575), peer-group scope "celostátní" share before/after. Could break: `/rozpocty` becomes a store-dependent route (needs the honest `DataUnavailable` path the doc already mandates) and ~360 prerendered town pages become dynamic like every other route (`revalidate-is-inert` memory).

#### Evaluation
Claim: quality - one municipality subject, live and cited, replaces three unlinked representations
Before: trail batch pass 41 vs graph pass 74; budget 132/6 254 obce; 1 575 `nejasne` authorities unresolved; 0 municipality nodes
After: same-pass trail; budget on every obec; MISSING INSTRUMENT: a per-obec coverage tile (budget years / contracts / tenders / owned bodies) that does not exist because no node holds the obec
Method: probe - compared the generated constants with STATE.md passes; read the trail's private direction rule and volby's registry correction
Result: unmeasurable
Gate: contract

#### Evidence
- `features/budget/data/municipalSuppliers.generated.ts:10-22`; `features/budget/data/budgetSnapshots.generated.ts:13-16`.
- `features/budget/supplierTrail.ts:9-24, 193-199, 243-256` — published rule, weight-only read, private direction test.
- `features/budget/getSupplierTies.ts:36-58` — live ties keyed on the batch's IČO set.
- `features/budget/peerGroups.ts:9-11, 30` — widening at `MIN_PEERS = 5`.
- `features/volby/tenderLayer.ts:19-29`; `docs/routes/volby.md` "What it refuses to derive".
- `docs/data-analysis/case-tender/STATE.md` open item 1 (1 575 `nejasne`, 134,8 mld. floor).
- `docs/data-analysis/budget-sources.md` — FINM bulk for all obce verified live.
- `docs/data-analysis/graph-schema.md:37-45` — no municipality kind; `lib/analysis/public-body.ts:52-57` — forms 801/804/811.

---

### #21 Release-stamped entity sitemap — case files discoverable without per-request scans

`landing-navigation#4` · theme T7 · corroboration 4 · size L · effort 4 / impact 6 / risk 2 · result `unmeasurable` · gate `contract`
contexts: app-shell, shell-navigation (cross: data-releases, graph-writers)
advances: law non-partisan-symmetry ("the method covers the whole population or covers nothing" — all 207 MPs, all firms, all laws listed, or none); accountability-publishing-ethics / public-role-scope-limit; repo convention: base URL from request headers, never a guessed domain

#### Summary
The sitemap deliberately omits every entity case file — `/poslanec/[id]`, `/zakony/[cislo]`, `/penize/firma/[ico]`, `/zdroj/[ref]` — because listing them "would mean enumerating specific MPs, tisky and firms, i.e. reading the store at request time" (`publicRoutes.ts:338-343`, `sitemap.ts:449-453`); the platform's most-cited pages depend on a crawler clicking through a directory page. The release train already produces the artefact that lifts the objection: a versioned, sealed cut of the graph.

#### Description
`app/sitemap.ts:477-494` is `force-dynamic`, builds only `publicStaticRoutes(DISALLOWED_PATHS)` plus the one static registry exception (`municipalRouteIcos()`, `:455-462` — allowed precisely because it is "a static module built into the build, reads no store"), and omits `lastModified` on principle (`:469-471`: "datum poslední změny stránky nikde neevidujeme … čas sestavení odpovědi není datum změny obsahu"). `robots.ts:432` already publishes the `Sitemap:` line. `features/data-releases/manifest.ts:46-51` gives every release a `version` (`YYYY.MM.DD`) and `cutAt`; the graph-writer step that cuts a release knows exactly which entity ids exist (kind counts are already in the manifest via `kgKindCounts`).

The objection has two halves and the release train answers both: the enumeration is symmetric (every current mandate holder, every company node with a canonical IČO, every law with a dossier — never a shortlist), and the read is per RELEASE, not per request. "Built": a `release_index` (ids per kind per release version) written once by the release-cutting writer; `app/sitemap.ts` becomes a sitemap INDEX pointing at `/sitemap/<version>/poslanci.xml`, `/sitemap/<version>/firmy.xml`, `/sitemap/<version>/zakony.xml` (plus the existing static list), each generated from `release_index` (one indexed read, capped and disclosed in a comment element) with `lastModified = cutAt` — which IS a content-change date, the exact thing the current file says it lacks. `/zdroj/[ref]` stays out: receipts are minted per claim, not per entity, and a sitemap of receipts would be a fabricated enumeration.

#### Flow
- `release_index` table (additive DDL via `db:migrate`; writer = the release-cut path in `scripts/data-analysis`; columns `version, kind, id`); `getDataReleasesData` exposes `entityIndexCounts` on `/data` so the sitemap's population is a published number.
- `features/shell/publicRoutes.ts`: keep `publicStaticRoutes`; add `entitySitemapRoutes(version)` returning the three sub-sitemap paths, unit-tested against `DISALLOWED_PATHS` and dynamic-route exclusion exactly as today.
- `app/sitemap.ts` → sitemap index (Next `generateSitemaps` or a manual index route); `app/sitemap/[version]/[kind]/route.ts` renders one kind from `release_index`; base URL from headers as now; an unknown version answers 404, a version whose index is empty renders an empty urlset with the population stated.
- Symmetry guard: a colocated test asserts the MP sub-sitemap's count equals the chamber's current-mandate count from the same release (the `membership` on organ 174 rule from memory) — a partial list fails the build.
- `docs/routes/app-shell.md` + `data.md`: the "přiznaná neúplnost" sentence is retired and replaced by the release-stamped rule.

#### Expected impact
Search engines and archive crawlers index the 207 spisy, the firm case files and the law dossiers from a dated, complete list instead of by luck; `lastModified` becomes an honest signal. Measured by: URLs in the sitemap set 46 static + 132 obce → +207 +196 +101 (release-bound); crawl discovery of `/poslanec/*` in server logs before/after. What could break: a release cut from a degraded manifest (`degraded: true`) must not become the "latest" sitemap — reuse the manifest's own `degraded` verdict as the gate.

#### Evaluation
Claim: other (reach) — the accountability record's primary pages enter the crawler's invitation list, symmetrically and dated
Before: `publicStaticRoutes()` + `municipalRouteIcos()` only; 0 entity case files in the sitemap; `lastModified` absent everywhere (`sitemap.ts:469-471`)
After: three release-stamped sub-sitemaps, counts published on `/data`; instrument: the symmetry test (sitemap MP count == current mandate holders) and the `/data` counts
Method: probe — read `sitemap.ts`, `publicRoutes.ts`, `robots.ts`, the release manifest
Result: unmeasurable
Gate: contract — a new table written by the release path; the sitemap index format is a public promise

#### Evidence
- `features/shell/publicRoutes.ts:338-343` "Dynamické segmenty … vypsat je by znamenalo vyjmenovat konkrétní poslance, tisky a firmy — tedy číst za běhu úložiště. Tahle sitemapa to VĚDOMĚ nedělá"
- `app/sitemap.ts:455-462` the municipal exception: static registry, no store read, `generateStaticParams` already emits them
- `app/sitemap.ts:469-471` `lastModified` omitted: "čas sestavení odpovědi není datum změny obsahu"
- `app/sitemap.ts:477` `export const dynamic = "force-dynamic";`; `:487-490` route list = static + `municipalRouteIcos()`
- `app/robots.ts:432` `sitemap: \`${proto}://${host}/sitemap.xml\``
- `features/data-releases/manifest.ts:46-51` `version: string | null` (`YYYY.MM.DD`), `cutAt`, `degraded`
- `CLAUDE.md` route table: 207 MPs (`/zebricek`), 141 bills → 101 laws (`/zakony`), 196 companies (`money-loop` skill description)

---

### #22 Structured graph provenance: source + run + pass on every kg row, sealed per pass

`infrastructure-observability#3` · theme T4 · corroboration 3 · size XL · effort 8 / impact 9 / risk 6 · result `better` · gate `irreversible`
contexts: graph-admin-data-routes, analysis-quality, testing-sentinel, data-releases, graph-writers, kg-pipeline
advances: claim-verification-and-provenance / derivation-comparison (uniform/mixed/absent over populations); law provenance-or-nothing; law every-cap-ships-its-population; civic-knowledge-graphs

#### Summary
`kg_node`/`kg_edge` carry a free-form `provenance jsonb` and no `source`/`ingest_run_id`, so the atlas cannot score 9 of 12 declared sources (including the entire `/penize` foundation), the Merkle seal covers eight entity tables and none of the graph, and the snapshot cannot say where a row came from.

#### Description
Today: `lib/db/pglite/ddl.ts:224-246` — `kg_node(id, kind, label, props, first_seen_pass, provenance jsonb)`, `kg_edge(src, rel, dst, weight, props, provenance jsonb)`; no `source`, no `ingest_run_id`, no index on provenance. `/atlas` records the refusal verbatim (`docs/routes/atlas.md` "Unrated is structurally never zero"): "`kg_node`/`kg_edge` have no `source` column and no `ingest_run_id`, so no join key runs to `ingest_run`" — nine of the twelve `INGESTED_SOURCES` (`lib/analysis/atlas.ts:192-208`, landing `graph`) print no number, and the route file explicitly says scoring `kg_*` was ruled out because it would break the integrity rule's printed claim that "the sealed tables and the scored tables are the same set". The seal is `RUN_TABLES` (`lib/db/pglite/repositories/ledger.ts:28-37`, eight entity tables); the sentinel's `SENTINEL_ENTITY_TABLES` (`facts.ts:101-110`) is the same list, so freshness/coverage/uniformity are judged for psp entity rows only while `supplies` (19 266 in the snapshot cut), `linked_to`, `amends` and `owns_stake` — the edges readers actually cite — have no run, no seal and no freshness. The graph writers merge-preserve props (memory: `kg-upsert-replaces-props`), `persist-batch` refuses keys not in `lib/kg/prop-registry.json`, and `contribution_provenance.{pass, ref}` is the one structured stamp — precisely the one the sentinel can compare (`invariants.ts:372-417`); every other layer's stamp is whatever its writer put in `provenance`. `/data`'s snapshot ships `nodeKinds`/`edgeRels` in-cut vs in-store but cannot ship "rows per source" because the column does not exist.

Built: a provenance CONTRACT — `provenance` becomes a validated shape `{source, ingest_run_id, pass, ref, writer}` enforced by the writers (`kg-compute`, `kg-promote`, `persist-batch`, `kg-forensics`, `kg-money-ingest`) with `source` ∈ `INGESTED_SOURCES` and `ref` a declared formula/batch ref; two generated columns (`source text generated always as (provenance->>'source') stored`, `ingest_run_id bigint …`) indexed, so `ingest_run` finally joins to the graph without a rewrite of `props`; graph passes become `ingest_run` rows (they already have start/finish/status semantics) and get Merkle-sealed over the rows they wrote, extending `RUN_TABLES` by `kg_node`/`kg_edge` filtered by run. Then: `/atlas` scores all twelve sources with the same four rules and the integrity rule's printed sentence stays true; the sentinel gains `graph-provenance-uniformity` per layer (contribution, money, law) and `graph-seal` (every graph run sealed and verifying); `/data` snapshot `limits` carries in-cut vs in-store per SOURCE; `/graf/p/[ref]` and `/zdroj` receipts print the run + pass their edge came from instead of the platform's four blanket sources (the "honest carry-over" in `docs/routes/graf-permalink.md`); the historical rows whose provenance cannot be reconstructed are stamped `source: unknown` and COUNTED on `/atlas` as such — never backfilled with a plausible source.

#### Flow
- `lib/kg/provenance.ts`: the one `KgProvenance` type + `assertProvenance()`; `prop-registry.json` gains the `provenance` keys; `persist-batch`/`kg-promote`/`kg-compute` refuse a write without it (behind `--allow-unstamped` for the migration pass only)
- `db:migrate`: two stored generated columns + indexes on `kg_node`/`kg_edge` (additive; `open()`'s DDL drift check must report it, snapshot first)
- Migration pass: walk every row, derive `{source, pass, ref}` from what `provenance` already holds where unambiguous (the contribution and forensic stamps are), else `source: "unknown"`; report counts per outcome; this is a `kg-promote`-style merge, never a props replace
- `ingest_run` rows for graph passes (writer = script name, note = batch id); `LedgerRepository.sealRun` extended to `kg_node`/`kg_edge where ingest_run_id = $1`; `getLedgerHeads` shows graph seals; `/data` §integrity lists them
- `/atlas`: `readEntityCoverage` over the twelve sources via the new columns; unscored list shrinks to `generated-module`/`none` landings only; `ATLAS_RULES` prose updated in the same change (rule text and score cannot drift — `messages.test.ts`)
- Sentinel: `graph-provenance-uniformity` (per layer: one `{pass, ref}` or a named split with counts), `graph-seal`, and `freshness` extended to graph sources with declared cadences
- Receipts and permalinks read `provenance.source`/`ingest_run_id` for the citation line; `sourceLinks.ts` keeps the URL rules

#### Expected impact
Readers of `/atlas` see the money and law foundations scored instead of absent; a citing reader of a `supplies` edge gets its run and pass; a half-applied money pass trips the sentinel the way a half-applied score pass does. Measured by: sources scored (3 → up to 12), sealed row population (8 tables → 8 + graph runs), rows with `source: unknown` after migration (a number to burn down, printed). What could break: the writers — every kg writer touches this; and a migration that guesses a source would violate disclose-never-repair, hence `unknown` is a first-class value.

#### Evaluation
Claim: quality — provenance becomes comparable over the whole graph population, not one layer
Before: 0 join keys from `kg_*` to `ingest_run`; 3/12 sources scored; 1 structured stamp (`contribution_provenance`); graph rows sealed: 0
After: 12/12 scorable; per-layer uniformity checks; every graph pass sealed; snapshot/receipts cite source + run
Method: probe — read `ddl.ts:224-246`, `ledger.ts:28-37`, `facts.ts:101-110`, `atlas.ts:192-208`, `docs/routes/atlas.md` refusal paragraph
Result: better
Gate: irreversible

#### Evidence
- `lib/db/pglite/ddl.ts:224-246` kg tables: no `source`, no `ingest_run_id`
- `docs/routes/atlas.md` "`kg_node` / `kg_edge` have no `source` column and no `ingest_run_id`, so no join key runs to `ingest_run`" and "Scoring `kg_*` coverage is deliberately out of scope"
- `lib/analysis/atlas.ts:192-208` `INGESTED_SOURCES` — nine with `landing: "graph"`
- `lib/db/pglite/repositories/ledger.ts:28-37` `RUN_TABLES` = eight entity tables; `lib/testing/sentinel/facts.ts:101-110` same pinned list
- `lib/testing/sentinel/invariants.ts:372-417` uniformity exists only for `contribution_provenance`
- `docs/routes/data.md` snapshot `limits` carries `nodeKinds`/`edgeRels`, nothing per source
- `docs/routes/graf-permalink.md` "for `cesta`/`trasa` the sources still fall back to all four registries"
- `memory/kg-upsert-replaces-props.md`, `memory/loop-artifact-hygiene.md` (`persist-batch` refuses unregistered jsonb keys — the enforcement point already exists)

---

### #23 World time on the graph: valid_from/valid_to written, read, and joined to votes

`data-layer#1` · theme T5 · corroboration 3 · size XL · effort 8 / impact 9 / risk 6 · result `better` · gate `policy`
contexts: db-store, db-repositories, kg-analysis, graph-writers (scripts/data-analysis), money-ledger (features/money), profile (features/profile), votetrack, volby
advances: civic-knowledge-graphs / civic-entity-ontology (temporal validity of a hop) + evidence-path-finding; conflict-of-interest-detection (tie held AT the vote); laws: missing-is-not-zero, non-partisan-symmetry

#### Summary
The DDL declares world time on every kg claim and no writer populates it (`ddl.ts:288-290`); four features carry their own ad-hoc world time in props or in registry rows, so the product cannot answer "did the tie hold on the day of the vote" for the whole population.

#### Description
`lib/db/pglite/ddl.ts:288-290`: "valid_from / valid_to — WORLD time … Writers do not populate these yet (KgNodeRow/KgEdgeRow carry no world-time fields)". `lib/db/types.ts:174-202` confirms neither row type has them; `KG_EDGE_COLS`/`NODE_CONTENT` in `kg.ts:94-99` exclude them from the content tuple, so even a writer that set them would not trip the changed-content guard. Meanwhile world time leaks into props: `features/money/moneyLoader.ts:223` reads `props.role_valid_from`; `features/profile/careerSpine.ts:45,299` derives spans from `membership` rows; `features/denik/deriveDenik.ts:272,714` dates from its own `validFrom`; `memory/or-shareholder-entry-semantics.md` records that OR dates are registration dates, not acquisition dates — a semantics that belongs on the column, not in four readers. The DDL comment also collapses two states into one NULL ("unknown/unbounded"), which the missing-is-not-zero law forbids: "not known" and "still holds" are different facts.

Built: `validFrom`/`validTo` plus a `validity` tri-state (`known | open | unknown`) on `KgEdgeRow`/`KgNodeRow`; `mapKgEdge`/`mapKgNode` (`mappers.ts`) surface them; `supersedeUpsertChunk` includes them in the content tuple so a corrected date supersedes the old version; every writer that already knows a date (money ties from ARES VR `spolecnik[]` dates, `supplies` from contract signature, `belongs_to`/`influential_in` from `membership.from_at/to_at`, `sponsors` from tisk dates) writes it, and writers that don't write `unknown` — never a default. Then the cross-module payoff: a `kgNeighboursAt({ id, rels, worldTime })` read, and a deterministic "tie-at-vote" derivation in `lib/analysis/` that joins `linked_to`/`owns_stake` validity spans to `vote_event.voted_on` for all 207 MPs symmetrically, surfaced on `/poslanec` (money ties beside the votes they overlap) and `/volby` (which findings held during the term). Absence of overlap is a first-class finding.

#### Flow
- Types + mappers + content tuples (`types.ts`, `mappers.ts`, `kg.ts`) — additive; existing rows read `unknown`.
- `lib/kg/prop-registry.json`: retire `role_valid_from`-style keys in favour of the columns; `persist-batch` refuses the old keys after a one-pass backfill (`kg-repair-*` precedent, replayable via `asOf`).
- Backfill pass per relation family, each with its own provenance stamp and ledger line; `guardKgReset` unchanged (no reset).
- `Store.kgNeighboursAt` + `asOf` combined read (record time × world time) with the same `warnIfTruncated` discipline.
- `lib/analysis/tieAtVote.ts` (deterministic, tested over fixtures): overlap spans → per-MP list of (vote, tie) pairs with the tie's `review_state` carried; `pending_review` never asserted as a conflict — it is a lead.
- Surfaces: `/poslanec` money section, `/volby` timeline, `/denik` dating from the column not the reader.

#### Expected impact
Readers and newsrooms get the accountability triangle on a time axis for every MP, not a shortlist; measured by the share of `linked_to`/`owns_stake`/`supplies` edges with `validity != unknown` (0 % today) and by a new "ties overlapping ≥1 roll call" count on `/poslanec`. Could break: a registry date read as an acquisition date — the column must carry the semantics label (`registration` vs `event`) or the join lies.

#### Evaluation
Claim: quality - the graph answers "connected WHEN", today only "connected"
Before: 0 kg rows with `valid_from` populated (DDL comment; no writer sets the columns; no mapper reads them); world time held in 4 feature-local shapes
After: per-relation coverage of world time reported on `/atlas` as a measured share; the tie-at-vote derivation runs over all 207 MPs
Method: probe - DDL, types, mappers, grep of `valid_from|validFrom` across features
Result: better
Gate: policy

#### Evidence
- `lib/db/pglite/ddl.ts:288-290,298-305` — world-time columns declared, "Writers do not populate these yet", NULL conflates unknown/unbounded
- `lib/db/types.ts:174-202` — `KgNodeRow`/`KgEdgeRow` without world-time fields
- `lib/db/pglite/repositories/kg.ts:94-99` — content tuples exclude `valid_*`
- `features/money/moneyLoader.ts:223` — `props.role_valid_from` (world time in props)
- `features/profile/careerSpine.ts:45,299`, `features/denik/deriveDenik.ts:272,714` — reader-local world time
- `memory/or-shareholder-entry-semantics.md` — registration-not-acquisition semantics currently held only in memory

---

### #24 Three money channels as dated graph edges (subsidy + donation join contracts)

`financial-transparency#0` · theme T5 · corroboration 3 · size XL · effort 8 / impact 9 / risk 5 · result `unmeasurable` · gate `contract`
contexts: kg-analysis, money-ledger-graph, money-cases-review, ingest-external-sources, graph-writers
advances: public-money-attribution/citable-money-claims + conflict-of-interest-detection/temporal-alignment-of-money-and-role + statute-relevance-mapping; laws every-cap-ships-its-population, deterministic-code-owns-numbers

#### Summary
Contracts are dated, directed, basis-tagged edges; subsidies and party donations are two undated scalars on the company node. Every surface that sums, gates or cites "reach" treats the three channels as one number, so two of the three cannot be dated, direction-checked, capped or cited.

#### Description
Today the money layer is asymmetric by construction. `lib/analysis/kg-money.ts:128` writes `supplies` edges with `signedOn` on the contract node and `direction`/`amountBasis`/`recipients_shared` on the edge (batch 014); `docs/data-analysis/graph-schema.md:42` records the company node carrying only `subsidies_total_czk` and `donated_to_party_czk` as props — no `subsidy` node, no `donates_to` edge. `mapLinkedToTie()` reads them straight off the node (`features/money/moneyLoader.ts:179-180`, `216-219`), and from there:
- `bucketReachCzk = contractCzk + subsidiesCzk` (`features/money/reachableMoney.ts:176`) adds a value that has no direction predicate (`moneyReachesCompany`, `:95`, runs on `supplies` only), no basis disclosure (`amountBasis.ts` counts contract rows only), no cap population and no per-grant citation. `moneyClaims.ts:116-144` mints `dosah-vazby` over that sum — a citable figure whose subsidy half cannot be re-derived at `/zdroj` below the company total.
- `/penize/strety`'s statute table fires `SUBSIDY_STATUTES` and `DONATION_STATUTES` on `subsidiesCount > 0` / `donatedToPartyCzk > 0` (`features/money/collisions/statuteRelevance.ts:38-51`), while gate 4 (`voteInRolePeriod`, `:80`) can only be day-precise for the vote — a grant drawn in 2012 makes a 2026 vote on 218/2000 a candidate exactly as a grant drawn last month does. The registry's temporal-alignment technique ("money flows are weighted by how much of them lands inside the window") is unimplementable for two of three channels.
- `docs/data-analysis/case-money/STATE.md` item 5: "SZIF subsidy channel absent from the corpus" — the largest Czech subsidy payer is not in the sum that the surface already labels as subsidies.
- `reviewSignal()` (`features/money/reviewTypes.ts:312-319`) lifts a tie for a "triangle" (contracts + subsidies + donation on one firm, `moneyLoader.ts:186`) — a compounding pattern the registry names, computed over one dated and two undated inputs.

Built: `subsidy:<source>:<id>` nodes (CEDR/DotInfo + SZIF open data, each with `amount`, `decidedOn`/`paidOn`, `provider`, `programme`, `amountBasis`) with `receives_subsidy` company→subsidy edges; `donation:<year>:<n>` or a direct `donates_to` company/person→party edge from the ÚDHPSH annual reports with `date`, `amount`, `donor_ico`. `kg-money.ts` gains two more pure builders beside `supplies`; `graph-schema.md` gains the rows; the supplies fold (`moneyLoader.ts:396-432`) becomes three channel folds sharing one `moneyReaches*` predicate family; `ReachableTie` carries per-channel `{czk, count, coverage}` and `reachableMoney` keeps the split; `MONEY_METRIC` gains `dotace-firmy` / `dary-firmy` so an aggregate claim is the sum of three citable sub-claims; `relevantStatutesFor` receives dated channel events and gates on the channel's date inside the role window, not on `count > 0`.

#### Flow
- Write the schema + prop-registry rows (`lib/kg/prop-registry.json`, `graph-schema.md`) for `subsidy`, `donates_to`, `receives_subsidy`; add the pure builders in `kg-money.ts` with fixtures (no invented data).
- Ingest adapters in `lib/ingest/sources/` (CEDR/DotInfo, SZIF, ÚDHPSH), IČO-keyed only (`canonicalIco`), every record with source URL + accessedAt; persist via `persist-batch.ts` with a new pass; keep the legacy scalar props untouched until parity is measured.
- Parity gate: replay `subsidies_total_czk` from the new edges per company and report every company where the two differ; do not rewrite the scalar, disclose the delta on the company file.
- Refactor the fold: three memoized channel folds on `MONEY_MEMO_TTL_MS`; `CompanyContracts` → `CompanyChannel` × 3; `toReachableTie()` is the only constructor, so every caller receives the new shape by construction.
- Claims: extend `moneyClaims.ts` with per-channel metrics; `/zdroj` re-derives each; `/penize/[pspId]/paket` cites channel sub-totals.
- Strety: `relevantStatutesFor(tie, events)` takes dated channel events; `voteInRolePeriod` stays the one window test; coverage gains "grants inside window / total".
- Surfaces: ledger reach cell and the company headline print the three-way composition through one shared `ChannelDisclosure` (the `BasisDisclosure` pattern).

#### Expected impact
Readers and journalists get a reach figure whose every koruna has a dated, directed, cited edge behind it; the review console sees which channel a lead rests on; strety stops manufacturing subsidy candidates from undated counts. Measured by: number of citable claims per tie (today 1 aggregate; after 3 + aggregate), strety candidate count before/after the date gate once ties are verified, and the parity delta table. What could break: the headline moves if Hlídač's subsidy totals disagree with the primary registers — the delta must be disclosed, never reconciled by repair.

#### Evaluation
Claim: quality - two of three money channels become dated, directed, citable facts instead of undated scalars
Before: 0 subsidy nodes / 0 donation edges in `graph-schema.md`; `bucketReachCzk` sums an undated `subsidiesCzk` into a figure minted as one claim (`moneyClaims.ts:116`); `SUBSIDY_STATUTES` gates on `subsidiesCount > 0` with no date (`statuteRelevance.ts:49`)
After: per-channel dated edges; strety subsidy relevance requires a grant date inside the role window; MISSING INSTRUMENT: a parity report new-edges-vs-`subsidies_total_czk` per company (does not exist yet)
Method: probe - read the writer, the fold, the claim minter and the statute table; confirmed no subsidy/donation node kind and no date on either channel
Result: unmeasurable
Gate: contract

#### Evidence
- `docs/data-analysis/graph-schema.md:42-43` — company props `subsidies_total_czk`, `donated_to_party_czk`; contract node carries `signedOn`; no subsidy/donation kind.
- `lib/analysis/kg-money.ts:128` — `props: { amount, signedOn, supplierIco }` on the contract node only.
- `features/money/moneyLoader.ts:179-180, 186, 216-219` — subsidies/donations read as node scalars; `triangle` computed over them.
- `features/money/reachableMoney.ts:95-97, 176` — `moneyReachesCompany` is `supplies`-only; `bucketReachCzk` adds undated subsidies.
- `features/money/collisions/statuteRelevance.ts:38-51, 80-85` — count-gated relevance, day-precise window only for the vote.
- `features/money/reviewTypes.ts:312-319` — `reviewSignal` lifts on the triangle.
- `docs/data-analysis/case-money/STATE.md` open item 5 — "SZIF subsidy channel absent from the corpus".

---

### #25 Sealed graph passes with per-claim inclusion proofs

`data-layer#2` · theme T4 · corroboration 3 · size L · effort 6 / impact 8 / risk 3 · result `better` · gate `contract`
contexts: db-store, db-repositories, graph-writers (scripts/data-analysis), data-releases (features/data-releases), provenance-receipts (features/shared/provenance), graph-permalink (features/graph), sentinel (lib/testing/sentinel)
advances: claim-verification-and-provenance / claim-ref-addressing ("permanent machine-checkable address") + recomputation-receipts; civic-knowledge-graphs / destructive-rebuild-guard; laws: provenance-or-nothing, incident-anchored-doctrine (the 2026-08-05 restore-under-you)

#### Summary
The ledger can seal an ingest run with a Merkle root, but nothing calls it, the derived graph (`kg_node`/`kg_edge`) is outside the sealable set, and no receipt can hand a reader a proof that the row it shows was in a sealed state — so the "tamper-evident" claim covers only the review chain.

#### Description
`lib/db/pglite/repositories/ledger.ts:31-40` pins `RUN_TABLES` to the eight raw tables; `kg_node`/`kg_edge` — the tables every published number rests on (`docs/db-architecture-guide.md` accounting: 232 655 nodes / 371 137 edges) — are not sealable. `sealIngestRun` (ledger.ts:168-190) has zero production callers (grep over `scripts/`, `lib/ingest/`, `features/`: only the repository and its test), so `/data`'s "sealed runs" block (`features/data-releases/DataReleasesPage.tsx:447-462`) renders roots that exist only if someone sealed by hand. The pure primitives are complete and versioned (`lib/db/pglite/ledger.ts` — domain-separated leaf/node hashes, `merkleRoot`), but there is no inclusion-proof function and no table binding a root to a kg pass. The incident that makes this a moonshot rather than a nicety is in `memory/live-store-can-be-restored-under-you.md`: a stale backup replaced the live store, "no error anywhere; the surfaces kept rendering — just less", caught only by counting rendered blocks. A sealed root per pass turns that into a one-line check: recompute root(pass N) and compare.

Built: a `kg_seal` table (pass, track, root, leaf_count, sealed_at, writer ref) written by every graph writer at commit (`nextPass()` is already shared by all seven, `docs/routes/graph-writers.md`); a `merkleProof(leaves, index)` primitive beside `merkleRoot`; the receipt (`/zdroj`) and the evidence bundle (`app/graf/p/[ref]/bundle/route.ts`) carry `{ pass, root, leafHash, siblings[] }` so an auditor with only the published root verifies one edge without the store; `/data/manifest.json` and the `/dukazy` feed publish the roots; the sentinel (`lib/testing/sentinel/invariants.ts`) gains "recomputed root of the current serving graph equals the seal of its highest pass" — the restore-under-you detector.

#### Flow
- `ledger.ts` (pure): `merkleProof` + `verifyMerkleProof`, same `politicas-merkle-*-v1` domains; leaf order pinned as `(src, rel, dst)` / `id` ascending (the listers' order, `kg.ts:250-268`).
- DDL: `kg_seal` (append-only, no updates), `disclosePendingDdl` reports it as additive; apply via `npm run db:migrate`.
- `LedgerRepository.sealKgPass(pass, track)` — hashes serving rows as served (mappers' output, not raw jsonb text, so key order cannot move the root); measure on the 2 GB copy and record the cost in `db-architecture-guide.md`.
- Every `da:*` writer seals after its transaction; `kg-repair-*` scripts included; the pass ledger line records the root.
- Receipt + bundle: proof for the cited claim at its current pass; `/overeni` verifies the proof locally before comparing values (a proof that fails is `unknown/zaznam-nenalezen`, never repaired).
- Sentinel invariant + `/data` root list from `kg_seal`; `sealIngestRun` finally called from `scripts/data-analysis/ingest.ts` on `finishIngestRun("ok")`.

#### Expected impact
An external auditor (or the next concurrent session) can prove a claim was in pass N and detect a time-travelled store in seconds; measured as: seals per pass (0 today), proof-carrying receipts (0 today), and the sentinel's new invariant evaluated on every local run. Could break: a writer that forgets to seal leaves a pass unprovable — the sentinel must flag "highest pass in provenance > highest sealed pass" as a violation, not skip it.

#### Evaluation
Claim: resilience - silent restores and partial wipes become detectable by hash, not by eyeballing counts
Before: `sealIngestRun` production callers = 0; sealable tables exclude kg_*; inclusion proofs = none; the 2026-08-05 restore was found by a manual render-count probe (59 → 27 blocks)
After: root per pass in `kg_seal`, proof per receipt, one sentinel invariant; the cost per seal recorded (MISSING INSTRUMENT today: hashing ~600 k rows has not been timed on this substrate)
Method: probe - repository grep for callers, ledger primitives, /data rendering
Result: better
Gate: contract

#### Evidence
- `lib/db/pglite/repositories/ledger.ts:31-40` — `RUN_TABLES` without `kg_node`/`kg_edge`
- `lib/db/pglite/repositories/ledger.ts:168-190` — `sealIngestRun`; grep shows no caller outside the repo/tests
- `lib/db/pglite/ledger.ts` — `merkleLeafHash`, `merkleRoot`, no proof primitive
- `features/data-releases/DataReleasesPage.tsx:447-462` — roots rendered, nothing to verify against
- `app/graf/p/[ref]/bundle/route.ts` — the bundle a proof would ride in
- `memory/live-store-can-be-restored-under-you.md` — the incident
- `scripts/sentinel/run.ts:36-42` — invariants "the only thing standing between a formula correction and a silently stale published ranking"

---

### #26 Sentinel-certified releases: the verdict travels with the manifest hash

`infrastructure-observability#1` · theme T4 · corroboration 3 · size L · effort 6 / impact 8 / risk 3 · result `better` · gate `contract`
contexts: testing-sentinel, db-hybrid-benchmarks, graph-admin-data-routes, data-releases, observability, admin-control
advances: claim-verification-and-provenance / derivation-comparison ("never ran rendered as passed"); law provenance-or-nothing; law every-cap-ships-its-population

#### Summary
The sentinel is the only instrument that can see a stale ranking, and its verdict currently evaporates on stdout: `/data` stamps `latest` from cardinality floors alone, no surface shows when the invariants last held, and the hosted workflow is red by design with no cron.

#### Description
Today: `scripts/sentinel/run.ts:35-44` says it plainly — "Local `npm run sentinel` against a copy of the real store is the ONLY path on which the invariants actually execute"; `.github/workflows/sentinel.yml` has `workflow_dispatch` only, an ADOPTION POINT comment, and no schedule. The report (`lib/testing/sentinel/report.ts:65-76`) carries `manifestHash` but is written to an optional `SENTINEL_JSON` file and read by nobody: `getAdminData.ts` shows vault heads, ties, forensic and audit counts, never a sentinel verdict; `features/data-releases/manifest.ts` derives `latest`/`degraded` from `CARDINALITY_FLOORS` (`docs/routes/data.md` "Cardinality floors are a real gate") — floors that certified a 0,98 % contract corpus as `latest` for weeks. The 11-check roster (`invariants.ts:307-319`) covers only the contribution layer's provenance (`formula-ref`, `provenance-uniformity`, `components-sum`, `recompute-sample`); the money layer (`review_tier`/`review_rank` — memory: "153/208 ranks wrong after batch-012") and the law layer (`forensic_*` props, `amends` regen) have no invariant, so a half-applied money or law pass is exactly the "single-row generalization" the sentinel was built to catch, one module over. Degradations at the loader boundary — 121 `reportLoaderFailure()` call sites — go to `console.error` + a Sentry no-op (`lib/db/loaderGuard.ts:13-16`, no DSN in this repo), so "which surfaces fell back to mock in the last 24 h" is unanswerable; the architect backlog still lists "/admin ingest-status surfacing remains".

Built: (1) `npm run sentinel` persists its canonical report into a `sentinel_run` table keyed by `manifestHash` (or a JSONL sidecar like `driveLog` when the store is the thing under test — the copy is read-only, the live store gets the row on the next boot via `pending.ts`); (2) `deriveReleaseManifest` gains a `certification` field: `ok | violation | unevaluable | none` = the newest sentinel verdict FOR THIS manifestHash; `/data` prints "vydání latest, invarianty ověřeny k <date> (11/11)" or "neověřeno — sentinel nad tímto otiskem neběžel", and `/data/manifest.json` carries it; (3) the roster grows money and law lanes: `money-rank-cache` (stored `review_rank` == pure `reviewTier` for every tie, population shipped), `law-provenance-uniformity` (every bill verdict on one `{pass, ref}`), `amends-closure` (every `amends` edge resolves both ends and every bill with a verdict has ≥1 amends); (4) `reportLoaderFailure` additionally appends to a bounded in-process ring + JSONL (`.data/loader-failures.jsonl`) that `/admin` "Stav systému" renders as "degradace za 24 h: N (loader, count, last)" and the sentinel reads as a `loader-degradations` check (unevaluable when the file is absent, never ok); (5) the workflow ADOPTION POINT is wired to `npm run db:backup`'s pruned copy uploaded as an artifact from the ingest machine, so the nightly cron can return.

#### Flow
- Add `sentinel_run(manifest_hash, ran_at, verdict, report jsonb)` to `CORE_DDL` via `db:migrate`; `scripts/sentinel/run.ts` writes it through a small `pending.ts`-style queue applied on next live open (the run itself never opens the live handle — keep that guarantee)
- `features/data-releases/manifest.ts`: `certification` derived from the newest `sentinel_run` row whose `manifest_hash` equals today's; `/data` §01 prints it; `loader.test.ts` pins page == `/data/manifest.json`
- Extend `SENTINEL_CHECK_LABELS`/`ORDER` with `money-rank-cache`, `law-provenance-uniformity`, `amends-closure`, `loader-degradations`; `facts.ts` collects the needed rows SELECT-only; the report schema stays `/1` (additive enum values only, per `report.ts:12-21`)
- `lib/db/loaderGuard.ts`: add a bounded JSONL append (same "write fails LOUD" contract as `driveLog.ts:3-6`), `/admin` SystemStateStrip shows the last-24 h roll-up with its source path
- `sentinel.yml`: download the latest `db:backup` artifact at the ADOPTION POINT; restore the cron; the job stays red on `unevaluable`
- Memory entry: a certified release is "manifestHash + sentinel ok", never floors alone

#### Expected impact
Readers of `/data` and machine consumers of `manifest.json` learn whether the release they cite was ever audited; operators see degradations instead of inferring them from empty pages; a half-applied money or law pass becomes an alarm the day it happens rather than a memory entry weeks later. Measured by: `certification != none` on the live manifest, and the count of layers with a provenance-uniformity invariant (1 → 3). What could break: the sentinel writing anything — hence the pending-queue indirection; a wrong `manifest_hash` join would certify the wrong release, so the join is exact-match only.

#### Evaluation
Claim: resilience — the "never ran" state becomes visible on every release artifact and every operator surface
Before: 0 in-product consumers of the sentinel report; `/data` `latest` derived from floors only; 121 degradation sites logging to a Sentry no-op; roster covers 1 of 3 scored layers
After: `manifest.certification` on `/data` + JSON; `/admin` degradation roll-up; 15-check roster; nightly cron live against a backup artifact
Method: probe — read `run.ts`, `sentinel.yml`, `invariants.ts` roster, `loaderGuard.ts`, grep `reportLoaderFailure(` (121 hits), `getAdminData.ts` for "sentinel" (0 hits)
Result: better
Gate: contract

#### Evidence
- `scripts/sentinel/run.ts:35-44` "`.github/workflows/sentinel.yml` runs it on a hosted runner where there is no `./.pglite` … Local `npm run sentinel` … is the only path on which the invariants actually execute"
- `.github/workflows/sentinel.yml` `on: workflow_dispatch: {}` and the "There is no cron" paragraph; ADOPTION POINT step
- `lib/testing/sentinel/report.ts:65-76` `SentinelReport` carries `manifestHash`; `run.ts:62-65` the only sink is `SENTINEL_JSON`
- `lib/testing/sentinel/invariants.ts:286-298, 307-319` the 11-check roster — four scoring checks, all contribution-layer
- `lib/db/loaderGuard.ts:13-16` `console.error` + `Sentry.captureException` (DSN absent per `docs/routes/app-shell.md` "this repo has none")
- `features/admin/getAdminData.ts:757-769` entry point composes loopProgress/vaultHeads/reviewHub/tripwires/systemState — no sentinel, no degradation read
- `docs/architect/backlog.md` "Ingest readiness guard" status: "/admin ingest-status surfacing remains"
- `memory/money-stored-review-rank-is-a-stale-cache.md` (153/208 ranks wrong) — the money-layer drift no invariant sees

---

### #27 Aligned reach: the role-window weight beside every raw money figure

`financial-transparency#1` · theme T5 · corroboration 3 · size L · effort 6 / impact 8 / risk 3 · result `unmeasurable` · gate `contract`
contexts: money-ledger-graph, money-cases-review, kg-analysis, graph-writers, mp-profile
advances: conflict-of-interest-detection/temporal-alignment-of-money-and-role ("alignment as weight, not only as gate"); beneficial-ownership-resolution/role-period-reconciliation; law missing-is-not-zero

#### Summary
Every tie carries a registered role window (`role_valid_from/to`, 211/211 since pass 16) and every contract carries a date, yet no surface computes how much of a tie's money fell inside the role — the ledger, the spis and the claims all print the raw total, and the per-tie `temporal_status` is a four-token summary written once by a batch.

#### Description
`mapLinkedToTie()` reads `roleValidFrom`/`roleValidTo`/`temporalStatus` (`features/money/moneyLoader.ts:223-225`), but the corpus fold deliberately reads `supplies` EDGES only — "NO line items … reading 152 788 [contract nodes] costs 7.8 s" (`moneyLoader.ts:110-116`) — and the contract date lives on the NODE (`kg-money.ts:128`). So on the ledger path no date exists to align against; on the case-file path dates are fetched per company (`CONTRACT_LINES_PER_COMPANY = 400`, `:52`) but only to list top lines (`profileMoney.ts:97-99`), never to sum inside the window. `temporalStatus` (`moneyTypes.ts:150`: current / historical / money-postdates-role / historical-no-money) is a batch-002 verdict on the tie as a whole; it cannot say "31 % of this firm's 2,1 mld. landed while the MP sat on the board". The registry technique states the required output: aligned amount and aligned fraction beside the raw figure, undated money counted and disclosed as a floor, computed in ONE imported window helper — and the repo already has that helper for votes (`voteInRolePeriod`, `statuteRelevance.ts:80-85`), unused for money.

Built: `kg-promote`/`kg-compute` materialize `signedOn` (already read from Registr smluv) onto the `supplies` edge props (prop-merge, never reset — `docs/routes/graph-writers.md` rules) so the corpus fold has dates at zero extra reads; `reachableMoney.ts` exports `alignedReach(tie, lines, window)` using the one `voteInRolePeriod`-style day-truncation helper (moved to a shared `roleWindow.ts` both strety and money import); `ReachableTie` gains `alignedCzk`, `undatedCzk`, `undatedCount`; `MoneyBucket` gains the same; `moneyClaims.ts` mints `dosah-vazby-v-obdobi` as a distinct metric (a different statement, per the two-metrics precedent); the ledger, `/penize/[pspId]`, `/penize/firma/[ico]`, the spis money section and the evidence packet print raw and aligned side by side, with "undated: n contracts, x Kč" as the floor caveat; `reviewSignal` gains the aligned fraction as an input (declared, versioned).

#### Flow
- Writer: add `signedOn` (and `direction`-style provenance) to `supplies` edge props in the materialization script under the prop-merge rule; verify on a store copy that no other prop moved (`kg-upsert-replaces-props` memory).
- Shared window helper: extract `voteInRolePeriod` into `features/money/roleWindow.ts`, import from strety and reach; boundary tests on both edge days.
- `reachableMoney.ts`: `alignedReach` + bucket fields; `toReachableTie()` carries them; corpus and slice paths both fold dates from edge props (no node scan).
- Claims + `/zdroj`: new metric, re-derivation from the same fold; aggregate status rule unchanged (verified only when every tie is).
- Surfaces: one `AlignedReach` primitive in `features/shared/components/` (raw · aligned · undated floor); ledger column, both case files, spis, packet.
- Review order: add aligned fraction to `reviewSignal` with the weight published in `TieClassExplainer`/metodika; recompute-replay gate for the stored `review_rank` cache.

#### Expected impact
A reader sees "firms tied to X received 2,1 mld. Kč, of which 640 mil. during X's registered role (nejméně; 12 smluv bez data)" — the sentence the conflict claim actually needs. Reviewers get a better lead order (high fraction on modest money). Measured: count of ties where aligned ≠ raw, distribution of aligned fraction across the 211 ties, undated share. Could break: the `review_rank` cache and the packet hash change for affected ties (expected, disclosed).

#### Evaluation
Claim: quality - the money figure next to a person gains the simultaneity dimension the conflict claim depends on
Before: 0 surfaces compute an aligned amount; `temporalStatus` is a 4-token tie verdict (`moneyTypes.ts:150`); corpus fold has no dates (`moneyLoader.ts:110-116`)
After: aligned/raw/undated on every reach figure; MISSING INSTRUMENT: the distribution of aligned fraction over the 211 ties (needs the edge-dated fold to exist)
Method: probe - traced date availability on both read paths and the writer
Result: unmeasurable
Gate: contract

#### Evidence
- `features/money/moneyLoader.ts:52, 110-116, 223-225, 396-432` — per-company cap, fold reads edges only, window fields read but unused for money.
- `lib/analysis/kg-money.ts:128` — `signedOn` on the contract node, not the edge.
- `features/money/moneyTypes.ts:150, 603-606` — `temporalStatus` tokens.
- `features/money/collisions/statuteRelevance.ts:80-85` — the only role-window test in the repo, vote-side only.
- `features/profile/profileMoney.ts:82-99` — spis carries `roleValidFrom/To` and top lines but no aligned sum.
- `features/money/reachableMoney.ts:176, 242-278` — reach arithmetic has no time dimension.

---

### #28 Derivation stamp for graph views: path-rule ref + whole-graph provenance aggregate

`knowledge-graph-explorer#2` · theme T4 · corroboration 3 · size L · effort 5 / impact 7 / risk 3 · result `unmeasurable` · gate `contract`
contexts: graph-explorer, claim-verifier, testing-sentinel, dashboard-instruments, kg-pipeline
advances: claim-verification-and-provenance / derivation-comparison (moved-by-basis; uniform/mixed/absent; store-vs-code sentinel); law: one-definition-one-import, missing-is-not-zero

#### Summary
A graph citation's content hash is compared, but its AUTHOR is not: the path rule (`EXCLUDED_RELS`, `HUB_DEGREE`, `MAX_COST`) and the graph pass that wrote the edges are outside the hash and outside the address, so a rule change that happens to yield the same path is stamped `verified`. `/graf` also publishes no provenance for the graph as a population — the leaderboard already solves exactly this for one relation.

#### Description
`hashViewContent` hashes `content` only (`getPermalinkData.ts` `resolveView`: `{kind:"cesta", from, to, path}`; `maxCost`/`hubDegree` live in `core`, not `content`), and the constants are literals in `trailPath.ts:191-200` with no version identity. Change `HUB_DEGREE` 120 → 90 and a two-hop path that survives re-derives byte-identical → `fresh: true` → `/overeni` says `verified` (`verdict.ts:198`), the exact "coincidence stamped verified" the technique names. Node views carry `provenance.pass/ref` for the ONE node (`graphLoader.ts:874-879`); path and trail views carry no pass at all, and the seed shows only counts (`GraphPage.tsx:109-113`). The repo already has the population aggregate for `contribution_provenance` — `summarizeContributionProvenance` with `uniform | mixed | absent` + variants (`features/civicscore/provenance.ts:23-60, 95`) — and a sentinel check `provenance-uniformity` + `formula-ref` (`lib/testing/sentinel/invariants.ts:374, 399`); none of it covers `kg_edge.provenance`. The tender pass (68) landed on a store still carrying earlier passes for other relations, so the graph is by construction `mixed` per relation — which is fine to publish as a variant table and wrong to publish as one number.

Built: `PATH_RULE_REF = "evidence-path/v1"` exported from `trailPath.ts` beside the constants (bumped on any semantic change, test-pinned); `summarizeGraphProvenance(edges)` (pure, reusing the civicscore aggregate's shape) grouped by `rel` → `{pass, ref, method}` variants with coverage `stamped/read`; `content` for `cesta` gains `{ruleRef, excludedRels, hubDegree, maxCost}` and every view gains `graphProvenance` (uniform → one `{pass,ref}`; mixed → per-rel variant table; absent → no field). `/overeni` compares the cited `ruleRef` to today's and returns `moved` with the "same path, different rule" sentence. `/graf` header and `/dashboard` state slice print the per-relation variant table; the sentinel gets `graph-provenance-uniformity` (mixed across rels is informational; mixed WITHIN one rel is the alarm) and `path-rule-ref` (store's stamped rule vs code's constant).

#### Flow
- `trailPath.ts`: export `PATH_RULE_REF` and a `pathRule()` object; `trailPath.test.ts` pins the ref against the constants (changing one without the other fails).
- `lib/kg/graphProvenance.ts` (pure): `summarizeGraphProvenance(edges: {rel, provenance}[])` → per-rel `{state, pass, ref, method, computedAt, variants, coverage}`; fixtures for uniform / mixed-within-rel / absent.
- `graphLoader.ts`: `buildIndex` computes the aggregate once (it already scans all edges at `:171-175`); `GraphSeed`, `Trail`, `PathQueryResult` carry it; `resolveView` puts `ruleRef` + the aggregate (only when uniform per rel, per the technique's "no single author → no derivation field") into `content`.
- `permalink.ts`: `toEvidenceJsonLd` adds `path_rule_ref`, `graph_provenance_state`, per-rel `provenance_pass`; `permalinkCardModel` adds `basisChanged`.
- `/overeni`: `hashedVerdict` gains `basis: {cited, current}`; verdict copy for moved-by-basis; the `g.` decoder tolerates absent basis (older refs: "stamps not compared", never a mismatch).
- `GraphPage` header SourceNote and `/dashboard` state-graph slice print the variant table (SourceNote citing `kg_edge.provenance`).
- `invariants.ts`: two new checks, "unevaluable" when the store is unreachable.

#### Expected impact
Editors and the gate can tell "same number, different formula" from "verified"; operators see a half-recomputed relation as a red check instead of a confident pass number. Measured by: sentinel output before (no graph checks) vs after (two checks, with the live per-rel variant table); count of `moved-by-basis` verdicts after the next `HUB_DEGREE`/`EXCLUDED_RELS` change (today undetectable). Could break: adding `ruleRef` to `content` moves every existing `cesta` ref once (a deliberate, announced basis bump — the alternative is hashing a lie).

#### Evaluation
Claim: quality - derivation identity becomes carried and compared for every graph citation and for the graph as a population
Before: `trailPath.ts:191-200` constants unversioned; `resolveView` content for `cesta` = `{from,to,path}`; `verdict.ts:198` verified on hash equality alone; `invariants.ts` has `formula-ref`/`provenance-uniformity` for persons only; graph-level pass distribution unpublished
After: the MISSING INSTRUMENT is the per-rel aggregate itself — `select rel, provenance->>'pass', provenance->>'ref', count(*) from kg_edge group by 1,2,3` over the live store (not run: store closed to this scout)
Method: probe - traced hash inputs and gate comparison; reasoning on a constant change
Result: unmeasurable
Gate: contract - JSON-LD fields and the one-time basis bump of issued refs

#### Evidence
- `features/graph/trailPath.ts:191-200` — `EXCLUDED_RELS`, `HUB_DEGREE = 120`, `MAX_COST = 6`, no ref
- `features/graph/getPermalinkData.ts` `resolveView` — `content: { kind: "cesta", from, to, path }`; `core` carries `maxCost/hubDegree` outside the hash
- `features/overeni/verdict.ts:190-198` — verdict from `fresh` only
- `features/civicscore/provenance.ts:23-60, 95, 142` — the aggregate to reuse; `lib/testing/sentinel/invariants.ts:374, 399` — person-only checks
- `features/graph/graphLoader.ts:171-175` — full edge scan already in `buildIndex`; `:874-879` — single-node provenance only

---

### #29 Club affiliation dated at the ballot: membership windows in the record

`voting-legislation#2` · theme T5 · corroboration 3 · size L · effort 6 / impact 7 / risk 6 · result `unmeasurable` · gate `policy`
contexts: votetrack-ledger, votetrack-kompas, volby-election-mirror, kg-analysis, mp-profile
advances: parliamentary-data-modeling (membership-window-modeling, party-club-vs-electoral-list); roll-call-vote-analysis (club-line-and-rebellion); law: incident-anchored-doctrine, non-partisan-symmetry

#### Summary
Every club line, rebellion rate and cohesion index in the group is computed against ONE club per mandate for the whole term, picked by row order from an undated join — an MP who changed club has every historical ballot repainted, and which club wins is decided by the dump.

#### Description
`lib/db/pglite/repositories/graph.ts:141-159` (`clubByMandate`) joins `membership` on `person_psp_id` and `kind = 'member'` to the term's `Klub` organs with no predicate on `from_at`/`to_at`, then `out.set(mandate, abbrev)` per row — a person with two club rows yields whichever row the query returned last. `MembershipRow` carries `fromAt`/`toAt` (`lib/db/types.ts:77-78`), so the windows exist and are ignored. Every consumer takes that single map: `record/derive.ts:250` (`clubByMandate.get(b.mandatePspId)` per ballot, regardless of `votedOn`), `lib/analysis/kg.ts:175-190` (`rebellion()` → the `rebels_against` edges and `rebellion_rate` prop on 207 persons), `partyCohesion()` (`kg.ts:257+`), `scripts/data-analysis/kg-compute.ts:107`, `getKompas.ts:147` (the MP's club on the result board), and through `rebellion_rate` the Nezávislost pillar. This is exactly the „current-party column" failure the modeling subject names, and the same dump-order defect `/zakony` fixed for committee dates on 2026-08-13 (`docs/routes/zakony.md`: „the committee date stopped being decided by dump ROW ORDER"). The repo already treats the open-ended membership as the current-holder signal (`memory/current-mandate-holder-signal.md`), so the windows are trusted elsewhere.

Built: `clubWindowsByMandate(term)` returns `Map<mandate, {club, fromAt, toAt}[]>`; a pure `clubAt(windows, mandate, isoDay)` picks the window containing the vote day (open `toAt` = current). `deriveVoteRecord` and `rebellion()` take the windows and the vote date; a ballot cast on a day inside no club window is a new bucket `outsideClubWindow` (counted in `coverage`, never folded into `unaffiliated`, never scored). Club aggregates split by era only where an MP's club changed; the profile's rebellion chronicle names the club AT the vote. The kompas board prints club-at-vote per question and current club per MP, with copy saying which is which. `kg-compute` recompute goes through the replay gate (`memory/recompute-replay-gate.md`): the old formula must reproduce every stored `rebellion_rate` first, then the windowed formula writes with a new pass.

#### Flow
- Probe on a store copy: persons with >1 `Klub` membership row in PSP10; ballots whose `votedOn` falls outside the window of the club `clubByMandate` currently assigns them; the number of MPs whose `rebellion_rate` moves.
- `store.clubWindowsByMandate(term)` beside the old method; `lib/analysis/clubAt.ts` pure + tests (window edges, open end, no window, two windows on one day = refuse + count).
- `derive.ts`: thread `votedOn` into pass 1 and pass 2; add `coverage.outsideClubWindow`; `derive.test.ts` fixture with a mid-term switch.
- `kg.ts` `rebellion()`/`partyCohesion()` + `kg-compute.ts` under the replay gate; `graph-schema.md` notes the basis change on `rebels_against` and `party.cohesion`.
- Surfaces: chronicle row + rebel board print the club at the vote; the profile and kompas copy distinguish „klub při hlasování" from „klub dnes".
- Retire `clubByMandate` once no consumer remains; `docs/routes/hlasovani.md` dated entry with the measured delta.

#### Expected impact
Named MPs who switched club stop being scored as rebels against a club they were not in (or shielded by one they were). Measured by the count of persons whose eligible/rebel counts change and by `coverage.outsideClubWindow`. What could break: published rebellion rates move for a handful of named people — the change must ship with its before/after table and the replay-gate proof, or it reads as an unattributable rewrite.

#### Evaluation
Claim: quality - every club-relative figure is computed against the club the MP was in on the day of the vote
Before: `clubByMandate` SQL has zero date predicates and last-row-wins semantics (graph.ts:141-159); 100 % of ballots are scored against a term-wide club
After: MISSING INSTRUMENT — count of PSP10 persons with >1 club window and count of ballots whose day is outside the assigned club's window; today not runnable here without the store
Method: probe - read the SQL, the membership shape, and all five consumers of the map
Result: unmeasurable
Gate: policy

#### Evidence
- `lib/db/pglite/repositories/graph.ts:141-159` — join on `ms.person_psp_id … and ms.kind = 'member'`, no `from_at`/`to_at`; `out.set(...)` per row
- `lib/db/types.ts:69-82` — `MembershipRow.fromAt/toAt` exist
- `features/votetrack/record/derive.ts:250,299` — `clubByMandate.get(b.mandatePspId)` without a date
- `lib/analysis/kg.ts:175-190` — `rebellion(… mandateToClub …)`; `scripts/data-analysis/kg-compute.ts:107`
- `features/votetrack/getKompas.ts:147` — `clubByMandate.get(b.mandatePspId) ?? null`
- `docs/routes/zakony.md` 2026-08-13 — the dump-row-order precedent this repeats

---

### #30 The vote→bill spine: a `decides` edge from the agenda-item join

`voting-legislation#0` · theme T8 · corroboration 2 · size XL · effort 8 / impact 9 / risk 5 · result `unmeasurable` · gate `contract`
contexts: votetrack-ledger, lawwatch, volby-election-mirror, votetrack-kompas, voting-legislation-routes, kg-analysis, psp-ingest
advances: legislative-change-tracking (bill-fate-dating, committee-routing-reconstruction) + roll-call-vote-analysis (club-line-and-rebellion); law: provenance-or-nothing, every-cap-ships-its-population

#### Summary
The graph has no edge between a roll call and the print it decided, so /zakony draws bills with zero votes, /hlasovani draws votes with zero bills, and /volby cannot date a single law finding. The join key already sits in the store on both sides.

#### Description
Today `vote_event` carries the agenda-item number: `lib/ingest/sources/psp.ts:77` names column 4 `bod` and `psp.ts:321` stores it as `agendaItem` (typed at `lib/db/types.ts:91`), beside `sessionNo` and `termPspId`. The bill side already parses `schuze.zip`: `lib/ingest/sources/psp-activity.ts:148-150` documents `schuze.unl` (id_schuze, id_org = term organ) and `bod_schuze.unl` (id_bod, id_schuze, id_tisk = the `bill:tisk:<id>` key), and `parseBillSpeeches` (`psp-activity.ts:166-176`) walks rec → bod_schuze → tisk for the `spoke_on` edge. Nobody has joined `(term, schuze, bod)` from the vote side to the same `bod_schuze` row. `docs/data-analysis/frontier.md:76` records the attempt as Q-law-2 „blocked (ingest)" because the probe (`scripts/case-loops/law/inspect-votes.ts:1-3`) tried `hist.unl` col 5 and found a document id, not a vote id — the agenda-item route was never tried. The consequences are visible on every surface of this group: `features/lawwatch/components/BillDetail.tsx` contains no vote reference at all (grep `vote|hlasov` = 0 hits), the `LawBill` shape (`getLawData.ts:200-245`) has no vote field, `docs/routes/zakony.md` states „the graph carries no bill-stage pipeline, so neither is drawn", and `features/volby/volbyLoader.ts:241-243` sets `sponsoredOn: null` on every sponsored bill because „no bill prop carries a date" — so `lib/analysis/volby/rules.ts:347-364` ships every `law_*` finding with `decidedOn: null` and the dated-outcome timeline has nothing to anchor on. The `about` edge in `docs/data-analysis/graph-schema.md` (vote → theme, 47 rows) is a verdict, not a link to a print.

Built: a deterministic ingest step joins each PSP10 `vote_event` to a `bod_schuze` row by `(termPspId, sessionNo, agendaItem)` and writes a `decides` edge `vote → bill` (rel registered in `lib/analysis/kg-verdict.ts` and `lib/kg/prop-registry.json`, props: `votedOn`, `outcome`, `readingStage` when `typ_akce` labels it, `joinBasis: "schuze+bod"`). Votes whose agenda item names no print stay unlinked and are counted (`coverage.votesWithoutPrint`), never guessed from titles. The record's `voteIndex` (`record/derive.ts:349-367`) already carries chamber tally + club lines per vote, so the bill dossier renders „how the chamber and each club stood on this print, reading by reading" with no second derivation; /hlasovani ledger rows link `/zakony/<cislo>`; /volby's list ledger gains a `law_final_vote` RECORD row (positions, not a finding — the `contested.ts` doctrine) with the vote date as `laterOn`; /denik gets a dated fact per decisive reading.

#### Flow
- Probe on a store copy: count PSP10 `vote_event` rows whose `(term, schuze, bod)` resolves to a `bod_schuze` row with non-empty `id_tisk`; count multi-vote agenda items; record the join rate in `docs/data-analysis/graph-log.md` and flip frontier Q-law-2.
- Register rel `decides` (enum + prop-registry + graph-schema.md row) with the join basis stated; write it from a new `scripts/data-analysis/kg-vote-bill-ingest.ts` under the `kg-upsert replaces props` guard (merge-preserving, pass provenance).
- Extend `LawBill` with `rollCalls: {votePspId, votedOn, outcome, readingStage|null, clubLines}` read through `getFullVoteRecord().voteIndex` (one derivation, imported); `BILL_WIRE` keeps it on `/zakony/[cislo]` only.
- `LedgerVote` gains `billCislo | null`; ledger rows link the dossier; `chronicleCap.test.ts` ruling amended for the new field.
- `SponsoredBill.finalVoteOn` in `volbyLoader.ts`; `rules.ts` emits the record row and keeps `decidedOn` null until a sponsorship date exists (the vote date is a later fact, not the choice).
- Deník + RSS entries for decisive readings; `docs/routes/{zakony,hlasovani,volby}.md` dated entries.

#### Expected impact
Readers of a bill dossier see the votes that decided it; readers of the ledger see what a vote was about; the volby timeline gains its first dated chamber outcome per list. Measured by join rate over 2 014 valid roll calls and by the count of bills with ≥1 linked vote (141 population). What could break: an agenda item that carries several prints or several votes — the join must be many-to-many with the ambiguity counted, or a wrong vote lands on a named sponsor's bill.

#### Evaluation
Claim: user - the vote→impact loop /zakony promised at founding closes on real data
Before: 0 vote→bill edges in the graph (graph-schema.md edge table); 141/141 bill dossiers render 0 roll calls; 48/48 ledger rows link no print; `decidedOn: null` on 100 % of law findings
After: the MISSING INSTRUMENT is the join-rate probe over `(term, schuze, bod)` — not runnable here (read-only, single-connection store); expected well above the 0 the frontier row leaves
Method: probe - read the ingest column maps, the frontier row, the dead-end probe, and every consumer that renders a bill or vote
Result: unmeasurable
Gate: contract

#### Evidence
- `lib/ingest/sources/psp.ts:77` — `HLASOVANI_COLS` includes `"bod"`; `psp.ts:321` `agendaItem: colInt(r, 4)`
- `lib/db/types.ts:91` — `agendaItem: number | null` on `VoteEventRow`
- `lib/ingest/sources/psp-activity.ts:148-150` — `bod_schuze.unl: 0 id_bod | 1 id_schuze | 2 id_tisk`
- `docs/data-analysis/frontier.md:76` — Q-law-2 `voted_in` blocked; `scripts/case-loops/law/inspect-votes.ts:1-3` probed `hist` col 5 only
- `features/lawwatch/components/BillDetail.tsx` — no `vote`/`hlasov` occurrence
- `features/volby/volbyLoader.ts:241-243` — `sponsoredOn: null`; `lib/analysis/volby/rules.ts:347,357,364` — `decidedOn = bill.sponsoredOn`
- `features/votetrack/record/derive.ts:349-367` — `voteIndex` already carries `clubLines` per valid vote

---

### #31 A deterministic theme layer for the dead Kompas, from committee routing

`voting-legislation#1` · theme T8 · corroboration 2 · size L · effort 5 / impact 8 / risk 3 · result `unmeasurable` · gate `contract`
contexts: votetrack-kompas, votetrack-ledger, lawwatch, kg-analysis
advances: political-compass-from-votes (theme-balanced-drawing, divisive-vote-selection); law: deterministic-code-owns-numbers, missing-is-not-zero

#### Summary
/kompas and the /hlasovani theme filter both wait on `vote_tag`, an LLM classification layer that has 0 rows and no owner; the theme can instead be derived deterministically from the bill's garanční committee once votes link to prints.

#### Description
`features/votetrack/silverLayer.ts:1-8` states the live fact: `vote_tag` was never computed. `getKompas.ts:63-64` returns `SILVER_NEVER_COMPUTED` before reading anything else, `getVoteThemes.ts:38-39` does the same, and `kompas/select.ts:139-141` treats an untagged vote as „the rule knows nothing about it" — so today the compass rule has zero candidates over 2 014 valid roll calls. The design leans on a haiku classifier (`select.ts:51-56`, `getVoteThemes.ts:2-3`, `scripts/hybrid-bench/materialize-tags.ts`) with a self-reported confidence and a floor (`MIN_TAG_CONFIDENCE`, `select.ts:66`); it is an un-owned batch job per `docs/routes/hlasovani.md`. Meanwhile the graph holds a deterministic route to a theme for every vote on a print: `assigned_to` edges carry `role: garancni | dalsi` (`getLawData.ts:470-471` ranks garanční first; `psp-legislation.ts:17-27` documents the source), and `owns` edges (committee → theme, 30 rows, graph-schema.md) already map committees to the 14 theme nodes. Composed with card 1's `decides` edge, theme(vote) = theme owned by the garanční committee of the print the vote decided — a closed vocabulary, a published rule, no model in the loop.

Built: `scripts/data-analysis/kg-vote-themes.ts` writes `vote_tag` rows with `model = "deterministic:garancni-committee"` and `confidence = null` (a rule does not have a confidence; `select.ts:158-160` already keeps a missing confidence and counts it under `withoutConfidence`, so no selection change is needed). Procedural and non-print votes stay untagged — counted in `coverage.tagged`, never forced into `jine`. The committee→theme map is one exported constant printed on /metodika, imported by the writer and by `themeLabels.ts`. The LLM classifier, if it ever runs, becomes a second `model` value that the surface can name; the two are never merged into one count.

#### Flow
- Land card 1 (or at minimum the `decides` probe) — this card's coverage is bounded by the join rate.
- Define `COMMITTEE_THEME` (organ pspId → theme slug) from the existing `owns` edges; reject any committee with two themes (ambiguous → untagged, counted).
- Writer: for each `decides` edge, garanční committee → theme → upsert `vote_tag {votePspId, theme, model, confidence: null}` through `lib/db/pglite/repositories/voteTags.ts`.
- `VoteThemeData.model` and `KompasData.coverage` print the model name and the untagged population; `KompasNeverComputed` copy retires only when rows exist.
- `select.test.ts` fixture with `confidence: null` on every row; `kompasIndex.test.ts` re-pinned; /metodika section for the rule.

#### Expected impact
The compass goes from structurally empty to live for every reader; the /hlasovani `#temata` rail lands on a list. Measured by `coverage.tagged / coverage.valid` and by `selection.candidates` after the floors (`MIN_POSITIONAL = 120`, `select.ts:44`). What could break: theme monoculture if one committee dominates the decided agenda — the round-robin draw (`select.ts:186-197`) and `PER_THEME_CAP` already defend, but the per-theme candidate counts must be printed.

#### Evaluation
Claim: user - the compass answers instead of explaining why it cannot
Before: `vote_tag` = 0 rows; `selection.candidates` = 0; /kompas renders `KompasNeverComputed` on every request
After: MISSING INSTRUMENT — tagged share = (votes with a `decides` edge to a print with a garanční committee mapped to a theme) / 2 014; candidates after floors printed by the existing coverage block
Method: probe - traced both silver-layer consumers and the selection floors; confirmed committee routing + `owns` mapping exist in the graph
Result: unmeasurable
Gate: contract

#### Evidence
- `features/votetrack/silverLayer.ts:5-8` — „na živém store je jich dnes NULA"
- `features/votetrack/getKompas.ts:63-64`, `getVoteThemes.ts:38-39` — `tags.length === 0 → SILVER_NEVER_COMPUTED`
- `features/votetrack/kompas/select.ts:139-141` — untagged vote skipped; `:158-160` missing confidence kept
- `features/lawwatch/getLawData.ts:470-471` — `ROLE_RANK = { garancni: 0, dalsi: 1 }`
- `docs/data-analysis/graph-schema.md` edge table — `owns` committee → theme (pass 8, 30 rows); `assigned_to` with `role`
- `features/votetrack/getVoteThemes.ts:64` — `model: tags[0]?.model` already shipped to the surface

---

### #32 Formula revision ledger — published correction history for the index

`mp-profiles-rankings#0` · theme T6 · corroboration 2 · size L · effort 6 / impact 8 / risk 3 · result `better` · gate `contract`
contexts: contribution-scoring, civicscore-leaderboard, mp-profile, mp-rankings-routes, db-store, civic-claim-gate
advances: politician-performance-scoring — golden path "Corrections are published, not absorbed"; formula-lineage-stamping; accountability-publishing-ethics "right of reply and correction — a published correction history"; laws provenance-or-nothing, incident-anchored-doctrine

#### Summary
The graph carries only the CURRENT `{pass, ref}` per person and the product prints only that; the one measured correction the index has had (pass 42: 33 MPs, 220,1 points, 184/207 ranks moved) exists solely in `docs/` and a console log, so no reader, no cited MP and no /overeni verdict can see what a score used to be or why it moved.

#### Description
Today `contribution_provenance` is a single `{pass, ref, computedAt}` (`features/civicscore/provenance.ts:29-72`), and /metodika states the rule explicitly: „No invented history — the graph carries only the current `{pass, ref}`, so only that is printed" (`docs/routes/metodika.md:501-503`). The recompute script already computes everything a correction history needs — a per-MP before/after audit array (`scripts/data-analysis/kg-contribution-recompute.ts:159-171`), the measured diff (`:317-331`: moved count, points removed, saturation before/after, rank movers), and the prose it invalidates (`:357-367`) — and then prints it to stdout and optionally to a `--json` file (`:38`) that nothing in the app reads. `CONTRIBUTION_FORMULA_REF`'s own doc comment carries the founding incident (`lib/analysis/contribution.ts:39-58`) as a code comment, not as data. The citation layer answers a pasted claim from an older pass with `moved/basis` naming both passes (`docs/routes/zebricek.md:192-198`, `scoreClaim.ts:49-52`) but cannot say what the basis change DID to that MP.

"Built" = a `formula_revision` table (via `npm run db:migrate`) with one row per ref transition: `{ref, predecessorRef, pass, computedAt, subjectsMoved, pointsMoved, ranksMoved, saturationBefore/After, staleProseFields, auditRef}` and a `formula_revision_subject` child table holding the per-MP `{scoreBefore, scoreAfter, rankBefore, rankAfter}` rows the audit already produces. The recompute writes both in the same transaction as the node stamps (the replay gate stays the precondition). /metodika gains a §"Opravy metodiky" listing every revision with its measured diff, all figures imported from the row. /poslanec prints one dated sentence under the score when the MP's row exists: „Při přepočtu č. 42 (2026-08-04) se skóre změnilo z 90,5 na 84,1 — oprava počítání výborů." /overeni's `moved/basis` verdict joins the ledger and explains the move instead of only naming two passes. The `LowScoreReasonChip`/`ScoreCitation` stale-prose report becomes a queryable column rather than a console warning.

#### Flow
- Add `formula_revision` + `formula_revision_subject` to `CORE_DDL`; `npm run db:migrate` (snapshot first); seed the pass-42 row from `docs/data-analysis/contribution-pass42-audit.json` (207 rows, byte-identical to July's per zebricek.md:88-90) in a one-off, provenance-stamped backfill.
- Teach `kg-contribution-recompute.ts` to write the revision + subject rows in the commit path, after the replay gate passes; extend `guardContributionWrite`'s refusal message to name the last revision.
- New pure `lib/analysis/formula-revisions.ts` (shape + ordering + the sentence builder as message KEYS) with tests; loader `features/civicscore/getFormulaRevisions.ts` (server-only, `reportLoaderFailure`, `KG_READ_CAP`).
- /metodika §"Opravy": table of revisions, every figure from the row, `SourceNote` citing the pass; `messages.test.ts` bans literal counts.
- /poslanec: one dated sentence under the score for MPs with a subject row; absent row renders nothing (not „no change").
- /overeni: when a pasted claim's `derivation` differs, look up the transition and render the revision line beside `moved/basis`.
- Sentinel invariant `revision-chain`: every distinct historical ref in `formula_revision` chains to `CONTRIBUTION_FORMULA_REF`; a stamped ref with no revision row is `unevaluable`, not ok.
- Update `docs/routes/metodika.md`, `poslanec.md`, `overeni.md`; `docs/feature-doc-map.json`.

#### Expected impact
Journalists and the 33 moved MPs can see, cite and contest the correction from the product instead of from a git log; the methodology page finally satisfies the golden path's "corrections ship with their measurement". Measured by: revisions rendered (0 → 1 today, and every future ref change lands with a row or the recompute refuses). Could break: the recompute's transactional scope grows — a partial write must roll back both tables or the `mixed` state gains a sibling.

#### Evaluation
Claim: quality — a public index that publishes its own correction history instead of absorbing it
Before: 0 reader-facing surfaces carry any prior `{pass, ref}` or delta; `grep -rn "scoreBefore" features/ app/` → 0 hits; the only record is `docs/data-analysis/graph-log.md` + a `--json` audit file
After: 1 revision row (pass 11 → 42) rendered on /metodika, 33 per-MP sentences on /poslanec, `moved/basis` verdicts carrying the explanation; count of revisions without a ledger row = 0 (sentinel-checked)
Method: probe — read the recompute's audit shape and the provenance aggregate; confirmed nothing in `features/` consumes the audit
Result: better
Gate: contract — `CORE_DDL` change through `db:migrate`; the recompute's commit path gains a second write

#### Evidence
- `features/civicscore/provenance.ts:29-72` — `ContributionProvenance` holds one `{pass, ref, computedAt}`; no predecessor field
- `docs/routes/metodika.md:501-503` — „No invented history — the graph carries only the current `{pass, ref}`"
- `scripts/data-analysis/kg-contribution-recompute.ts:159-171, 317-331, 357-367` — audit array, measured diff, stale-prose report, all console-only; `:38` `--json=<path>` flag
- `lib/analysis/contribution.ts:39-58` — the founding incident lives in a comment
- `features/civicscore/scoreClaim.ts:49-52` + `docs/routes/zebricek.md:192-198` — `moved/basis` names two passes, explains nothing
- `docs/routes/zebricek.md:70-94` — the pass-42 correction: 33 MPs, 220,1 points, 184/207 ranks, six-day divergence

---

### #33 Roll calls in the deník: the chamber's own activity as dated rows

`civic-feed-transparency#2` · theme T8 · corroboration 2 · size L · effort 7 / impact 8 / risk 5 · result `unmeasurable` · gate `none`
contexts: civic-chronicle, schranka-notifications, civic-feeds-verification, votetrack-ledger, mp-profile
advances: roll-call-vote-analysis (measured deviation framing) + accountability-publishing-ethics / severity-free-factual-framing; law: deterministic-code-owns-numbers, non-partisan-symmetry

#### Summary
The Deník republiky is keyed by contracts, registry roles, bill steps and gate decisions — and is blind to what an MP does most: vote. `vote_event.voted_on` and per-ballot deviation from the club line already exist as dated, deterministic facts on `/poslanec`, but no chronicle, feed or inbox carries them.

#### Description
Today: `features/denik/deriveDenik.ts:57-69` enumerates nine `DenikKind`s, none a vote; `features/dashboard/datedFacts.ts:224-275` (the book the deník inherits its rules from) likewise. Yet `lib/db/pglite/ddl.ts:119-148` gives every roll call `voted_on` (indexed `vote_event_term_idx`), `features/profile/rebellionRecord.ts:23-26` carries `votedOn` per `RebellionInstance`, and `features/profile/getRebellionRecord.ts:66-77` builds a chamber-wide rebellion index — at a measured 15,8 s for the full `vote_ballot` read (`:35-36`), which is why nothing else reads it. `docs/routes/schranka.md` and `denik.md` contain no vote row; the `recompute` delta is the only MP-personal fact the inbox can speak.

Built: a `votes` deník layer producing two symmetric row kinds — `rollCall` (a `tisk:`-keyed registry fact when LawWatch links the vote to a tisk: "o tisku N se hlasovalo D, výsledek", source = `vote_event.source_url`) and `deviation` (a `poslanec:`-keyed derived figure: "hlasoval(a) jinak než většina klubu při hlasování N", with the club-line basis printed — the same "measured deviation" grammar `/hlasovani` already uses, no adjective). Coverage is the whole chamber or nothing; an MP with zero deviations in the window is a row-less entity, not an absent one, and the page says which layer it read. Reads are scoped: `vote_psp_id = any(...)` over the window's roll calls rides `vote_ballot_vote_idx` (29 ms / 4 000 rows per `MEMORY.md` scoped-ballot-read), memoised on `MONEY_MEMO_TTL_MS` like the other batch layers.

#### Flow
- `features/votetrack/ledgerRead.ts`: windowed `listVoteEvents({since})` + scoped ballot read; club line via the existing `lib/analysis/kg.ts` party-line function (one definition, imported).
- `deriveDenik.ts`: kinds `rollCall` / `deviation`, `timeBasis: "ucinne"`, id from `vote_psp_id` + `mandate_psp_id`; `limits.votesTruncated` + `votesWithoutTisk` counted.
- `kindLabels.ts`, `/schranka` `kindVocabulary.ts` + `KIND_ORDER`, `denik.entry.*` catalog keys through the language gate.
- `/hlasovani` and `/poslanec` link their deník day via `entityDayHref` (imported, never retyped).
- Feeds inherit automatically through `feedCodecs.ts`; `feedNotes.ts` names the new layer's cap.
- Docs: `docs/routes/{denik,schranka,hlasovani,poslanec}.md`.

#### Expected impact
A follower of an MP learns the day their MP broke with the club, from the record, with the basis printed; measured by the share of `poslanec:` deltas that are vote rows vs recompute rows (today 0 %). Could break: memo-window cost — the windowed ballot read must be measured before merge (the unscoped read is 15,8 s and would blow the deník's ~1,5 s batch budget).

#### Evaluation
Claim: user - the deník carries the chamber's primary activity for a followed MP
Before: 0 vote kinds in `DenikKind` (`deriveDenik.ts:57-69`); rebellion facts exist only on `/poslanec` behind a 15,8 s chamber read (`getRebellionRecord.ts:35-36`)
After: MISSING INSTRUMENT — wall time of a 30-day scoped ballot read on the live store (the 29 ms / 4 000-row figure is for one MP's slice, not a window)
Method: probe - read of DenikKind, datedFacts kinds, vote DDL, rebellion record modules, MEMORY read-cost notes
Result: unmeasurable
Gate: none

#### Evidence
- `features/denik/deriveDenik.ts:57-69` — nine kinds, no vote
- `features/dashboard/datedFacts.ts:224-275` — inherited book, no vote
- `lib/db/pglite/ddl.ts:119-148`, `:151-164` — `voted_on`, `vote_ballot_vote_idx`
- `features/profile/rebellionRecord.ts:23-26`, `:56-60` — `votedOn` per deviation instance
- `features/profile/getRebellionRecord.ts:35-36`, `:66-77` — 15,8 s full-corpus read, chamber index cached per request
- `MEMORY.md` → scoped-ballot-read-beats-the-relation (29 ms indexed vs 7,3 s)

---

### #34 Score-move ledger — the recompute becomes a dated fact with a size

`landing-navigation#3` · theme T6 · corroboration 2 · size XL · effort 8 / impact 8 / risk 6 · result `unmeasurable` · gate `irreversible`
contexts: dashboard-state-graph, dashboard-instruments (cross: graph-writers, schranka, denik, civicscore)
advances: claim-verification-and-provenance / derivation-comparison (carry `ref@pass` per version, compare across passes); accountability-publishing-ethics / severity-free-factual-framing (a move is a fact about the DATA, framed by formula, never a verdict about the person); laws: deterministic-code-owns-numbers, missing-is-not-zero (pre-epoch versions stay "size unknown")

#### Summary
Three surfaces independently refuse the same thing: the velín's feed says "the graph keeps no changelog, so no 'what changed' is invented" (`datedFacts.ts:4-9`), the dashboard lead dropped its promise of "pohyby v žebříčku" (`dashboard.md:264-266`), and `/schranka`'s `recompute` row states "the size of the move is unknown — the graph keeps no prior-value snapshots" (`schranka.md:46-48`). Yet the store already appends every superseded node version to `kg_node_history` with its `props` (`ddl.ts:313-325`), and `change_event` has a typed, idempotent stream with GIN-indexed entity keys (`ddl.ts:352-365`) that no production writer fills (`getDenikData.ts:425-432`: empty on the pass-55 backup).

#### Description
What exists: `kg_node_history(id, kind, props, provenance, recorded_at, superseded_at)` written by the supersede paths in `repositories/kg.ts:346,377`; `CHANGE_EVENT_TYPES` (`lib/ingest/changeEvents.ts:27-40`) with `tie-*`, `contract-new`, `review-decision` backfilled from edge history and `review_audit` (`changes.ts:200-232`) and mandate/role types that "accrue forward via the adapter snapshot-diff layer" — which does not run. There is NO `score-recomputed` type, and nothing reads `kg_node_history` for person nodes. The dashboard's `DatedFact` (`datedFacts.ts:58-84`) is world-time only (contract signature, role start, Sbírka), by design, with `subjectRef` and `refs` keyed to slice node ids that `entityLinks.ts:408-423` already translates to the public keys `change_event.entity_keys` uses.

What must be measured first (the whole card hangs on it): whether `kg-compute`'s person writes go through the supersede path (history captures the previous `contribution_score` + `contribution_provenance`) or overwrite in place. `MEMORY.md` records that upsert wholesale-replaces props; if history is populated, the prior value is already there and "size unknown" is an outdated refusal; if not, the writer must supersede. Either way the recompute-replay gate (`memory/recompute-replay-gate.md`) is the discipline: a move is only claimable between two versions that both carry `{pass, ref}`.

"Built": a deterministic deriver (`lib/ingest/changeEvents.ts`) emits `score-recomputed` events `chev:score:<pspId>|<pass>` with payload `{from, to, fromRef, toRef, fromPass, toPass}` from consecutive `kg_node_history`/`kg_node` versions of the same person; `kg-compute`/`kg-promote` call the changes repository after a pass (graph-writers doc records the reset/prop-merge rule); the velín's feed gains a second, visually distinct "zaznamenáno" column for the slice's own entity keys (record time, never mixed into the dated world facts — the deník's own two-stream rule); `/schranka`'s `recompute` row prints the size when both versions carry a stamp and keeps "size unknown" for pre-epoch versions; the deník renders the type via `DENIK_CHANGE_TYPES`. Copy is formula-framed by construction: "index přepočten z 61,2 na 58,9 (contribution-committee-dedupe@42 → @43)" — never "klesl" as a judgement, and the same sentence for every MP.

#### Flow
- Probe on a store copy: count `kg_node_history` rows with `kind='person'` and a `contribution_score` in `props` across ≥2 passes; record the answer in `docs/routes/graph-writers.md` before any code.
- If the compute path overwrites: route person prop writes through the supersede path (merge-preserving, `kg-upsert-replaces-props` memo) — a graph-writers change with its own replay verification on a copy.
- `changeEvents.ts`: add `"score-recomputed"` to `CHANGE_EVENT_TYPES`; pure builder + codec test; `changes.ts` backfill derives it from history pairs (epoch guard as for ties).
- Writers: `scripts/data-analysis/kg-compute` (or the promote step) invokes the backfill after a successful pass; `npm run db:accounting` retention for `change_event` reviewed.
- `getDashboardData.ts`: one `listChangeEvents({entityKeys: slice keys, limit: cap})` read (indexed GIN; cap disclosed) → `DashboardData.recorded: RecordedFact[]`; `publicWire` classifies it; `GraphFeedPanel` renders a labelled second stream with its own `SourceNote` (`change_event — záznam grafu`).
- `/schranka` `recompute` row: size from the event when present; `/denik` `DENIK_CHANGE_TYPES` widened; `kindLabels.ts` word for it; both language-gate pinned.
- `docs/routes/dashboard.md` + `schranka.md`: the refusal is lifted with its condition (stamped versions on both sides) written next to it.

#### Expected impact
Followers of an MP see "your MP's index moved from X to Y under formula F@P" as a literal, dated, deterministic record; the velín's feed can say what the GRAPH recorded about its entities without inventing prose. Measured by: `change_event` rows of type `score-recomputed` after one pass = number of MPs whose score changed (pass 42 moved 33 — `memory/recompute-invalidates-prose.md`); `/schranka` recompute rows with a size / total recompute rows. What could break: a move derived across a formula change (ref differs) is a `moved (by basis)` fact, not a delta — the event must carry both refs and the sentence must not subtract across them.

#### Evaluation
Claim: quality — a refused derivation becomes an honest, addressed record; the feed's "no changelog" premise is replaced by a real ledger
Before: `change_event` = 0 rows on the pass-55 backup (`getDenikData.ts:431-432`); `CHANGE_EVENT_TYPES` has no score type; `kg_node_history` person coverage UNKNOWN (the missing instrument)
After: score events per pass = MPs whose value changed; the velín feed carries a recorded stream capped and disclosed — instrument: a one-off probe script over a store copy counting person versions per pass
Method: probe — read the DDL, the change-event backfill, the schranka recompute rule, the graph-writer memos
Result: unmeasurable
Gate: irreversible — changes how the graph writer versions person nodes; must be replayed on a copy and pass the recompute-replay gate before touching the live store

#### Evidence
- `features/dashboard/datedFacts.ts:4-9` "Znalostní graf nevede changelog, takže „co se v grafu změnilo" se nedá postavit poctivě."
- `docs/routes/schranka.md:46-48` "the graph keeps no prior-value snapshots … emitted only when the chamber is UNIFORM"
- `lib/db/pglite/ddl.ts:313-325` `create table if not exists kg_node_history (id, kind, label, props jsonb, first_seen_pass, provenance, …, recorded_at, superseded_at)`
- `lib/db/pglite/repositories/kg.ts:346,377` the only `insert into kg_node_history` writers (supersede paths)
- `lib/ingest/changeEvents.ts:27-40` `CHANGE_EVENT_TYPES` — no score type
- `lib/db/pglite/repositories/changes.ts:200-232` backfill covers `supplies` history + `review_audit` only
- `features/denik/getDenikData.ts:425-432` "mandátové/orgánové typy nemají v produkci žádného pisatele … `change_event` PRÁZDNÁ (0 řádků)"
- `features/dashboard/entityLinks.ts:408-423` slice node id → `poslanec:`/`firma:`/`tisk:` keys — the same vocabulary as `change_event.entity_keys` (`ddl.ts:349-351`)

---

### #35 Served reproduction — the index replays itself on every read and ships its inputs

`mp-profiles-rankings#2` · theme T6 · corroboration 2 · size L · effort 5 / impact 7 / risk 2 · result `better` · gate `none`
contexts: civicscore-leaderboard, contribution-scoring, civic-claim-gate, data-releases, mp-profile
advances: politician-performance-scoring — "the moment one number in the ranking cannot be re-derived by an outsider, the whole product's claim to checkability collapses"; laws deterministic-code-owns-numbers, provenance-or-nothing; repo convention: `npm run sentinel` is the only path on which the recompute invariant executes (zebricek.md:126-128)

#### Summary
The only instrument that proves the stored scores are what the deployed formula produces is a sentinel that runs on nobody's schedule and on no CI runner; the product serves the score and the formula but never the fact that the two agree, and no outsider can re-run the formula because the per-MP inputs are not released.

#### Description
`lib/testing/sentinel/invariants.ts:296, 455-467` re-runs `computeContribution()` over stored inputs for 40 MPs at ±0,1 — reconstructing seats as synthetic distinct organs from `committee_count`/`leadership_count` (`:342-357`), which is faithful because the corrected formula counts distinct bodies. But `docs/routes/zebricek.md:126-128`: „`npm run sentinel` is the only path on which these actually execute — the workflow has no `./.pglite` on a hosted runner; there is no nightly". The read side already holds every input for all 207 MPs in memory on every chamber pass (`getLeaderboardData.ts:538-587` reads `participation_rate`, `absence_rate`, `committee_count`, `leadership_count`, `bills_authored`, `interpellations`, `speech_turns`) and re-derives the six component POINTS from them (`componentPoints`, `:137-159`) — but deliberately never re-sums the composite („the headline score always comes from the graph, never re-summed here", `:17-21`). /metodika prints the formula and the pass (`metodika.md:494-499`) and /data ships a manifest and a snapshot (`features/data-releases/feedIndex.ts:83-96`), neither of which carries the per-MP inputs.

"Built" = (1) the chamber pass runs the sentinel's exact reproduction over ALL 207 (207 × `computeContribution` — microseconds) and publishes `reproduction: {reproduced, total, tolerance, ref}` on `LeaderboardData.provenance`; a shortfall is a labelled state („N/207 skóre se z uložených vstupů nereprodukuje") beside the existing `formulaMatch` sentence — the six-day-divergence class of failure becomes visible on every request, not once per manual sentinel run. (2) A `/data` release `prispevkovy-index-<pass>.csv` (207 rows: pspId, the seven inputs, stored score, `{pass, ref}`) plus the formula ref — the outsider's reproduction kit; the README line cites `lib/analysis/contribution.ts` by ref. (3) /overeni verdict for a score claim gains `reproduced: true|false` from the same pass. (4) /metodika §"Ověřeno na datech": „207/207 skóre se reprodukují z uložených vstupů (±0,1), průchod 42" — a served number, not a promise.

#### Flow
- Lift the reconstruction helper out of `lib/testing/sentinel/invariants.ts:342-357` into `lib/analysis/contribution-reproduce.ts` (pure, tested; sentinel imports it — one definition).
- `readChamber()` calls it over all 207 and attaches `reproduction` to `ContributionProvenance` (extend `provenance.ts`; pin in `provenance.test.ts` that a pass-11-shaped store fails it).
- Render: `/zebricek` provenance block, `/metodika` §04, `/poslanec` score footnote (per-MP `reproduced` boolean), `/kraj` poster footer via `posterProvenanceNote()`.
- `features/data-releases`: add the inputs CSV as a `MACHINE_ENDPOINTS` entry generated from the same chamber pass (route handler, `cache-control` like `/embed/zebricek`).
- /overeni: `scoreClaim` verdict gains `reproduced` from the loader that owns it.
- Sentinel keeps its invariant but now compares its own result to the served `reproduction` (a served pass that disagrees with the sentinel is the finding).
- Docs: `zebricek.md`, `metodika.md`, `data.md`, `overeni.md`.

#### Expected impact
Newsrooms and rival campaigns can re-derive any MP's score from a CSV and the published constants; the product proves its own consistency on every read instead of relying on a job that has never run in CI. Measured by: reproduction served (absent → 207/207), downloadable inputs (0 → 207 rows). Could break: nothing numeric — the composite is still read, not re-summed; the risk is a false „nereprodukuje" from a rounding edge (the ±0,1 tolerance is measured at exactly 0,1 on 13/207 today, `zebricek.md:120-122`).

#### Evaluation
Claim: resilience — the stale-store failure that shipped for six days becomes visible on every request, and checkability becomes external
Before: recompute invariant evaluated 0 times on any schedule; 0 served reproduction figures; 0 input rows released; `grep -rn "computeContribution" features/` → 0 hits
After: `provenance.reproduction = {207, 207, 0.1, "contribution-committee-dedupe"}` on every chamber pass; the CSV endpoint returns 207 rows; a fault-injected copy with one stored score altered renders the shortfall sentence
Method: simulation — planned; the sentinel test already reconstructs the pass-11 store and proves the invariant fires (`sentinel.test.ts:337`), so the same fixture proves the served figure
Result: better
Gate: none — additive fields, a new read-only endpoint, no writer touched

#### Evidence
- `lib/testing/sentinel/invariants.ts:296, 342-357, 455-467` — the reproduction exists, 40-MP sample, synthetic seats
- `docs/routes/zebricek.md:126-128` — „`npm run sentinel` is the only path on which these actually execute"; `:110-125` the four invariants
- `features/civicscore/getLeaderboardData.ts:17-21, 137-159, 538-587` — inputs in memory, points re-derived, composite deliberately not re-summed
- `features/data-releases/feedIndex.ts:83-96` — machine endpoints: novinky, manifest, snapshot; no inputs release
- `lib/analysis/contribution.ts:148-154` — `round3` rates published so the read side can re-derive within 0,02 pt

---

### #36 Three-state gate and source on every graph edge (rejected is not verified)

`knowledge-graph-explorer#0` · theme S · corroboration 1 · size L · effort 6 / impact 9 / risk 4 · result `better` · gate `contract`
contexts: graph-explorer, shared-provenance, claim-verifier, db-repositories, civic-kg-primitives
advances: civic-knowledge-graphs / per-claim-provenance-stamping + evidence-path-finding; claim-verification-and-provenance / gate-state-as-modifier; law: lead-not-finding, provenance-or-nothing

#### Summary
The whole graph surface reduces the store's three-state human gate (`verified | pending_review | rejected`) plus per-edge `provenance` to one boolean `pending`, so a human-REJECTED tie is drawn solid, ranks as a verified hop in "Spoj dva body", and is exported in the citation bundle as `review_state: verified`. No edge in `/graf` can name its source.

#### Description
`kg_edge` carries `props` (with `review_state`) and a `provenance` jsonb per row (`lib/db/pglite/ddl.ts:234-241`), and the review writer makes `rejected` a terminal state that stays in the graph (`lib/db/pglite/repositories/review.ts:66-71`). The graph loader collapses that to `pending: e.props?.review_state === "pending_review"` (`features/graph/graphLoader.ts:250-263`, again at `:703`, and `pendingLink` at `:492-494`); nothing under `features/graph/**` or `lib/kg/**` mentions `rejected` (grep: 0 hits). Consequences today: `trailPath.ts:385-391` counts only `pending` hops, so a rejected `linked_to` edge is a full-strength documentary hop and outranks a pending one; `GraphStage` draws it solid (dashed is `pending` only); `forensicEdges()` keeps it in the "verified-only" landscape (`forensicView.ts:459-476`); and `toEvidenceJsonLd` prints `prop("review_state", pending ? "pending_review" : "verified")` (`permalink.ts:505-519`, `edgeClaim`), so the least-correctable artifact certifies a human refusal as verification. Separately, `PathEdge`/`PathHop`/`PathLedgerRow` carry no source at all (`trailPath.ts:204-237`, `graphTypes.ts:107-118`), while the receipt layer already knows how to read one (`features/shared/provenance/receipt.ts:165-200` `toProvenance`, `gateFromEdge`) and already mints a permanent address per edge (`claimRef.ts` `edgeClaimRef`). The golden path's definition of a checkable path is "every hop names its source, its derivation, and its review status" — `/graf` delivers only the third, and delivers it wrong for one state.

Built: `GraphEdge` gains `gate: GateStatus | null` (null = ungated deterministic relation, per `GATED_RELS`) and `provenance: {pass, method, ref}`; `pending` becomes a derived accessor kept for the stage. `buildAdjacency` excludes `rejected` hops from evidential traversal (a refused claim is not a documented connection) and reports how many it excluded; the ledger row, the trail edge, the permalink `Claim` and the OG review line each print the gate token via `gateStatusInfo` and link the hop to `/zdroj/<edgeClaimRef>`. The receipt page becomes the inspector for an edge the way `NodeInspector` is for a node.

#### Flow
- Extend `toEdge`/`PathEdge` in `graphLoader.ts` to carry `gate` (from `gateFromEdge`'s rule, imported, not restated) and `provenance` (`toProvenance`); add a `rejected` fixture to `trailPath.test.ts` and `forensicView.test.ts` that fails today.
- `buildAdjacency`: drop `rejected` edges, return `excludedRejected` count; `FindPathsResult`/`PathQueryResult` ship it; the printed ordering rule (`TrailFinder` ruleNote, `permalink.rule`) states "N zamítnutých vazeb se nepoužívá".
- `forensicEdges`: three buckets (verified / pending hidden-and-counted / rejected hidden-and-counted); hover card rows gain a `rejected` column.
- `GraphStage`: a third stroke for rejected in a requested lens (a rejected hop inside a curated trail must still render, marked) — trails are built from `linked` edges without filtering (`graphLoader.ts:451-501`).
- `permalink.ts` `edgeClaim`: `review_state` = literal token, plus `provenance_ref`/`provenance_pass`/`claim_url` (absolute `/zdroj/…` when origin known); `permalinkCardModel.review` splits `pendingEdges` / `rejectedEdges`; OG image prints both.
- `PermalinkPage` ledger rows and `TrailFinder` rows link each hop to its receipt; `NodeInspector` adds a "vazby tohoto uzlu" list from `kgNeighbours` with gate + receipt link (also wires `caseFileLink.ts`, unused in graph today).
- `/overeni` `grafVerdict`: `verdictGate` for the `graf` family stops returning `ungated` blanket and reports the worst gate among the view's edges as the modifier.

#### Expected impact
Journalists citing a path get a citation whose every hop is itself a citable receipt; a rejected tie can no longer surface as evidence anywhere on `/graf`, `/graf/p`, the JSON-LD bundle or the OG card. Measured by: count of `rejected` edges in the live store that today render solid / rank as verified (before) vs 0 (after), and the JSON-LD `review_state` token distribution. Could break: permalink content hashes shift for every view containing a gated edge (the `content` object changes shape) — every issued `g.` ref reads as `moved` once; announce it as a derivation bump (see card 3) rather than silently.

#### Evaluation
Claim: quality - the graph surface stops certifying refused claims as verified and every hop becomes source-addressed
Before: `grep -rn rejected features/graph lib/kg` = 0 hits; `toEdge` (`graphLoader.ts:262`) maps `rejected` → `pending:false`; `edgeClaim` (`permalink.ts`) prints `verified` for it; `review.ts:71` confirms `rejected` is written and retained. Live count of rejected `linked_to` edges is the missing number (needs `select count(*) from kg_edge where props->>'review_state'='rejected'`, store not opened by this read-only scout).
After: the same query's count equals the "zamítnuté (nepoužité)" figure printed on `/graf` and in the bundle; a fixture path through a rejected edge returns "no documented connection".
Method: probe - code path traced from writer to three renderers; fixture reasoning on `trailPath.test.ts`
Result: better
Gate: contract - the JSON-LD bundle's `review_state` vocabulary and the `content` hash shape are external contracts

#### Evidence
- `lib/db/pglite/ddl.ts:234-241` — `kg_edge(props, provenance)`; `lib/db/pglite/repositories/review.ts:66-71` — `reject → "rejected"`, terminal, edge retained
- `features/graph/graphLoader.ts:250-263` — `pending: e.props?.review_state === "pending_review"`; `:492-494` (`pendingLink`), `:696-705` (path adjacency), provenance never read for edges
- `features/graph/trailPath.ts:204-211, 385-391, 415-420` — `PathEdge` has no source; ranking counts `pending` only
- `features/graph/forensicView.ts:459-476` — two buckets, rejected passes as verified
- `features/graph/permalink.ts` `edgeClaim` — `prop("review_state", pending ? "pending_review" : "verified")`
- `features/shared/provenance/receipt.ts:61, 165-200` — `GATED_RELS`, `toProvenance`, `gateFromEdge` (the one rule to import); `claimRef.ts` `edgeClaimRef`
- `features/overeni/verdict.ts:238-239` — `graf` family always `ungated`
- `features/shared/provenance/caseFileLink.ts:1-25` exists; `grep caseFileLink features/graph` = 0

---

### #37 Ingest run as a diffed, event-emitting, recompute-triggering protocol

`data-ingestion#0` · theme T9 · corroboration 1 · size XL · effort 8 / impact 9 / risk 6 · result `unmeasurable` · gate `contract`
contexts: ingest-psp-sources, kg-pipeline, effort-case-loop, law-triage-batch, money-contract-ingest
advances: civic-source-adapters / fail-loud-schema-drift (snapshot diffing for versionless sources, epoch discipline); civic-knowledge-graphs / pass-based-incremental-enrichment; law: missing-is-not-zero (a row that left the dump is a fact, not a ghost)

#### Summary
The seismograph's forward stream is fully built and fully unfed: `mandateChangeEvents`/`membershipChangeEvents` have zero production callers, `ingest.ts` re-upserts every psp.cz snapshot in place with no diff against the previous one, and nothing downstream (kg-compute, contribution ingest, legislation ingest, slice staleness) is told that the corpus moved. "Re-ingest" today is a manual, un-diffed, unguarded act.

#### Description
`scripts/data-analysis/ingest.ts:94-113` opens an `ingest_run`, calls `store.upsertMandates` / `store.upsertMemberships` (lines 109-110) and closes the run with a row count — that is the whole contract (`IngestRunRow`, `lib/db/types.ts:155-165`: `rowsWritten`, `note`, nothing about what changed). `lib/ingest/changeEvents.ts:178` (`diffSnapshots`) and `:251/:267` (the mandate/membership event builders) are pure, tested, and called only from `changeEvents.test.ts`; `lib/db/pglite/repositories/changes.ts:19-21` states that mandate/role events "have NO backfill source … they only accrue forward, via the adapter snapshot-diff layer" — which nothing invokes. `/denik` even grew a `mandate`/`organRole` kind for these events (`docs/routes/denik.md:158-166`) and cites the literal source string `diff snímků ingestů — psp.cz` (`changeEvents.ts:224`) that has never been emitted outside a test. Staleness is equally manual: no code sets a `slice_quality` row to `stale` (grep over `lib/db`, `scripts/data-analysis`, `lib/analysis` finds the token only as an `/atlas` rule label, `lib/analysis/atlas.ts:83`), and the knowledge-graph loop's two remaining frontier items (F5/F10) are "re-run on re-ingest" with no trigger. Meanwhile a re-ingest is dangerous by construction: `kg-legislation-ingest.ts:147` still builds bill `props: {…}` from scratch (no read-merge — confirmed by `memory/kg-upsert-replaces-props.md`: a re-run erases 140 summaries + 141 forensic verdicts), and `kg-contribution-ingest.ts` must be run with `--supersede` after any recompute. "Built" is one refresh protocol every adapter run passes through: (1) fetch + shape-assert (already there for smlouvy; add the column-count assertion to `psp.ts`), (2) snapshot-diff against the previous run's rows over the natural key, (3) emit `ChangeEvent`s through `getChangesRepo().upsertChangeEvents` with the run's `finishedAt` as record time and the first run as epoch, (4) write a per-table `{added, removed, changed}` triple onto `ingest_run`, (5) mark the affected `slice_quality` rows stale and enqueue the deterministic recompute chain (`kg-compute` merge-preserving, contribution ingest under its guard, legislation ingest ONLY after it adopts `mergeComputedNodeProps`), (6) refuse to finish the run if any adopted writer would drop a later-pass prop. Rows that vanished upstream are not deleted: they get a `removed_at` and a `mandate-removed` event, so "left the psp.cz dump" is a dated claim rather than a ghost row.

#### Flow
- Add `{added,removed,changed}` per table + `previousRunId` to `ingest_run` (CORE_DDL via `npm run db:migrate`); keep `rowsWritten` for compatibility.
- In `ingest.ts`, before each `upsert*`, `list*` the prior rows for the same source, run `diffSnapshots` with a projection that ignores `fetchedAt`/`ingestRunId`, then upsert and emit events via the changes repository (epoch = the first run that carries a diff).
- Add `removed_at` to mandate/membership (never delete), and make `listMandates({termCode})`/the current-mandate signal (`memory/current-mandate-holder-signal.md`) respect it.
- Convert `kg-legislation-ingest.ts` to the `mergeComputedNodeProps` read-merge idiom (the one remaining from-scratch writer in the group) with a copy-verified test that later-pass props survive.
- Introduce `scripts/data-analysis/refresh.ts`: runs ingest → diff → events → marks `slice_quality` stale for touched slices → runs the deterministic writers in dependency order (kg-compute → contribution ingest → legislation ingest → bill roles/engagement), each dry-run first, `--commit` only when every guard prints clean; ends by regenerating the case `STATE.md` "as of" lines.
- Wire `/atlas` freshness and `/data` release train to read the new `ingest_run` triple (touched contexts outside this group: data-releases, atlas).

#### Expected impact
A reader of `/denik` and `/schranka` finally sees mandate and committee-role changes as dated, cited rows the day they land upstream; the operator gets a single, replayable "refresh" command instead of a sequence of hand-run writers with two override flags; the graph's PSP10 data stops being a frozen 2026-07 snapshot. Measured by: forward `change_event` rows of type `mandate-*`/`role-*` > 0 after the first diffed run, and `ingest_run` rows carrying a non-null diff triple. What could break: a wrong projection in the diff makes every row "changed" on the first run (the first-run flood the epoch rule exists for); a re-ingest before the legislation writer is merge-preserving would erase the forensic layer — the protocol must refuse to run that writer until its test exists.

#### Evaluation
Claim: quality | resilience - the platform's freshness claim becomes a measured, replayable fact instead of a manual ritual
Before: 0 production call sites for `mandateChangeEvents`/`membershipChangeEvents` (grep over lib, scripts, features, app); `ingest.ts` contains 0 references to diff/snapshot/stale; `change_event` declares 10 types, 6 of them (`mandate-*`, `role-*`) have never been produced outside tests; no code path sets `slice_quality` to `stale`
After: same greps show ≥1 caller in `ingest.ts`; the first diffed run emits N `mandate-*`/`role-*` events with `recordedAt = finishedAt`; `ingest_run` carries `{added,removed,changed}` per table — the MISSING INSTRUMENT today is the per-run diff triple itself
Method: probe - static grep for callers + read of the write path (`ingest.ts:94-185`) and the changes repository header
Result: unmeasurable
Gate: contract

#### Evidence
- `lib/ingest/changeEvents.ts:178` `export function diffSnapshots<T extends { id: string }>(`; `:251` `export function mandateChangeEvents(`; `:267` `export function membershipChangeEvents(`; `:224` `const SOURCE_SNAPSHOT = "diff snímků ingestů — psp.cz";`
- Callers of the two builders (grep, glob `{lib,scripts,features,app}/**/*.ts`): only `lib/ingest/changeEvents.test.ts:70,76,84`
- `lib/db/pglite/repositories/changes.ts:19-21` — "Mandate/role events have NO backfill source (those tables keep no history) — they only accrue forward, via the adapter snapshot-diff layer"
- `scripts/data-analysis/ingest.ts:94` `startIngestRun`, `:109-110` `upsertMandates`/`upsertMemberships`, `:113` `finishIngestRun(runPoslanci, "ok", rows1, …)` — no prior-row read, no diff
- `lib/db/types.ts:155-165` `IngestRunRow { … rowsWritten: number; note: string | null }`
- `lib/db/pglite/repositories/graph.ts:38-49` — `upsertMany` in-place upserts; no delete path for rows absent from the new dump
- `scripts/data-analysis/kg-legislation-ingest.ts:147` `props: {` built from the parsed bill, no read-merge (memory `kg-upsert-replaces-props.md` — "re-running it today would erase 140 summaries + 27 forensic verdicts")
- `docs/routes/denik.md:158-166` — the deník added `mandate`/`organRole` kinds for events "emitted" only in tests
- `lib/analysis/atlas.ts:83` — the only `"stale"` token outside docs is an `/atlas` rule label

---

### #38 Term-keyed chamber pass — per-term rankings, archived, for the whole career spine

`mp-profiles-rankings#1` · theme S · corroboration 1 · size XL · effort 9 / impact 9 / risk 6 · result `better` · gate `irreversible`
contexts: civicscore-leaderboard, contribution-scoring, mp-profile, mp-rankings-routes, psp-ingest, db-store
advances: politician-performance-scoring — weighted-component-index over every population the registry covers; formula-lineage-stamping per term; laws non-partisan-symmetry (whole population per term), missing-is-not-zero (coverage per term), every-cap-ships-its-population; repo convention: politicas.md §9 Phase 4 election-cycle hardening

#### Summary
The chamber pass is hard-wired to one term literal, the career spine admits activity exists „ONLY for PSP10 (+ a partial PSP9)", and the ingest adapters are already term-parametrised — so the product can rank one parliament and will rank zero the day PSP11 opens.

#### Description
`readChamber()` reads `listMandates({termCode: "PSP10"})` and `clubByMandate("PSP10")` (`features/civicscore/getLeaderboardData.ts:517-518`); the profile loader sets `const term = "PSP10"` (`features/profile/getProfileData.ts:452`) and states „Activity beyond presence exists ONLY for PSP10 (+ a partial PSP9 via the `contribution_psp9` prop)" (`:567-569`). The career spine renders every earlier term as `coverage: "none"` (`features/profile/careerSpine.ts:57, 280`). The prior-term baseline is a one-off prop, `contribution_psp9` on 109 continuing MPs (`docs/data-analysis/graph-log.md:383-384, 991-994`), consumed only by the profile-only `trend` (`getLeaderboardData.ts:291-293`), while /zebricek states „delta / trend has NO real backing — single term" (`:33-36`). Yet the ingest is already term-scoped by `termPspId` on every dataset (`lib/ingest/sources/psp-activity.ts:54, 88, 111, 129`), the PSP9 roll calls are ingested in full (9 016 roll calls, 1,8 M ballots), and mandates for PSP1–PSP10 are in the store (`getProfileData.ts:561-563`).

"Built" = the contribution index becomes a `(person, term)` fact instead of a person prop: a `contribution_term` row set (or `contribution:<term>` namespaced props) per term with its OWN `{pass, ref, computedAt}`, written by the same writers with the same replay/ref guards; `buildLeaderboard(term)` keyed by term with the memo keyed by term; `/zebricek?obdobi=9` and `/zebricek/psp9` archived rankings (competition ranks, the same six components, the same caps, formula ref stamped per term — a formula correction re-stamps every term or reports `mixed` per term); the career spine gains a per-term score cell with coverage stated per term (`full`/`partial`/`none` already exists); the trend generalises from one hard-coded prior prop to „every term the person served that the store has scored". On term rollover the new term renders as „zatím neměřeno" with its coverage, never as an empty chamber.

Honesty constraints that keep this a moonshot and not a formula change: the saturation caps are per-term counts and stay so — a short term is disclosed as short (session days, roll calls held printed beside the score), never pro-rated (that would be an unpublished formula). Cross-term comparison stays what `contribution-trend.ts` already does: subtract two authored numbers, only where both exist.

#### Flow
- Decide storage with the DB owner: `contribution_term` table keyed `(person_psp_id, term_code)` vs. namespaced props; the table wins if `prop-registry.json` would otherwise need one key per term. `db:migrate`.
- Generalise `kg-contribution-ingest.ts` / `psp9-contribution.ts` into one term-parametrised writer (`--term=PSP9`) that stamps `CONTRIBUTION_FORMULA_REF` per term; the replay gate and `guardContributionWrite` run per term.
- Backfill PSP9 for ALL 200+ PSP9 MPs, not only the 109 continuing (symmetry: the population is the whole PSP9 chamber); backfill PSP8 if the dumps ingest cleanly, otherwise stop and record coverage.
- `buildLeaderboard(term = CURRENT_TERM)`; `CURRENT_TERM` derived from the open chamber membership (memory: `current-mandate-holder-signal`), not a literal; memo keyed by term; `provenance` per term.
- Routes: `/zebricek` = current term; `/zebricek/[obdobi]` archived; `/poslanec` per-term score cells on the career spine; `/metodika` states which terms are scored and their per-term `{pass, ref}`.
- `contribution-trend.ts` generalised to N terms; `TrendPanel` copy already names the dump per term (`trendCopy.ts:25`).
- Sentinel: `formula-ref` and `provenance-uniformity` evaluated per term; `messages.test.ts` term-digit bans already in place.
- Docs: `zebricek.md`, `poslanec.md`, `graph-schema.md`, `coverage-ledger.md`.

#### Expected impact
Voters get the thing an election surface needs most — how an MP worked in the PREVIOUS parliament they are asking to return to — and the product survives the term boundary instead of going dark. Measured by: terms rankable (1 → ≥2), MPs with a full prior-term score (109 → whole PSP9 chamber), `/zebricek` after a simulated rollover renders a stated-coverage page rather than `null`. Could break: every consumer of `contribution_score`/`contribution_provenance` person props (dashboard, schranka's `getRecomputeFact`, kompas, sentinel `facts.ts`) must move to the term-keyed read in one wave or two vintages will print side by side.

#### Evaluation
Claim: user — the flagship ranking exists for more than one parliament and survives an election
Before: `grep -c '"PSP10"' features/civicscore/getLeaderboardData.ts features/profile/getProfileData.ts` → 3 literals; career spine coverage for PSP1–PSP9 = `none` for 92/207 first-termers' predecessors and `partial` for 109; 0 archived rankings
After: 0 term literals in loaders; ≥2 terms with uniform per-term provenance; a rollover simulation (PGlite copy with organ PSP11 opened, 0 activity) renders „zatím neměřeno" with coverage
Method: probe — traced the term literal through both loaders and the ingest adapters' `termPspId` parameter; confirmed PSP9 ballots already ingested (graph-log pass 14)
Result: better
Gate: irreversible — data model change over 207 person nodes' flagship props plus a multi-term backfill; needs a verified snapshot and a copy-first full props diff (memory: `kg-upsert-replaces-props`)

#### Evidence
- `features/civicscore/getLeaderboardData.ts:517-518` — `termCode: "PSP10"`, `clubByMandate("PSP10")`; `:33-36` trend omitted as single-term; `:291-293` `contribution_psp9` profile-only
- `features/profile/getProfileData.ts:452` `const term = "PSP10"`; `:567-569` activity only PSP10 + partial PSP9; `:561-563` mandates PSP1–PSP10 ingested
- `features/profile/careerSpine.ts:57, 280` — `TermCoverage` and the `none` default for every non-current term
- `scripts/case-loops/effort/psp9-contribution.ts:16-19, 154` — one-off prior-term writer, `complete` flag
- `docs/data-analysis/graph-log.md:383-384` — PSP9 ingested in full, 109 continuing MPs scored; `:991-994` baselines corrected at pass 42
- `lib/ingest/sources/psp-activity.ts:54, 88, 111, 129` — every adapter already takes `termPspId`

---

### #39 Statute version graph — e-Sbírka znění as nodes, real before/after on /zakony

`data-ingestion#1` · theme S · corroboration 1 · size XL · effort 9 / impact 9 / risk 5 · result `unmeasurable` · gate `contract`
contexts: law-amends-analysis, law-triage-batch, kg-pipeline, ingest-external-sources
advances: legislative-change-tracking / amendment-instruction-grammar + bill-fate-dating; civic-knowledge-graphs / civic-entity-ontology (a new kind by deliberate amendment); law: provenance-or-nothing (the refusal in `docs/routes/zakony.md` is lifted only with verbatim `text-fragmentu`)

#### Summary
`/zakony` refuses to draw paragraph diffs because "the graph carries no paragraph diffs"; the group already owns a verified, negligible-bandwidth point source for exactly that text (`esbirka-sparql-diff.ts`), but it runs one law × one § × one date pair at a time into a payload file on disk, and `law` nodes carry four props. The version timeline of each amended statute is the missing layer of the ontology.

#### Description
Today a `law` node is `law:sb:<n>-<rok>` with `{ref, esbirka_eli, esbirka_exists, esbirka_title}` (`lib/kg/prop-registry.json` → `nodes.law`), minted by `kg-legislation-ingest.ts:168` from a title regex, and enriched by `esbirka-laws.ts` with the act master only. `scripts/case-loops/law/esbirka-sparql-diff.ts:1-30` documents the verified IRI shape of e-Sbírka's SPARQL store — every enacted znění (`účinnost-znění-od/do`) and every § fragment's `text-fragmentu` are addressable point queries — and `esbirka-versions.ts:20-23` streams dataset 001 for a HARDCODED list of 9 laws into `docs/…/esbirka-version-history.json`. Neither writes the graph. `docs/routes/zakony.md:23-25` states the standing rule: no paragraph diffs, no bill-stage pipeline, "fabricating them would violate the brand rule". The 141 forensic verdicts are therefore adjudicated over the bill's own text (`collision-core.ts` reads `.data/law-collision-cache/tisk-*/`), never over what the statute actually became. "Built" is a `law_version` node kind (`law_version:sb:<n>-<rok>:<účinnost-od>`, props `{ucinnost_od, ucinnost_do, typ_zneni, fragment_count, eli}` — all register facts), an `has_version` edge law → law_version ordered by date, an `enacted_as` edge bill → law_version where the bill's `fate_sb` (present on 12/141 bills per `graph-schema.md:44`, more after each Sbírka publication) matches the version's amending act, and a deterministic fragment diff (`op: added|removed|changed`, verbatim before/after) computed on demand per (law, version_i, version_i+1) and cached as a `diff` artifact whose every hunk cites the two fragment IRIs. `/zakony/predpis/[ref]` gets a dated version rail; a bill dossier gets "co se v zákoně skutečně změnilo" — verbatim, with the missing side rendered as `null`, never synthesized. The forensic gate (`law-verdict.ts:247-262` checks cited statutes against `known-laws.json`) gains a second closed set: cited § fragments must exist in the version the verdict names.

#### Flow
- Amend `KG_NODE_KINDS`/`KG_EDGE_RELS` (`lib/analysis/kg-verdict.ts:24-49`) with `law_version`, `has_version`, `enacted_as`; register the props in `prop-registry.json` + `graph-schema.md` in the same change; `CASE_OWNED_NODE_KINDS` in `kg-promote.ts` refuses the new kind by construction.
- Generalize `esbirka-versions.ts` from the 9-law `TARGETS` to "every `law` node in the graph" (293 laws), streaming dataset 001 once; write `law_version` nodes + `has_version` edges through `apply-batch.ts` (insert-capable, node-then-edge ordering, enum-enforced).
- Link bills: for each bill with `fate_sb`, resolve the amending act in e-Sbírka (SPARQL: the version whose amending-act citation equals `fate_sb`) and emit `enacted_as`; count and disclose bills whose fate is not yet in Sbírka (missing is not zero).
- Promote `esbirka-sparql-diff.ts` into `lib/ingest/sources/esbirka.ts` (adapter, shape-asserted, cached under `.data/esbirka/fragments/`) and a pure `lib/analysis/law-diff.ts` (fragment-set diff, tested on fixtures) — one definition for the diff, imported by the loader and by the forensic gate.
- Surface: `/zakony/predpis/[ref]` version rail; `/zakony/[cislo]` "skutečná změna" block reading the cached diff via the loader pattern, with a `SourceNote` per hunk citing both fragment IRIs; language gate not needed (text is the statute's own).
- Extend `gate-verdicts.ts` and `law-verdict.ts` with the fragment-membership check; re-run the whole stack over the stored verdicts (the re-runnable gate rule).

#### Expected impact
A journalist reading a bill on `/zakony` sees what the amendment actually did to the statute, with two e-Sbírka addresses per hunk — the flagship LawWatch promise (`ROADMAP.md:25` "paragraph diffs → linked roll-call votes") restored on real data; the forensic verdicts get a text they can be checked against instead of a bill PDF; `/overeni` gets a new claim family (a hunk is a citable claim). Measured by: count of `law_version` nodes (expect ≥ 1 per law, hundreds for 586/1992-class statutes) and count of bills with an `enacted_as` edge vs bills with `fate_sb`. What could break: the SPARQL endpoint's IRI shape drifts (assert the shape on every fetch, fail loud); fragment text is HTML-ish with internal `<a>` cross-refs — a naive strip could eat digits (run the whole-artifact digit-multiset invariant on every hunk).

#### Evaluation
Claim: user | quality - the route's stated refusal is lifted with verbatim register text rather than fabrication
Before: `law` nodes carry 4 props (`prop-registry.json` nodes.law); 0 version nodes; `esbirka-versions.ts` covers 9 hardcoded laws of 293; `/zakony` renders 0 paragraph diffs (`docs/routes/zakony.md:23`); 12/141 bills carry `fate_sb` (`graph-schema.md:44`)
After: `law_version` nodes for 293/293 laws with a dated range each; `enacted_as` edges = the number of bills whose `fate_sb` resolves (disclosed against 141); N cached fragment diffs each with two IRIs per hunk — the instrument is the `has_version` count per law and the resolved/unresolved `fate_sb` split
Method: probe - read of the two e-Sbírka scripts, the law node contract, the route's standing rules and the verdict gate
Result: unmeasurable
Gate: contract

#### Evidence
- `docs/routes/zakony.md:23-25` — "The graph carries no paragraph diffs and no bill-stage pipeline, so neither is drawn — fabricating them would violate the brand rule."
- `scripts/case-loops/law/esbirka-sparql-diff.ts:5-11` — the SPARQL point-query discovery; `:16-25` verified IRI shape (`účinnost-znění-od/do`, `má-fragment-znění`, `text-fragmentu`); `:27-30` anti-fabrication rule for hunks
- `scripts/case-loops/law/esbirka-versions.ts:20-23` `const TARGETS = new Set(["586/1992", … "45/2013"] …)` — 9 laws; output to `docs/…/esbirka-version-history.json`, no graph write
- `lib/kg/prop-registry.json` → `nodes.law = ["esbirka_eli","esbirka_exists","esbirka_title","ref"]`
- `scripts/data-analysis/kg-legislation-ingest.ts:168` law node minted with `props: { ref }`
- `docs/data-analysis/graph-schema.md:44` — `fate_sb`/`fate_published_on` "(Sbírka publication — 12/141)"
- `lib/analysis/law-verdict.ts:247-262` — statute citations checked against `knownLawRefs`; no fragment-level set exists
- `lib/analysis/kg-verdict.ts:24` `KG_NODE_KINDS = [… "bill", "law", "notice", "tender"]` — no version kind
- `docs/case-loops.md` R9–R11 — "when full texts land (steno, e-Sbírka), index with tsvector+GIN from the start"

---

### #40 Gate replay lane: one gate registry, re-run over every stored reader string

`infrastructure-observability#2` · theme S · corroboration 1 · size L · effort 6 / impact 8 / risk 3 · result `unmeasurable` · gate `contract`
contexts: analysis-quality, testing-sentinel, db-hybrid-benchmarks, effort-loop-scripts, law-loop-scripts, money-loop-scripts
advances: llm-forensic-gating ("the whole stack must be re-runnable as one command over stored verdicts"), prose-register-gates, whole-artifact-invariants; law one-definition-one-import

#### Summary
The persist-time gates live in three per-case scripts that take payload FILES, the render-time gates are re-imported ad hoc by each loader, and no command re-verifies what is already in the store — so the two doors have forked twice (measured), and the sentinel that guards numbers has no invariant over prose.

#### Description
Today: `lib/analysis/public-copy.ts:16-25` states the doctrine — "run at BOTH ends … this module is the one definition both import" — and then records, at `:60-66`, that `gate.ts` carried a LOCAL fork so strings dropped at persist were rendered anyway (Q-effort-15). `lib/analysis/committee-claims.ts:13-17` records the same fork class twice more ("gate.ts vs public-copy.ts's jargon rules; kg-compute.ts vs contribution.ts's committee predicate … silently diverged and shipped a defect"). Persist gates are three unrelated CLIs: `scripts/case-loops/effort/gate.ts`, `scripts/case-loops/law/gate-verdicts.ts:1-18` (which calls itself "the re-verify command the fleet orchestrator runs"), and the money persist path — each reading `docs/data-analysis/**/payloads/*.json`, none reading the store. Render gates are opt-in per loader: `language-gate` is imported by `features/lawwatch/getLawData.ts` and `features/money/ownership.ts` but the memory file records it was FOUND MISSING on three surfaces across three passes, and it false-positives on registry Czech (14/211 tie notes). The whole-artifact invariants the registry names (digit multiset, quote locatability, count closure) exist only as a one-off archived script (`scripts/case-loops/law/archive/sweep-old27-015.ts`) and `memory/whole-artifact-invariants-beat-pattern-gates.md` says every per-string gate stayed green through four audit rounds while a falsified CZK figure shipped. The sentinel roster (`invariants.ts:286-298`) has zero prose checks, so a stale sentence (`memory/recompute-invalidates-prose.md`: pass 42 moved 33 scores and left 30 dossier sentences wrong) is invisible to the only nightly instrument.

Built: `lib/analysis/gates/` — a registry `READER_STRING_GATES: Gate[]` where each gate is `{id, incident, appliesTo: field-selector, check(text, ctx) → violations}` and each entry carries its dated incident (the law: doctrine anchored to its incident). Members: jargon (`public-copy`), Czech register (`language-gate` — scoped to fields WE author, per its memo), committee-claims-vs-props, numeric-claims-vs-props (lifted out of `effort/gate.ts`), statute-reference sweep (from `law-verdict.ts`), graph-id membership, and the four whole-artifact invariants where a before/after pair exists (`kg_node_history`). A `FIELD_REGISTRY` (extending `lib/kg/prop-registry.json`) declares which props are reader-facing prose. Then ONE command, `npm run gates:replay`, walks every reader-facing string in the store copy and emits a canonical report with population per gate; the sentinel consumes it as a `prose-gates` check (violations named with node id + field, count/population); every loader's render-time withhold and every case gate's persist-time drop import the same registry — the per-case gate scripts shrink to payload loading + `runGates()`.

#### Flow
- Create `lib/analysis/gates/registry.ts` with the `Gate` shape and the incident field; move existing rule arrays in unchanged (imports only — no rule text edited in this step), pinned by a test that the registry's members equal the union of what `gate.ts`, `gate-verdicts.ts`, `getProfileData.ts`, `getLawData.ts` import today
- Add `readerFields` to `lib/kg/prop-registry.json` per node kind (`effort_notes`, `forensic_*` prose, tie `note`, …); `persist-batch` already refuses unknown keys, so the declaration lives where writers are checked
- `scripts/gates/replay.ts`: copy-store pattern from `sentinel/run.ts`, SELECT every reader field, run the registry, write `politicas.gates/1` canonical JSON with `{gate, population, violations[]}`
- Sentinel: `prose-gates` check reads the replay (or runs it inline over the same copy); `unevaluable` when no reader field exists, never ok by absence
- Whole-artifact lane: for each `kg_node_history` pair of a reader field, assert digit multiset ⊆, quoted spans locatable, counts closed against props — the rule set from the archived sweep, promoted to `lib/`
- Refactor `effort/gate.ts`, `law/gate-verdicts.ts`, `getProfileData.ts`, `getLawData.ts`, `AnalystNote.tsx` to import `runGates()`; delete the local forks; `test:rules`-style test asserts no `PIPELINE_JARGON`-shaped literal exists outside the registry

#### Expected impact
Operators can answer "does anything in the store violate a gate today" in one command with a denominator; a recompute that strands prose is caught the same night; the fork class that shipped three defects becomes structurally impossible. Measured by: number of gate definitions (1 registry vs 3 scripts + N loaders) and `prose-gates` violations on the live copy (unknown today — the instrument does not exist). What could break: the language gate's known false positives would fire at scale — scope it to authored fields first.

#### Evaluation
Claim: quality — the render/persist doors run one definition and the stored corpus is re-verifiable
Before: 3 persist-gate CLIs over payload files, 0 commands over the store, 0 prose invariants in the sentinel, 2 recorded fork incidents + 30 stale sentences after pass 42
After: `npm run gates:replay` report with per-gate population; sentinel `prose-gates` row; forks impossible by import test
Method: probe — read `public-copy.ts`, `committee-claims.ts`, `gate.ts`, `gate-verdicts.ts` headers; grep `language-gate` importers (loaders: 4 of ~20 surfaces)
Result: unmeasurable (the instrument that would take the "after" number is the deliverable)
Gate: contract

#### Evidence
- `lib/analysis/public-copy.ts:16-25` both-doors doctrine; `:60-66` "gate.ts carried this rule as a LOCAL fork … DROPPED at persist time but NOT withheld at render time"
- `lib/analysis/committee-claims.ts:13-17` "The two previous times this loop forked a shared rule into a script … silently diverged and shipped a defect"
- `scripts/case-loops/law/gate-verdicts.ts:1-18` gate over `payloads/verdicts/`, "the re-verify command the fleet orchestrator runs before writing to live .pglite" — payload-scoped, not store-scoped
- `scripts/case-loops/effort/gate.ts:1-12,25-50` local numeric-claims extractor duplicating `CZECH_NUM_WORDS` from `committee-claims.ts:43-54`
- `lib/testing/sentinel/invariants.ts:286-298` roster: no prose check
- `scripts/case-loops/law/archive/sweep-old27-015.ts:1-12` the only whole-artifact implementation, archived
- `memory/whole-artifact-invariants-beat-pattern-gates.md`, `memory/recompute-invalidates-prose.md`, `memory/language-gate-false-positives-on-registry-czech.md`, `memory/reader-facing-loaders-need-the-language-gate.md`

---

### #41 The procurement layer reaches the canvas: on-demand neighbourhoods as a citable view

`knowledge-graph-explorer#3` · theme S · corroboration 1 · size L · effort 7 / impact 8 / risk 5 · result `unmeasurable` · gate `contract`
contexts: graph-explorer, db-repositories, shared-provenance, money-cases-review
advances: civic-knowledge-graphs / forensic-view-filtering ("show less by default, never less than asked") + evidence-path-finding; law: every-cap-ships-its-population, missing-is-not-zero

#### Summary
48 647 tender nodes and 12 467 procurement-only companies are in the search index and in the path adjacency but deliberately not on the map, so a computed path that routes through them lights nodes the stage has no position for and silently skips their edges. The indexed `kgNeighbours` read exists and the graph never calls it.

#### Description
`buildMapData` excludes `tender` and procurement-only companies from `visible` and admits it in `omitted` (`graphLoader.ts:339-344, 370-374, 405-412`); `buildPathAdjacency` includes every edge whose endpoints are in the index (`:694-706`), and the index includes all `KgNodeKind`s including `tender` (`:180-189`, `kindStyle.ts:87-95`). So `findEvidencePaths` can return a person → company → tender → zadavatel path; `VariantMapa` builds the lens from `activePath.nodeIds` (`VariantMapa.tsx:162-175`) and `positions` from `data.nodes` only (`:189-202`); `GraphStage` then drops every lens edge with a missing endpoint (`GraphStage.tsx:258-260 if (!a || !b) continue`, same at `:221-223`, `:282-284`) and `fitBounds` frames the nodes it can find. The ledger panel prints all hops; the canvas draws a subset with no count of what it could not draw — the requested-answer exception of the golden path ("a requested answer with omitted steps is a lie") is honoured in the ledger and broken on the stage. The loader header itself records the plan: "produkce by štítky dotahovala podle výřezu" (`graphLoader.ts:287-288`) and "zakázky dostanou vlastní plochu, až ji případ postaví" (`:342-343`); `kgNeighbours` (`lib/db/pglite/repositories/kg.ts:48`, indexed, with a truncation warning at `:81-88`) is the primitive, and `NodeInspector` already has an `onExpand` slot (`NodeInspector.tsx:723`) that `VariantMapa` does not wire (grep `onExpand` in VariantMapa = 0).

Built: a fourth citable view kind `okoli` (`{kind:"okoli", node, rels?, depth:1}`) resolved by `getNeighbourhood(id)` over `kgNeighbours` with its cap and population (`shown/total` per rel), laid out deterministically around the anchor (the hash-ring the contract halo already uses at `:385-394`) and merged into the stage as an overlay layer with its own positions; a path whose nodes are off-map pulls their neighbourhood positions the same way, so a lens never lights a node it cannot place, and `StageOverlays` prints "N kroků mimo mapu, dokresleno na vyžádání". The permalink, JSON-LD (`hasPart` = the neighbourhood's edges with gate tokens), OG and `/overeni` treat `okoli` exactly like `trasa` (fingerprinted, three states). `/penize/firma/[ico]` and the tender surface link into `/graf?okoli=<id>` and back through `caseFileLink`.

#### Flow
- `graphLoader.ts`: `getNeighbourhood(id, {rels, limit})` → `{anchor, nodes, edges, omitted:{perRel: shown/total}}` via `kgNeighbours`, ordered by `byListOrder` (`lib/db/kgOrder.ts`) so the cut is deterministic; never memoised (per-node, on demand).
- `graphActions.ts`: `neighbourhoodAction(id)` with input validation like `pathAction`; `permalink.ts` `parseViewState` admits `okoli`; `resolveView` resolves it (gone when the anchor vanished); `hashViewContent` over `{anchor, edges}`.
- `VariantMapa`/`GraphStage`: an `overlay` positions map layered over `positions` (`toWorld/toScreen` from `viewTransform.ts`, no second geometry); off-map path nodes get positions by fetching their anchor's neighbourhood; `StageOverlays` shows the off-map count; `fitBounds` includes overlay nodes.
- `NodeInspector.onExpand` wired in `VariantMapa`; expand = load neighbourhood, add to overlay; `onRemove` drops it; forensic mode applies `forensicEdges` to the overlay with its counts.
- `PermalinkPage`: `OkoliExhibit` (anchor + per-rel ledger with cap/population line); `permalinkCardModel.review` covers it; OG image prints "N z M vazeb".
- Cross-links: `CompanyCaseFilePage` and the tender case surface get a "zobrazit v grafu" link; `NodeInspector` gets `caseFileLink` targets.
- `docs/routes/graf.md` records the new view kind and its cap doctrine.

#### Expected impact
Readers can finally see the Case ④ layer (61 k nodes) in context instead of only as search hits, and a computed path is drawn in full or says how much it could not draw. Measured by: number of distinct nodes reachable on the canvas (today `visible.length`, ~3 200 + contract ring; after: any indexed node via expansion), and a fixture path through a tender that today yields ledger 3 hops / stage 1 edge (after: 3/3 or an explicit off-map count). Could break: neighbourhood fan-out on hubs (a zadavatel with thousands of `procures` edges) — the cap and its population must ship on every response, and the overlay must never grow past a printed node budget.

#### Evaluation
Claim: user - the requested answer renders completely on the stage, and the procurement layer becomes explorable and citable
Before: `omitted.tendersTotal`/`procurementCompaniesTotal` acknowledged but unreachable on the canvas; `GraphStage.tsx:258-260` skips lens edges with missing positions with no count; `grep onExpand VariantMapa.tsx` = 0; `kgNeighbours` unused by `features/graph`
After: the MISSING INSTRUMENT is a path fixture whose nodes are absent from `MapData` — assert the stage's drawn lens-edge count equals `activePath.edges.length` or an off-map count is rendered
Method: probe - traced index → adjacency → lens → stage; cap reasoning on `omitted`
Result: unmeasurable
Gate: contract - a new permalink view kind is an append-only address promise; the JSON-LD `hasPart` shape for `okoli`

#### Evidence
- `features/graph/graphLoader.ts:180-189` (index admits every `KgNodeKind`), `:339-344, 370-374, 405-412` (tenders/procurement companies omitted from map), `:694-706` (adjacency over ALL indexed nodes), `:287-288, 342-343` (recorded plan)
- `features/graph/kindStyle.ts:38, 87-95` — `tender` is a drawable kind
- `features/graph/VariantMapa.tsx:162-175, 189-202` — lens from path ids, positions from map nodes only; `onExpand` never passed
- `features/graph/components/GraphStage.tsx:221-223, 258-260, 282-284` — `if (!a || !b) continue` (silent)
- `features/graph/components/NodeInspector.tsx:723` — `onExpand?` slot
- `lib/db/pglite/repositories/kg.ts:48, 81-88` — `kgNeighbours` indexed read + truncation warning; `lib/db/kgOrder.ts:1-30` — the deterministic order it needs
- `features/graph/graphTypes.ts:76-88` — `MapData.omitted` already ships the population

---

### #42 One Store contract, two drivers: a hosted-Postgres implementation proven by a shared conformance suite

`data-layer#3` · theme S · corroboration 1 · size XL · effort 9 / impact 8 / risk 8 · result `better` · gate `irreversible`
contexts: db-store, db-repositories, sentinel (lib/testing/sentinel), graph-writers (scripts/data-analysis), deploy (docs/deploy), data-releases
advances: repo convention: `lib/db/config.ts` "the move to a hosted Postgres is a new file, not a rewrite"; roadmap Next-1 "Postgres per politicas.md §5" made concrete; laws: one-definition-one-import (the store contract), incident-anchored-doctrine ("never ran must not render as passed")

#### Summary
The escape hatch to hosted Postgres is documented as "a second driver file", but the contract a second driver would have to satisfy is not one interface: three entry points, one repo declared outside the facade, scripts opening PGlite directly, and a correctness guarantee (review lost-update) that exists only because PGlite serializes transactions.

#### Description
`lib/db/config.ts:14` — `DbDriver = "pglite"`; `:10` and `pglite-store.ts:12-13` promise a second driver is "a new file". What the code actually requires of that file: `Store` (`store.ts:253-262`) plus `getLedgerRepo()` (`ledger.ts:222`) and `getChangesRepo()` (`changes.ts:240`, header: "NOT folded into the Store facade"), plus `asOf` declared only on the PGlite repo (`kg.ts:53-66`), plus scripts that bypass `getStore()` and call `open()` directly (`scripts/data-analysis/kg-repair-orphans.ts:22` "Opens PGlite DIRECTLY"). And `review.ts:50-55,89` closes the lost-update race on the audit chain and the tie flip by relying on "PGlite serializes transactions against every other query" — on a real Postgres with concurrent connections that guarantee vanishes unless the chain head is read `for update` / the transaction is `serializable`. The consequences of one-driver-forever are recorded, not hypothetical: single instance, `strategy = "immediate"`, "never `fly scale count 2`" (`docs/deploy/container.md:12-19`); every analysis script runs on a copy of the store (`config.ts:24-29`); the PG17 volume cannot take PGlite 0.5 (`memory/pglite-05-ships-postgres-18.md`) so the engine is frozen; and the sentinel "goes RED with an unevaluable report" in CI because a hosted runner has no `.pglite` (`scripts/sentinel/run.ts:36-42`) — the eleven live invariants execute only on a developer's laptop.

Built: (1) the contract made one thing — `asOf`, `LedgerRepository`, `ChangeEventRepository` folded into `Store`; (2) a driver-agnostic conformance suite (`lib/db/store.contract.test.ts`) that runs every repository test through `Store` only, executed against the PGlite template store AND against a Postgres service container in CI; (3) `lib/db/pg-store.ts` over `pg`/`postgres.js` with the review path rewritten to `select … for update` on the chain head and edge, so serialization is a property of the SQL, not of the engine; (4) `scripts/**` forbidden from `open()` by a lint rule (extend `no-server-import-in-client`'s family); (5) CI runs `npm run sentinel` against a seeded Postgres — the invariants finally evaluate on every push; (6) the deploy doc's route (b) becomes real: multi-instance reads, the writer (`/penize/kontrola`) still single. The PGlite driver stays the default for local and for the sealed release artifacts.

#### Flow
- Fold the side repositories and `asOf` into `Store`; delete `getLedgerRepo`/`getChangesRepo` call sites in favour of `getStore()`.
- Write the conformance suite from the existing repository tests (`kg.test.ts`, `review.test.ts`, `ledger.test.ts`, `changes.test.ts`, `weights.test.ts`) — `Store`-only, driver injected.
- Make `setTieReviewState` and the chain-head read concurrency-safe in SQL (`for update`), prove it with a two-connection test on Postgres and an unchanged single-connection test on PGlite.
- Implement `pg-store.ts`; `dbDriver()` gains `"postgres"` + `DATABASE_URL`; `disclosePendingDdl`/`db:migrate` run the same `CORE_DDL` (already plain Postgres) with the same snapshot ceremony.
- `.github/workflows/ci.yml`: Postgres service, load `benchmark-data/*.csv`-style fixtures or a `pg_dump` of a sealed release; run the conformance suite and the sentinel.
- `docs/deploy/container.md` route (b) rewritten from measured numbers; PGlite 0.5 migration becomes a dump/restore through the same driver.

#### Expected impact
Operators lose the single-machine and frozen-engine constraints; the repo gains invariants that actually run in CI (0 of 11 evaluated on hosted runners today); analysts run scripts against the live data without copying 2 GB. Measured by: conformance suite green on both drivers, sentinel exit 0 in CI, deploy doc's `count 2` prohibition lifted. Could break: subtle PGlite/Postgres behaviour drift (`jsonb` equality, `real` rounding in `weight`, `timestamptz` text forms) — the conformance suite is the only thing that makes the claim "same numbers on both engines" honest, and it must include byte-identity loader dumps (the `tsx-can-run-server-only-loaders` memo's method).

#### Evaluation
Claim: resilience - the product's correctness guarantees stop depending on one engine's incidental serialization and one laptop's data dir
Before: 1 driver; 3 entry points + direct `open()` in scripts; review race safety by engine property (review.ts:50-55); sentinel in CI = 0/11 invariants evaluated (run.ts:36-42); deploy = 1 machine forever (container.md:12-19)
After: 2 drivers behind one contract; conformance suite as the instrument (count of repository tests that run through `Store` only, target 100 %); sentinel evaluated in CI
Method: probe - read store.ts, config.ts, review.ts, ledger.ts, changes.ts, run.ts, container.md
Result: better
Gate: irreversible

#### Evidence
- `lib/db/config.ts:10-14` — one driver, "a new file" promise
- `lib/db/store.ts:253-262` — `Store` facade; `lib/db/pglite/repositories/changes.ts:4-6,240` and `ledger.ts:222` — repositories outside it
- `lib/db/pglite/repositories/kg.ts:53-66` — `asOf` outside the contract
- `lib/db/pglite/repositories/review.ts:50-55,89` — race safety attributed to PGlite's serialization
- `scripts/data-analysis/kg-repair-orphans.ts:22` — direct `open()`
- `scripts/sentinel/run.ts:36-42` — invariants unevaluable in CI
- `docs/deploy/container.md:12-31` — single instance, immediate strategy, PG17 volume pin
- `memory/pglite-05-ships-postgres-18.md` — the engine freeze

---

### #43 Registry-link contract sentinel: every built source link is probed against the entity it names

`shared-ui-primitives#3` · theme S · corroboration 1 · size L · effort 5 / impact 7 / risk 3 · result `better` · gate `policy`
contexts: civic-kg-primitives, testing-sentinel, data-quality (features/atlas), shared-provenance, graph-explorer, civic-chronicle
advances: _laws.md#provenance-or-nothing + #every-cap-ships-its-population + #incident-anchored-doctrine; claim-verification-and-provenance/derivation-comparison (the sentinel discipline: "never ran" must not read as "passed")

#### Summary
`lib/kg/sourceLinks.ts` is the sentence "every item is traceable to an official source" in code, and its truth rests on a comment ("Ověřeno 2026-07-26") plus one known-wrong pattern (bill links hardcode `o=10`; next term they will resolve to a LIVE page about a different bill). Nothing probes a built link, and no surface states the probe's population.

#### Description
`sourceLinksFor` (`lib/kg/sourceLinks.ts:220-333`) builds URLs from stored ids for 11 node kinds; the verification is a dated comment (`:24-26`) and the module header records a known silent failure (`:34-46`, `TERM_NUMBER = 10` at `:85`). Its consumers are the receipt (`receipt.ts` via `toEndpoint`), `/graf` `NodeInspector.tsx`, `PermalinkPage.tsx`, `deriveDenik.ts` — i.e. every citation surface. The sentinel (`lib/testing/sentinel/invariants.ts`, workflow `.github/workflows/sentinel.yml`, no cron because the store is not reachable from hosted CI) judges freshness, floors, audit chain and recompute — not links. `/atlas` scores sources by coverage/freshness/integrity/completeness, and explicitly declines to score `kg_*` because those tables have no `source` column (`docs/routes/atlas.md` "Name the gap"). So a reader who clicks "psp.cz" under a tisk has no way to know whether the pattern still resolves, and the product cannot say "N of M detail links were probed on <date>, K pointed at a page that names the entity".

"Built" = a deterministic sampler (hash-ordered like `layout.ts` `hashId`, so the sample is the same across runs) draws n per `(kind, registry, tier)` pattern from the store, fetches each `tier: "detail"` link and checks the **entity contract** (the page contains the entity's `citableId` — psp id, IČO, tisk number, `č. N/RRRR`), never only a 200; `search`-tier links are checked for reachability only, because they promise less. The report is the sentinel's own schema (`report.ts`, `SENTINEL_SCHEMA`) with `ok / violation / unevaluable` per pattern and the population line (`probed / drawn-from`), written to the store as an `ingest_run`-like `link_probe_run` so `/atlas` gains a fifth dimension "dohledatelnost" that is UNRATED when no run exists (never zero). The receipt and `/graf/p` print the last probe day per registry next to the link. The `o=10` bug becomes an alarm the day a PSP11 tisk enters the graph, instead of a comment.

#### Flow
- `lib/kg/linkContract.ts`: pure `entityContract(subject, html) → boolean` per kind (what string must appear), tested on fixtures.
- `scripts/sentinel/probe-links.ts`: deterministic sample per pattern, rate-limited fetch (429 discipline from memory: registr-smluv), emits the sentinel report; `unevaluable` when the network is absent — a failing exit, never a silent green.
- `link_probe_run` table (writer only in `lib/db/pglite/repositories/`), `db:migrate` path per CLAUDE.md.
- `/atlas` fifth dimension reading the latest run; unrated arm carries no score (existing discriminated-union discipline).
- `receipt.ts` endpoint links gain `lastProbed: string | null`; `ReceiptBody`, `NodeInspector`, `PermalinkPage` typeset it.
- Add the `term` prop ingest fix as the first violation the sentinel is expected to raise, then remove `TERM_NUMBER`.

#### Expected impact
Operators learn about a moved registry URL scheme from a red check, not from a reader's mail; readers see how recently the "look here" links were confirmed. Measured: patterns with a dated probe (0 of ~14 today), and the count of `detail` links failing the entity contract. What could break: hosts that block automated fetches (hlidacstatu.cz, or.justice.cz) yield `unevaluable`, which must be displayed as such and never as `ok`.

#### Evaluation
Claim: resilience — link correctness is a measured invariant, not a dated comment
Before: `sourceLinks.ts` verified by comment dated 2026-07-26; 0 automated probes; 1 known silent-wrong pattern (bill `o=10`); `/atlas` has no traceability dimension
After: per-pattern probe report with population; the MISSING INSTRUMENT is exactly this probe run — until it exists, the dimension is unrated
Method: probe — read `sourceLinks.ts` header/patterns, sentinel invariants + workflow (no cron, no link check), atlas dimensions, consumer list
Result: better
Gate: policy — outbound fetches against public registries need the rate-limit rule stated per host

#### Evidence
- `lib/kg/sourceLinks.ts:24-26` "Ověřeno 2026-07-26: …"; `:34-46` ZNÁMÁ MEZ — `o=10` returns a live page about a different bill after the next term; `:85` `const TERM_NUMBER = 10;`
- `lib/kg/sourceLinks.ts:252,259` ARES / OR are `tier: "search"` (filters), `:262` Hlídač subjekt is `detail`
- consumers: `features/shared/provenance/receipt.ts`, `features/graph/{NodeInspector,PermalinkPage,graphLoader,permalink}.ts(x)`, `features/denik/deriveDenik.ts`
- `lib/testing/sentinel/invariants.ts:1-20` ground truths: manifest, floors, freshness, audit chain — no link check; `.github/workflows/sentinel.yml:28` "There is no cron"
- `lib/analysis/atlas.ts:34` `AtlasDimension = "coverage" | "freshness" | "integrity" | "completeness"`
- `memory/kg-has-no-source-urls.md` — "ask the node's props first, rebuild from the stored id second, guess never" (the contract the probe enforces)

---

### #44 Registry sector codes (CZ-NACE) on every company node, one sector definition

`data-ingestion#2` · theme S · corroboration 1 · size L · effort 5 / impact 7 / risk 3 · result `unmeasurable` · gate `contract`
contexts: ingest-external-sources, law-collision-analysis, money-analysis-triage, kg-pipeline
advances: conflict-of-interest-detection / statute-relevance-mapping; law: one-definition-one-import (the sector rule exists as a name heuristic + 18 hand overrides today); disclose-never-repair (no NACE → `null`, never a guessed sector)

#### Summary
The LawWatch collision signal and the money loop's public-mandate reasoning both depend on "what sector is this company in", and the graph answers it with a legal-name regex plus a hand-maintained override table — while ARES returns the company's CZ-NACE codes on the very endpoint the ingest already calls and `parseAresCompany` throws them away.

#### Description
`scripts/case-loops/law/company-sectors.ts:6-7` states it plainly: "The graph has NO NACE/sector code on company nodes … so this module supplies a bounded, reviewable, NAME-BASED heuristic". It carries `MUNICIPAL_SOE_PATTERNS` (a regex list) and `SECTOR_OVERRIDES` (18 hand entries "sourced from well-known public facts"), and its own comments record the heuristic being wrong on the facts and corrected by reading ARES NACE BY HAND: `:81` ("ARES gives NACE 38 and 38210"), `:126` ("was simply wrong on the facts (ARES: NACE 38/38210 …)"), `:133` ("batch-012 ARES sweep: NOT health/biotech despite the name"). `triage-core.ts:26-37` keys the law side on the same `Sector` buckets via `THEME_KEYWORDS`; the frontier still holds Q-law-1 open ("tie NACE vs law domain", `docs/data-analysis/frontier.md:75`). On the ingest side `lib/analysis/money-feed.ts:215-219` `parseAresCompany` keeps exactly `{ico, name}` from `/ekonomicke-subjekty/{ico}` — the response also carries `czNace[]` and `pravniForma`, which the money loop re-fetches separately for `public-body.ts`. `kg-money-ingest.ts:158` is where every tied company passes through that parser. Company props in the registry (`prop-registry.json` → `nodes.company`) have `legalForm`, `public_mandate_*`, `tender_authority_category` — 43 keys and no activity code. "Built" is: the ARES adapter emits `nace_codes: string[]` (register facts verbatim, `null` when ARES has none) and `nace_provenance`; a single pure `lib/analysis/sector.ts` maps NACE section/division → the existing `Sector` union with the mapping table published on `/metodika`; `company-sectors.ts` becomes a thin importer whose override table shrinks to the documented exceptions the register cannot express (state-owned by statute), each with its incident; the tender loop's `tender_authority_category` and the money loop's `public_mandate` reasoning consume the same module; `/penize` company case files and `/zakony` collision rows render the NACE code with its ARES citation.

#### Flow
- Extend `parseAresCompany` (and the `Company` type) with `naceCodes: string[] | null` and `legalFormCode`; add fixture tests from a real ARES payload.
- Register `nace_codes`, `nace_provenance` in `prop-registry.json` + `graph-schema.md`; one merge-preserving sweep script `scripts/case-loops/money/nace-sweep.ts` over all `company` nodes (350 tied + tender authorities/winners), throttled like `reconcile-ares-vr.ts`, dry-run default, payload through `persist-batch.ts`.
- Write `lib/analysis/sector.ts` (NACE → `Sector`, tested; unknown → `null`), import it from `company-sectors.ts`, `triage-core.ts`, and the tender arena/authority code; delete the duplicated regex list where the register answers.
- Re-run `collision-check.ts` on a copy and record the discriminative-power change (fire rate before/after) in the law ledger — the kernel's "validate a signal before trusting it".
- Surface the code on `/penize/firma/[ico]` and in `/zakony` collision rows with a `SourceNote` citing the ARES record + fetch date; publish the NACE→sector table on `/metodika`.

#### Expected impact
The collision and public-mandate signals stop depending on a legal-name string, and every "sector-adjacent" claim on `/zakony` and `/penize` cites a register code rather than an editor's table; the 18-entry override list — the kind of restated literal the laws call a future lie — shrinks to statutory exceptions. Measured by: share of company nodes with `nace_codes` (expect > 95 % of ARES-resolvable companies) and the collision signal's fire rate on the 141-bill queue before/after. What could break: ARES lists several NACE codes per company (primary first) — a mapping that reads all of them as equal will over-fire; take the primary and disclose the count.

#### Evaluation
Claim: quality - one register-sourced sector definition replaces two hand heuristics
Before: 0 company nodes carry a NACE prop (`prop-registry.json` nodes.company has no such key); `parseAresCompany` keeps 2 fields; `SECTOR_OVERRIDES` = 18 hand entries; 3 in-file comments document hand ARES lookups correcting the heuristic
After: N/350 tied companies + tender authorities with `nace_codes`; `SECTOR_OVERRIDES` reduced to statutory exceptions; collision fire-rate measured on the same 141 bills — the instrument is the fire-rate table `measure-signal-009.ts` already produces
Method: probe - read of the parser, the heuristic module and the prop registry
Result: unmeasurable
Gate: contract

#### Evidence
- `scripts/case-loops/law/company-sectors.ts:6-7` — "The graph has NO NACE/sector code on company nodes"; `:18-24` `SECTOR_OVERRIDES` "small explicit map … sourced from well-known public facts"; `:81`, `:126`, `:133` — hand ARES NACE corrections
- `lib/analysis/money-feed.ts:215-219`:
  ```ts
  export function parseAresCompany(raw: unknown): Company | null {
    const s = (raw ?? {}) as AresSubject;
    if (!s.ico || !s.obchodniJmeno) return null;
    return { ico: s.ico, name: s.obchodniJmeno };
  }
  ```
- `scripts/data-analysis/kg-money-ingest.ts:158` `c = parseAresCompany(await ares.subject(ico));`
- `scripts/case-loops/law/triage-core.ts:26-37` `THEME_KEYWORDS: Record<Sector, string[]>` — the law-side buckets
- `docs/data-analysis/frontier.md:75` — Q-law-1 "conflict-by-sector-adjacency (tie NACE vs law domain)" status open
- `lib/kg/prop-registry.json` → `nodes.company` (43 keys, `legalForm`, `public_mandate_*`, `tender_authority_category`; no activity code)

---

### #45 Reader-seeded slices — the velín as a lens over any neutral population

`landing-navigation#1` · theme S · corroboration 1 · size L · effort 6 / impact 7 / risk 4 · result `unmeasurable` · gate `contract`
contexts: dashboard-state-graph, dashboard-instruments, landing-page (cross: volby, overeni)
advances: claim-verification-and-provenance / claim-ref-addressing ("content-hash permalink family: address = view config + hash; format migrations decode both shapes"); law: non-partisan-symmetry (the reader picks the population by a registry key, the ordering rule inside stays metric-free and printed)

#### Summary
The state graph draws exactly ONE slice — the first three MPs by ascending pspId that carry both a tie and an amending bill — and its exhibit address `rez.<hash>` encodes no view configuration because "the slice is by definition only one". `/volby` now knows a reader's kraj and obec, but the velín cannot be pointed at "the MPs of my kraj", and the address scheme could not carry that even if it could.

#### Description
`stateSlice.ts:170` `SLICE_SEEDS = 3`; `:192-200` builds `dual` sorted by `pspId` and takes `slice(0, SLICE_SEEDS)`; the rule is printed under the picture (`StateSliceRule`, `:124-133`). `exhibit.ts:111-112` documents the address `rez.<otisk8>` — "druh + otisk stačí: výřez je z definice jen jeden, plyne z pravidla". `refDetect.ts:132` matches only that shape. `getKrajData.ts:33-35` already filters current MPs by `region` for a kraj; the leaderboard carries `region` per entry (`getLeaderboardData.ts:207`). The seed rule's neutrality argument (`stateSlice.ts:12-18`: never order by a metric) is about ORDER, not about POPULATION — a reader choosing the population by kraj, club or obec is a registry key, not an accusation, provided the same rule then applies inside it and the population is printed.

"Built": `/dashboard?vyrez=kraj:<slug>` (and `klub:<abbrev>`, later `obec:<ico>` via the `/volby` crosswalk) seeds the slice from that population with the unchanged pspId-asc rule; the printed rule sentence names the population and its size; `/volby/kraj/[slug]` and `/kraj` link "otevřít ve velínu"; the exhibit address becomes `rez.<config>.<hash>` where `<config>` is the base64url view key (`cely` for today's default), and the decoder keeps accepting bare `rez.<hash>` as the default view forever (append-only address space). The recorded follow-up in `exhibit.ts:63-70` (evict `x`/`y`/`partyCode` from the hash) rides the same migration window, since a new shape with a transition sentence on the page is exactly the door that follow-up asks for.

#### Flow
- `stateSlice.ts`: `SliceInput` gains `population?: {kind: "kraj"|"klub"|"obec", key, pspIds: Set<number>, label, size}`; `buildStateSlice` filters `tiedMps` by it BEFORE the existing pspId sort; `StateSliceRule` gains `population` (printed); tests pin that the default population yields the byte-identical graph as today.
- `exhibit.ts`: `ExhibitParams` `rez` gains `view: string` (`"cely"` default); `encodeExhibitId` writes `rez.<view>.<hash>` for non-default views and keeps `rez.<hash>` for the default; `decodeExhibitId` accepts both; `hashSlice` unchanged in the first commit (the layout eviction is its own commit with both hashes shown during the window).
- `getDashboardData(options.view)` resolves the population from the leaderboard payload (kraj/club need no new read; obec needs the `/volby` crosswalk loader) — a view whose population is empty returns a labelled "no MP in this population carries both bands" rather than the default slice.
- `app/dashboard/page.tsx` reads `searchParams.vyrez`; `useGraphSelection` keeps `?uzel=` alongside; `refDetect.ts` regex + `guide.ts` example widen to the new shape; `/overeni` verdicts unchanged in vocabulary.
- Entry links: `features/volby/KrajPage.tsx` + `/kraj` card → `/dashboard?vyrez=kraj:<slug>`; `VolbySection` on the landing gets a third door only if the reader's kraj is known client-side (localStorage, the `/schranka` pattern) — otherwise nothing.
- Docs: `docs/routes/dashboard.md` records the population rule and the address migration; `entityLinks.ts` is untouched (node ids stay real).

#### Expected impact
Election-season readers get the one thing the velín could not do — show the money/legislation picture of THEIR representatives — with the same neutral rule and a citable address per view. Measured by: distinct `rez.` addresses issued (1 today → 14 kraje + clubs); every issued pre-migration address still decodes (test). What could break: a population small enough that the slice identifies one MP's ties as "the kraj's picture" — the printed population size and the existing pending_review labels are the mitigation, and a population of size < 3 seeds should print that it is partial.

#### Evaluation
Claim: user — the velín becomes addressable by the reader's own constituency without changing the seed rule
Before: 1 possible slice; `decodeExhibitId` accepts 1 shape (`exhibit.ts:129-140`); `/volby/kraj` lists MPs but links no graph
After: 1 + 14 + N club views, all sharing `buildStateSlice`; decoder accepts 2 shapes; migration window verified by a test that every `rez.<hash>` fixture in `exhibit.test.ts` still decodes
Method: probe — read the seed rule, the address codec, the kraj loader's MP filter
Result: unmeasurable
Gate: contract — the exhibit address format is a published promise; the widening must be append-only and documented on the surface

#### Evidence
- `features/dashboard/stateSlice.ts:170` `export const SLICE_SEEDS = 3;`
- `features/dashboard/stateSlice.ts:192-200` `dual … .sort((a, b) => a.pspId - b.pspId); … const primary = dual.slice(0, SLICE_SEEDS);`
- `features/dashboard/exhibit.ts:111-112` `rez.<otisk8> — výřez grafu (druh + otisk stačí: výřez je z definice jen jeden)`
- `features/dashboard/exhibit.ts:63-70` recorded follow-up: layout fields inside the hash, needs a transition window
- `features/dashboard/exhibit.ts:129-140` `decodeExhibitId` — two shapes only (`rez`, `fakt`)
- `features/overeni/refDetect.ts:132` `/\/dashboard\/exponat\/([A-Za-z0-9._~%-]+)/`
- `features/volby/getKrajData.ts:33-35` `.filter((m) => m.current && m.region === region …)`
- `features/civicscore/getLeaderboardData.ts:207` `region: string | null;`

---

### #46 Legislative substance ledger — first-signature, fate and interpellation kind beside the count

`mp-profiles-rankings#4` · theme S · corroboration 1 · size L · effort 6 / impact 6 / risk 3 · result `better` · gate `contract`
contexts: contribution-scoring, mp-profile, civicscore-lens-duel, civicscore-leaderboard, psp-ingest
advances: politician-performance-scoring — golden path "narrow the claim": raw counts reward the gameable (mass-filed questions, co-signatures); laws missing-is-not-zero, every-cap-ships-its-population, deterministic-code-owns-numbers; NOT a formula change (annotate, never adjust)

#### Summary
The legislative component is `(bills + interpellations) / 4` where a co-signature and a first signature, a bill that became law and one that died, a written and an oral interpellation all count one; the graph already carries the distinctions (first/co-signed since pass 34, bill `stav`/`fate_sb`, written vs oral computed at ingest) and the product renders none of them where the score is read.

#### Description
`lib/analysis/contribution.ts:259` scores `legislative = clamp01((billsAuthored + interpellations) / LEGISLATIVE_SATURATION) × 20` — the exact gameable count the golden path names. The node carries `bills_first_signed` / `bills_co_signed` (`features/profile/getProfileData.ts:835-836`, „sums to bills_authored") and no consumer in `lib/analysis/` reads them (`grep bills_first_signed lib/analysis/*.ts` → 0). Bill nodes carry `stav` and `fate_sb` (`getProfileData.ts:730-731`) rendered per row in the dossier (`DossierSection.tsx:288-296`) but never aggregated into „N of this MP's M sponsored bills became law". Interpellations: the ingest computes written and oral separately (`lib/ingest/sources/psp-activity.ts:44-49, 111`) and `kg-contribution-ingest.ts:195` writes the SUM; the profile discloses „composition unavailable" (`docs/routes/poslanec.md:784-792`). The Souboj compares `amendmentsAuthored`, `interpellations`, `rapporteurLoad` (`duelFacts.ts:42-47`) without any of these splits; the legibility panel prints the legislative value as one count (`score-legibility.ts:86-90`).

"Built" = a `legislative substance` fact set — deterministic, per MP, never entering the score: `{billsFirstSigned, billsCoSigned, billsEnacted, billsRejected, billsPending, interpellationsWritten, interpellationsOral, amendmentsAdopted?}` — stored as node props by the ingest (split, not summed), exposed as `DuelFacts` rows with real chamber medians and `chamberN`, rendered in the legibility panel under the legislative row as the count's composition, and on /zebricek as an optional „složení legislativy" column in the expanded row. /metodika gets one honest paragraph: the score counts instruments; the ledger shows what the instruments were; the weights are unchanged and this is why. The lens (`participatory-reweighting`) is explicitly NOT extended to these facts — the technique forbids widening the parameter grammar past weights.

#### Flow
- Ingest: `kg-contribution-ingest.ts` writes `interpellations_written` + `interpellations_oral` beside the sum (prop-registry extension; the score input stays the sum so `CONTRIBUTION_FORMULA_REF` does not change — pin with a test that `computeContribution` output is byte-identical).
- A pure `lib/analysis/legislative-substance.ts` aggregating sponsored-bill fates from the `sponsors` edges + bill node `stav`/`fate_sb` (vocabulary of psp.cz `typ_stavu` names — disclosed, never remapped); tested.
- Loader: chamber pass attaches the split facts to `DuelFacts`; profile loader aggregates fates from the neighbour read it already performs (no new store read).
- Render: legibility panel composition rows (`ScoreLegibilityPanel`), Souboj rows (`DUEL_FACT_DEFS` extended, medians with `chamberN`), /zebricek expanded-row column, /metodika paragraph.
- Symmetry check in the same change: the split is rendered for all 207 or none; a missing split renders „údaj chybí", never 0.
- Docs: `zebricek.md`, `poslanec.md`, `metodika.md`, `coverage-ledger.md`.

#### Expected impact
A reader can tell a first-signing legislator whose bills passed from a serial co-signer at the same 20 points — without the product editorialising the formula. Measured by: substance facts rendered per MP (0 → 6–8), Souboj rows (4 → 7), interpellation split on the wire (0 → 207). Could break: the ingest prop write must be merge-preserving (memory: `kg-upsert-replaces-props`); the fate vocabulary must be the registry's own words or it becomes a repaired value.

#### Evaluation
Claim: quality — the narrowed claim („instruments counted") is made legible beside the count it summarises
Before: `bills_first_signed` consumers in `lib/analysis` = 0; interpellation split props on nodes = 0/207 (poslanec.md:788-789); Souboj compares 4 facts; enacted-bill count per MP rendered nowhere
After: split props on 207/207; 7 Souboj facts with medians + `chamberN`; the legibility legislative row carries its composition; `computeContribution` output unchanged (test-pinned)
Method: probe — read the formula term, the node prop reads, the duel defs and the dossier's per-row fate render
Result: better
Gate: contract — ingest writes new props (prop-registry), no formula ref change; reader-facing copy on three surfaces

#### Evidence
- `lib/analysis/contribution.ts:259` — `legislative = clamp01((billsAuthored + interpellations) / LEGISLATIVE_SATURATION) * weight`
- `features/profile/getProfileData.ts:835-836` — `bills_first_signed` / `bills_co_signed` read for the profile only; `:730-731` `stav` / `fate_sb` per bill
- `features/profile/components/DossierSection.tsx:288-296` — fate rendered per row, never aggregated
- `docs/routes/poslanec.md:784-792` — written vs oral computed at ingest, summed at `kg-contribution-ingest.ts:195`, „207/207 carry the sum and no split prop exists"
- `features/civicscore/duelFacts.ts:42-47` — four compared facts, no composition
- `lib/analysis/score-legibility.ts:86-90` — legislative value = bills + interp as one number

---

### #47 Statute collisions computed for the whole corpus, close-reads as the overlay

`voting-legislation#4` · theme S · corroboration 1 · size L · effort 7 / impact 6 / risk 4 · result `better` · gate `contract`
contexts: lawwatch, voting-legislation-routes, civic-chronicle, kg-analysis
advances: legislative-change-tracking (amendment-instruction-grammar, statute-collision-clustering); law: lead-not-finding, every-cap-ships-its-population, one-definition-one-import

#### Summary
/zakony/kolize shows the pairs an LLM army close-read, drawn from a candidate population the surface never states; the deterministic §-instruction partitioner that defines that population lives in a case-loop script and never reaches the graph or the page.

#### Description
`features/lawwatch/getCollisionData.ts:3-12` describes the pipeline: a deterministic §-overlap pre-check in `scripts/case-loops/law/collision-check.ts` (batch-004 `--v2`, `partitionParagraphsByStatute` at `collision-check.ts:327`, scoping each bill's §-set to the statute its own novelization instructions target), followed by an LLM close-read of surviving pairs; 63 pairs were close-read across batches 001–008 and the loader renders the non-incidental ones from disk payloads (`collision-reasoning-cz.json`, classification vocabulary drift already bit once, `:19-24`). The graph carries none of it: `lib/kg/prop-registry.json:251-256` lists the `amends` edge props as `amends_precision_note`, `amends_regen_005`, `ref`, `source` — no §, no instruction verb, no article — and `docs/routes/zakony.md` states „the graph carries no paragraph diffs". So the published collision set is an editorial sample by construction: which pairs were close-read depends on which batches ran, and the page cannot print „N of M candidate pairs" because M exists only in a script's stdout. The frontier keeps the deterministic half open as Q-law-4 (`frontier.md:79`, sibling-print §-collision pre-check).

Built: the instruction census becomes graph data — `amends` edges (581 after pass 30 per graph-log) gain `instructions: [{article, paragraph, verb: replace|insert|repeal|renumber, span}]` written whole-corpus by `amends-census.ts` with pass provenance under the prop-registry contract; a pure `deriveCollisions(bills)` in `features/lawwatch/` computes every same-(statute, §) pair with ≥2 pending instructions at read time, groups N-way clusters, and counts the full population. Close-read payloads become the gated overlay on those clusters (`classification` + Czech reasoning where it exists; `pending_review`), never the population. The radar (`deriveRadar.ts`) dates deterministic entries by the ingest pass and overlay entries by `generatedAt`, as it already does for flags. The /zakony/predpis critical edition gains the instruction trail per § from the same props, replacing the disk-only `paragraphDiffs` (`getLawData.ts:233`).

#### Flow
- Register `amends.instructions` (+ `instructions_provenance`) in prop-registry + graph-schema.md; extend `amends-census.ts` to emit the grammar output per edge; validate on the 6 audit-confirmed false edges of batch 006 (must emit no instruction).
- Writer through `persist-batch.ts` (merge-preserving); `npm run da:props-check` green.
- `deriveCollisions.ts` pure + fixtures (omnibus partition, incidental mention excluded, N-way cluster, undercount stated); population counters (`candidatePairs`, `clusters`, `closeRead`, `withheld`).
- `getCollisionData.ts` composes deterministic clusters + overlay; `/zakony/kolize` prints „close-read K of M candidate pairs"; feeds unchanged in shape, entries gain the population line.
- Statute dossier: instruction trail per § from edge props; `docs/routes/zakony.md` entry.

#### Expected impact
Every pending-bill overlap on every statute is on the page as a LEAD with its population, independent of which batch ran; the LLM reasoning is visibly an overlay. Measured by candidate pairs (M) vs close-read (K) printed, and by the number of statutes with ≥1 deterministic cluster. What could break: the grammar's known undercount on `ČÁST`/`§`-organised omnibus bills (graph-log batch 006, N1) must be printed as a stated undercount, not silently treated as closure.

#### Evaluation
Claim: quality - the collision population is deterministic and stated; the model output is an overlay
Before: 63 pairs close-read (getCollisionData.ts:11), rendered as the whole; `amends` edges carry 0 §-level props (prop-registry.json:251-256); candidate population unprinted
After: M candidate pairs computed from graph props on every request, K close-read shown as a subset; instrument = the two counters on /zakony/kolize and `deriveCollisions` fixtures
Method: probe - read the loader header, the registry, the partitioner's location, and the frontier row
Result: better
Gate: contract

#### Evidence
- `features/lawwatch/getCollisionData.ts:3-12,19-24` — pipeline and vocabulary-drift note
- `scripts/case-loops/law/collision-check.ts:268-287,327` — partitioner lives in a script
- `lib/kg/prop-registry.json:251-256` — `amends` props: no § / instruction keys
- `features/lawwatch/getLawData.ts:233-241` — `paragraphDiffs` from disk artifacts; `amendedLawsFull` census not merged
- `docs/data-analysis/frontier.md:79` — Q-law-4 open; `docs/data-analysis/graph-log.md` pass 30 — 581 `amends`, citation universe „NOT claimed closed"

---

