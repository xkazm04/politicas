# Architect Backlog

Durable queue of architectural decisions. Sorted by `(reach × payoff) / (risk × effort)`.
Status values: `proposed | approved | in-progress | shipped | abandoned | blocked`.

## Pending

- **[2026-09-01] Moonshot deck — 47 L/XL cards in 9 corroborated themes, all pending a human decision** — type: portfolio, risk: n/a, effort: l–xl, payoff: see per-card scores, reach: 10 groups / 49 contexts
  Record: [[moonshot-2026-09-01]] (decision table + full §4.10 bodies) · Lens: ad-hoc `moonshot-architect` via scan-sweep · Status: triaged 2026-09-04 — 34 accepted, 3 folded, 10 rejected (per card in the record); nothing built yet
  Strongest spines by corroboration: as-of re-derivation from the bitemporal store (5 scouts), a claim address on every figure (5), one audited review door for every claim kind (5), the municipality as one graph subject (4).
- **[2026-07-26] One fallback-state contract (labelled mock / honest empty / DataUnavailable)** — type: weak-pattern, risk: 3, effort: l, payoff: 5, reach: 16 pages / 5 idioms / 6 mock-welded components
  ADR: [[decisions/2026-07-26-fallback-state-contract]] · Scan: [[scans/2026-07-26-data-loading-boundary]] · Status: proposed
  Notes: contains the highest-severity brand item — fabricated 2,1 mld Kč cited to the real contracts registry with no sample banner (`FollowTheMoneyPage.tsx:66-71`).
- **[2026-07-26] Ingest readiness guard + cardinality-floor test** — type: structural-bug-class, risk: 2, effort: m, payoff: 4, reach: 7 loaders / 0 ingest_run consumers / 0 cardinality tests
  ADR: [[decisions/2026-07-26-ingest-readiness-guard]] · Scan: [[scans/2026-07-26-data-loading-boundary]] · Status: in-progress (25a7b65 gate + floors + test shipped; /admin ingest-status surfacing remains)

- **[2026-09-01] Empty slices are scored, not withheld — `scoreSlice` gives a 0-row slice 1/5 on four criteria** — type: convention-gap (law: missing-is-not-zero), risk: 2, effort: m, payoff: 3, reach: every source×term×entity `slice-stats.ts` emits with 0 rows
  Found by: scan-sweep (analysis-quality, registry-conformance lens) · Site: `lib/analysis/quality.ts` `scoreSlice` (`fracScore(0) = 1`), emitted unconditionally by `scripts/data-analysis/slice-stats.ts` · Status: proposed (gate: contract — `SliceQualityRow.scores` would need a not-measured state, and `slice_quality` + DataHub consumers read the six numbers)
  Notes: the outbox finding cap was full when found, so this line is the record.

- **[2026-09-05] CLAUDE.md counts nine custom ESLint rules; the plugin, its self-test and its README count ten** — type: prose-rule-drift, risk: 1, effort: s, payoff: 3, reach: 2 lines in CLAUDE.md (138, 201) + "Next 16.2" vs package.json 16.3.1
  Found by: scan-sweep (app-config, parity-auditor) · Result: better (3 stale claims -> 0), gate: none · Status: proposed — vetoed in-round only because CLAUDE.md is outside app-config's paths
  +2026-09-06 (eslint-rules-shims sweep): AGENTS.md:21 „custom rules in eslint-rules/" and CLAUDE.md:84 „eslint-rules/ custom lint rules" still name the shim directory as the rules' home; the rules live in the package and the shims re-export them.
  +2026-09-08 (app-config round 50): `components/**` joined the three provenance rules' scope (dcdd453); CLAUDE.md's rule list still says `error` under `app/**` AND `features/**` only.
  Root cause is structural: `docs/feature-doc-map.json` couples NOTHING to `eslint.config.mjs`, so the commit-msg `doc-sync` hook could not owe a trailer when `a038aaa` added the tenth rule. Fix both: correct the count, and add an entry coupling `eslint.config.mjs` + `packages/eslint-plugin-civic-transparency/index.cjs` to CLAUDE.md and `eslint-rules/README.md`.
- **[2026-09-05] Widen `no-source-note-size-override` to static prop defaults — its declared blocker is gone** — type: gate-gap (renameable-detector-key: the override moves one hop and the matcher goes blind), risk: 2, effort: s–m, payoff: 4, reach: 404 `<SourceNote>` sites in 107 files; 0 hits today for `className = "… !text-[10px]"` defaults
  Found by: scan-sweep (app-config, parity-auditor) · Site: `packages/eslint-plugin-civic-transparency/rules/no-source-note-size-override.cjs:60-69` still says "fix that one default, then widen" — `BasisDisclosure.tsx` default is `mt-2` since 2026-08-24 · Result: better by probe (seed `className = "mt-2 !text-[10px]"` default -> 0 hits now, 1 after), gate: policy-tighten with a RuleTester case · Status: proposed — vetoed in-round (lint-rules context)
- **[2026-09-05] `/:path*` sends no `Permissions-Policy`** — type: hardening, risk: 1, effort: s (+ s instrument), payoff: 3, reach: every response (4 security headers today, measured via the config's resolved `headers()`: 2 rules)
  Found by: scan-sweep (app-config, security-auditor) · Result: better (4 -> 5 headers), gate: policy-tighten — auto-acceptable ONLY once a header contract test pins the set. Instrument: a vitest that imports `next.config.ts` (the export is double-wrapped: unwrap `.default` twice, then `await cfg.headers()`; probed 2026-09-05, resolves to 2 rules) and asserts the `/:path*` key set + `frame-ancestors *` on `/embed/:path*`. Not built in-round: it drags `@sentry/nextjs` + `next-intl/plugin` into the unit lane and its file must live under `lib/**`, outside app-config's paths · Status: proposed
- **[2026-09-05] The catalog boundary bans `lib/civic` and `features/*` only, while `features/shared` imports `lib/db` ×6, `lib/analysis` ×3, `lib/kg`, `lib/claims`** — type: stated-rule-vs-enforced-rule, risk: 3, effort: m, payoff: 4, reach: 11 import sites in 6 files (`provenance/getReceiptData.ts`, `provenance/receipt.ts`, `components/AnimatedScore.tsx`, 3 tests)
  Found by: scan-sweep (app-config, architecture-analyst) · `eslint.config.mjs:257-259` says the catalog "must not import domain data or feature code"; the `no-restricted-imports` groups enforce a narrower rule, and CLAUDE.md states the narrower one · Result: measurable (11 -> 0) but every fix relocates a module out of `features/shared` (a loader lives IN the catalog) — escalation: architecture · Status: proposed; decide whether the principle or the enforced rule is the contract
- **[2026-09-05] context-map `app-config` entry is stale** — type: map-drift, risk: 1, effort: s, payoff: 2, reach: 1 entry
  Found by: scan-sweep (app-config) · description says "4 custom rules" (10 registered), names tsconfig/lefthook/env template that `file_paths` does not carry (only `next.config.ts`, `postcss.config.mjs`, `eslint.config.mjs`), `entry_points` lists `tsconfig.json` outside its own `file_paths` · escalation: architecture (context-map edit) · Status: proposed

- **[2026-09-05] One request-base-URL helper instead of twelve copies — and one canonical origin instead of two** — type: duplication (pair rule: env `metadataBase` in `app/layout.tsx` vs `host` + `x-forwarded-proto` at 12 sites), risk: 2, effort: m, payoff: 4, reach: 12 sites (`app/robots.ts`, `app/sitemap.ts`, 4 feed routes, `/kraj/[kraj]`, `/plakat/[view]`, `/zdroj/[ref]`, `features/denik/feedRequest.ts`, `features/schranka/feedRequest.ts`, `features/graph/getPermalinkData.ts`) all defaulting proto to `http`; +1 found 2026-09-05: `app/zdroj/[ref]/page.tsx` `absoluteReceiptUrl` (13 sites; the two /dukazy copies were folded into `features/denik/feedRequest.ts` in 650a4d9, so 11 remain); +1 classifier 2026-09-06: `features/overeni/refDetect.ts` `politicasPath` hard-codes the accepted hosts (`politicas.cz`, `*.politicas.cz`, `localhost`) - the same origin, decided a 14th way
  +2026-09-08 (app-shell round 51): `app/robots.ts` and `app/sitemap.ts` now read `lib/routing/liveUrl` — two copies fewer; the env-vs-header precedence decision is untouched.
  +2026-09-08 (civic-feeds-verification round 54): `app/zdroj/[ref]/page.tsx` now reads `lib/routing/liveUrl` too — the header-derived copies left are the two feed `requestOrigin`s and `features/graph/getPermalinkData.ts`.
  Found by: scan-sweep (app-shell, parity-auditor) · Two mechanisms answer "what is this deployment's origin": `NEXT_PUBLIC_SITE_URL` for social cards only (`.env.example` says so on purpose), request headers everywhere else — so a proxy that drops `x-forwarded-proto` yields `http://` in the sitemap while og:image is `https://`, and a Host header is trusted verbatim in a crawler-facing file. Result: measurable (12 -> 1 derivation site; 2 -> 1 origins of truth) · escalation: architecture — a new `lib/` module consumed by six contexts, and the env-vs-header precedence is a deploy-contract decision (the record's "never a guessed domain" rule must survive it) · Status: proposed
- **[2026-09-05] context-map `app-shell` says Next.js 16.2; package.json is 16.3.1 — the exact delta that broke both error boundaries** — type: map-drift, risk: 1, effort: s, payoff: 3, reach: 1 entry (`tech_stack`), same drift as CLAUDE.md "Next 16.2"
  Found by: scan-sweep (app-shell, dependency-auditor) · The 16.2 -> 16.3 upgrade renamed `unstable_retry` to `retry` (fixed in 3731550); a version pinned in prose in two places and in code in one is how it went unnoticed · escalation: architecture (context-map edit) · Status: proposed

- **[2026-09-05] 144 supplier rows assert „0 Kč" where the register carries no amount — and they sit inside the peer medians** — type: law-violation (missing-is-not-zero, median-over-mean-for-peers), risk: 3, effort: m, payoff: 5, reach: 144 of 1 026 rows across 116 towns in `municipalSuppliers.generated.ts`; every `supplierPeerStats` / `peerSupplierTotals` median that includes one
  Found by: scan-sweep (budget-mirror, registry-conformance) · `asCzk` in `supplierTrail.ts` maps an absent `supplies` weight to 0 at DERIVATION time, so the batch cannot tell „contract for 0 Kč" from „amount not recorded"; the §04 volume column and bar print `0 Kč` for both, and the zero enters the peer medians as a real value, dragging them down · Fix: carry an `unpricedCount` (contracts with no weight) per row in the codec, print „částka neuvedena" when every contract of the row is unpriced, exclude such rows from the peer medians, and say how many were excluded — then regenerate the batch (`tools/generate-municipal-suppliers.ts`, needs the store; the pass moves from 41 to today's graph, so every §04 figure on the page changes with it) · Result: measurable (144 -> 0 false zeros; medians re-taken over priced rows only) · escalation: contract (batch format + codec + two median consumers, all in-tree and under test) AND a data refresh the owner should schedule, not a sweep · Status: proposed
- **[2026-09-05] `/rozpocty` ships 352 KB of generated source to every client** — type: performance, risk: 2, effort: m–l, payoff: 3, reach: every /rozpocty and /rozpocty/[ico] visit (`registryData.generated.ts` 243 762 B, `municipalSuppliers` 76 700 B, `budgetSnapshots` 31 601 B, all imported by a "use client" page)
  Found by: scan-sweep (budget-mirror, code-optimizer) · The registry is needed client-side for the search-first picker (a documented trade-off: the packed format already halves it), but the supplier batch and the snapshots are read once per town and could come down as props from the server page or as a lazy chunk · Before: 352 063 B source (compressed transfer not measured — instrument: `next build` route size or a served-HTML `<script src>` A/B as the 2026-08-13 shell pass did) · escalation: architecture (moves data from module import to server loader across the page/feature seam) · Status: proposed

- **[2026-09-05] Bill citations hard-code election term 10 — after the next term's ingest they resolve to a LIVE page about a different bill** — type: known-limit-without-owner (recorded in `lib/kg/sourceLinks.ts` header 2026-08-13, never queued), risk: 4 at the next ingest / 0 today, effort: m, payoff: 5, reach: every `bill` link on /graf, /zakony, /denik, /poslanec (`historie.sqw?o=10&t=<cislo>`, `sd.sqw?o=10&cd=<n>`)
  Found by: scan-sweep (civic-kg-primitives, risk-assessor) · Bill numbers restart at 1 each term, and the bill node carries no term prop, so `TERM_NUMBER = 10` is an assumption about the whole graph. Fix belongs to ingest: `scripts/data-analysis/kg-legislation-ingest.ts` writes `props.term` (number), `sourceLinksFor` reads it and emits NO link when absent (rule 1). Result: measurable — links minted from a stored term vs an assumed one (probe: a node without `term` → 0 links) · escalation: contract (a kg_node prop added by the ingest writer; consumers enumerable in-tree, but the graph must be re-ingested to carry it) · Status: proposed — becomes urgent the day a second term is ingested

- **[2026-09-05] context-map `civic-transparency-eslint-plugin` describes eight rules and lists 8 of 10 rule files** — type: map-drift, risk: 1, effort: s, payoff: 2, reach: 1 entry (`no-hardcoded-display-string`, `no-source-note-size-override` and their suites are unmapped, so a sweep of this context never reads them); +1 entry 2026-09-06: `eslint-rules-shims` lists 8 of 10 shims — the same two are unmapped there too
  Found by: scan-sweep (civic-transparency-eslint-plugin, documentation-auditor) · The package index, README, run-all and CLAUDE.md's own count disagree in three ways (eight / nine / ten); the plugin exports ten · escalation: architecture (context-map edit) · Status: proposed — fold into the CLAUDE.md nine-vs-ten card from round 1
  +2026-09-08 (round 56): `docs/rules/*.md` are unmapped too, in the context map AND in docs/feature-doc-map.json — a rule's doc page cannot be owed by the doc-sync hook; after this round `docs/rules/require-source-citation.md` still names five formatters where the rule triggers on seven

- **[2026-09-06] `getLedgerHeads` caps `sealedRuns` at 50 rows and carries no total** — type: contract, risk: 2, effort: s, payoff: 3, reach: 2 surfaces (`/data` sealed-runs list, `/data/manifest.json` `integrity.sealedRuns`)
  Found by: scan-sweep (claim-verifier, state-coverage) · `lib/db/pglite/repositories/ledger.ts` reads `order by merkle_sealed_at desc, id desc limit 50`; the page now counts what was returned (f520bbb) but neither surface can say how many sealed runs the store holds once there are more than 50, and the manifest's `sealedRuns` silently becomes a window. Fix: add `sealedRunsTotal` (a `count(*) where merkle_root is not null`) to `LedgerHeads` and print it on both surfaces · escalation: contract (`LedgerHeads` shape + `politicas.data-release/1` manifest field)

- **[2026-09-06] `computeTrend` prints a missing prior-term count as 0** — type: contract, risk: 2, effort: s, payoff: 3, reach: 1 module + 1 consumer (`lib/analysis/contribution-trend.ts` `counts.*.prior: num(...) ?? 0`; `features/civicscore/components/TrendPanel.tsx:133` renders `f.int(s.v.prior) → f.int(s.v.current)`)
  Found by: scan-sweep (contribution-scoring, copy-auditor) · The module's sibling `score-legibility.ts` states the rule ("MISSING IS NOT ZERO") and types every value `number | null`; the trend's five count pairs default an absent prior to 0, so a PSP9 prop without `speechTurns` renders „0 → 48" as a rise from zero. Fix: `CountTrend.prior: number | null` + a null branch in TrendPanel (two contexts) · escalation: contract

- **[2026-09-06] Two writers re-declare the tenure vocabulary as inline unions instead of importing `TenureClass`** — type: duplication, risk: 1, effort: s, payoff: 2, reach: 2 scripts (`scripts/case-loops/effort/tenure.ts:79`, `triage.ts:83`)
  Found by: scan-sweep (contribution-scoring, parity-auditor) · `lib/analysis/tenure-copy.ts` owns `TenureClass` + `isTenureClass`; the writer that stamps `effort_tenure_class` types it by hand, so a fifth class added there renders as graceful null everywhere with no compile error. Fix: import the type (data-ingestion context) · escalation: architecture (cross-context edit)
  SHIPPED 2026-09-06 (effort-case-loop sweep): `triage.ts`, `tenure.ts`, `extract-dossiers.ts` import `TenureClass`; `roles-triage.ts` never spelled the union.

- **[2026-09-06] `features/civicscore/provenance.ts` re-implements `storedFormulaRef`** — type: duplication, risk: 1, effort: s, payoff: 2, reach: 1 file (`provenance.ts:79-83` vs `lib/analysis/contribution.ts` `storedFormulaRef`)
  Found by: scan-sweep (contribution-scoring, parity-auditor) · Same semantics today (string, non-empty ⇒ ref, else null); the write guard and the read-side `formulaMatch` are the two halves of one contract and should read the ref through one function. Fix: import `storedFormulaRef` (civicscore-leaderboard context) · escalation: architecture (cross-context edit)

- **[2026-09-06] `readZipMap` silently keeps the last of two members with one basename** — type: contract, risk: 1, effort: s, payoff: 2, reach: 1 function (`packages/czech-civic-data/src/zip.ts` `readZipMap`, every ingest adapter reads through it)
  Found by: scan-sweep (czech-civic-data-parsing, error-handler) · The map is keyed by lower-cased basename, so `a/organy.unl` and `b/organy.unl` collapse to one entry and the earlier member is dropped with no error — against the module's "reject loudly rather than silently mis-read" rule. psp.cz dumps are flat today. Fix: throw a named error on a duplicate key (a behaviour change for any archive that relies on last-wins) · escalation: contract

- **[2026-09-06] `/dashboard/exponat/[id]` renders its unavailable state in Czech for every locale** — type: i18n, risk: 1, effort: s, payoff: 3, reach: 1 route (`app/dashboard/exponat/[id]/page.tsx:46` `what="Exponát" backLabel="zpět do velína"`)
  Found by: scan-sweep (dashboard-instruments, copy-auditor) · Every other route passes `DataUnavailable` translated strings (`app/volby`, `app/poslanec/[id]`, `app/kraj/[kraj]`, …); this one hands it literals, so an English reader of a citation page meets Czech. Fix: two `meta.*` keys + `getTranslations` (mp-rankings-routes context) · escalation: architecture (cross-context edit)

- **[2026-09-06] `FollowTheMoneyPage` prints a null per-company cap as 0, twice** — type: contract, risk: 1, effort: s, payoff: 2, reach: 1 file (`features/money/FollowTheMoneyPage.tsx:156,161` `perCompanyCap ?? 0`)
  Found by: scan-sweep (dashboard-instruments, state-coverage) · `MpCaseFilePage` guards the same sentence with `!== null` and the velín now does too (this round); the module's own front page still collapses null to „strop 0". Fix: the `!== null` guard or a cap-less variant sentence (money-ledger-graph context) · escalation: architecture (cross-context edit)

- **[2026-09-06] A bill assigned to the same committee twice yields two dated facts with one id** — type: contract, risk: 2, effort: s, payoff: 3, reach: 1 module + every exhibit address of an `assigned:` fact (`features/dashboard/datedFacts.ts:236` `id: assigned:${cislo}:${organLabel}`)
  Found by: scan-sweep (dashboard-state-graph, bounty-hunter) · The id omits role and date, so a re-assignment (return to the same committee) collides: duplicate React keys in the feed and `locateDatedFact` resolves the citation to the first row. Fact ids are deliberately stable (they ARE the exhibit address), so the fix is a contract change: measure first (count `(cislo, organLabel)` duplicates in `committeesByBill` on the live store), then suffix only the second and later duplicates in a deterministic order so every issued single-assignment address survives · escalation: contract

- **[2026-09-06] `scripts/db-bench/node-sqlite.d.ts` is a dead type shim with a stale reason** — type: tech-debt, risk: 1, effort: s, payoff: 1, reach: 1 file
  Found by: scan-sweep (db-hybrid-benchmarks, dependency-auditor) · `tsconfig.json` excludes `scripts/db-bench`, so tsc never reads the shim, and `@types/node@^24.13.3` ships `sqlite.d.ts`; the shim's comment still says „not yet in @types/node@20". Fix: delete the file and drop it from `context-map.json` (a map edit) · escalation: architecture (context-map edit)

- **[2026-09-06] `scripts/sentinel/inspect-findings.ts` is a one-off pass-11 inspection left in the live tree** — type: tech-debt, risk: 1, effort: s, payoff: 2, reach: 1 file (defaults to `.pglite-backup-pass11`, header dates it 2026-07-31)
  Found by: scan-sweep (db-hybrid-benchmarks, tech-debt-tracker) · The repo archives finished one-offs under `scripts/case-loops/effort/archive/` with `lib/testing/archivedScripts.test.ts` pinning them; this script has no npm entry, no test and a hard-coded July backup name. Fix: move to an archive folder + map update · escalation: architecture (context-map edit)

- **[2026-09-06] `.github/workflows/sentinel.yml` comment still says the unevaluable report has „sixteen checks"** — type: docs-drift, risk: 1, effort: s, payoff: 1, reach: 1 line (`sentinel.yml:20`; the list is 18 since 2026-09-04, fixed in the runner header this round)
  Found by: scan-sweep (db-hybrid-benchmarks, documentation-auditor) · Same stale count as the runner header; the workflow file is another context · escalation: architecture (cross-context edit)

- **[2026-09-06] `context-map.json` lists 16 files for `db-repositories` and omits five test files that live in the same directory** — type: map-drift, risk: 1, effort: s, payoff: 2, reach: 1 entry (`truncationGuards.test.ts`, `graph.test.ts`, `votes.test.ts`, `review-kinds.test.ts`, and the new `clubByMandate.test.ts`)
  Found by: scan-sweep (db-repositories, documentation-auditor) · The sweep's veto 1 scopes edits by `file_paths`; unlisted tests in the repository directory belong to no context, so every sweep of this directory has to decide their ownership ad hoc. Fix: add them to the entry · escalation: architecture (context-map edit)

- **[2026-09-06] The effort gate's deterministic-owned prop list stops at pass 11** — type: policy-tighten, risk: 2, effort: s, payoff: 3, reach: 1 regex (`scripts/case-loops/effort/gate.ts` `FORBIDDEN_PROP`)
  Found by: scan-sweep (effort-case-loop, risk-assessor) · The gate refuses a batch that proposes `bills_authored`, `speech_turns` and the other pass-11 numbers, but the pass-34/35 deterministic props the same loop now reads (`bills_first_signed`, `bills_co_signed`, `amendments_authored`, `effort_rapporteur_load`) are not in the list — an LLM payload could overwrite a counted number. Fix: extend the regex, or better derive it from the writers that own those props · escalation: policy-tighten (which props are deterministic-owned is the loop owner's call)

- **[2026-09-06] run-all's shim-equivalence block SKIPs on any `MODULE_NOT_FOUND`, so a missing shim for a new rule passes as "standalone checkout"** — type: policy-tighten, risk: 2, effort: s, payoff: 3, reach: 1 block (`packages/eslint-plugin-civic-transparency/__tests__/run-all.mjs` §3)
  Found by: scan-sweep (eslint-rules-shims, test-strategist) · The catch was written for a package consumed outside the repo (no `eslint-rules/` at all), but it fires identically when the directory exists and one shim is absent — the eleventh rule would ship without its compat path and `npm run test:rules` would print SKIP. Fix: check `existsSync('../../../eslint-rules')` first; inside the repo, require every rule's shim and also assert no orphan shim (`readdirSync` set parity, the pattern §4 of the same file already uses for rules/docs/tests) · escalation: policy-tighten (plugin context)

- **[2026-09-06] ~~A path citation (`cesta`) can never report a rejected edge — `Trail` carries `pendingCount` only~~ WITHDRAWN 2026-09-06 (graph-explorer sweep)** — type: contract, risk: 2, effort: s, payoff: 3, reach: 2 renderers (`features/graph/permalink.ts:514` `reviewOf(view.trail.pendingCount, 0, stale)`, `app/graf/p/[ref]/opengraph-image.tsx` `pendingLine`) + `features/graph/graphTypes.ts:172`
  Found by: scan-sweep (graph-admin-data-routes, parity-auditor) · The 2026-09-04 fix made the OG card and the page say „N hran zamítnuto" ahead of „vše ověřeno" for `trasa` and `okoli` by counting `gate === "rejected"` over the edges; a `cesta` view exposes no per-edge gate, so both renderers hard-code zero and a path through a rejected tie still prints the confirming line. Fix: add `rejectedCount` to `Trail` in the loader (graphLoader.ts:834 already counts pending) and read it in both places · escalation: contract (graph-explorer context)
  Withdrawn: read from inside the context, the premise is false — `buildAdjacency` drops every `rejected` edge before the search runs (`features/graph/trailPath.ts:185`, counted in `excludedRejected`), so a found path cannot contain one and the zero at `permalink.ts:512` is a claim the comment there already makes; `PathTrailDto.edges` also DO carry `gate`. A card written from another context's vantage must be re-read from inside the context before it is built.

- **[2026-09-06] `GraphNode.degree` falls back to `0` when the in-memory index is unavailable — missing is not zero** — type: contract, risk: 2, effort: m, payoff: 2, reach: `features/graph/graphLoader.ts` `getNodeDetail` (2 sites, `idx?.byId.get(row.id)?.degree ?? 0`) + `getNeighbourhood` (1 site) → `NodeInspector`, `UzelExhibit` print „stupeň: 0" as a fact
  Found by: scan-sweep (graph-explorer, bounty-hunter) · The detail read succeeds while `graphIndex()` is null (store hiccup, empty read), and the surface then states a degree of zero over a node that has edges. Fix is a contract change: `degree: number | null` on `GraphNode` with every consumer (stage sizing, search ranking, permalink JSON-LD, OG card) reading null as „nezjištěno" · escalation: contract (GraphNode is shared by 8 files and the OG image)

- **[2026-09-06] `worstGateOfView` has zero consumers — `/overeni` still returns a blanket `ungated` for the `graf` family** — type: integration, risk: 2, effort: s, payoff: 3, reach: `features/graph/permalink.ts` `worstGateOfView` (exported, tested, 0 importers outside the module — grep 2026-09-06) + `features/overeni` verdict for refs `g.`/`g2.`
  Found by: scan-sweep (graph-explorer, integration-planner) · The function was written on 2026-09-04 „aby ho /overeni jen zavolalo" and `docs/routes/graf.md`'s carry-over still names the gap; a citation whose trail crosses a rejected or pending edge verifies as `ungated` today. Fix: the overeni verdict resolves the view (getPermalinkData) and applies `worstGateOfView` as the gate modifier · escalation: contract (overeni context; verdict semantics)

- **[2026-09-06] `MonitorClient.harvestBudgetSnapshots` aborts the whole harvest on one town's failure and orphans the other workers** — type: contract, risk: 2, effort: s, payoff: 3, reach: `lib/ingest/sources/monitor.ts:296-317` → the budget batch (132 towns × 5 years = 660 calls)
  Found by: scan-sweep (ingest-external-sources, error-handler) · `Promise.all` over N workers rejects on the first `fetchBudgetYear` throw; the remaining workers keep running detached (their later rejections are unhandled) and the caller gets no snapshot for the 659 calls that succeeded. Fix: collect per-(ic, year) failures into the result (`{ snapshots, failed: {ic, year, error}[] }`) so a batch disclose its own holes — a return-shape change for `features/budget` generation scripts · escalation: contract (return shape; budget-mirror consumers)

- **[2026-09-06] `harvest-contract-dumps.ts` lists companies with a literal `100_000` — the cap `readCap.ts` names as the class of bug it ends** — type: defect, risk: 2, effort: s, payoff: 3, reach: `scripts/case-loops/money/harvest-contract-dumps.ts:61` (the GDPR allow-list is built from this read)
  Found by: scan-sweep (ingest-external-sources, parity-auditor; veto 1 — money-contract-ingest context) · The graph carries ~16k company nodes today, so the literal holds, but it is the same ad-hoc cap that silently truncated `supplies` at 100 000 in batch 012; five loaders and (since ace0ce0) the graph trail loader read companies with `KG_READ_CAP`. Fix: `limit: KG_READ_CAP` and the repository's `warnIfTruncated` · escalation: none (other context)

- **[2026-09-06] `pspIdFromNodeId` exists twice with two semantics — strict `psp:person:<n>` in changeEvents, any integer tail in moneyLoader** — type: duplication, risk: 2, effort: s, payoff: 3, reach: `features/money/moneyLoader.ts:107` (6 importers: admin tripwire, budget supplier ties, rentgen, collisions, company detail, …) vs `lib/ingest/changeEvents.ts:143`
  Found by: scan-sweep (ingest-psp-sources, parity-auditor; veto 1 — money-ledger-graph context) · The loose reader accepts `kg:company:ico:04544152` (→ 4544152) or `psp:organ:174` (→ 174) as a person id; every caller feeds it `e.src` of a `linked_to` edge, where the shape holds today, but nothing pins it. Fix: one prefix-checked reader in a lib module both sides import (`lib/kg/` is the natural home; changeEvents already has it) and the six callers on it · escalation: none (other context)

- **[2026-09-06] `tieChangeEvents` passes any `review_state` string verbatim; `gateFromEdge` collapses unknown tokens to `pending_review`** — type: architecture, risk: 2, effort: m, payoff: 2, reach: `lib/ingest/changeEvents.ts:340` payload vs `features/shared/provenance/receipt.ts:177` (the one interpretation of `review_state`)
  Found by: scan-sweep (ingest-psp-sources, parity-auditor) · Two readers of one field; a mistyped token in kg_edge.props would reach the deník's change stream as a state the receipt would have read as pending. Not fixable in place: `lib/` must not import `features/`, so the gate rule has no lib home today. Fix: move `gateFromEdge`'s status mapping (and GATED_RELS) to `lib/kg/` and have receipt.ts, edgeGate.ts and changeEvents.ts read it · escalation: architecture (layering; three contexts)

- **[2026-09-06] `parseSubsidies` keys an id-less subsidy by array position, so two pages collide** — type: contract, risk: 2, effort: s, payoff: 2, reach: `lib/analysis/money-feed.ts:341` (`id: String(r.id ?? `${ico}:${out.length}`)`) → any consumer that dedupes subsidies by `id` across pages
  Found by: scan-sweep (kg-analysis, bounty-hunter) · Hlídač's /dotace/hledat is paged; a result without `id` on page 2 gets the same `${ico}:0` as page 1's first id-less row, and a Map-by-id drop is silent. The honest id is a content key (ico + year + provider + amount) or a per-page offset supplied by the caller; either changes what `Subsidy.id` promises · escalation: contract (Subsidy.id semantics; money-ledger-graph consumers)

- **[2026-09-06] Four kg writers stamp a plain `{pass, method, ref, computedAt}` while five siblings carry the structured provenance stamp** — type: contract, risk: 3, effort: m, payoff: 3, reach: `kg-bill-roles-ingest.ts:108`, `kg-bill-engagement-ingest.ts:92`, `kg-committee-routing.ts:86`, `kg-forensics.ts:152` (edge/prop stamps without `source` / `writer`; no `guardStampedRows` before their upserts)
  Found by: scan-sweep (kg-pipeline, parity-auditor) · `lib/kg/provenance.ts` (G5) made `{source, ingest_run_id, pass, ref, writer}` the writers' contract and the atlas counts rows per `source`; the rows these four write land on `unknown` or are invisible to the per-writer sentinel check. kg-compute, kg-money-ingest, kg-legislation-ingest and kg-promote already stamp through `makeProvenance`. Fix: the same `makeProvenance({source, pass, ref, writer})` + `guardStampedRows` in the four, with the `source` each actually reads (psp-tisky / psp-tisky-law) · escalation: contract (provenance sources; atlas counts move)

- **[2026-09-06] `promote-verdicts` reconstructs `rowsValid` from an integer-rounded percentage** — type: contract, risk: 1, effort: s, payoff: 2, reach: `scripts/data-analysis/promote-verdicts.ts:47` (`Math.round((s.pct.valid / 100) * s.rows)`) → `slice_quality.rows_valid`
  Found by: scan-sweep (kg-pipeline, bounty-hunter) · `SliceStats.pct.valid` is `Math.round(100 * valid / rows)`, so the stored row count can be off by up to 0,5 % of the slice (≈ 2 000 rows on the 406k-ballot slice) and reads as exact. Fix: carry the raw `rowsValid` count in `SliceStats` (lib/analysis/context-model.ts — other context) and write that · escalation: contract (SliceStats shape; analysis-quality context)

- **[2026-09-06] `SENTINEL_ENTITY_TABLES` claims to be the seal's list but lacks `kg_node`/`kg_edge`** — type: contract, risk: 2, effort: s, payoff: 3, reach: `lib/testing/sentinel/facts.ts:193` (8 tables; header: "the SAME pinned list as RUN_TABLES … and ENTITY_TABLES") vs `lib/db/pglite/repositories/ledger.ts:35` (10 since 2026-09-04) and `features/atlas/getAtlasData.ts` (10)
  Found by: scan-sweep (landing-page, parity-auditor) · three hand copies of one pinned list; the sentinel's provenance-coverage read (`readEntityCoverage`, facts.ts:359) skips the two graph tables the seal and the atlas now cover, so a graph ingest without `ingest_run_id` is invisible to the sentinel while the atlas scores it. `features/atlas/sealTables.test.ts` (403f28e) pins atlas↔seal; the sentinel copy is another context. Fix: export `RUN_TABLES` from ledger.ts and import it in both readers (or extend the test to the third file) · escalation: contract (sentinel facts shape; testing-sentinel + data-layer contexts)

- **[2026-09-06] `submitLensVector` is an anonymous, unthrottled INSERT that moves a published figure** — type: policy, risk: 3, effort: m, payoff: 3, reach: `features/landing/referendum/actions.ts:23` → `lens_submission` → `deriveWeightAggregate` median printed on /referendum
  Found by: scan-sweep (landing-page, security-auditor) · the only guards are the codec, a 64-char cap and a localStorage courtesy lock; a loop of POSTs from one origin shifts the k-anonymous median at will and inflates `n`. The doctrine ("sebevýběr, ne průzkum") is printed, but the surface still publishes the number. Options that change contract: per-IP/UA token bucket in the action, a signed one-shot ballot token issued with the page, or a daily cap per `lens_submission` source hash (needs a column) · escalation: policy (anonymous write path; what the median is allowed to claim)

- **[2026-09-07] `collision-check.ts` carries the pre-NFC copy of the psp.cz tisk pipeline** — type: contract, risk: 2, effort: s, payoff: 2, reach: `scripts/case-loops/law/collision-check.ts:77-165` (fetchWithRetry/parseIndex/fetchIndexHtml/fetchPdf/extractText) vs `scripts/case-loops/law/tiskText.ts` (7ee9bed)
  Found by: scan-sweep (law-amends-analysis, parity-auditor) · the census copy got batch-008's NFC normalisation and now the refusal classification; the collision copy has neither, and both write the same `.txt` sidecars under `.data/law-collision-cache/`. `collision-core.ts` already NFC-normalises on read, so the live effect is on retries (a 503 skips the bill) and on the two copies drifting further. Fix: import the five functions from `tiskText.ts` and delete the copy (law-collision-analysis context) · escalation: contract (shared cache format; other context) · **closed 2026-09-07 by 53ac45f** (scan-sweep round 28: collision-check imports `tiskText.ts` and `collision-core.ts`)

- **[2026-09-07] The live-named law-loop scripts are the OLDEST versions; the current method sits in `archive/`** — type: architecture, risk: 2, effort: m, payoff: 3, reach: `scripts/case-loops/law/validate-amends-regen.ts` (checks the batch-004 payload, 3 checks) vs `archive/validate-amends-regen-008.ts` (batch-008 payload, 5 checks incl. provenance shape and act-type gate); `amends-regen.ts` (batch-004) vs `archive/amends-regen-008.ts`; `apply-amends-regen.ts` applies batch-008; `collision-check.ts` (batch-002 method, flat + `--v2`) vs `archive/collision-check-008.ts` (the version `collision-core.ts` was extracted from); `triage.ts` (batch-001 bootstrap, wholesale ledger write — now refuses without `--replace`, 75b4a1c) vs `archive/retriage-009.ts` (the merge-preserving current re-triage)
  Found by: scan-sweep (law-amends-analysis, documentation-auditor) · the archive headers say "original file kept unchanged for history" about the LIVE file, so a reader running `validate-amends-regen.ts` before `apply-amends-regen.ts` validates a different payload than the one applied. STATE.md lists the live names as the toolchain. Either promote the -008 files to the live names (archive the -004 originals) or make the live files payload-parameterised with defaults equal to apply's · escalation: direction (which file is canonical; the case loop's own convention)

- **[2026-09-07] `context-map.json` misses 316 tracked source files** — type: tooling, risk: 1, effort: s, payoff: 2, reach: `context-map.json` vs `git ls-files features lib scripts app packages` (316 `.ts`/`.tsx` unmapped, including every helper and test this sweep added since round 20)
  Found by: scan-sweep (law-amends-analysis, registry-conformance) · the map is Personas-generated ("edits between the markers are overwritten on the next scan"), so a hand edit would be undone; the sweep picks contexts by map order and stays inside declared paths, so unmapped files are swept only as pair partners. Fix: rescan · escalation: tooling (owner's generator; not a code change)

- **[2026-09-07] The write-time verdict gate accepts every node id; the pre-write gate accepts five kinds** — type: contract, risk: 2, effort: s, payoff: 2, reach: `scripts/data-analysis/kg-forensics.ts:113` (`knownIds = new Set(nodes.map((n) => n.id))`) vs `scripts/case-loops/law/verdictScope.ts` (`company/person/law/bill/organ`, 59ad87b)
  Found by: scan-sweep (law-triage-batch, parity-auditor) · gate-verdicts.ts's header claims to match kg-forensics "exactly"; it does not — a `graph_fact` citing a theme or committee-membership node passes the live write and fails the gate that is supposed to precede it, so the two disagree in the direction that lets a verdict land unchecked when the pre-write gate is skipped. Fix: kg-forensics imports `verdictGateScope` (or `VERDICT_ID_KINDS`) — one definition for all three sites · escalation: contract (what a forensic citation may point at; kg-pipeline context)

- **[2026-09-07] `readForensic` fabricates severity `low` when a verdict carries a review state but no stored severity** — type: contract, risk: 2, effort: m, payoff: 2, reach: `features/lawwatch/getLawData.ts:202` (`severity: severity ?? "low"`) → `LawForensicView.severity` (lawTypes.ts:42), BillDetail's severity label and „co analýza netvrdí“ sentence, forensicIndex, publicWire `hasForensic`
  +2026-09-08 (civic-chronicle round 53): the SAME default sat in `features/dukazy/getDukazyData.ts` (`asStr(p.forensic_severity) ?? "low"`) and is gone there — `ForensicSignoffLike.severity` is `string | null`, the bulletin cites „neuvedena"; `getLawData.ts:203` still carries `severity ?? "low"`.
  Found by: scan-sweep (lawwatch, state-coverage) · the same fabricated-default class G2 removed from `reviewState` on 2026-09-04 ("NOT `?? \"pending_review\"`" sits four lines below), still applied to severity: an absent `forensic_severity` renders as the LOWEST severity, a claim the graph does not carry. 0 such nodes measured on the current corpus (kg-forensics writes both props together), so the fix is a contract change, not a hot defect: `severity: string | null` and a „závažnost neuvedena“ branch at the three render sites · escalation: contract (LawForensicView shape; lawwatch + publicWire consumers)

- **[2026-09-07] `SmlouvyClient` throws prose errors; callers regex the status out of the message to decide a retry** — type: contract, risk: 2, effort: s, payoff: 2, reach: `lib/ingest/sources/smlouvy.ts:395,415,427` (`throw new Error(\`smlouvy.gov.cz search → ${status} …\`)`) → `scripts/case-loops/money/agrofert-sweep.ts:76` (`/→ (?:429|5\d\d) /.test(msg)`), other sweeps
  Found by: scan-sweep (money-analysis-triage, error-handler) · `refusal-class.ts` (`classifyResponse`, `RefusedError.kind`) exists for exactly this; kiosek and money-feed adopted it in rounds 22/24. A caller that parses "→ 503 " out of a sentence breaks the day the message is reworded. Fix: the client throws `RefusedError` and callers test `isTerminalRefusal` / `kind === "pressure"` · escalation: contract (error shape of an ingest client; ingest-external-sources context)

- **[2026-09-07] `ReviewState` has no runtime list; three sites spell it** — type: contract, risk: 1, effort: s, payoff: 2, reach: `features/money/reviewTypes.ts:16` (type only) → `scripts/case-loops/money/triage.ts` (KNOWN_REVIEW_STATES, ad964c0), review-console and graph-gate narrowing sites
  Found by: scan-sweep (money-analysis-triage, parity-auditor) · a stored graph prop is a string; every place that narrows it needs the member list, and a type cannot be iterated. `export const REVIEW_STATES = [...] as const` + `type ReviewState = typeof REVIEW_STATES[number]` is the repo's own pattern (`BILL_ORIGINS`, `CORROBORATIONS`) · escalation: contract (money-cases-review owns the module) · progress 2026-09-07 (round 33, a86cdad): `REVIEW_STATES` + `reviewStateOf()` exist in reviewTypes.ts and getVerificationData reads them; `features/money/moneyLoader.ts:175` (money-ledger-graph) and `scripts/case-loops/money/triage.ts` (money-analysis-triage) still spell the ternary · progress 2026-09-07 (round 35, 6f1fc6d): the ledger mapper reads `reviewStateOf`; only triage.ts remains

- **[2026-09-07] `getMoneyMpDetail` returns null for an outage AND for an MP with no ties; the case file prints „žádné vazby“ for both** — type: contract, risk: 3, effort: s, payoff: 3, reach: `features/money/getMpDetail.ts:21` (`if (!slice) return null` — store/readiness) and `:37` (`if (ties.length === 0) return null`) → `features/money/MpCaseFilePage.tsx:68` (`caseFile.noTies`)
  Found by: scan-sweep (money-budget-routes, state-coverage) · a store outage renders as a clean money record for a named MP — the opposite of the repo's fallback rule ("never plausible fiction presented as real"); /penize/firma/[ico] already separates the two sentences. Fix: `{ status: "unavailable" } | { status: "empty", person } | detail`, or a null + `DataUnavailable` branch when `storeReady` fails · escalation: contract (loader shape; money-cases-review) · refined 2026-09-07 (round 33): `loadMpMoneySlice` (moneyLoader.ts:703-720, money-ledger-graph) returns null for FOUR causes - no store, store not ready, person node missing, zero ties - and the page's one sentence is true for the last only; `features/profile/getProfileData.ts:562-573` already tells them apart by pre-counting resolvable ties, so the pattern exists. The honest sentence needs a new `caseFile.*` key in messages/cs.json + en.json, which no context owns

- **[2026-09-07] `/poslanec/[id]` spells the digits-only segment rule that `lib/routing/pspIdParam.ts` now owns** — type: parity, risk: 1, effort: s, payoff: 1, reach: `app/poslanec/[id]/page.tsx:49,79` (`/^\d+$/`) vs `lib/routing/pspIdParam.ts` (round 32)
  Found by: scan-sweep (money-budget-routes, parity-auditor) · the three routes' comments name each other as the same rule; two now import it. Fix: `pspIdFromParam(id)` in both the metadata and the page branch · escalation: none needed beyond the context boundary (mp-rankings-routes) · CLOSED 2026-09-07 (round 37, 95972dd): both sites read `pspIdFromParam`

- **[2026-09-07] `loadMpMoneySlice` dates its signature-plausibility bound in UTC while `/penize/firma/[ico]` now uses the Prague day; the loader's comment says the two must agree** — type: parity, risk: 2, effort: s, payoff: 2, reach: `features/money/moneyLoader.ts:746` (`datesCheckedOn = new Date().toISOString().slice(0, 10)`, comment: „Týž tvar jako /penize/firma/[ico], aby dvě sousední plochy nekreslily hranici k jinému dni“) vs `app/penize/firma/[ico]/page.tsx` (`pragueDay()` since 7225d18, round 32)
  Found by: scan-sweep (money-cases-review, parity-auditor) · the sweep itself opened this gap in round 32 and could not close it here (money-ledger-graph owns the loader). Between 00:00 and 01:00/02:00 Prague the MP case file and the company file draw the plausibility boundary on different days for the same contract. Fix: `datesCheckedOn = pragueDay()` in `loadMpMoneySlice` and rewrite the comment (the objection it records - „sjednocení by pohnulo publikovaným počtem“ - is a one-to-two-hour window, not a corpus change) · escalation: none beyond the context boundary (money-ledger-graph) · CLOSED 2026-09-07 (round 35, 2b5f8c1): `loadMpMoneySlice` reads `pragueDay()`; the comment now records why

- **[2026-09-07] The review DECISION list is spelled in three places; `ReviewDecision` is a type only** — type: contract, risk: 1, effort: s, payoff: 2, reach: `lib/db/pglite/ddl.ts:259` (`check (decision in ('confirm', 'reject', 'needs-more'))`), `features/money/reviewActions.ts:27` (`VALID_DECISIONS`), `scripts/case-loops/money/migrate-review-audit-check.ts:56` (`ALLOWED_DECISIONS`) vs `features/money/reviewTypes.ts:54` (`export type ReviewDecision`)
  Found by: scan-sweep (money-contract-ingest, parity-auditor) · the migration script's whole purpose is to retrofit the DDL's list onto a live table, and it spells the list a third time. Same shape as the ReviewState card (round 31, half-closed in round 33): `export const REVIEW_DECISIONS = [...] as const` in reviewTypes.ts, `VALID_DECISIONS` and `ALLOWED_DECISIONS` import it, and a test derives the DDL's parenthesised list from the constant · escalation: contract (a runtime list on a shared module; money-cases-review owns reviewTypes, data-layer owns the DDL)

- **[2026-09-07] The ledger's club filter `<select>` has no accessible name** — type: a11y, risk: 1, effort: s, payoff: 1, reach: `features/money/components/TiesLedger.tsx` (the `<select value={clubFilter}>` after the temporal chips; its first `<option>` is „všechny kluby“, which is an option, not a label)
  Found by: scan-sweep (money-ledger-graph, accessibility-checker) · the search input beside it now carries `aria-label` (457f7cd) because a catalog key already existed; the select needs a new key („filtr podle klubu“ / "filter by club") in messages/cs.json + en.json, which no context owns. Fix: add the key pair and `aria-label={t("real.ledger.filterClubLabel")}` · escalation: none beyond the unowned messages files

- **[2026-09-07] The spis cannot adopt the loader's `dateWithheldOn` verdict: an unowned test pins the old pass** — type: contract, risk: 2, effort: s, payoff: 2, reach: `features/money/contractDates.test.ts:88-97` (`expect(profile).toMatch(/plausibleIsoDateOrNull\\(/)` over `features/profile/profileMoney.ts`) vs `features/money/moneyTypes.ts::ContractLine.dateWithheldOn` (names „jedna brána v getMpDetail.ts, spis čte dateWithheldOn“ as the final shape)
  Found by: scan-sweep (mp-profile, parity-auditor) · `toProfileMoney` re-runs the plausibility bound over every contract against its OWN day while `loadMpMoneySlice` already drew it on the Prague day and attached `dateWithheldOn` to the row - two surfaces, one signature. The change (7c92228, with `profileMoney.test.ts`) was built, green in the profile lane and RED in the full gate on this pin; reverted in db3b020 because the test file belongs to no context. Fix: re-point the pin to `expect(profile).toMatch(/dateWithheldOn/)` (its stated purpose - the spis keeps admitting bad dates - is met by the new read), then re-apply 7c92228 · escalation: contract (a cross-context test asserts an implementation detail; the pin's owner is unassigned)

- **[2026-09-07] `regionLabelOf` in the spis loader mirrors `getLeaderboardData`'s private `regionLabel()`** — type: parity, risk: 1, effort: s, payoff: 1, reach: `features/profile/getProfileData.ts` (`regionLabelOf`, comment: "Mirrors getLeaderboardData's private regionLabel()") vs `features/civicscore/getLeaderboardData.ts:174` (`function regionLabel`, not exported) — and `lib/analysis/volby/kraje.ts:19` (`regionLabelFromPspName`, the THIRD copy, added to this card 2026-09-07 by round 47); `lib/civic/leaderboard.ts:41` REGIONS is the mock's fourth spelling of the same 14 labels (added 2026-09-08 by round 55)
  Found by: scan-sweep (mp-profile, parity-auditor) · two copies of the kraj-label rule (Praha / Vysočina / `<name> kraj`) held together by a comment; the leaderboard's `person.region` and the spine's per-term region must read identically. Fix: `export function regionLabel` in getLeaderboardData.ts and import it in the spis · escalation: none beyond the context boundary (civicscore-leaderboard owns the original)

- **[2026-09-07] The leaderboard poster cannot say „datum neuvedeno“: `retrievedAt` is a non-null string** — type: contract, risk: 1, effort: s, payoff: 2, reach: `features/shared/poster/demo/LeaderboardPoster.tsx:26` (`retrievedAt: string`) → `features/shared/poster/PosterFrame.tsx:99` (`poster.retrievedLine` with `f.date(citation.retrievedAt)`); caller `app/plakat/[view]/page.tsx` (`data.provenance.computedAt ?? pragueDay()` since b8f4e98)
  Found by: scan-sweep (mp-rankings-routes, state-coverage) · the OG card and the kraj card print a sentence when the chamber provenance carries no uniform day; the poster type forces a date, so the route falls back to the Prague print day - honest about the calendar, silent about the data's age. Fix: `retrievedAt: string | null` and a `poster.retrievedUnknown` line in PosterFrame · escalation: contract (shared-primitives owns features/shared/poster) · UPDATE 2026-09-07 (round 41, shared-provenance): the type half landed in 6d78843 - `retrievedAt: string | null`; `buildPosterCitation` already prints „—“ for the day and the reason in the methodology line, so no new catalog key is needed. What remains is the caller's `?? pragueDay()` in `app/plakat/[view]/page.tsx:58` (mp-rankings-routes): pass `data.provenance.computedAt` as-is

- **[2026-09-07] `/referendum` metadata is hard-coded Czech while `/zebricek` reads the catalog** — type: parity, risk: 1, effort: s, payoff: 1, reach: `app/referendum/page.tsx:29-38` (literal title/description) vs `app/zebricek/page.tsx` (`t("civicscoreLensTitle")` …); `messages/cs.json` has no `meta.referendum*` keys
  Found by: scan-sweep (mp-rankings-routes, copy-auditor) · an EN reader sharing /referendum gets a Czech preview; the route's own comment records the precedent ("messages/*.json mimo plochu — precedens /kompas"). Fix: add `meta.referendumTitle/LensTitle/Description/LensDescription` to both catalogs and read them as /zebricek does · escalation: none beyond the unowned messages files

- **[2026-09-07] The IČO node-id grammar lives twice: `caseFileLink.ts` and `features/money/companyId.ts`** — type: parity, risk: 2, effort: m, payoff: 2, reach: `features/shared/provenance/caseFileLink.ts` (`COMPANY_ID = /^company:ico:(\d{1,8})$/`, `icoFromEntityId`) vs `features/money/companyId.ts` (`canonicalIco`, `companyNodeId`, `icoFromCompanyNodeId`)
  Found by: scan-sweep (shared-provenance, parity-auditor) · the person half was unified in ff78b2f (lib/ingest owns it); the company half cannot be, because `features/shared/**` may not import `@/features/*` (eslint no-restricted-imports, widened 2026-08-13). Fix: move `companyId.ts` to `lib/kg/companyId.ts` (or `lib/ingest/`), re-export from features/money for its 9 importers, then `icoFromEntityId = icoFromCompanyNodeId` · escalation: architecture (cross-context move: money-ledger-graph + shared-provenance)
  +2026-09-08 (civic-kg-primitives round 55): a THIRD copy — `lib/kg/sourceLinks.ts` `canonicalIcoOf` (pinned equal to `canonicalIco` by its test, because lib may not import features); `lib/ingest/changeEvents.icoFromNodeId` is a lib-side canonicaliser the move should unify with

- **[2026-09-07] The receipt's review state is a second spelling of `reviewStateOf`** — type: parity, risk: 2, effort: m, payoff: 2, reach: `features/shared/provenance/receipt.ts:65` (`type ReviewStatus`) and `:181-182` (`rawState === "verified" ? … : rawState === "rejected" ? … : "pending_review"`) vs `features/money/reviewTypes.ts` (`REVIEW_STATES`, `ReviewState`, `reviewStateOf(raw)` - round 33)
  Found by: scan-sweep (shared-provenance, parity-auditor) · the header says „TÁŽ interpretace jako moneyLoader.mapLinkedToTie" and holds it by comment; the money side reads `reviewStateOf` since a86cdad, the receipt cannot import it (catalog boundary). Fix: lift `REVIEW_STATES`/`reviewStateOf` to `lib/db/reviewState.ts` (the store is the writer), import from both · escalation: architecture (cross-context: money-cases-review + shared-provenance + data-layer)

- **[2026-09-07] `GATED_RELS` is held in sync with the review repository by a comment** — type: parity, risk: 1, effort: s, payoff: 1, reach: `features/shared/provenance/receipt.ts:61` (`GATED_RELS = new Set(["linked_to"])`, „Drží se v syncu s lib/db/pglite/repositories/review.ts") vs `lib/db/pglite/repositories/review.ts:42` (`const LINKED_TO = "linked_to"`, not exported)
  Found by: scan-sweep (shared-provenance, parity-auditor) · a second gated relation added to the repository would leave the receipt printing „ungated" for it. Fix: export `GATED_RELS` from lib/db (beside the repository or in `lib/db/types.ts`) and import it in receipt.ts · escalation: none beyond the context boundary (data-layer owns review.ts)

- **[2026-09-07] The poster's histogram axis end hard-codes the leaderboard's 5-point band** — type: parity, risk: 1, effort: s, payoff: 1, reach: `features/shared/poster/demo/LeaderboardPoster.tsx:160` (`(histogram[last]?.from ?? 0) + 5`) vs `features/civicscore/getLeaderboardData.ts:468` (`from += 5`, label `${from}–${from + 5}`)
  Found by: scan-sweep (shared-provenance, parity-auditor) · a band-width change in the loader would leave the printed axis ending one band short or long; the poster is an archival print nobody corrects. Fix: `histogram[].to` filled by the loader, passed through `app/plakat/[view]/page.tsx`, read by the poster · escalation: none beyond the context boundary (civicscore-leaderboard + mp-rankings-routes)


- **[2026-09-07] Five files in swept folders belong to no context** — type: config, risk: 1, effort: s, payoff: 1, reach: `context-map.json` — `features/shell/sidebarParts.test.ts`, `features/shell/sitemapRoutes.test.ts`, `features/shared/provenance/asOfLens.ts`, `features/shared/provenance/asOfLens.test.ts`, `features/shared/provenance/messages.test.ts`, `features/votetrack/kompas/KompasNeverComputed.tsx` (mounted by app/kompas/page.tsx; added 2026-09-07, round 44); +2026-09-08 (budget-mirror round 52): `features/budget/a11y.test.ts`, `features/budget/messages.test.ts`, `features/budget/municipalRoutes.ts` + `.test.ts` (imported by app/sitemap.ts), `features/budget/supplierTiesTypes.ts` (imported by three owned budget files), `features/budget/supplierYears.test.ts` — eleven files now +2026-09-08 (dashboard-instruments round 62): `features/dashboard/componentSource.test.ts`, `features/dashboard/pageSource.test.ts` (and the sweep's own new `features/dashboard/instrumentsSource.test.ts`). +2026-09-08 (db-hybrid-benchmarks round 64): `scripts/hybrid-bench/materialize-tags.test.ts`, `scripts/sentinel/run.test.ts` (and the sweep's new `scripts/hybrid-bench/semop.test.ts`).
  Found by: scan-sweep (shell-navigation + shared-provenance, documentation-auditor) · veto 1 forbids editing an unowned file, so a sweep of the owning folder reads them for pins and leaves them; the doc-sync coverage block already reports 3 unmapped contexts. Fix: add the five paths to `shell-navigation` / `shared-provenance` `file_paths` (or rescan) · escalation: none (map hygiene)

- **[2026-09-07] The rentgen terminal's five instrument links are not checked against the app tree** — type: test, risk: 1, effort: s, payoff: 1, reach: `features/labs/rentgen/VariantRentgen.tsx` (`INSTRUMENTS`: /graf, /penize, /dukazy, /denik, /overeni; `SOURCE_REGISTRY` external hosts)
  Found by: scan-sweep (shell-navigation, test-strategist) · `navModel.test.ts` scans `app/` for every rail href; the archived terminal keeps its own list of product surfaces with no such scan, so a renamed route would leave a dead link on the press page. Fix: move `INSTRUMENTS` to `terminalModel.ts` (pure) and assert each `href` has an `app/<path>/page.tsx` in `terminalSource.test.ts` · escalation: none (labs, S)

- **[2026-09-07] „A bill with a forensic verdict" is defined three ways in the sentinel's facts** — type: parity, risk: 2, effort: s, payoff: 2, reach: `lib/testing/sentinel/facts.ts` — `readReviewCoverage` counts `props ? 'forensic_review_state' or props ? 'forensic_severity'`; `readLawProvenance` and `readAmendsClosure` count `props ? 'forensic_severity'` only
  Found by: scan-sweep (testing-sentinel, parity-auditor) · a bill carrying a review state but no severity is in the review-coverage denominator and outside the two provenance/closure populations; whether such bills exist (and which predicate the lawwatch loader treats as "has a verdict") needs one read-only count on a store copy before a single `BILL_VERDICT_WHERE` fragment replaces the three. Fix: probe, then one exported SQL fragment used by all three reads, pinned by sentinelSource.test.ts · escalation: none (S once the probe answers)

- **[2026-09-07] Atlas `ENTITY_TABLES` omits the graph tables the ledger has sealed since G5** — type: parity, risk: 2, effort: m, payoff: 2, reach: `features/atlas/getAtlasData.ts:36` (8 tables) vs `lib/db/pglite/repositories/ledger.ts:35` (`RUN_TABLES`, 10 since 2026-09-04: + `kg_node`, `kg_edge`); mirror `lib/testing/sentinel/facts.ts` `SENTINEL_ENTITY_TABLES` (8, asserted equal to the atlas list by ea9df53)
  Found by: scan-sweep (testing-sentinel, parity-auditor) · /atlas publishes per-source provenance coverage for eight entity tables while the two graph tables readers actually cite (19 266 `supplies` rows in the snapshot cut) now carry `ingest_run_id` and a seal but no coverage row. Fix: decide whether kg_node/kg_edge join the atlas coverage (per `source` they group differently); if yes, extend ENTITY_TABLES and the sentinel mirror together (the parse test will fail until both move) · escalation: contract (atlas owns the published coverage)

- **[2026-09-07] `RUN_TABLES` and `INGEST_RUN_LIMIT` have no exported home, so their mirrors are guarded by parsing source text** — type: architecture, risk: 1, effort: s, payoff: 1, reach: `lib/db/pglite/repositories/ledger.ts:35` (`const RUN_TABLES`, not exported), `features/data-releases/getDataReleasesData.ts:80` (`const INGEST_RUN_LIMIT = 500`, server-only module), `features/admin/loops/getLoopState.ts:34` (`INGEST_RUNS_LIMIT = 500`, the third copy — added 2026-09-08 by round 48), mirrors in `lib/testing/sentinel/facts.ts`
  Found by: scan-sweep (testing-sentinel, architecture-analyst) · ea9df53 checks the mirrors by regex over the partner files - honest, but a rename of either constant turns the guard into a "not found" throw rather than a type error. Fix: `export const RUN_TABLES` from lib/db (pure) and move `INGEST_RUN_LIMIT` to `features/data-releases/manifest.ts` (pure, already imported by the sentinel); the sentinel then imports both and the parse test shrinks to the atlas list · escalation: none beyond the context boundaries (data-layer, data-releases)

- **[2026-09-07] Percent formatting is hand-spelled on six surfaces** — type: parity, risk: 1, effort: s, payoff: 1, reach: `features/votetrack/kompas/ResultsBoard.tsx:25` (`${f.dec(Math.round(rate * 1000) / 10)} %`), `features/civicscore/MetodikaVolbySection.tsx:58` (`${f.int(Math.round(x * 100))} %`), `features/budget/BudgetMirrorPage.tsx:71`, `features/dashboard/DashboardPage.tsx:306`, `features/money/components/VerificationConsole.tsx:933` (`${f.dec(n)} %`), `features/admin/components/LoopProgressGrid.tsx:45` (`${czech(p)} %`)
  Found by: scan-sweep (votetrack-kompas, parity-auditor) · `lib/format.ts` owns every other display number (int, dec, date, czk) but has no percent rule, so each surface decides its own rounding (0 vs 1 decimal) and its own space before „%" (Czech typography wants one; all six comply today by hand). Fix: `czechPct(rate, decimals)` in lib/format + `f.pct` in useFormat, adopt at the six sites fix-as-you-touch · escalation: none (lib/format is shared; S per site)

- **[2026-09-07] `czDateTimeToIso` stamps the Czech wall clock as UTC** — type: data, risk: 3, effort: m, payoff: 3, reach: `packages/czech-civic-data/src/unl.ts:146-155` (`${iso}T${hh}:${m[2]}:00.000Z` from a `HH:MM` psp.cz column that is Prague local time; `czDateHourToIso` likewise), consumers: `vote_event.voted_at` → `features/votetrack/record/derive.ts` (`votedAt.slice(11, 16)` — right only because the Z is a lie), anything that does `Date.parse(votedAt)` and converts to Prague (`pragueDay`, feeds) — 1 h off in winter, 2 h in summer
  +2026-09-08 (czech-civic-data-parsing round 55/61): read in context — the package is pure but may compute the Prague offset with `Intl.DateTimeFormat({ timeZone: "Europe/Prague" })` exactly as `features/denik/pragueDay.ts` does (no app import needed); the blocker is not code but DATA: every stored `voted_at` / `fromAt` / `toAt` string changes shape, so the fix ships with a re-ingest and a check of every reader that slices or sorts those strings. Not built by the sweep for that reason.
  Found by: scan-sweep (votetrack-ledger, parity-auditor) · the ingest owns the meaning of the instant; today the ledger's `HH:MM` and the stored instant disagree about what „Z" means, and a consumer that trusts the suffix (a feed's `updated`, a since-day threshold) shifts every roll call by the Prague offset. Fix: emit the true instant (`pragueDayStart`-style offset arithmetic from features/denik/pragueDay.ts, or `+01:00`/`+02:00` suffix) AND change derive.ts to format HH:MM in Europe/Prague in the same change, with a re-ingest or a migration of `voted_at`; the two must move together · escalation: contract (Data Ingestion group; the package context is unmapped in context-map.json)

- **[2026-09-07] The kompas card keeps a fourth copy of the outcome vocabulary** — type: parity, risk: 1, effort: s, payoff: 1, reach: `features/votetrack/kompas/QuestionCard.tsx` (`const KNOWN_OUTCOMES = new Set(["accepted", "rejected"])`, 27a77dc) vs `features/votetrack/record/outcome.ts` (4c42a4a: `KNOWN_OUTCOMES`, `isKnownOutcome`, `outcomeTone`)
  Found by: scan-sweep (votetrack-ledger, parity-auditor) · round 44 fixed the card with a local set one round before the shared module existed. Fix: import `isKnownOutcome`/`outcomeTone` from `../record/outcome`, drop the local set, extend `ledgerSource.test.ts`'s file list with the card · escalation: none (votetrack-kompas owns the card; S)

- **[2026-09-07] `pspIdFromParam` spells the rule `positiveIntParam` now owns** — type: parity, risk: 1, effort: s, payoff: 1, reach: `lib/routing/pspIdParam.ts:9` (`/^\d+$/.test(raw) ? Number(raw) : null`) vs `lib/routing/intParam.ts` (`positiveIntParam`, round 46: same regex + safe-integer + > 0)
  Found by: scan-sweep (voting-legislation-routes, parity-auditor) · two spellings of „a route segment is a bare positive integer"; the MP one also accepts `0` and an unsafe run of digits. Fix: `export const pspIdFromParam = positiveIntParam` (keep the name for its four importers) · escalation: none (lib/routing is unowned in context-map.json — veto 1 kept this round from editing it; add the folder to a context)

- **[2026-09-07] `ObecData.obec.krajSlug` uses `""` as its „kraj unresolved" sentinel** — type: contract, risk: 1, effort: s, payoff: 1, reach: `features/volby/getObecData.ts` (`krajSlug: kraj?.slug ?? ""`), `lib/analysis/volby/types.ts:82` (`krajSlug: string` — the brief's verbatim contract), `features/volby/ObecPage.tsx:25-26` (`krajBySlug("")` → null → `krajName = ""` → `obec.meta` prints „· kraj: ")
  Found by: scan-sweep (volby-election-mirror, state-coverage) · an obec whose MONITOR kraj index maps to no crosswalk row renders an empty kraj in its eyebrow instead of a sentence; the type forbids `null`. Fix: `krajSlug: string | null` in the wire contract, `obec.metaNoKraj` sentence in the catalog, ObecPage branches · escalation: contract (types.ts is copied verbatim from the election-replay brief; the catalog is unowned)

- **[2026-09-08] `AUDIT_READ_CAP = 10_000` lives twice** — type: parity, risk: 1, effort: s, payoff: 1, reach: `features/money/getVerificationData.ts:42` and `features/admin/getAdminData.ts` (d58af03, both `const AUDIT_READ_CAP = 10_000` with a warn when a `listReviewAudit` read fills it)
  Found by: scan-sweep (admin-control, parity-auditor) · two contexts declare the review-audit read window; `lib/db/readCap.ts` already owns the graph cap (`KG_READ_CAP`) and is the home for this one. Fix: `export const AUDIT_READ_CAP = 10_000` in lib/db/readCap.ts, both loaders import it · escalation: none beyond the context boundary (data-layer owns readCap.ts)

- **[2026-09-08] `ageDaysBetween` / `stalenessOf` live twice** — type: parity, risk: 2, effort: s, payoff: 2, reach: `lib/analysis/atlas.ts:421-429` (exported) and `features/admin/loops/loopState.ts:239-247` (a verbatim copy, exported, typed over `LoopStaleness` instead of `Staleness`)
  Found by: scan-sweep (analysis-quality, parity-auditor) · one staleness rule with two implementations; a threshold change in atlas leaves the admin loop console disagreeing with /atlas. Fix: loopState imports both from atlas and aliases the type · escalation: cross-context (admin-control owns loopState.ts; veto 1)

- **[2026-09-08] tripwires keeps a private `canonicalJson` + `fnv1a` beside exhibit's** — type: parity, risk: 1, effort: s, payoff: 1, reach: `lib/analysis/tripwires.ts:267-285` vs `features/dashboard/exhibit.ts:32` (`canonicalJson`, `contentHash`)
  Found by: scan-sweep (analysis-quality, parity-auditor) · the two are proven equal by a test since a08cf60, but they are still two copies because `lib/analysis` may not import `features/**`. Fix: move the pair to `lib/hash/contentHash.ts`, both modules import it · escalation: architecture (a new lib/ home touches the dashboard context's exhibit.ts)

- **[2026-09-08] The public entity-key grammar has two homes** — type: parity, risk: 2, effort: s, payoff: 2, reach: `features/denik/deriveDenik.ts` (`mpEntityKey` / `companyEntityKey` / `billEntityKey`, and since round 53 `pspIdFromEntityKey`) vs `lib/ingest/changeEvents.ts:161-275` (`edgeEntityKeys`, `mandateChangeEvents`, `membershipChangeEvents` spell `poslanec:${…}` / `firma:${…}` under a „mirror of features/denik/deriveDenik" comment)
  Found by: scan-sweep (civic-chronicle, parity-auditor) · the `?entita=` filter, the schránka follow keys and change_event.entity_keys must agree byte for byte; today the shape is held by a comment. Fix: a pure `lib/civic-keys.ts` (or the ingest module) owning the three builders + the parser, deriveDenik re-exporting · escalation: cross-context (ingest-psp-sources owns changeEvents.ts; veto 1)

- **[2026-09-08] `isLocale(raw) ? raw : defaultLocale` is spelled at 17 sites** — type: duplication, risk: 1, effort: s, payoff: 2, reach: 17 sites across 10 contexts (`app/atlas`, `app/data`, `app/denik`, `app/dukazy`, `app/overeni`, `app/svedectvi`, `app/graf/p/[ref]/opengraph-image.tsx`, `features/dashboard/graphText.ts`, `features/graph/getPermalinkData.ts`, `features/landing/serverIntl.ts`, `features/profile/serverIntl.ts`, `features/volby/volbyIntl.ts`, `features/lawwatch` ×4, `features/shared/poster/PosterFrame.tsx`, `lib/i18n/useFormat.ts`)
  Found by: scan-sweep (civic-feeds-verification, parity-auditor) · one rule („an unknown locale is the default") with no home; a change to the fallback (or a third locale) is 17 edits. Fix: `export const resolveLocale = (raw: unknown): Locale => (isLocale(raw) ? raw : defaultLocale)` in `lib/i18n/config.ts`, every site imports it · escalation: architecture (lib/i18n is unowned; the edit spans ten contexts — veto 1)

- **[2026-09-08] `KrajPickerPage` re-implements the catalog `Combobox`** — type: duplication, risk: 2, effort: m, payoff: 2, reach: `features/civicscore/KrajPickerPage.tsx:35-195` (own open/active state, ArrowUp/Down/Home/End/Enter/Escape handling, listbox markup, blur timeout) vs `features/shared/components/Combobox.tsx` (the catalog primitive `TownPicker` adopted)
  Found by: scan-sweep (civicscore-leaderboard, architecture-analyst) · the file's header calls TownPicker „pattern, no import" — it predates the primitive's extraction; two comboboxes now drift independently (the picker's blur-timeout and Home/End handling are its own). Fix: mount `Combobox` with `items={kraje}`, `search` over `foldQuery`, `renderItem` for the count column, `onSelect` → `router.push(hrefFor(slug))` · escalation: architecture (M; the catalog primitive may need a `Home`/`End` addition first)

- **[2026-09-08] Competition ranking is spelled three times, the 5-point histogram and the score summary twice** — type: parity, risk: 2, effort: s, payoff: 2, reach: `features/civicscore/getLeaderboardData.ts:436-448` + `:461-472` + `:445-456`, `features/civicscore/lens.ts:179-191` + `histogramOf` + `summarizeScores`, `features/civicscore/kraj.ts:102-114`
  Found by: scan-sweep (civicscore-lens-duel, parity-auditor) · `lens.ts` is pure (no server imports) and already exports `histogramOf` and `summarizeScores` „mirroring the loader's rules" by comment; the loader could import them, and a `competitionRank(rows)` helper in `lens.ts` would serve all three sites. Fix: import direction loader ← lens, kraj ← lens · escalation: cross-context (civicscore-leaderboard owns the loader and kraj.ts; veto 1)

- **[2026-09-08] `DUEL_FACT_DEFS` carries Czech labels, units and sources outside the catalog** — type: copy, risk: 1, effort: m, payoff: 2, reach: `features/civicscore/duelFacts.ts:45-50` (4 rows × label/unit/source), rendered verbatim by `features/civicscore/components/HeadToHead.tsx` (`r.def.label`, `r.def.unit`, `r.def.source`)
  Found by: scan-sweep (civicscore-lens-duel, copy-auditor) · every other verdict/preset copy in this feature went to `messages/{cs,en}.json` as keys (verdicts, lensPreset); the duel's fact rows reach an English reader in Czech. Fix: `labelKey`/`unitKey` in `civicscore.duelFact.*`, sources stay verbatim (citations), HeadToHead translates · escalation: contract (catalog keys in both locales + messages.test pin; HeadToHead is civicscore-leaderboard's)

- **[2026-09-08] `CopyLensLink` duplicates the catalog `CopyLinkButton`** — type: duplication, risk: 1, effort: s, payoff: 1, reach: `features/civicscore/components/WeightPanel.tsx:48-80` vs `features/shared/components/CopyLinkButton.tsx`
  Found by: scan-sweep (civicscore-lens-duel, architecture-analyst) · the panel must compose the address AT CLICK and write it to history (`shareHref`), which the primitive's render-time `path` prop cannot express. Fix: a `getPath?: () => string` prop on `CopyLinkButton` (evaluated in `copy()`), then the panel mounts it · escalation: cross-context (shared-primitives owns the catalog)

- **[2026-09-08] The book of facts orders same-date rows by a locale-less `localeCompare` over ids that carry Czech committee names** — type: determinism, risk: 2, effort: s, payoff: 2, reach: `features/dashboard/datedFacts.ts:293` (`a.id.localeCompare(b.id)`; `assigned:<cislo>:<organLabel>` ids carry „Výbor…“ labels)
  Found by: scan-sweep (dashboard-state-graph, risk-assessor) · The order decides which twelve rows the front page shows and whether a cited fact is `beyondWindow`; `localeCompare` with no locale follows the host's default collation, so two hosts (or one host after a LANG change) can disagree on same-date order. Every other comparator in the module is codepoint or explicit `"cs"`. Measure first: count same-date pairs whose ids differ only in non-ASCII characters over the live book; if zero, the fix is a codepoint comparison with no visible change, else it re-orders a window and needs a note on the surface.
- **[2026-09-08] `buildDatedFacts` compares dates as strings with no shape check** — type: contract, risk: 1, effort: s, payoff: 2, reach: `features/dashboard/datedFacts.ts:289` (`r.date! >= PLAUSIBLE_FROM && r.date! <= today`)
  Found by: scan-sweep (dashboard-state-graph, bounty-hunter) · A datetime-shaped `signedOn` (`2025-03-04T00:00:00Z`) sorts inside the plausible range and passes, then renders through `f.date`; `denikFactHref` separately refuses it for the day anchor, so the two rules disagree. The writers' contract (psp-legislation `assignedOn`, smlouvy-dump `signedOn`) is YYYY-MM-DD but is not asserted at the boundary. Measure first: grep the store's `signedOn` / `roleValidFrom` / `assignedOn` values outside `^\d{4}-\d{2}-\d{2}$`; the answer decides whether a malformed date is dropped-and-counted (an ingest defect, like an impossible year) or excluded as no date.
- **[2026-09-08] `parseLabels` / `parseTags` fall back to POSITIONAL ids when a row's id is missing** — type: contract, risk: 2, effort: s, payoff: 2, reach: `scripts/hybrid-bench/semop.ts` (`batch[i]?.id`), `scripts/hybrid-bench/materialize-tags.ts:73` (`batch[i]?.votePspId`)
  Found by: scan-sweep (db-hybrid-benchmarks, bounty-hunter) · When the model returns ids the fallback is unused; when it returns none the positional contract holds. In the MIXED case — one row omitted, some ids missing — every later label lands on the wrong item, and for the materializer that is a wrong `vote_tag` verdict written to the store. Measure first: over the saved `.hybrid-bench/*.json` outputs, count batches whose returned length differs from the batch length AND carry at least one missing id; if non-zero, the fallback should apply only when NO row carries an id.
- **[2026-09-08] One review decision writes three instants** — type: contract, risk: 2, effort: s, payoff: 2, reach: `lib/db/pglite/repositories/review.ts` (`decidedAt = new Date().toISOString()` at the audit insert; `last_reviewed_at: new Date().toISOString()` inside the tie handler's write, a second clock; `recorded_at = now()` in the supersede statement, a third)
  Found by: scan-sweep (db-repositories, parity-auditor) · The file's own header promises that „this tie sat in pending_review for 4 months“ is a queryable fact from edge history joined with the audit chain — but the three timestamps of one decision differ by milliseconds and by clock, so no equality join exists and a reader must window. The fix is to thread the audit's `decidedAt` into `SubjectHandler.write` (and stamp `last_reviewed_at` / `forensic_review_at` / `decided_at` from it) and to decide whether `recorded_at` should be that instant too (a stored-value change for new rows only). Contract gate: the effort and bill handlers' props are read by loaders that may compare them.
## Shipped

- **[2026-09-07] `app/sitemap.ts` is the fourth `host` + `x-forwarded-proto` copy** — type: parity, risk: 1, effort: s, payoff: 1, reach: `app/sitemap.ts` (`h.get("host")`, `h.get("x-forwarded-proto") ?? "http"`) vs `lib/routing/liveUrl.ts` (`liveUrlFrom`, `liveUrl` — rounds 37/39 unified /kraj, /plakat and the schránka feed)
  Found by: scan-sweep (shell-navigation, parity-auditor) · the sitemap's base-URL rule must equal the feeds' and the poster's; today it is spelled a fourth time. Fix: `const base = await liveUrl(""); if (!base) return [];` (the empty-host branch stays, `sitemapRoutes.test.ts` pins the shape) · Status: SHIPPED 2026-09-08 by app-shell round 51 (73454a4) — robots.ts folded in as well

- **[2026-07-26] Bring the loader chain under test** — shipped 2026-09-02 via `/architect resume` (commits 6753f8b, 366e866, 1c035c4, b9684ae, 75798b1)
  ADR: [[decisions/2026-07-26-loader-test-coverage]] · every wired loader has a direct test (loaders.test.ts, one boot), the row mappers have parity + coercion tests, the getStore() lockstep contract is pinned, and the absorbed duplicate suite is gone.

- **[2026-07-26] Enforce the server-only loader boundary mechanically** — code complete 2026-09-01 via `/architect resume`, browser smoke pending (commits 431d147, 4e1f112, 45d8fea, 09004fa, 751b100, b22d1a9, 0b217ff)
  ADR: [[decisions/2026-07-26-server-only-boundary-enforcement]] · 9 loaders' prop types moved to sibling `*Types.ts` (29 client import sites), `getPermalinkData` guarded, and the rule now runs with `typeImports: "forbid"` — a `"use client"` file imports nothing from a loader, not even a type. Not browser-verified (no JSX touched).

- **[2026-07-26] One mapper for the money tie** — shipped same day (commit 8dddf90, live-verified)
  ADR: [[decisions/2026-07-26-money-tie-mapper-dedup]] · `mapLinkedToTie()` owns the projection; ReviewTie deliberately left as a separate projection (rationale in the ADR).

- **[2026-07-26] Rejected first `open()` poisons the process** — shipped same day (commit 45220bb, with regression test)
  ADR: [[decisions/2026-07-26-memoised-rejection-open]] · graphLoader null-memo invalidation deferred to the round-4 session.

- **[2026-07-26] Narrow `kg_*.props` by guard, never by `as`** — shipped same day (commit a2a70cd)
  ADR: [[decisions/2026-07-26-props-union-narrowing]] · 4 of 6 casts fixed via `asUnion()`; graphLoader's 2 deferred to the round-4 session.

- **[2026-07-26] Silent degradation to mock — observability for `catch { return null }`** — shipped same day (commits d315eb7, 223a727, cd80b51)
  ADR: [[decisions/2026-07-26-silent-degradation-observability]] · 14 sites wired + lint rule; `features/graph` (4 sites) deferred to the concurrent round-4 session.

## Abandoned / Blocked
_None yet._
