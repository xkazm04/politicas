/** Patička plakátu. */

export default function SiteFooter() {
  return (
    <footer className="border-t-4 border-ink py-6">
      <div className="mx-auto flex max-w-6xl flex-wrap items-center justify-between gap-3 px-6 font-mono text-[11px] uppercase tracking-widest text-steel">
        <span>Politicas — empirický záznam republiky</span>
        <span>otevřená data · cc by · metodika v1.4</span>
      </div>
    </footer>
  );
}
