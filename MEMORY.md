# Memory Index

Durable, cross-session knowledge for Politicas — one line per entry. Read this
at session start; the linked files hold the fact + why it matters. Convention
and upkeep rules are in `CLAUDE.md` → "Agent memory". Add an entry only for a
learning worth recalling in three months that isn't derivable in ten seconds
from `docs/`.

## Decisions & strategy (do not relitigate casually)
- [Konstrukt visual philosophy](memory/konstrukt-visual-philosophy.md) — Sutnar functionalist poster won the prototype round; Broadsheet fused, Rentgen archived. Don't re-propose a redesign.
- [Sample-data-first strategy](memory/sample-data-first.md) — UI built against final data shapes over a deterministic, test-pinned mock (`lib/civic/`) before real ingestion; keep the shapes when ingestion lands.
- [Evidence-citation doctrine](memory/evidence-citation-doctrine.md) — every number cites its source; non-partisanship is enforced in the data model (unverified ties never feed the score). The brand rule.
- [Impeccable detector triage](memory/impeccable-detector-triage.md) — 38/38 false positives unconfigured (`border-l-4` IS the Sutnar idiom), dormant without a frontmatter DESIGN.md, and one rule can't tell `truncate` from a break. Four rejections are on record; don't re-litigate them.

## Project state
- [Architect graph deferrals](memory/architect-graph-deferrals.md) — /architect (2026-07-26) deferred 4 follow-ups on features/graph behind an in-flight round-4 arc that never landed; **3 applied 2026-08-13** (failure traces + the eslint exclusion deleted, the memoised-null index, `server-only`). Only the `asUnion` narrowing is open, and it is a behaviour ruling, not a tidy-up.
- [Graph bench: no graph DB (case #4)](memory/graph-bench-kuzu-x64-ready.md) — ran `graph.ts` on x64; Kuzu lost every workload to recursive-CTE/DuckDB on the dense co-voting graph. Verdict recorded (R12–R15); don't re-propose Kuzu until a millions-edge sparse graph exists.
- [/poslanec takes a pspId](memory/profile-route-takes-pspid.md) — the profile route resolves real psp.cz ids, so links built from mock MP slugs (dashboard leaderboard, graph nodes) 404.
- [The KG stores no source URLs](memory/kg-has-no-source-urls.md) — provenance ≠ citation; links are rebuilt from stable ids — except `contract` and `notice`, which store their own URL in props (that half was believed false until 2026-08-13), and which registry patterns actually resolve is recorded there.
- [electoral_arena is declared, not true](memory/electoral-arena-prop-is-declared-not-true.md) — 1 296 obce self-declare as „příspěvková organizace kraje"; join the obec registry / kraj crosswalk before windowing or baselining by arena.
- [Current mandate holder = open chamber membership](memory/current-mandate-holder-signal.md) — mandates carry no dates; `membership` on organ 174 with `to_at IS NULL` is exactly the 200 sitting MPs.
- [Company node ids are 8-digit zero-padded IČOs](memory/ico-node-id-canonical-form.md) — an unpadded id silently duplicates a node and makes every IČO join a false negative; it already severed one ownership chain.
- [Registr smluv needs no token](memory/registr-smluv-token-free-access.md) — per-IČO contract search is open; no structured export, Nette session pagination, aggressive 429s.
- [OR shareholder entries mean *sole* shareholder](memory/or-shareholder-entry-semantics.md) — an a.s. shareholder is registered only when there's one; and those dates are registration, never acquisition, dates.

## Conventions & traps
- [A gate's output is not a KPI](memory/gates-report-failures-meters-need-denominators.md) — both gates report failures only, so neither can produce a ratio; the citation rule's `census` mode and the stage-splitting `kpi:verify` supply the denominators, and the one-bit `&&` chain was hiding a 17,7 s whole-tree test flaking against a 5 s budget that nothing ever registered.
- [Robocopy of a live PGlite store can corrupt](memory/robocopy-of-a-live-pglite-store-can-corrupt.md) — a copy taken mid-write aborts on every open; verify the copy opens before briefing a builder, and treat "store won't open" as data, not code.
- [A held store mimics corruption — find the live holder FIRST](memory/held-store-mimics-corruption.md) — two agents diagnosed a torn WAL while a concurrent `next dev` held the single-connection store; „reproduces on a byte copy" does NOT rule out a holder, and a Permission-denied on renaming the dir IS the holder check. Sweep node processes and curl any dev server before any restore.
- [Vitest+PGlite needs tamed workers](memory/vitest-pglite-needs-tamed-workers.md) — full-suite runs flake with hook timeouts at default parallelism; the taming (60 s timeouts + `maxWorkers: 3`) is checked into `vitest.config.ts` since 2026-08-24, so a bare `npx vitest run` is already the reliable gate — and a failure that passes in isolation is contention, not code.
- [One worker cap taxed the whole suite](memory/one-worker-cap-taxed-the-whole-suite.md) — the taming above was right and was charged to the 203 files that never boot a store (86,6 s of wall clock); and a PGlite fixture's cost is `initdb` (~4,1 s), NOT the DDL (~0,3 s, and free to re-run over a provisioned db). Build-once template + a `unit`/`pglite` lane split took `npm run test` 276,7 s → 70,6 s at an identical 3 111 tests. Copying a store IS safe when it is closed, probed and unheld — that is the boundary the robocopy memo actually draws.
- [Agent worktrees: four traps](memory/agent-worktrees-three-traps.md) — worktree branches are cut from origin/master (merge local master as step 0), builders share the scratchpad and one %TEMP% PGlite template (stale after DDL changes), and worktrees inside the repo dir triple any tree-walking test; remove them after the wave.
- [A shared index needs `git commit --only`](memory/shared-index-needs-commit-only.md) — under a concurrent session a plain commit carries whatever the OTHER session staged (three foreign app pages rode into a two-file commit on 2026-09-01, caught only by doc-sync); `git commit --only <paths>` commits exactly your files and leaves theirs staged.
- [Dirty-tree build failures need a clean probe](memory/dirty-tree-build-failures-need-a-clean-probe.md) — an opaque `next build` crash on the shared tree is diagnosed by building the same commit in a detached worktree; round 4's was a corrupted `.next` from racing sessions.
- [A small LIMIT is ~200× slower than the cap](memory/small-limit-is-slower-than-the-cap.md) — a kind-filtered `kg_node` read at `limit:30` costs 500–720 ms and at `KG_READ_CAP` costs 2–3 ms; never size a kg read to what you expect back, and ask for a COUNT rather than a limited row list.
- [A scoped `vote_ballot` read beats the relation](memory/scoped-ballot-read-beats-the-relation.md) — `vote_psp_id = any(...)` rides `vote_ballot_vote_idx` for 4 000 rows in 29 ms against 406 000 rows in ~7,3 s; the small-LIMIT hazard does NOT apply when the predicate itself is indexed, and a loader must check its cheapest disqualifying precondition first.
- [kgNeighbours' weight order is not total](memory/kgneighbours-weight-order-is-not-total.md) — the indexed per-node read ties densely; a ranked top-N cut must re-sort via `byListOrder` or the page shuffles between builds (it reordered 202/207 MPs' ally lists, silently).
- [`revalidate` is inert — every route is dynamic](memory/revalidate-is-inert-every-route-is-dynamic.md) — `cookies()` in the i18n request config opts the whole app out of static generation; what actually bounds staleness is loader memoization.
- [An `infinity` timestamp collapses a whole surface](memory/infinity-timestamp-collapses-a-whole-surface.md) — a legal `timestamptz` threw in the mapper, and the loader convention turned one bad cell into an empty page.
- [Token + catalog discipline](memory/token-and-catalog-discipline.md) — colors only in `globals.css` tokens (3 declared exceptions); shared catalog is a lint-enforced import boundary. Know these before fighting lint.
- [Rendering gotchas](memory/rendering-gotchas.md) — recharts livelock, SVG float drift, Czech formatting via `lib/format.ts`, SSR==CSR determinism. Four hydration/layout landmines.
- [React state patterns the linter demands](memory/react-state-lint-patterns.md) — `set-state-in-effect` is an error; use `useSyncExternalStore` for localStorage state and rAF/observer callbacks for DOM measurement.
- [The rAF guard / StrictMode trap](memory/raf-guard-strictmode-trap.md) — zero the frame ref on cleanup or the canvas never draws in dev only; plus how to drive Chrome to measure a blank canvas.
- [Prototype rejection ≠ concept rejection; one label engine](memory/prototype-rejection-and-labels.md) — ask which axis a rejected variant failed on before deleting; all canvas text shares one collision queue; verify by eyeballing screenshots at two viewports.
- [Reader-facing loaders need the language gate](memory/reader-facing-loaders-need-the-language-gate.md) — analyst prose ships to Czech readers as English unless the loader imports `lib/analysis/language-gate.ts`; found on three surfaces across three passes. Also: a payload vocabulary mismatch drops every row silently.
- [KG upsert replaces props](memory/kg-upsert-replaces-props.md) — upsertKgNodes/Edges wholesale-replace props, so a writer that rebuilds a node erases every other pass. `kg-compute` was the worst case (it wiped the whole effort layer off all 207 MPs) and `clearKg()` behind `--reset` is worse still; both are guarded since 2026-08-13. Backfill merge-preservingly, verify on a copy.
- [The recompute replay gate](memory/recompute-replay-gate.md) — a scorer correction must replay the OLD formula and abort unless it reproduces every stored value; otherwise "correction" is an unattributable rewrite. Never re-run a network-fed ingest to fix a formula.
- [`kg_edge` review_tier/review_rank are a pass-24 cache, not an authority](memory/money-stored-review-rank-is-a-stale-cache.md) — `tie_class` is a judgement to honour; the two order keys are a stale snapshot of a pure function (153/208 ranks wrong after batch-012). Ask: did a PERSON decide it or a FUNCTION compute it?
- [An isolated dev server needs a worktree](memory/isolated-dev-server-needs-a-worktree.md) — Next 16 locks per project DIR, not per port, and a `node_modules` junction breaks Turbopack; worktree + real `npm ci` + `PGLITE_PATH` at a copy. Also: a wedged PGlite store is rescued by copying, not repairing.
- [Run a server-only loader under tsx](memory/tsx-can-run-server-only-loaders.md) — `NODE_OPTIONS="--conditions=react-server" npx tsx` imports `features/**/get*Data.ts` directly; the cheap way to prove a read-path refactor changed nothing (byte-identity dumps).
- [Two mappers over one edge starve the decider](memory/two-mappers-over-one-edge-starve-the-decider.md) — the review console re-declared a narrower copy of the money tie, so the human deciding saw less evidence (211/211 analyst notes, 82 flag sets) than the public; extend the shared shape, and beware conditions written against a shape that moved (a prompt matching 0 of 211 ties).
- [The language gate false-positives on registry Czech](memory/language-gate-false-positives-on-registry-czech.md) — `isCzechSafe` calls 14/211 genuinely Czech tie notes English ("OR", "evidence" are English stopwords); gate the copy WE write, never withhold graph prose with it.
- [A recompute invalidates the prose that quoted the old value](memory/recompute-invalidates-prose.md) — pass 42 moved 33 scores and left 30 dossier sentences wrong; check it in the recompute, not a gate
- [Corpus role snapshots go stale](memory/corpus-role-snapshots-go-stale.md) — a targets-file registry role is point-in-time with no as-of date; in one day the loop both asserted an expired role as current AND rejected a current fact against a stale snapshot. Rank/tense of a role resolves at ARES VR, cited web with dates.
- [The live store can be restored under you](memory/live-store-can-be-restored-under-you.md) — a concurrent session restored .pglite from a stale backup, wiping passes 43–47; caught by the manifestation probe, replayed in minutes because every write is a committed gated payload. Probe pass provenance before writing; refresh the named backup after each persist.
- [Whole-artifact invariants beat pattern gates](memory/whole-artifact-invariants-beat-pattern-gates.md) — every per-string gate stayed green while a false ownership claim, a falsified CZK figure and fabricated quotes shipped through four audit rounds; digit/syntax/arithmetic/quotation invariants over the whole string are what caught them. Reference impl in `scripts/case-loops/law/archive/sweep-old27-015.ts`.
- [Agent worktrees carry CRLF](memory/agent-worktrees-carry-crlf.md) — a killed builder worktree diffs as if EVERY file changed (CRLF vs LF); harvest with `diff --strip-trailing-cr` / `git diff --ignore-cr-at-eol`, then `git commit --only` the real deltas. Paid for in perfect round 7.
- [`kgNeighbours` defaults to limit 500](memory/kgneighbours-default-limit-is-500.md) — below the ~784 supplies-edge mean per company, and until 2026-08-12 the ONE kg read with no `warnIfTruncated`; /denik lost 4 872 contracts to it and /dashboard ran the same unguarded read for another week. Never take the default; ordered by weight desc, so truncation drops the cheapest edges of the busiest entity.
- [PGlite 0.5 ships Postgres 18](memory/pglite-05-ships-postgres-18.md) — 0.5.x cannot open our `PG_VERSION 17` store (A/B on one healthy dir: 0.4.6 opens, 0.5.4 refuses); the bump is a dump + re-ingest + volume reseed, and dependabot ignores minor/major until then.
- [`supplies` is not attribution](memory/supplies-is-not-attribution.md) — the edge means "party to the contract", not "got the money"; 27,44 % of /penize's headline (11,77 mld., 99,9 % one company) was co-signed contracts the register hands to somebody else. Reach is decided by `moneyReachesCompany`, and silence still counts.
- [Case-loop scripts must exit](memory/case-loop-scripts-must-exit.md) — a tsx script that opens PGlite prints and then hangs forever; killed runs orphan processes holding the data dir until reads crawl, which mimics a slow loader. `main().then(() => process.exit(0))`, never `main(); process.exit(0)`.
- [Attribution has two axes](memory/attribution-has-two-axes.md) — `tie_class` is the ROLE, `public_mandate` is the COMPANY; a municipal utility's turnover is not the MP's whatever their role (11,82 mld. on Teplárny Brno alone). Use `tieIsAttributable()` and build ties only with `toReachableTie()`; register silence is not evidence.
- [Loop artifact hygiene](memory/loop-artifact-hygiene.md) — resume from `STATE.md` not the ledger; back up with `npm run db:backup` (checkpoint, keep 2) never bare cp; probes in `archive/`, raw data in `data/raw/`; `persist-batch` refuses jsonb keys not in `lib/kg/prop-registry.json`. 22 GB of redundant backups pruned 2026-08-22.
- [dataor transport and limits](memory/dataor-transport-and-limits.md) — for s.r.o. owners use ARES VR per IČO (`spolecnik[]`, share %, dates; a.s. lists only a sole akcionář); sro-full-praha is 2 452 MB (past V8's string cap; use the streaming `fetchAndFindRecords`), the server caps ~70 MB/~90 s per connection with no Range, the ARES→slug table was wrong 9/11 (now verified; `961→sf` svěřenský fond exists). Cache = `<id>.csv` only; tools write dated payloads.
